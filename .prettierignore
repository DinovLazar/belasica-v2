# Generated, and formatted by its own generator. Prettier and
# `scripts/build-season-tables.mjs` disagree on how to print 1,2 MB of nested
# data, and letting both touch it makes `--check` fail after every format run —
# so the generator owns this file's formatting outright (3.28).
src/content/season-tables.ts

# Same rule, same reason (3.34): generated by
# `scripts/build-statistika-extra.mjs`, whose `--check` diffs the committed file
# byte for byte. If Prettier reformatted it, `--check` would fail forever.
src/content/statistika-extra.ts

# Build output and dependencies.
.next/
node_modules/
