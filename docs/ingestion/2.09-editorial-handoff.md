# Phase 2.09-Run — editorial hand-off

The scripted ingestion created **96 season shells** and **881 photos** (now published,
D-2.09R-2) plus **2 players with career stats** and **1 season final table**. Everything
below is what a **human editor** must still do in `/studio` — the script sets only what a
folder name deterministically implies (slug/title/decade/relatedSeason/provenance) and the
few figures transcribed for OV-4/OV-7. Nothing here may be invented; every factual claim
goes to `facts.md` before it is trusted (content-truth).

Companion: [`2.09-run-report.md`](2.09-run-report.md) · [`2.09-dry-run-report.md`](2.09-dry-run-report.md).

---

## 0. Rights — settle before this archive stays public

⚠️ **All 881 ingested photos are PUBLISHED (public) as of this run (D-2.09R-2, owner-directed).**
`facts.md` still records photo **screenshot rights as legally-nuanced / not settled in
writing**. Most images are third-party screenshots (Facebook / Messenger / newspaper). Every
photo's `provenance` field still reads „…Потекло и права за објавување НЕПОТВРДЕНИ…". **The
first editorial priority is to settle the rights with Ace in writing**, and either confirm the
photos may stay public or unpublish them. This is a standing item on the owed-verification
register (OV-1 caveat).

## 1. Per-season work (all 96 seasons)

Every season shell has **title + decade + linked photos** only. Still needed, per season:

| Field | Status after ingestion | Source(s) for the editor |
|---|---|---|
| `story` (Приказна) | **none** (all 96) | main history docx (per-season narrative); the 3 season docs |
| `finalTable` (Конечна табела) | **1 of 96** (only `1982-83`) | `Беласица 1926-2025-финал-20.05.2025.xlsx` — has Belasica's own row (pos/P/W/D/L/GF/GA/pts) for **most** seasons 1926/27→2024/25. NB: full standings (other clubs) are **not** in the source; the xlsx gives only Belasica's row. |
| `squad` (Состав) | **none** | season docs give per-season squad with `apps/goals` (e.g. `1971-72` lists 24 players „25/0" style). Requires the players to exist as `person` docs first. |
| `trainers` (Тренери) | **none** | `011. Treneri/Тренери на Беласица.docx`; season docs name coaches |
| photo `caption` | **none** (all 881) | the photos themselves (newspaper captions, dates) |
| photo `relatedPerson` | **none** | portrait/person photos in the person folders (§3) |

## 2. Seasons flagged `manual-title` — confirm what they represent (9)

These are era ranges and lone years; the script gave a provisional title but a human must
confirm the real one (and fold in any dropped label). None is a defect — they are inherently
ambiguous folders.

| slug | Provisional title | Why it needs a human |
|---|---|---|
| `1922-26` | „Беласица 1922–1926" | era range — early years grouped; no per-season granularity |
| `1926-1930` | „Беласица 1926–1930" | era range |
| `1945-48` | „Беласица 1945–1948" | era range; folder label **„Илинден" was dropped from the slug** — fold into title/story |
| `1942` | „Сезона 1942" | lone year (war year) — may be a partial/uncertain season |
| `1943` | „Сезона 1943" | lone year |
| `1944` | „Сезона 1944" | lone year |
| `1950` | „Сезона 1950" | lone year — **coexists with `1950-51`**; confirm they are distinct seasons |
| `1951` | „Сезона 1951" | lone year — coexists with `1950-51` / `1952-53` neighbours |
| `1952` | „Сезона 1952" | lone year — coexists with `1952-53` |

## 3. Persons — create the roster and link portraits

Only **7 persons** exist (5 demo + Роберт Христовски + Љупчо Мафков). The archive names dozens
more. Photos of people sit in these **thematic folders** (classified, uploaded as photos with
no `relatedSeason`, flagged `needs-manual-linking` — actually **not uploaded** this run since
`--include-thematic` was off; they remain in the mirror for a later pass):

| Folder | Images | Becomes |
|---|---|---|
| `009. Фудбалски легенди` | 80 | player `person` docs + portrait `relatedPerson` links |
| `010. Репрезентативци на Македонија` | 10 | players who were internationals |
| `011. Treneri` | 2 | trainer `person` docs (+ `Тренери на Беласица.docx`) |
| `012. Pretsedateli` | 2 | president `person` docs (+ `Претседатели на ФК Беласица.docx`) |

Per person: `bio`, `careerStats` (career totals only — never sum per-season; D-2.01-3),
`playingYears`, `role`, and a portrait via `photo.relatedPerson`.

**careerStats sources are thin.** Whole-career Belasica totals barely exist in the docs — only
two were found (used for OV-7). Most stat docs are **per-season** (`Најдобри стрелци` = top
scorer + goals per season; season docs = per-season squad apps/goals). Those populate
`season.squad`, not `careerStats`.

## 4. Thematic folders — curate or leave (21, not uploaded)

`--include-thematic` was off, so these were classified and counted only. A later pass decides
each: person portraits (§3), themed galleries (`021. Стадион` 6, `022. Ајдуци` 8,
`020. Партизан и Беласица` 7, `Куп на Македонија од 1992` 19, `15. Uefa natprevari` 6,
`024. Znamenca Belasica` 10, …), or book-production artifacts (`000. Корица…` 5). Four thematic
folders hold **0 images** (`008. статистика`, `014. Прва македонска лига`, `25.Спонзори`,
`СТАТИСТИКА`) — they carry only documents.

## 5. `manual-review` folders (0) and skipped (2)

- **`manual-review`: none.** Every folder matched a plan §3 rule — no folder's meaning was guessed.
- **Skipped (owner „- не" exclude marker):** `Sostavi mkd sport - не`, `Листа на стрелци - не`. Left untouched by Ace's own instruction; ~82 images inside them were **not** ingested.

## 6. Source documents available to the editor (17)

At mirror root:
- `ФК Беласица - гордоста на Струмица - finalna verzija - 04.10.2025.docx` — **the main history** (per-season narrative, some career totals). Primary `story` source.
- `Беласица 1926-2025-финал-20.05.2025.xlsx` — **Belasica's own record per season** 1926–2025. Primary `finalTable` (Belasica row) source.
- `Беласица - младинска школа и Виареџо…текстот оттука не го земај.docx` — **do NOT use** (Ace's own note: „текстот оттука не го земај" = don't take the text from here).

Inside folders:
- `СТАТИСТИКА/Најдобри стрелци на Беласица.docx` — top scorer + goals **per season** (from 1950).
- `СТАТИСТИКА/Ранг листа…по број на сезони.docx` — players ranked by # of top-scorer seasons.
- `СТАТИСТИКА/Беласица  голмани.docx` — goalkeepers with active-year ranges (no appearance totals).
- `011. Treneri/Тренери на Беласица.docx` — trainers.
- `012. Pretsedateli/Претседатели на ФК Беласица.docx` — presidents.
- `1971-72/…`, `1972-73/…`, `1981-82/…` `.docx` — three per-season narratives with full squad apps/goals.
- `014. Прва македонска лига/Прва лига - ранг листа.xlsx`, `008. статистика/…`, `012…/…финал.xlsx` — league/standings sheets (some duplicates of the root xlsx).

## 7. Owed to Lazar — spot-check these 10 season pages against their folders

The mapping is deterministic, but „right" for an ambiguous folder needs a human eye. Compare
each `/arhiva/<slug>` (title + photos) to the source folder `~/belasica-ingest-source/<folder>`:

1. `/arhiva/1922-26` — era range (folder `1922-26`)
2. `/arhiva/1926-1930` — era range, 4-digit end (folder `1926-1930`)
3. `/arhiva/1942` — lone war year (folder `1942`)
4. `/arhiva/1945-48` — era range, dropped label „Илинден" (folder `1945-48 Илинден`)
5. `/arhiva/1950` — lone year (folder `1950`) — distinct from #6
6. `/arhiva/1950-51` — slash-form on Drive, hyphen on disk (folder `1950-51`)
7. `/arhiva/1985-86` — normal span (folder `1985-86`)
8. `/arhiva/1999-2000` — century rollover (folder `1999-2000`)
9. `/arhiva/1992-93` — pre-existing demo season, now +16 ingested photos (folder `1992-93`)
10. `/arhiva/2025-26` — most recent (folder `2025-26`)
