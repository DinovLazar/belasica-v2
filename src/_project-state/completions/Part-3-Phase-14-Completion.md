# Part 3 · Phase 14 · Code — Completion Report

**Date:** 2026-08-07 · **Outcome (one line):** Three headings on `/legendi` and the season page now name what actually sits under them, and Петар Андреев's record no longer says he was a club president.

## 1. What shipped (plain language)

Four corrections Goran asked for on pages that were already live. On `/legendi`, the third band stops being called „Раководство" (the *body*) and becomes **„Претседатели"** — the role its members are actually filed under, and the same word already printed on each of their cards; the line under it now reads „28 претседатели" instead of „28 членови". On a season page, two section titles change: „Играчи и тренер" becomes **„Статистика на играчи"**, and „Резултати" becomes **„Резултати и стрелци"**. Nothing else on those pages moved — every shared link like `…/1985-86#trener` still lands where it always did, and the jump-rail down the side is unchanged.

The fourth correction is to the archive's content rather than its code: the person record for **Петар Андреев** said he was a *player and president*; the owner states he was never president and did coach the club, so it now says *player and trainer*. That is live on the site already. He stays where he was on `/legendi` — under Играчи, because a player is always filed as a player — and only the two small labels on his card change.

**Two things the owner needs to look at.** Calling the band „Претседатели" is now a factual claim about all 28 people in it, which nobody has checked (OV-35). And „Резултати и стрелци" promises scorers on all 95 season pages that show results, but 19 of them — every season before 1949, plus 2012-13 — have scores only (OV-36).

## 2. Definition of Done

- ✅ **`BAND_TITLE.president === "Претседатели"`, `BAND_COUNT_NOUN.president === ["претседател","претседатели"]`, no other entry changed** — evidence: [src/lib/people.ts:25](src/lib/people.ts:25) and [src/lib/people.ts:248](src/lib/people.ts:248); `git diff --stat main -- src/lib/people.ts` = 14 lines (2 values + 12 comment lines), `player` and `trainer` entries untouched in the diff.
- ⚠️ **`grep -rn "Раководство" src/` returns nothing** — **met in substance, not as literally written.** `grep -rn "Раководство" src/app src/components src/lib src/sanity` returns **nothing** (all five code occurrences the brief enumerates were changed). The literal path `src/` also covers `src/_project-state/`, where `decisions.md` is **append-only by CLAUDE.md** and seven closed completion reports record the old label as shipped history; satisfying the literal grep would mean falsifying them. The two *live* state mirrors (`current-state.md`, `file-map.md`) **were** updated — `grep -c "Раководство" src/_project-state/file-map.md` = 0. Recorded as **D-3.14-7**; direct precedent D-3.13-8 and D-3.10-9.
- ✅ **`SECTIONS.staff.heading === "Статистика на играчи"`, `SECTIONS.results.heading === "Резултати и стрелци"`, all four `id`/`label` values unchanged** — evidence: [src/app/(site)/arhiva/[slug]/page.tsx:162](src/app/(site)/arhiva/[slug]/page.tsx:162) and :170; built HTML of `/arhiva/1985-86` gives `section ids: ['prikazna', 'rezultati', 'trener', 'tabela', 'fotografii']` and rail labels `Преглед · Резултати · Играчи · Табела · Фотографии` — byte-identical to `main`.
- ✅ **`grep -rn "Играчи и тренер" src/` returns nothing** *(code scope, same caveat as above)* — `grep -rn "Играчи и тренер" src/app src/components src/lib src/sanity` returns nothing. Three historical mentions remain in the state files, all now written as „renamed at 3.13 → renamed at 3.14".
- ✅ **`npm run build` 271/271, `npm run lint`, `npx tsc --noEmit` all pass; `package.json` + lockfile byte-unchanged** — evidence: `Generating static pages (271/271)`, build exit 0; `eslint` exit 0 with no output; `tsc --noEmit` exit 0; `git diff --stat main -- package.json package-lock.json` returns **empty**.
- ✅ **Built HTML of `/arhiva/1985-86` shows РЕЗУЛТАТИ И СТРЕЛЦИ and СТАТИСТИКА НА ИГРАЧИ, and the staff section still contains `<h3>` „Тренер: Благој Истатов"** — evidence: extracted `<h2>`s = `['Преглед', 'Резултати и стрелци', 'Статистика на играчи', 'Табела', 'Фотографии']`; extracted `<h3>`s = `['Тренер: Благој Истатов', 'Состав и статистика']`. (Both headings are natural-case in the HTML and uppercased by the `u-h2` class at render, as the brief anticipated. Note: a raw substring search for `Тренер: Благој Истатов` *fails* — React splits it as `Тренер:<!-- --> </span>Благој Истатов` — so the check must be run on tag-stripped text.)
- ✅ **Built HTML of `/legendi` shows third band ПРЕТСЕДАТЕЛИ with „28 претседатели"; band sizes Играчи 86 · Тренери 46 · Претседатели 28 = 160** — evidence: band `<h2>`s = `['Играчи', 'Тренери', 'Претседатели']`; count lines = `['86 играчи', '46 тренери', '28 претседатели']`. 86+46+28 = 160.
- ✅ **`scripts/fix-andreev-role.mjs` passes `node --check`; dry run printed `["player","president"] → ["player","trainer"]` for Петар Андреев** — evidence: `node --check` exit 0; the pre-write dry run printed exactly `Петар Андреев  (petar-andreev)  f69a1977-…` / `улоги: ["player","president"] → ["player","trainer"]`.
- ✅ **After applying: `*[_id=="f69a1977-…"].role` returns `["player","trainer"]`; no `drafts.` copy exists** — evidence: post-write GROQ under the **`raw`** perspective returned **one** document, `role: ["player","trainer"]`, and no `drafts.` row. Independently confirmed against both `https://f8rmnfry.apicdn.sanity.io` and `https://f8rmnfry.api.sanity.io`. ⚠️ Applied via the authenticated Sanity MCP, **not** via `--commit` — see §3/§4 and **D-3.14-5**.
- ✅ **On `/legendi`, Петар Андреев appears once, under Играчи, chips Играч · Тренер, no „Претседател"; `/legendi/petar-andreev` shows Тренер not Претседател** — evidence: `/legendi` built HTML contains `/legendi/petar-andreev` exactly **once**, its card text is `['Петар Андреев', 'Играч', 'Тренер', '1974–1995']`, and it sits inside the Играчи band. Page-wide chip totals moved by exactly one in each direction: Тренер 65 → 66, Претседател 30 → 29, Играч 86 → 86. On `/legendi/petar-andreev`, chips = `['Играч', 'Тренер']` and the string „Претседател" does not occur.
- ✅ **Four state files updated; decision entries exist; completion report filed** — §6. **Seven** entries D-3.14-1…-7, not four: three extra decisions the brief did not specify (§3).
- ✅ **PR merged after self-review** — PR [#44](https://github.com/DinovLazar/belasica-v2/pull/44) opened, self-reviewed against the `main` diff (9 files, +379/−19; the three source files are 4 value changes and 5 comment updates, no collateral churn), then **squash-merged on the owner's explicit instruction, without a Vercel-preview review** — **D-3.14-8**. *(This line and §4 were updated on the branch before the merge, so `main` does not land carrying a false "not performed". The rest of the report is as originally filed.)*

## 3. Decisions I made during this phase

- **Applied the role patch through the authenticated Sanity MCP instead of the script** · this machine's `.env.local` holds only the three public `NEXT_PUBLIC_SANITY_*` values, no `SANITY_API_WRITE_TOKEN`, so Task 10's `--commit` route was unavailable; the session had an OAuth-authenticated Sanity connection as `DinovLazar` that could do it without a token ever touching the repo · **alternative rejected:** follow Task 11 literally and report the write as owed — which is what I recommended; **the owner was shown both routes and chose to apply it now**. Because the MCP cannot write published content directly, the sequence was patch-draft (with `ifRevisionId` as an optimistic lock) → verify the draft → publish → re-read under `raw` to confirm no draft remained. **Decision-log entry: YES — D-3.14-5.**
- **Made the script idempotent rather than failing its own `president` guard on a second run** · the brief asks for both "idempotent" and "assert the current role contains `president`", which cannot both hold after one successful run · **alternative rejected:** fail hard on re-run, per the literal reading — that is not idempotence. Already-correct now exits 0; only a genuinely unexpected role array exits 1. **Decision-log entry: YES — D-3.14-6.**
- **Added two guards the brief did not ask for** · an **unknown-role check** (`role` is a free string array in the schema, so a value outside the Studio list would be silently dropped when the array is re-ordered) and a **draft check**, reported as *inconclusive* without a token rather than as a pass, because an anonymous read cannot tell "no draft" from "not permitted to see one" · **alternative rejected:** trust the brief's stated guards only. Covered by **D-3.14-6**.
- **Scoped the „Раководство" DoD grep to code and left the append-only history alone** · see §2 · **alternative rejected:** rewrite `decisions.md` and seven closed reports to make the literal grep pass. **Decision-log entry: YES — D-3.14-7.**
- **Measured the scorer claim instead of asserting it** · I wrote in D-3.14-3 that the results rows carry scorers, then checked it across all 96 built season pages rather than leaving it as an assumption; 19 of the 95 with results have none · **alternative rejected:** ship the heading and file a vague "not verified" note. The measurement became **OV-36** with the seasons named. No separate decision entry — it is recorded inside D-3.14-3.
- **Indexed the *3.13* completion report in `file-map.md` retroactively** · 3.13 filed `Part-3-Phase-13-Completion.md` but never added its line to the file map's completions list, so the map was one file behind reality before this phase started · **alternative rejected:** add only my own line and leave the gap — the map would then be wrong about a file it does list every sibling of. One line, flagged in place as retroactive. No decision-log entry — it is a state-file correction, not a choice about the product.
- **Corrected D-3.13-1's Status line to record partial supersession** · CLAUDE.md permits exactly one edit to a filed decision — its Status — when a later decision reverses it; D-3.14-2 reverses only the `staff.heading` half · **alternative rejected:** leave D-3.13-1 untouched, which would leave two live decisions setting the same field to different values. Recorded in the D-3.13-1 Status line itself and in D-3.14-2.

## 4. Deviations from the brief / spec

- **Task 10 (`node scripts/fix-andreev-role.mjs --commit`) was not the route used.** The correction is applied and verified live, but through the MCP (D-3.14-5). Consequence: **the script's write path has never executed.** Its read, guard and dry-run paths are exercised; `--commit` is not. It is now a no-op — re-running it today reports „веќе исправено" and exits 0.
- **Task 19's "then merge" was not performed on the executor's own authority.** `CLAUDE.md` §Branch & PR rules says **„Never merge your own PR. Lazar reviews the Vercel preview and merges"**, and the last two phases' merges were each recorded as an explicit *owner instruction* departing from that rule (D-3.12-9, D-3.13-10), not as something the executor decided. The PR was therefore opened, self-reviewed and **held** — and then **merged when the owner gave the instruction** (D-3.14-8). No Vercel preview was reviewed by anyone. **This is the third consecutive phase to bypass the preview gate**, which has not been exercised since 3.11.
- **Seven decision entries, not the four the brief listed** (D-3.14-1…-4 as specified, plus -5, -6, -7 for the choices in §3).
- **The pre-flight unpushed-commit check could not be run meaningfully.** `git log origin/main..HEAD` is structurally empty in this checkout, so it is *not* evidence that Lazar's working copy is clean — the D-3.12-8 trap. Lazar should confirm his own machine holds no unpushed commits before merging.

## 5. Changed files / deliverables

Branch **`phase-3.14-label-content-corrections`** → PR into `main`.

**Code (3 files, +17/−12):**
- `src/lib/people.ts` — `BAND_TITLE.president`, `BAND_COUNT_NOUN.president`, and three doc comments that named the band by its old label.
- `src/app/(site)/arhiva/[slug]/page.tsx` — `SECTIONS.results.heading`, `SECTIONS.staff.heading`, and the staff section's inline comment (now records the owner-accepted heading/trainer-card mismatch).
- `src/app/(site)/legendi/page.tsx` — one comment.

**New file:**
- `scripts/fix-andreev-role.mjs` — one-off, idempotent, guarded single-field role correction. Dry-run default; `--commit` requires `SANITY_API_WRITE_TOKEN`, which is **not** on this machine and is **not** in any tracked file. No secret appears in the repo.

**Content (not in the diff — a live Sanity write):**
- `person` `f69a1977-1ddd-4201-8649-33187ad837c2` (`petar-andreev`): `role` `["player","president"]` → `["player","trainer"]`, published, no draft left behind.

**State (4 files):** `current-state.md`, `file-map.md`, `decisions.md`, `00_stack-and-config.md`.

## 6. State updates done

- ✅ `src/_project-state/current-state.md` — `NEXT:` still **3.08 — Domain cutover**, unchanged by design; leading paragraph and Summary now describe 3.14; **Last updated** stamped 2026-08-07; **OV-35, OV-36, OV-37 added**; **OV-34 rewritten** (it named „Играчи и тренер", which no longer renders anywhere — it now lists ПРЕГЛЕД, ПРЕТСЕДАТЕЛИ, СТАТИСТИКА НА ИГРАЧИ, РЕЗУЛТАТИ И СТРЕЛЦИ).
- ✅ `src/_project-state/file-map.md` — `scripts/fix-andreev-role.mjs` added under a new „One-off content correction (Phase 3.14)" heading; the three „Раководство" mentions updated (`grep -c` = 0); the season-section entries updated to the current headings with the rename chain preserved.
- ✅ `src/_project-state/decisions.md` — **D-3.14-1 … D-3.14-7** appended; **D-3.13-1's Status** amended to record partial supersession (Status line only, per CLAUDE.md).
- ✅ `src/_project-state/00_stack-and-config.md` — 2026-08-07 section appended: no dependency change (lockfile byte-identical), the new script and its token requirement, no schema/config/token change, and the stale-cache warning below.

## 7. Risks, follow-ups, what the next phase needs to know

- ⚠️ **Next's persistent fetch cache will lie to you about content edits.** After the role patch went live, a full rebuild *still* rendered the old value: `useCdn = true` in `src/sanity/env.ts` plus the fetch cache under `.next/cache`. The Sanity CDN itself was already correct — checked directly against `apicdn.sanity.io`. **`rm -rf .next/cache` before verifying any content change against a local build**, or you will verify the previous state and believe it. This cost a full verification cycle here and is logged in `00_stack-and-config.md`.
- ⚠️ **OV-35 — „Претседатели" is now a factual claim.** „Раководство" claimed only that the 28 were officials; the new label and count claim the specific role. If the band holds a vice-president or secretary, the label is wrong in a way the old one was not, and the fix is either a different label or splitting `president` in the schema.
- ⚠️ **OV-36 — „Резултати и стрелци" over-promises on 19 of 95 season pages** (every season before 1949, plus 2012-13). The heading is one shared constant, so making it honest per-season needs a conditional the brief did not scope.
- ⚠️ **OV-37 — Петар Андреев's trainer service is uncorroborated.** The role change came from the owner, who is the source of record for `person.role`, but the book records him primarily as a player from 1974 and no repo source names him as a trainer. He now holds `trainer`, so a future phase that orders the Тренери band by service year will see him and find no year — he would sort to the end of that band (D-3.13-4's fallback).
- **The script is dead weight once ratified.** It cannot revert the change and cannot re-apply it; it is kept as the record of what changed and as a re-runnable assertion that it is still true.
- **OV count is now high.** OV-32, -33, -34 (3.13) and -35, -36, -37 (3.14) are open, plus the 3.12 items. Per the register's own rule — *at 3+ items, the next phase becomes a verification phase* — 3.08 (domain cutover) may be the wrong thing to run next.

## 8. What's now possible that wasn't before

The site's own labels can be trusted to describe what sits under them — which is what makes the two places they still over-claim (OV-35, OV-36) worth an owner's decision rather than a shrug.
