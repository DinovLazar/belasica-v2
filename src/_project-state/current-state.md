NEXT: **3.25-Cowork — Слики, сезона 2025/26 и податоци за личности**. — Phase **3.28-Code (Резултати и состав како табели, од податоците на книгата)** is COMPLETE on branch `phase-3.28-season-tables`. **The season page's two longest sections are tables now, generated from Аце's own book — with no Sanity write, no schema change and no migration.** `scripts/build-season-tables.mjs` projects `data/book/*.json` into `src/content/season-tables.ts`: **92 seasons with structured matches (2.267 in all) and 73 with a structured squad**, keyed by slug. **Nothing was lost:** seasons showing results **96 → 96**, seasons showing a squad **84 → 84**, and **0 of the 96 pages render both a table and the prose** for one section — counted across the built output, not spot-checked. Section `id`s are **byte-identical to `main` on all 96 pages**, and the four seasons that keep prose (`1922-26`, `1926-1930`, `1936-37`, `2025-26`) render **byte-identical** to `main`. ⚠️ **Three brief premises did not survive the data, and the owner chose the replacements.** There is **no cup axis at all** — `competitionType` is `league` for all 2.252 typed matches, `competition` holds **31 league names** rather than categories, and the 7 rows matching /куп/ are the opponent **Шкупи**; grouping is by **`stage`** (53 ties over 17 seasons) falling back to competition, which is exactly why 1987/88 now shows its two promotion-qualification ties in their own table (D-3.28-3). **„Коло“ exists on 12 of 2.267 matches**, so the column self-omits and is absent on 94 of 96 pages — provable on one page, `1938-39`, where „Група Струмица“ has three header cells and its three sibling tables have four (D-3.28-4). And **squad names effectively do not link**: 82,7 % of the 831 book strings are „К. Костадинов“ while person documents are „Коце Костадинов“, and **121 surnames carry more than one distinct string** (Митев ten), so only exact whole-string matches link — **1 of 1.982 rows** (**OV-63**, D-3.28-7). ⚠️ **One real regression was caught by diffing against `main`, not by reading code:** ten seasons list a roster with **no appearances or goals**, and on `2025-26` the prose carries „22+0/0“ where the structured rows carry none — the table would have replaced real numbers with two empty columns, so a statless squad is no longer emitted (D-3.28-9). ⚠️ **The figures after a scorer are printed without a unit**: 1.323 of 1.435 numbered matches are demonstrably minutes, but „5:0 (Иванов 4, Чулевски)“ is a goal *count* and **27 matches read exactly as counts** — so nothing is labelled (**OV-64**, D-3.28-5). **Zero horizontal scroll at 375 px** on `1987-88` (38 matches), `1938-39` and `2018-19`, verified after fixing three measured defects: scorer spans with no break opportunity (429 px in a 335 px column), a wrapper that would have **silently clipped** it, and a grid item that would not shrink below its longest word (D-3.28-6). Header ink on the navy band **14.95:1**, body text on paper **10.37:1** — measured, both AAA. Build **330/330 — unchanged**; `/arhiva/[slug]` First Load JS **123 kB, identical to the other two dynamic routes**, so none of the 1,2 MB module reaches the browser. `tsc`/lint/prettier clean, **zero new dependencies**, no new `brand.md` token, **no Sanity write and no schema change**. Decisions **D-3.28-1…-11**; owed **OV-63…OV-66**. — Phase **3.27-Code (Профили по улога: нови полиња и приказ што одговара на страницата)** is COMPLETE on branch `phase-3.27-role-scoped-profiles`. **Each of the four tabs on „Легенди" now shows only what belongs on it.** A coach's card no longer carries his player ranking, and an international's card no longer shows his Беласица appearance count under a heading about his career elsewhere. The card is told which tab it is in (`category` prop) and the server sends it only that tab's facts — two independent guards, so one careless edit cannot put a rank back on a coaching card. **Three new optional fields** — `trainerYears`, `officialYears`, `nationalStats{appearances, goals, sourceNote}` — deployed to workspace `belasica-v2`, **all shipping empty**: the data comes from Ace and from public records, and deriving it would be inventing facts (D-3.27-5). ⚠️ **The fourth field the brief asked for was NOT added:** Репрезентативци membership is already `INTERNATIONAL_SLUGS`, whose ORDER is Ace's own numbering, and a boolean could not carry it — adding one would have created the second source of truth the brief forbids (D-3.27-1). **„Играчи" is now `legendRank`, not the `player` role: 153 → 138, and 15 men drop off** — ⚠️ **this is a visible change to the site's most-read page and needs the owner's confirmation** (**OV-58**); the 15 are named in the report. **Nobody leaves the site**: all 15 remain in another tab, so the distinct roster is unchanged at **211**. On a person's own page „Кариера" now separates **„Беласица"** from **„Цела кариера"** with `sourceNote` beneath it, never summed and never substituted (OV-47's shape). ⚠️ **The brief's roster figures were stale by 51 people** — it said 160 (98·34·28); live is **211** (153·69·29·10), measured before any code (D-3.27-10). Build **659/659 — unchanged**, `tsc`/lint/prettier clean, zero new dependencies, no `brand.md` token, **no Sanity write**. Decisions **D-3.27-1…-13**; owed **OV-58…OV-62**. **PR [#55](https://github.com/DinovLazar/belasica-v2/pull/55) отворен, Vercel preview потврден:** https://belasica-v2-l9rnnvqof-sunset-services-team.vercel.app — 10/10 рути 200 (вклучително страница на тренер и на претседател), „211 личности" во заглавието, четирите таба измерени на распоредената верзија (**138 · 69 · 29 · 10**), кај Тренери/Претседатели/Репрезентативци **нема ни „Ранг" ни „настапи"**, картичките на Панче Стојанов различни во трите таба, и **0 px хоризонтално прелевање на 375/768/1280/1408**. — Phase **3.24-Code (Пет ситни поправки: лајтбокс, две врски на почетната, праг кај настапите, релативна врска)** is COMPLETE and **merged to `main` as `f1fab83` via PR [#54](https://github.com/DinovLazar/belasica-v2/pull/54)**. **The lightbox finally fills the screen** — it was rendering a scan at its own pixel size inside a full-screen overlay, so `/arhiva/1985-86` opened a photograph **331×372px, 46,5 % of the viewport height**; it now scales to the overlay's usable area and lands at **84 %** (D-3.24-1), with the arrow and close-button gutters 3.19 established now **structural rather than arithmetic**. A wrapping-flex trap was caught by measuring rather than looking — the first cut rendered the figure **1 258px tall in an 800px viewport** (D-3.24-1b). **OV-52 is CLOSED**: the overlay was opened at both call sites and both widths, and the per-topic dialog name is confirmed live. **„Момент од историјата" is pinned by `_id`** (the 1993 youth-Cup side) with the deterministic lookup kept as an exercised fallback, and it now links to `/razno/mladinska-skola` — **the link is gated on the pin, not on the picture**, so it disappears rather than becoming false (D-3.24-2). **„Разно" joined „Каде понатаму"**, which went to five tracks with the last box closing the tablet row (D-3.24-3). ⚠️ **Two brief premises were stale, and one matters.** „Најмногу настапи" was cut at 46 appearances on the belief it held **71** rows; it holds **119**, of which **116** clear the cut — so it removes **three** players, and the three are **Горан Пандев, Ацо Стојков и Горан Попов** (**OV-57**, D-3.24-4). And the „absolute Vercel link on `/za-nas`" this phase was sent to fix **does not exist** — it has been relative since 3.15, and the sweep of **2 443 published Sanity documents** found **0** occurrences of `vercel.app` anywhere (D-3.24-6). Build **330/330 — unchanged**, `tsc` + lint + prettier clean, **zero new dependencies**, no schema change, no `brand.md` token, **no Sanity write**. Decisions **D-3.24-1…-7**; owed **OV-53…OV-57**. **PR [#54](https://github.com/DinovLazar/belasica-v2/pull/54) отворен, Vercel preview потврден:** https://belasica-v2-ipzu0y6gj-sunset-services-team.vercel.app — 6/6 бараните рути 200, закачената фотографија и врската кон „Младинска школа“ потврдени на распоредената верзија, пет полиња во „Каде понатаму“, 116 реда кај „Најмногу настапи“, 0 placeholder-чипови, а на „За нас“ 0 апсолутни врски кон preview-доменот.
>
> Still queued behind this: **3.08-Ops — Domain cutover to `www.belasicahistory.mk` (DNS + NEXT_PUBLIC_SITE_URL)**. Phase **3.23-Code (Ревизија на целиот сајт: аудит, поправки и завршни детали)** is COMPLETE and merged as PR #51. **PL-4 is CLOSED in code**: the origin now lives once, in `src/lib/site.ts`, read from `NEXT_PUBLIC_SITE_URL` and defaulting to the Vercel domain; `layout.tsx`, `robots.ts` and `sitemap.ts` all import it. **The cutover is now one variable plus a redeploy** (`NEXT_PUBLIC_*` is inlined at build time), and `public/llms.txt` is a **fourth place** that hardcodes the origin and must be edited by hand in the same step — `site.ts` says so in a comment. The variable is set NOWHERE today, deliberately: canonical tags and a sitemap advertising a host that does not resolve are worse than none. This phase also gave the site the furniture it was missing — a Macedonian 404 inside the site chrome, an error boundary, canonical URLs on all 322 indexable pages, a real 1200×630 share card, JSON-LD (`WebSite`/`BreadcrumbList`/`Person` and nothing else), `llms.txt`, and a privacy section — and closed four register items: **OV-40** (the header nav wrap), the sitemap's missing `/pravni-informacii`, the four unhardened fetch call sites, and the misnamed `RoleBandGrid`. **The audit found 42 correctness defects: 0 P1, 14 P2, 28 P3.** Eight P2s were fixed; **six were deliberately not** (D-3.23-16) — two are Sanity content, two are the owner's design calls on the page he had rebuilt at 3.22, one is a naming judgement for Аце, one is its own phase. Decisions **D-3.23-1…-16**. Audit at `docs/audits/Part-3-Phase-23-Audit.md`. No Sanity write, no schema change, no `brand.md` token added, zero new runtime dependencies (two were REMOVED). **PR #51 отворен, Vercel preview потврден:** https://belasica-v2-7gpb3ll6v-sunset-services-team.vercel.app — 15/15 рути 200, непозната адреса 404 со сопствената 404 страница, канонски URL/OG/JSON-LD потврдени на распоредената верзија, sitemap 322.

# current-state.md — Belasica-V2

> Location in repo: `src/_project-state/current-state.md`. This is a **snapshot of the live repo, overwritten as things change** — not a log. The plan is aspirational; this file follows the code. Updated by the executor when closing every phase; the phase is not closed until this file matches reality.

**Last updated:** 2026-08-12 · by Claude Code (Phase **3.22-Code** — „Легенди“: четири категории со табови; verified, built and shipped code written by a Cowork session that could not commit), from a checkout of `~/Projects/belasica-v2`. Prior: 2026-08-11 · by Claude Code (Phase **3.20-Code** — „Разно": фотографии кај сите седум теми), from a checkout of `~/Projects/belasica-v2`. Prior the same day: Phase **3.19-Code** (три знаменца, натпревари кај сите играчи, скок-лента во „Разно", поправен лајтбокс), from the same checkout. Prior: 2026-08-09 · by Claude Code (Phase **3.17-Code** — скок-ленти, „Назад на врвот", прередена статистика, стартна единаесторка), from a checkout of `~/Projects/belasica-v2`. Prior the same day: Phase **3.16-Code** („Разно“: седум теми од историјата), from the same checkout. Prior: 2026-08-07 · by Claude Code (Phase **3.14-Code** — четири исправки на етикети и содржина), on Petar's machine, from a checkout of `~/Projects/belasica-v2`. Prior: 2026-08-02 · by Claude (Cowork) (Phase **3.12-Code** — биографии од книгата, рангирање по настапи, праг од 21 гол, четири фотографии, Фејсбук во подножјето), written into Lazar’s working copy. Prior the same day: Phase **3.11-Code** (book extraction data drop + season content fill script) on Lazar’s machine

## Summary (plain language)

- **[3.27-Code — role-scoped profiles COMPLETE — on branch] One man can be a player, a coach and an international, and until now the site showed all of it everywhere. Now each page shows only what belongs on it.** **Панче Стојанов is the proof** — the one man who appears in three tabs. Under **Играчи** his card reads „Ранг 54 · Панче Стојанов · 127 настапи · 1992–2012"; under **Тренери** and under **Репрезентативци** it reads his name and his chips and nothing else. Same man, same component, three different cards, because the card is now told which tab it is in instead of guessing from his roles — which is what could never work, since his roles cannot say which list he is being read in. **His player tag stays everywhere** (Ace was explicit: a coach who played keeps the *tag*, he just does not carry the *detail*), so the chips „Играч · Тренер" are on all three. **Three new fields** give the missing years and career numbers somewhere to live — coaching years, service years, and a whole-career appearances/goals pair with a source note beside it. ⚠️ **All three ship EMPTY, deliberately.** The numbers come from Ace and from public records; a script inventing them would be inventing facts, and the 3.13 sort years are a hidden sort key, not a span anyone could print (D-3.27-5). So on merge day the Тренери and Претседатели cards show a name and chips, and Репрезентативци shows no numbers — the shape waiting for the content pass, exactly as `/statistika` was built ahead of its data. ⚠️ **The consequential change is who counts as a player.** „Играчи" is now Ace's ranked list rather than the `player` role, which takes it from **153 to 138** and drops **15 men** — including **Аце Стојанов himself** (he stays in Тренери) and five of the ten internationals: **Пандев, Стојков, Горан Попов, Ѓузелов, Бандулиев** (they keep their own tab). **Nobody vanishes from the site** — every one of the 15 was checked against live data and is still in another tab, so the roster stays at **211** and no page is orphaned — but **this is the owner's call and he should see the list before merging** (**OV-58**). ⚠️ **Two things the brief got wrong, both caught by measuring.** It put the roster at **160**; it is **211**, a 51-person gap (D-3.27-10). And it required an example „holding three or more roles" — **nobody holds three**; the maximum in the data is two, so the nearest real thing was used instead: the one man in three *tabs* (D-3.27-11). ⚠️ **The fourth field was not added** — Репрезентативци membership already lives in a hand-ordered list carrying Ace's own numbering, which a boolean cannot hold, so extending what exists beat adding a rival source (D-3.27-1); the consequence is unchanged and pre-existing: **that tab is still edited in code, not in Studio.** On a person's page „Кариера" splits into labelled groups so a Беласица figure and a whole-career figure can never be read as the same claim. **The empty branches were tested** by injecting values, building, checking the HTML and reverting — otherwise „0 голови" would have shipped unverified (D-3.27-13). Build **659/659 — unchanged**, zero new dependencies, **no Sanity write** (only the schema manifest). Decisions **D-3.27-1…-13**; owed **OV-58…OV-62**.

- **[3.24-Code — five small fixes COMPLETE — on branch] A photograph in the archive can finally be looked at, and two of the five things this phase was sent to fix turned out not to be what the brief said they were.** **The lightbox was the real one.** Ace said photographs open small, and they did — but not because the box was too tight: nothing was ever scaling the picture *up* to meet it. The scan rendered at its own pixel size in the middle of a full-screen overlay, measured at **331×372px on a 1280×800 screen — 46,5 % of the height**. It now fills the space the arrows and the close button leave it, **84 %**, and the 1950 newspaper clipping on „Младинска школа" is legible where before it was a thumbnail. **The gutters 3.19 fought for are safer than they were**, because they are now made of the layout rather than of a hand-computed `11rem` that could drift out of step with the buttons. ⚠️ **A scan smaller than the box now looks softer** — that trade was decided in advance and is accepted (D-3.24-1); it affects a minority, since most published scans are big enough to be *downscaled* into the new box. **OV-52 is closed** — the overlay had never been opened in a browser in three phases; it has now been opened on both pages, at desktop and phone width, and stepped through. **The front page's „Момент од историјата" is nailed down**: the 1993 youth-Cup photograph is pinned by id instead of being whatever the ordering returned, and it carries a new link to „Младинска школа" — **the link is tied to the pin, so if the photograph is ever unpublished the link vanishes instead of pointing at the wrong picture.** „Разно" now has a box in „Каде понатаму". ⚠️ **The two stale premises.** The appearances table was cut at 46 on the belief it showed **71** players; it shows **119**, so the cut removes **three** — and those three are **Горан Пандев, Ацо Стојков и Горан Попов**, whose totals are small because they left young. The instruction was explicit that the threshold wins and the real number gets reported, so that is what happened, but **the owner needs to pick the real figure** (**OV-57**). And the „absolute link to the preview domain on За нас" **was not there** — it has been a normal relative link since 3.15, and a read-only sweep of **2 443 Sanity documents** found no such address anywhere in the content either, so nothing was changed and nothing is owed. Build **330/330 — unchanged**, zero new dependencies, no Sanity write. Decisions **D-3.24-1…-7**; owed **OV-53…OV-57**. **PR [#54](https://github.com/DinovLazar/belasica-v2/pull/54) отворен, Vercel preview потврден:** https://belasica-v2-ipzu0y6gj-sunset-services-team.vercel.app — 6/6 бараните рути 200, закачената фотографија и врската кон „Младинска школа“ потврдени на распоредената верзија, пет полиња во „Каде понатаму“, 116 реда кај „Најмногу настапи“, 0 placeholder-чипови, а на „За нас“ 0 апсолутни врски кон preview-доменот.

- **[3.22-Code — „Легенди": four categories as tabs COMPLETE — on branch] The Legends page stopped being one long scroll, and the four names Ace's own list opens with are finally where he expected them.** The page is now **four tabs** — Играчи, Тренери, Претседатели, Репрезентативци — and only one shows at a time, so when the players end, the coaches do not simply carry on. **The reason his list was missing from Тренери is now fixed:** a man used to be filed in one category only, by his highest role, so all **39 player-coaches** sat under Играчи and Тренери held only the men who never played. A person now appears in **every** category he belongs to — which is what Ace said was fine („не е проблем и да се споменуваат и кај играчи и кај тренери"). **Тренери now opens Мартин Алаѓозовски → Панче Стојанов → Александар Стојанов → Васе Беќаров**, his list, in his order. **Претседатели opens with the sitting president** — Славчо Васков-Пинда was sorting second because the alphabet was breaking a tie the dates could not. A **fourth category** holds the ten internationals from Ace's own numbered Drive folder, opening on Горан Пандев; ⚠️ **four men he named have no record in the archive yet** (Васил Рингов, Благој Георгиев, Сашко Пандев, Дејан Илиев) and are therefore absent — owed, not dropped. **„Разно" now shows a photograph on all seven cards.** ⚠️ **Nobody has looked at any of this yet** — no visual sign-off, no design audit; the checks were functional and done against the built page. ⚠️ **One trap worth knowing about:** the first build passed while quietly serving months-old data, and would have shipped a page contradicting `facts.md`; it was caught by checking the numbers against Sanity rather than trusting the green build. ⚠️ **The duplicate Томе / Томче Ефтимов is now visible on the page**, exactly as 3.21 warned — one man in two records, still to be merged in Studio. Build **330/330**, zero new dependencies, no Sanity write. Decisions **D-3.22-1…-9**.

- **[3.20-Code — „Разно" photographs COMPLETE — on branch, PR #49] The seven topics stopped being text-only.** **64 archival photographs** now sit under „Фотографии" on every „Разно" page — 3 on Тиверија, 29 on the младинска школа, 64 in all, and the count in each built page matches its folder exactly. **Every topic now has the jump rail**, where before only the three with records did — which is what Ace asked for („нека ги има сите горе, како што е кај архивата"). The overlay he had fixed in 3.19 is **the same code here, not a second copy of it**, so a wide scan behaves the same way in „Разно" as in the archive. **The book's credit still closes the book's text, not the page** — putting it under the photographs would read as saying his chapter is where they came from, and nobody has said that. ⚠️ **29 of the 64 have a caption; 35 have none and are given none** — they arrived with machine-generated filenames that describe nothing, and inventing a description would be inventing a fact (**OV-50**). ⚠️ **Three things the owner should know.** These are **static files, not Sanity photos**: **Ace cannot manage these galleries in Studio** (the same trade D-3.16-2 made for the text), and **9,3 MB of images now live permanently in the git history**. **Nothing records where the 64 came from** — the 881 ingested photos each carry a provenance line; these carry none, and the blanket rights confirmation in `facts.md` speaks of „all archive photos" without anyone having tied these to it (**OV-51**). And the **gallery's lightbox has still never been opened in a browser** — it is not in the server HTML at all, so neither its new per-topic name nor its behaviour was confirmed (**OV-52**). Build **279/279 — unchanged**, zero new dependencies, no Sanity write. Decisions **D-3.20-1…-8**.

- **[3.19-Code — three pennants, a count on every player, a rail in „Разно", a fixed lightbox COMPLETE — on branch, PR #48] Four things Ace asked for after looking at the site, and one contradiction caught by measuring rather than reading.** **The front door leads with three pennants instead of the crest** — znamenca 1, 6 and 9, in his order, sized to read as a set. A screen reader announces them **once**, as one object, and the label says only what they are: nothing in the archive records their dates or occasions, so nothing was invented (**OV-46** asks him). The club crest is unchanged in the header. **Every player now shows how many matches he played** — 80 of 98, where before only 32 did. Where the book prints no figure the card falls back to the career total the man's own page already shows. **This deliberately reverses a decision from 3.15**, which refused the same fallback while it was still an open question whether the book printed those numbers; Ace has now answered it by asking for the number either way. ⚠️ **Nothing was written into the book's field** — the transcription keeps its provenance — but the card no longer says which of the two sources a number came from (**OV-47**), and **18 players still show nothing** because the archive holds no figure for them at all, including two of the ten on the homepage (**OV-48**). **That homepage band was the contradiction:** the built page showed a number on **2 of its 10 cards** while `/legendi` showed one on all it could — the same ten men, described differently, which the code's own comment said must never happen. Fixed in-phase; it now shows 8, and the two blanks are the two men nobody has a figure for, on **both** pages. **„Разно" reads like the archive now** — the topics split into „Преглед" and „Рекорди" with the same jump rail, cut at the point the chapter turns from story to lists so **not one line of Аце's text moves**. Four of the seven topics have no lists and are untouched. **A wide photograph is readable in the lightbox again**: the arrows used to sit on top of his newspaper clipping, and now they hold a real gutter beside it — on a phone they drop below the picture instead. Build **279/279 — unchanged**, zero new dependencies, no Sanity write. ⚠️ **The roster split has drifted in Studio since 3.17 — it is now 98 · 34 · 28, not 86 · 46 · 28** (still 160 people); twelve gained a `player` role and nobody has reviewed which (**OV-49**, not caused by this phase). ⚠️ **Verification is thinner than this project's rules require: no `/impeccable audit`, no contrast measurements, no Tab check, and the rearranged lightbox was never opened in a browser.** Decisions **D-3.19-1…-8**; owed **OV-45…OV-49**.

- **[3.17-Code — jump rails, back-to-top, reordered statistics, roster divider COMPLETE — on branch] Four fixes to how the site is read, and not one new claim about the club.** The two longest pages after the archive get the same **sticky jump rail** `/arhiva` has had since 2.02 — „Скок по улога" on `/legendi`, „Скок низ статистиката" on `/statistika` — and all three now share **one** component instead of three copies of the same markup. `/arhiva` itself is untouched: its rendered page is **byte-identical**, proved against a baseline build of `main` rather than assumed. **Every page gets a way back to the top** — a navy circle bottom-right that appears once you are a viewport and a half down, returns you to the top, and hands the keyboard back to the start of the page instead of leaving it in the footer. It **does not exist** until it is needed: not in the HTML, not with JS off, and it sits **under** an open photograph rather than floating over it. **`/statistika` now opens with the scorers table** people come for; the curated records close the page instead of standing in front of it. **A season's roster shows where the first eleven end** — a hairline after number 11, on **51 of the 96 seasons**, drawn only when a twelfth name follows. **It says nothing.** No label, no „стартна единаесторка", and invisible to a screen reader — because the book prints a numbered list and never claims those eleven started, and this archive does not add the claim (D-3.17-4). Build **279/279 — unchanged**; zero new dependencies; no Sanity anything; the only `brand.md` change is one line naming the back-to-top button's circle as **the** exception to „radius 0 everywhere" (D-3.17-2). ⚠️ **For the orchestrator:** the brief contradicts itself on where focus lands after the click (the specified code was implemented and the outcome measured — D-3.17-8); its 45-page breakdown is off by one season (`1942`, whose roster stops at seven) although its 51/45 total is exact; and the button is the repo's **seventh** client component, not the sixth. ⚠️ **OV-40 is inherited, not fixed** — between 769 and 898 px the wrapped header pushes a jumped-to section ~18px under the bars on both new rails, and `SiteHeader.tsx` was out of scope. Owed: **OV-43** and **OV-44**. Decisions **D-3.17-1…-9**.

- **[3.16-Code — „Разно“, seven topics from the book COMPLETE — on branch] The archive stops being only seasons and people.** Seven parts of the club's story that fit neither shape now have their own pages: the two **УЕФА Cup** ties, the **младинска школа** that produced Пандев, Стојков, Масев and Балдовалиев, eight editions of **Виареџо**, four **Партизан** visits to Струмица, twenty-nine **Тиверија** derbies with their scorelines, the **стадион** from the 1931 pitch to the arena that carries Благој Истатов's name, and the **„Ајдуци“** from „Блу Драгон“ in 1988 to the north stand. **Not one sentence of it was written here.** Every paragraph is Аце Стојанов's, transcribed character for character — his typos included — and every page ends by naming him and the book, so the claims are **attributed, not asserted**. The transcription is **provable**: the module is generated from a committed source file and re-diffed against it, 101 blocks out of 101 identical. „Разно“ is now in the menu, between „Статистика“ and „За нас“. The pages are **text-only on purpose** — the photographs are a later phase, and nothing here pretends to hold their place. Build **279/279** (271 → 279), zero new dependencies, no Sanity anything. ⚠️ **Two for the orchestrator:** a card summary the brief called verbatim claimed the Виареџо opponents were European when five of them were not, and was corrected (D-3.16-9); and a **seventh nav item pushes the header into two rows between ~769 and ~898 px** — already broken at 768 before this phase, now ~130 px wider, and `SiteHeader.tsx` is outside this phase's edit list (**OV-40**). Decisions **D-3.16-1…-14**.

- **[3.15-Code — „За нас", copy posture, socials, ranked legends, zero placeholders COMPLETE — on branch, PR open] The archive stops saying „coming soon" about itself.** `/za-nas` now carries **Аце Стојанов's own words**, rendered byte-for-byte: he is named as the author of the site, of the Facebook profile *FK Belasica 1956-2006* and of **the book** — which supersedes a standing rule, in place since intake, that the site never mention it (D-3.15-8). The page is dedicated to his late father **Томе Стојанов (1953–2020)**, whose name links to his own page in the archive, and whose figures in the copy (328 league matches in 14 seasons) **match what the archive already holds**. The „page under construction" banner is **gone** — no route is provisional any more. The footer now states, on every page, that its texts and photographs are not free to take; it asks **permission** rather than claiming **ownership**, because this archive publishes material whose rights sit elsewhere and its own legal page promises an unconditional takedown (D-3.15-1). **Facebook and Instagram are real icon links** on both the footer and `/kontakt`, drawn by hand rather than pulled from an icon library — **zero new dependencies**. Legend cards now read **„1. Петар Андреев 555"**, rank and appearance count together, announced properly to a screen reader. ✅ **Ace can finally edit a rank in Studio** — the schema is deployed and **OV-27 closes**. ✅ **Not one `[PLACEHOLDER]` chip renders anywhere on the site** — verified across all 264 built pages. **That is a launch blocker cleared** ⚠️ *but it is not the domain cutover*: PL-4 is still open as an invisible metadata item, and 3.08 is still next. ⚠️ **Owed: OV-38** (a native speaker should read the new copy on screen) and **OV-39 — 48 of the 80 ranked legends still show no appearance count**, because the book extract only captured them for ranks 51–80; the archive probably already knows the rest, but confirming that is Ace's call, not a guess this phase would make (D-3.15-12). Decisions **D-3.15-0…-12**. ✅ **Unlike the previous three phases, the Vercel preview was checked before merging** — nine routes 200 and every change verified on the deployed preview.

- **[3.14-Code — Four owner-requested label & content corrections COMPLETE] Three headings now name what sits under them, and one man's record is corrected.** On `/legendi` the third band stops being „Раководство" — the *body* — and becomes **„Претседатели"**, the role its members are actually placed by and the plural of the chip they already carry; its count noun follows, so it reads **„28 претседатели"** instead of „28 членови" (D-3.14-1). On the season page two `<h2>`s change and **nothing else does**: „Играчи и тренер" → **„Статистика на играчи"** (D-3.14-2) and „Резултати" → **„Резултати и стрелци"** (D-3.14-3). The section **`id`s and the jump-rail labels are untouched** this time — `prikazna`/`rezultati`/`trener` still resolve for every link already shared (D-3.13-3), and each rail label still leads the heading it jumps to. The fourth correction is **content, not code**: `person` `petar-andreev` held `role: ["player","president"]` and the owner states he was never a president; it is now **`["player","trainer"]`, live in `production`** (D-3.14-4). He **does not change bands** — placement is by priority player > trainer > president (D-2.05-2) and he keeps `player` — so band sizes are unchanged at **Играчи 86 · Тренери 46 · Претседатели 28 = 160**; only his chips move, „Играч · Претседател" → „Играч · Тренер". ⚠️ Two things the owner should know: the „Претседатели" label now **asserts** that all 28 are presidents, which nobody has checked (**OV-35**), and **„Резултати и стрелци" over-promises on 19 of the 95 seasons that render results** — every pre-1949 season plus 2012-13 carries scores with no scorers (**OV-36**). `npm run build` 271/271, `lint` and `tsc` clean; no dependency, schema, token or design-token change.

- **[3.13-Code — Two season sections renamed + Тренери/Раководство ordered newest-first COMPLETE — merged to `main` via PR #43, preview review waived by the owner (D-3.13-10)] The pages now say what they hold, and the staff bands read as a chronology.** Two copy fixes on the season page: „Приказна за сезоната" → **„Преглед"**, „Тренер и статистика" → **„Играчи и тренер"**, with the jump-rail label renamed alongside each heading so a rail link cannot misdescribe its own target (D-3.13-1). The section **`id`s stay `prikazna`/`trener`** so links already shared keep resolving (D-3.13-3). Third fix: **Тренери and Раководство on `/legendi` stop being alphabetical** and run newest-first by most recent service — a year **derived at build time from `season.trainer` and from the term in each official's biography, with no new field, no schema change and no Sanity write** (D-3.13-4). **The year is never rendered**; it is a sort key only, and the bands remain a chronology rather than a ranking. **46/46 trainers and 28/28 officials resolve**; a person who ever stops resolving falls to the end of their band alphabetically (exercised with synthetic data, then reverted). **The Играчи band and every season page's `<section id>` set are byte-identical to `main`**, both diffed against a baseline build. Build **271/271 — unchanged**; `tsc` and lint clean; **zero new dependencies**. Decisions **D-3.13-1…-10**. ⚠️ **One row diverges from the brief's supplied table** — the brief's tie-break *rule* and its *table* disagree for Александар Оздоленовски; the rule was implemented and the divergence reported (D-3.13-7). Three items owed: **OV-32…OV-34**.

- **[3.12-Code — Biographies, ranking, scorer threshold, photographs, footer link COMPLETE — committed and pushed directly to `main` on 2026-08-02, D-3.12-9] The archive can finally be read.** Where 3.11 dropped the book’s **season** half into the repo, this phase takes its **people** half: chapters 9 and 10 extracted to `data/book/legends.json` and written into `person.bio` **verbatim** — **126,297 characters, 60 people**, one Portable Text block per source paragraph, each carrying its line number in the book. Nothing summarised or composed (D-3.12-1). The remaining 26 ranked players keep their 3.02F summary, because the book gives them nothing longer to transcribe. **The Играчи band is ordered by the club’s all-time appearance ranking**, via a new `person.legendRank` (1–80) — not by `careerStats.appearances`, which would drop the fifteen players the book ranks on a range to the bottom of the page (D-3.12-2). The homepage’s ten follow the same key, which **changes that band’s membership** (D-3.12-3). **„Најдобри стрелци“ stops at 21 goals** — 28 rows, ending on Ристо Панов — and says so under the heading (D-3.12-5). **Петар Андреев’s years → `1974–1995`.** **Four photographs published**: Марко Божинов’s portrait and squads for 2008/09, 2010/11, 2022/23 (D-3.12-7). **The footer links the Belasica fan page**, labelled as a fan page because this archive does not run it — PL-15 cleared in the footer, still open on `/kontakt` (D-3.12-6). Build 271/271, `tsc` and lint clean; zero new dependencies. Decisions **D-3.12-1…-8**. ✅ **Committed and pushed to `main`** on 2026-08-02 — directly, without a PR, at the owner’s instruction (D-3.12-9), so 3.12 carries no Vercel preview URL. ⚠️ **A recovered mistake is logged (D-3.12-8):** the phase was begun against `origin/main`, missed the three unpushed commits on the owner’s machine, and overwrote 3.11’s files; **all were restored from `HEAD`, nothing was lost**, and this phase was renumbered 3.11 → 3.12. Five owed items: **OV-27…OV-31**.

- **[3.11-Code — Book extraction data drop + season content fill COMPLETE] Аце's book is in the repo, and the script that puts it on the site is written but not fired.** The first phase here that ships **no application code at all** — not one file under `src/` outside `src/_project-state/`. **`data/book/`** holds seven tracked files (~6,3 MB) machine-extracted from „ФК Беласица – гордоста на Струмица" (финална верзија 04.10.2025): **96 сезони · 2.267 натпревари · 2.683 записи за стрелци** (3.270 гола со минута) **· 2.170 записи во составите · 28.504 збора наратив**, with **0 unused lines** from the book's season part. Every match and squad row carries **`sourceLine`** — the book's original line, verbatim — so the three typo fixes (`Вотра`→`Втора`, `11 Октмври`→`11 Октомври`, `Кумананово`→`Куманово`) are reversible by inspection (D-3.11-3), and player-name variants like Пантазиев/Пандазиев are **deliberately not merged**, because merging asserts an identity the book does not (D-3.11-2). Consequence stated plainly: scorer tallies group by the book's surname string, so distinct players sharing a surname across eras collapse together — a reading aid, not a career record. **`scripts/fill-season-content.mjs` ships unrun.** It fills the **existing** `season.story` / `season.results` `block[]` fields — **no schema change, none needed** — every write is `setIfMissing` so existing content is never overwritten, and keys are a deterministic SHA-1 committed with `autoGenerateArrayKeys: false`, making the patch idempotent. **The `--commit` run is the owner's to trigger; this phase deliberately never made it.** The dry run exits 0 across all **96** seasons and writes `docs/ingestion/season-content-report.md`: **96 end with a story** (66 already live from 3.02-Story + 30 new), **95 with results** (10 already live from 3.04 + 85 new), and **`2025-26` is the only „нема"** — a season still in play. ⚠️ **Three seasons need Аце before they can be called verified:** `1926-1930`'s twelve lines are **assembled from the book's sentences**, and `1922-26` / `1936-37` carry a **note** because the book prints no results. That is machine-readable rather than buried in prose — every season carries **`resultsProvenance`** (92 печатена листа · 1 склопено · 2 белешка · 1 нема), printed as its own report column (D-3.11-4). **OV-14 is half-settled:** the book's own summary table reproduces **exactly** as `2275 · 1030–421–824 · 3907:2976 · 2885`, re-derived from `master-table.json` here rather than quoted, so the figure OV-14 calls „the book" **is** the book's and the spreadsheet gap is bounded at 10 matches / 4 wins / 12:6 goals / 14 points — **Аце still decides which wins; nothing was applied.** **D-2.01-2's premise is void** (the book **is** the per-match source it said did not exist) **and its Status is deliberately untouched** — reopening the content model stays an owner decision, so `schemaTypes/index.ts` is byte-identical and `match` stays unregistered (D-3.11-1). ✅ The `_to_delete/` junk 3.07 flagged is **gone**. `npm run build` **271/271 — page count unchanged**, lint + `tsc` clean, **zero new dependencies**, **no Sanity write**. ⚠️ **Two DoD items reported UNMET, both brief/repo mismatches, neither a code fault:** most of the drop was **already committed and pushed** as `7ef3547`, and rewriting a shared pushed commit was refused (D-3.11-5); and **no GitHub Action exists** to review the PR — `.github/` is absent, D-1.01-4 dropped the gate by owner instruction, so CLAUDE.md's self-review ran instead (D-3.11-6). Decisions **D-3.11-1…-6**.
- **[3.10-Code — Contact banner, Latin search, 1982/83 hero COMPLETE] Three owner-requested corrections to pages that were already live.** **`/kontakt` stopped apologising for itself:** the „Страницата е во подготовка" banner is deleted, retiring D-2.05-1 for that page along with the two same-day rewrites it forced (D-3.03b-2/-3). The form, the `mailto:` link and the PL-15 socials chip are untouched — **PL-15 stays open**. **The legends search moved into the navy header and learned Latin.** `LegendsBrowser` now renders `PageHeader` itself with the field as its `children` under a new „160 личности" meta line, so the field no longer floats alone in a white band — measured at 375/768/1280/1408, no strip at any width, only the standard `py-section` rhythm. The matcher is a new `src/lib/translit.ts`: a per-letter table of accepted Latin spellings walked by a backtracking substring matcher, with the variant sets **overlapping on purpose** (`c` → ц/ч/ќ, `z` → з/ж/ѕ) because over-matching is the right failure direction for a 160-name filter (D-3.10-7). **All 30 DoD query rows return exactly their specified counts**, verified twice — in Node against the live roster and again live in the browser — and **Cyrillic search is provably unchanged**: an exhaustive comparison of **9,871** distinct Cyrillic substrings against the old `normalise().includes()` behaviour returns identical result sets, every one. No debounce and no memoisation were needed: worst measured pass over 160 names is **0.23 ms**, including adversarial queries. **The homepage opens on history.** The hero is pinned to the 1982/83 squad photograph via module constants passed as query parameters, with `heroPinned` added *beside* the two older sources so the fallback chain stays load-bearing — **exercised, not assumed**: pointing the slug at a non-existent season fell back to the 2025/26 photo with no placeholder. Alt follows the same order and renders „Тимска фотографија — Сезона 1982/83", derived from the fetched title rather than hardcoded. „Момент од историјата" now excludes the hero by `_id`, closing a trap where one caption typed in Studio would have put the same picture at the top and bottom of the page (D-3.10-5). ⚠️ **One DoD item is not met and is escalated:** at **375 px** the mandated `3/2` base ratio cannot show a 1.934:1 photograph whole — the visible window is **11.2 %–88.8 %**, so 22.4 % is cropped and the outermost kneeling player at each edge is cut. `objectPosition` cannot fix it (at that width it has no vertical effect at all and can only trade one edge for the other); the ratio is the owner's call, so it was followed and the conflict reported (D-3.10-6). Contrast measured against each element's **real** backdrop: label 14.95:1, count/meta 9.95:1 on navy; input text 18.11:1 and placeholder 6.69:1 on the white field (D-3.10-8). Focus ring confirmed **orange 3 px / 2 px offset** under a real Tab. `npm run build` **271/271** — page count unchanged; `tsc` and `npm run lint` clean; prettier applied; **package.json and the lockfile untouched — zero new dependencies**; no schema change, no `brand.md` token, no Sanity write. Decisions **D-3.10-1…-9**.

- **[3.09-Code — Performance & accessibility COMPLETE] The launch quality gate now has numbers behind it.** All eight template types measured from scratch, fixed, re-measured with the identical method, and the method written down in `docs/audits/` so **3.11 can repeat it** (108 local Lighthouse runs + 34 against Vercel). **Desktop 100 on all nine sampled routes; mobile ≥95 on eight of nine** — `/arhiva` **86 → 94**, `/legendi` **89 → 95**. Accessibility **100**, CLS **0.000**, TBT **≤10 ms** throughout. **The largest defect was invisible without the trace:** the LCP element on `/arhiva` and `/legendi` sat inside a `Reveal` at `opacity: 0` waiting for hydrate → observe → a 260 ms transition; an element at `opacity: 0` cannot be the LCP, so the paint waited — `elementRenderDelay` **370 ms** / **331 ms** against **14 ms** on the never-wrapped homepage. `Reveal` gained `immediate` (D-3.09-3). Also: exactly one priority image per template, read from the trace rather than guessed (D-3.09-2); the crest off `next/image` (it is `unoptimized`, so next/image added only a 33 KB preload on every route); **AVIF ahead of WebP with no photograph's `quality` reduced** (D-3.09-4); and **`@sanity/image-url` removed from the client bundle** it had been reaching through `LegendsBrowser → LegendCard → PhotoFrame` — 17,266 bytes, `/legendi` page chunk **6.74 → 3.79 kB** (D-3.09-1). ⚠️ **Lighthouse Accessibility was 100 before any change and missed two real failures.** `/statistika` scrolled **the page** 279 px sideways at 375 px — `sr-only` labels are `position: absolute` inside a `static` overflow container, so they escaped its clip and stretched the document (D-3.09-6); and the breadcrumb separator measured **3.48:1** (D-3.09-7). Both fixed; the manual pass across 9 routes × {1280, 375, 320} then found **zero** contrast failures, **zero** sub-24×24 targets, **zero** focus obscured by the sticky header (hit-tested, not rectangle-compared), and re-verified all eight lightbox behaviours. ⚠️ **Two items deliberately left open for the owner:** `/arhiva` stays at **94** on mobile because the only remaining lever — dropping the 92 KB font preload — was measured to reach 95/98 while making homepage FCP bimodal (**760 / 1518 / 1512 ms**), which is D-3.04d-5's principle applied to type (D-3.09-8); and **the Vercel preview did not reproduce the local numbers** (`/arhiva` 87, `/legendi` 88), with the cause unresolved — see the register. Decisions **D-3.09-1…-8**.

- **[3.07-Code — „Правни информации" + footer link COMPLETE] The archive now states in public what it is, who it is not, that AI helped build it, and how to get a photograph taken down.** One new static route, `/pravni-informacii`, renders the owner's final ten-section legal text — unofficial-archive declaration, purpose, the **AI-assisted-build disclosure**, data-accuracy caveat, the photo/copyright posture, club marks, liability limit, external links, amendments, contact. **The strongest sentence on it is a promise, not a disclaimer:** anyone who owns rights in material published here can ask, and it comes down „без одлагање и без прашања", no legal process needed (D-3.07-3). That is an **operational** commitment — `info@belasicahistory.mk` now has to be watched — and it is the first public answer the site has ever given to the standing rights caveat behind OV-RIGHTS. **Verbatim was proven, not eyeballed:** the copy lives in a `SECTIONS` data array (D-3.07-7), so the rendered text could be extracted from the built HTML and `diff`ed against the brief — **41/41 lines identical**. The one character-level judgement is the two quote pairs, rendered „…“ (U+201C) to match Macedonian convention and the repo's existing 4 instances, against 0 of U+201D (D-3.07-9). **The contact email stopped being a placeholder:** VERIFIED in `facts.md`, it now renders from one `CONTACT_EMAIL` constant on three surfaces (D-3.07-6), and because PL-3 was open on **two** of them, `/kontakt`'s chip was cleared alongside the footer's — otherwise that page would have declared it had no email directly above a footer displaying one (D-3.07-5). The link sits in the footer's **bottom bar** beside the ©, never the header: `src/lib/nav.ts` and `SiteHeader.tsx` are absent from the diff (D-3.07-2). Measured on the production build at **375 / 1280 / 1408**: zero horizontal overflow and no element wider than the viewport at any width; the 62ch measure holds (615px at 1408); contrast **9.90–14.95:1** across every new pair — computed with a hand-rolled oklab→sRGB conversion after Chrome's own readback proved unable to parse the theme's `oklab()` values and, separately, was found to **lag one read behind** on `:focus-visible` (a measurement trap worth knowing: it first appeared to show a broken 50%-alpha focus ring that does not exist). Both rings confirmed on the real elements — **3px solid navy** on paper, **3px solid orange** on navy, 2px offset. **Zero chips in the page's `<main>`.** Build **271/271** (270 → 271), `/pravni-informacii` reported `○ (Static)` with an **empty** Revalidate column (D-3.07-8); `tsc` + lint clean; **zero new dependencies**; no schema, token or Sanity change. Decisions **D-3.07-1…-10**.
- **[3.06a — Crest crop, season reorder, legend portraits, record board · SHIPPED TO `main`, PAPERWORK MISSING]** Merged as [PR #35](https://github.com/DinovLazar/belasica-v2/pull/35) plus `bcd6196` and `2c94141`, but it filed **no completion report** and logged **no decisions** — `grep -c "D-3.06" decisions.md` returns **0**. Reconstructed from the diff so the snapshot is not simply wrong: the crest artboard was cropped and the white blocks behind it dropped (`public/crest.png` 359 KB → 198 KB, `public/crest.svg`, `SiteHeader.tsx`); the season page was reordered and gained a **league-record board** (new `src/components/archive/SeasonRecordBoard.tsx`, `arhiva/[slug]/page.tsx` ±358 lines); `/legendi` gained a **client-side name search** over the role bands (new `src/components/legends/LegendsBrowser.tsx`); homepage and legends-index legend portraits changed. **The reasoning behind all of it is permanently unrecorded** — 3.07 deliberately did not invent it (D-3.07-10). ⚠️ The same phase also committed **11 files of git plumbing junk** under `_to_delete/`.
- **[3.05b-Code — Homepage owner follow-up + season photo lightbox COMPLETE] The homepage became a front door again, and an archive photograph can finally be looked at.** Three things shipped. **(1) The homepage stopped rendering the whole archive.** `HOME_QUERY` read `person` and `clubRecord` unsliced — correct at 5 players and 7 records, wrong at 160 and 30 — so 86 legend tiles and 30 record cells scrolled past before anything else. Legends are now the **ten most-capped players**, ranked and sliced in GROQ (`coalesce(careerStats.appearances, -1) desc, name asc` — the `-1` keeps an unrecorded player below one with a single appearance, and the career total is never summed from `season.squad`, D-2.01-3); records are **six**, chosen by an **explicit label whitelist** so a 31st record published in Studio cannot change the front page (D-3.05b-1). Capping the strip surfaced a layout consequence the old 7 had hidden — five cells in a 2/3-col grid leave an empty track, and since the cells sit *on* the orange ground that track paints as an orange rectangle next to the one deliberate orange cell — so the last cell now widens to close the row (D-3.05b-2). **(2) The crest is vector.** Header and hero render **`/crest.svg`** (916×1294, pure paths, 31 KB, zero `/_next/image` requests), a repaired rebuild of the same artwork whose source was clipped on the right edge with the pennant point cut flat. Every raster dimension was preserved (864×1220 · 400×565 · 512² · 180² · 64²), so `layout.tsx`'s Open Graph block is **untouched**, and the header re-measured at **78px** at 1280 and 375 — `--spacing-header` did not move. ⚠️ **D-3.05-11's 402 has expired:** production's `/_next/image` now returns 200 at `w=640/1920/3840`, measured before deciding; the crest keeps `unoptimized` anyway because Next refuses SVG without `dangerouslyAllowSVG` (D-3.05b-7). **(3) The season gallery opens.** New client component **`PhotoLightbox`** shows a scan at up to 92vw × 82vh on navy with its date, caption and an `aria-live` „N / M" counter; navigation **wraps**, both arrows vanish for a one-photo season, and focus is trapped then returned to the triggering thumbnail. `PhotoGrid` and the season page stay **server** components — URLs are built server-side and only plain data crosses — and the lightbox is **opt-in**, set only by the season page, so `/legendi/<slug>` is unchanged (**0** trigger buttons there, verified). ⚠️ **Three latent defects were caught by measuring, not looking, and fixed in-branch:** the scroll lock was a **no-op** (`body { overflow: hidden }` never propagated because `document.scrollingElement` is `<html>`; the page still scrolled 3000 → 3800 with the overlay open — D-3.05b-5), and both focus restoration and the entry motion hung on **`requestAnimationFrame`, which does not fire in a hidden document** — focus stranded on `<body>`, dialog stuck at `opacity-0` while trapping the keyboard (D-3.05b-6). Stated honestly: with a visible page rAF fires within a frame, so no user watching the screen could have hit either. Verified on the built output — 10 cards (2 portraits + 8 monogram tiles, zero grey boxes), 6 cells in the briefed order, open/wrap/Escape/backdrop/focus-return all exercised, scroll offset identical across open→close, controls exactly **48×48**, new contrast **9.10–14.95:1** worst-case, no horizontal scroll at 1280 or 375 overlay-open or closed, `/statistika` still 30 in 5/13/8/4, `/legendi` still 86·46·28 = 160. Build **270/270**, lint + `tsc` clean, **zero new dependencies**, no schema/token/`@theme`/Sanity change. Decisions **D-3.05b-1…-7**.
- **[3.02F-Code — Build hardening + Клупски рекорди COMPLETE] The build stopped being able to lose a page, and 30 curated records stopped being invisible.** Content phase 3.02F took the archive to 92 final tables, 160 people and 30 `clubRecord` docs, which strained two things at once. **The build:** 270 static pages × 2 reads each against a CDN that intermittently connect-times-out (D-3.05a-9). The fix is two layers — a shared `fetchOrThrow` on the season and person templates (5 attempts, exponential backoff 1/2/4/8 s, every retry logged, then a throw naming the slug) plus Next’s own `experimental.staticGenerationRetryCount: 2`. **The diagnosis mattered more than the fix:** the brief said the season template had no error handling; it had the *wrong* error handling — a `catch → notFound()` standing since 2.03 that turned a network blip into a **silent 404 for a real season**, leaving the build green and the archive one page short. Both retry numbers were measured against three-run batches rather than assumed (3 attempts → 2 failures, 5 → 1 failure, 5 + page-retry → **3/3 clean while absorbing 125 transient read failures**), and the loud-failure path was proven by pointing a scratchpad mirror at an unreachable host. **The records:** `Клупски рекорди` now leads `/statistika` with all 30 documents in four Studio-titled groups. It deliberately does **not** use the brand’s scoreboard — one value is a 481-character roster of internationals, and `u-stat` would turn it into a wall — so it is a label/value register that keeps the scoreboard’s label vocabulary and the 6px orange bar (D-3.02F-C-4). Everything was verified against the **built output**: 160/160 person routes 200, bands 86/46/28, tables 55/71/92, coverage line 92 seasons, unknowns sort last both ways, zero `0`s for unrecorded metrics, zero chips in `<main>`, zero horizontal scroll at 1280/375, new contrast 14.95–18.11:1. **No new token, dependency, schema change or Sanity write.** Decisions **D-3.02F-C-1…-6**. ⚠️ Shipping the records made a source contradiction visible on one screen (**OV-13**); this phase records six content questions for Ace (**OV-13…OV-18**) and resolves none of them, by design.
- **[3.05 — „Трибина" adopted site-wide COMPLETE] The site the owner picked is now the site.** Lazar chose direction **В „Трибина"** from 3.05a and asked for full adoption, so `brand.md` was amended to В and every route was rebuilt on it. **The key move was the token layer:** rewriting `globals.css` `@theme` to the new palette, faces, clamp type scale, `0` radii and 1248 page width flipped ~5,100 lines of TSX across 39 files at once, leaving only В's own vocabulary — full-bleed navy/paper blocks that butt together, the **6px orange bar** banding the header, footer, tiles and cards, the **scoreboard** records strip (orange ground, 2px gaps as rules, featured cell orange with navy ink), and hover as a **fill swap** — to be applied by hand. Because В's chrome is part of В, `SiteHeader`/`SiteFooter` were restyled (not rewritten, so `--spacing-header` survived with a re-measure 77 → **78px** and every `top-header`/`scroll-mt-header` consumer still lands right), and a shared navy **`PageHeader`** now opens `/arhiva`, `/statistika`, `/legendi`, `/za-nas` and `/kontakt`; the season and person heroes moved onto the same navy block with the breadcrumb pulled inside (`Breadcrumb` gained `onNavy`). `PersonHero`'s two variants both went navy, so a person page stops changing shape depending on whether a portrait exists. `PhotoFrame`'s `fit` now picks the **surround** as well as the crop — `cover` is a hard block, `contain` keeps the mist mat the archival mixed-quality rule needs (D-3.05-5). The three radius tokens are **kept, defined at `0`**, so no stray or future `rounded-*` can reintroduce rounding (D-3.05-10). **The most important find was invisible:** the new `outline`-based focus ring, defined in `@layer components`, was being beaten by an `&:focus-visible { outline-style: none }` rule in the vendored shadcn sheet's `@layer utilities` — **every focus ring on the site was off**, and the old code had been immune only because its ring was a `box-shadow`. It is now unlayered, with `outline-color` as its own longhand (the shorthand let the base `outline-ring/50` through at 50% alpha). Verified on a real `Tab`: 3px navy on light, 3px orange on navy. Two more WCAG fixes followed (three sub-24px targets; card heights). Verified at 1280 + 375 across all 8 routes: **0** contrast failures, **0** undersized targets, **0** horizontal scroll, one H1 each, no heading skip, every image alt'd; `/statistika`'s tables scroll inside their own region with the first column sticky while the page does not move. `npm run build` (96 seasons + 88 people + 8 static) + `npm run lint` + `tsc` clean; Impeccable detector `[]`; audit **20/20**. Decisions **D-3.05-1…-10**.
- **[3.05a — Visual direction exploration COMPLETE] The owner can now look at three different-looking versions of the same homepage and pick one.** The audits kept scoring the craft high while the owner stayed unhappy with the look, so this phase treats the *direction* as the open question and builds it three ways on real content. **`/predlog-a` „Спортски весник"** — the archive as a digitised vintage sports paper: aged paper surface, a static masthead with a double rule, hairline navy rules dividing every zone, navy-duotone photographs with print captions, a column-set feature with a drop cap, the records as an agate **box score**, the decades as a dotted-leader **index**; Playfair Display + PT Serif + PT Sans Narrow; orange demoted to marker-only, never a letterform (stricter than D-1.02-1). **`/predlog-b` „Клупски музеј"** — the archive as a room: a deep-navy wall carries the page, paper survives only as **mat board** around generously mounted photographs with a brass keyline and a spotlight wash, records are **plaques**, the vertical rhythm is roughly double the live site's; Cormorant Garamond + Commissioner. **`/predlog-c` „Трибина"** — a contemporary club identity: oversized condensed Cyrillic caps, hard navy/orange **blocking** with panels butting together, a crest-forward hero, a **scoreboard** stat strip, the tightest rhythm and the only sticky header; Oswald + Golos Text, with orange promoted from marker to a **block fill carrying navy ink** (5.8:1 — D-1.02-1 is not broken, the pair is inverted). **Same content everywhere:** one GROQ query transcribed unchanged from the live homepage plus one copy module feed all three (D-3.05a-1). **Seven typefaces, Cyrillic verified from each font's own `cmap`** rather than from Google's subset declaration — all 62 Macedonian letters incl. Ѓ ѓ Ѕ ѕ Ј ј Љ љ Њ њ Ќ ќ Џ џ (D-3.05a-2); Anton, Bebas Neue and Archivo Narrow were rejected for having no Cyrillic at all. Fonts are declared **per variant** because `next/font` preloads at module granularity — one shared module had every route preloading **26** woff2; now 18/8/8, live homepage unchanged at 4 (D-3.05a-3). Three craft findings were caught by measuring rather than eyeballing: **Б's brass hero label fell to 1.99:1** over the worst case a photograph can present and is now paper at 13.5:1 (D-3.05a-5, re-deriving D-3.03-5); **four of six placeholder-chip states failed AA** where a hatch line crosses a glyph, since the automated sweep skips anything over a background image (D-3.05a-8); and the **Impeccable detector** flagged В's 6px orange left-tab on all 15 tiles as the classic generated-UI tell, so it was rotated to the top edge where it became the same bar that opens the header and closes the footer (D-3.05a-7). `docs/design-handovers/Part-3-Phase-05a-Directions.md` records each direction's intent, its exact token deltas vs `brand.md`, and what adopting it would touch — **with no recommendation; the pick is the owner's**. **No shared component, no shared token, no `brand.md` change, no schema change, no Sanity write, no new npm dependency.** `npm run build` (120 pages) + `npm run lint` clean. **Owner feedback received and actioned mid-review:** Lazar picked **В „Трибина"** as the direction he likes but rejected its hero, so the hero was rebuilt as a **matchday poster** — the photograph now leads full-bleed and the crest is a white block capped by the orange bar, pinned over the picture's bottom edge with the wordmark locked to it on solid navy (D-3.05a-10). The first cut repeated the crest AND the wordmark the sticky header carries 60px above, put the badge on a plain white rectangle, and used none of the direction's own blocking. А and Б are untouched. Decisions **D-3.05a-1…-10**. Ships via **[PR #29](https://github.com/DinovLazar/belasica-v2/pull/29)** (`phase-3.05a-direction-exploration` → `main`) — **preview verified**: `https://belasica-v2-git-phase-305a-directio-952037-dinovlazars-projects.vercel.app` returns 200 on all three `/predlog-*` routes and all six live routes, with `noindex, nofollow` on the three variants and absent from the live six. **Owed to Lazar: open all three at desktop and on a phone and pick a direction — A, B, C, or a named combination. 3.05 is blocked until then** (5-item checklist in the completion report).
- **[3.04d — Audit fixes COMPLETE] The site now renders the type scale `brand.md` specifies, every small link is comfortably tappable, and the site is ready to be found and shared.** An Impeccable audit (18/20) surfaced one P1 and a short tail; all five recommended passes ran in one phase. **(1) The `cn()` bug is dead at the source** (D-3.04d-1, closes D-3.04-12): `src/lib/utils.ts` registers the eight custom `@theme` font sizes (+ `tracking-overline`) with `tailwind-merge`, so a size and a colour in one `cn()` call no longer silently drop the size — every `SectionOverline` now renders **12px** (was 16), every `PlaceholderChip`, `RoleChips` chip and header/footer nav link **14px** (was 16), proven by a 14-case node script against the shipped file and measured in-browser at 1280 + 375; the two old workaround comments (SeasonRecordList, MattedPhoto) were rewritten so nobody re-learns the dead bug. Maintenance duty: a new `@theme` type token must also be registered in `utils.ts`. **(2) Tap targets** (D-3.04d-2): jump-rail links 21→**37px** with both rails' heights **byte-identical** (46/47px — the sticky/anchor arithmetic untouched), footer links **29px** and breadcrumbs **25px** via padding+negative-margin (zero layout shift; breadcrumb capped so wrapped rows never overlap). **(3) Launch metadata** (D-3.04d-3/-4): `metadataBase` (Vercel origin, synced hardcode in 3 files), Open Graph + Twitter `summary` card with the crest at its true 864×1220, per-page metadata confirmed already present on all routes, **`robots.ts`** (allow all, disallow `/studio`), **`sitemap.ts`** (6 static + 96 seasons + 7 people = **109 URLs**, live GROQ with `_updatedAt`, ISR 60, static fallback). **(4) Performance** (D-3.04d-5): LCP images carry **`fetchPriority="high"`** (Next 15.5's `priority` alone does not hint — verified in `node_modules`), homepage legend portraits stop overfetching (106→21 KiB est.); Lighthouse v13.4.1 local prod: home mobile **92** simulated (was 88 at 2.08) / **96 with devtools throttling** (≥95 gate met under the applied method), season mobile 92–93, desktop **99**, A11y/SEO **100** everywhere; the hero photo's quality deliberately NOT reduced for simulator points (archive dignity). **No new dependency, no new token, no schema change, no Sanity write.** Adversarially reviewed by a 3-lens workflow (2 stale-comment P3s found and fixed; 0 other confirmed findings). `npm run build` (117 pages — +robots +sitemap) + `npm run lint` clean; detector: 1 known false positive (the documented orange-marker motif). Decisions **D-3.04d-1…-6** (incl. the phase-id rename 3.04c→3.04d to avoid colliding with Cowork's `D-3.04C-*`). Ships via PR `phase-3.04d-audit-fixes` → `main`.
- **[3.04b — Owner follow-up: every section on every season page + correct archive photos COMPLETE] The archive now reads the same from page to page, and the cards finally show the photo the curator chose.** Two owner-requested changes on top of 3.04. **(1) All five season sections always render** (D-3.04b-1): a season with no table, no trainer, no results or no story keeps its heading and shows one line of structural copy („Во архивата сѐ уште нема…“) in a bordered card, instead of the section disappearing. This **reverses the self-omit rule** (D-2.02-3 / D-3.01-6) for the season page — the owner was shown the cost (86 pages gain an empty „Резултати“, 30 an empty „Приказна“) and chose structural consistency. Two behaviours were kept: the two halves inside „Тренер и статистика“ still self-omit individually, and a season with nothing at all still collapses to the single archive notice (D-2.02-8). The photo-less **hero is untouched** — still the navy title band, never a greybox (2.02 §6.2b). Deliberately **no `PlaceholderChip`** in the empty notes: always-on sections would have emitted ~170 chips across the archive and swamped the placeholder register, which is a launch blocker checked at 3.05. **(2) `/arhiva` card photos corrected** (D-3.04b-2): the index now leads with `season.teamPhoto` — the field 3.01 modelled and 3.02/3.04-Cowork filled for exactly this purpose. It had been ignoring it and taking `[0]` of the season's photos by a `date` key that 886 of 889 photos lack, so **69 of 96 cards showed an arbitrary image** (a clipping, a table screenshot, a formation graphic) and the index disagreed with the season page about the same season. The old back-reference stays as the fallback for the 13 seasons with no `teamPhoto`, now on the deterministic 2.08 key. New component **`SeasonSectionEmpty`**. **No new dependency, no new token, no schema change, no Sanity write; ISR 60.** Verified on all 96 built pages (five sections + five rail links **96/96**; empty notes rezultati 86 · prikazna 30 · trener 13 · tabela 8 · fotografii 5, matching the data exactly; **0 chips**; 83/83 cards on the curated photo, 0 mismatches, no card lost its image); at 1280 + 375 (no horizontal scroll, rail scrolls inside its own container, H1 44 → H2 30 → H3 22); `npm run build` (115 pages) + `npm run lint` clean; all routes 200, unknown slug 404. Decisions **D-3.04b-1, D-3.04b-2**.
- **[3.04 — Season page redesign & restructure COMPLETE] The heart of the archive finally shows what the archive holds.** All **96 season pages** were rebuilt from one template into the crnobelanostalgija-style long-scroll document, and the five fields 3.01 added plus `story` **now render for the first time** — the 3.01 known issue „nothing renders it yet" is closed. Structure: breadcrumb → a **navy title band with that season's `teamPhoto` mounted on a mist mat inside it** (D-3.04-4) → a **non-sticky jump rail** listing only the sections that rendered (D-3.04-5) → **Табела** (the `tablePhoto` scan, D-3.01-2) → **Тренер и статистика** (`trainer` in the heading + `lineupAndStats`) → **Резултати** → **Приказна за сезоната** → **Фотографии** → **prev/next season** + back-links. Every section self-omits; a photoless season gets the same navy band **without** the photo (2.02 §6.2b, never a greybox); the two lead photos are **excluded from the gallery by `_id`** (D-3.04-2, superseding D-2.02-6, whose reason — „the hero shows no caption" — no longer holds now that the lead renders its own caption **and date**). Four new components: **`MattedPhoto`** (mats a solo scan at **its own** aspect ratio — the live leads span 0.49–2.63 and 0.36–2.86, so any one fixed frame letterboxes the extremes, D-3.04-3), **`SeasonAnchorNav`**, **`SeasonRecordList`** (a dense renderer for the two record fields — all 1401 `lineupAndStats` blocks are single lines, so `SeasonStory`'s prose rhythm was wrong for them; `SeasonStory` itself untouched, D-3.04-9) and **`SeasonNeighbourNav`** (the archive spine, ordered `decade` then **`slug.current`** — the brief's `title` key is chronologically wrong on six seasons, D-3.04-1). `StandingsTable`/`SquadTable`/`PersonChip` are now **unrendered but retained** with legacy headers, to be deleted with the legacy fields after 3.06 (D-3.04-6). **Only `brand.md` tokens; no new token; no new dependency; no schema change; no Sanity write; ISR 60.** Verified by a **structural sweep of all 96 rendered pages** — sections match the live data 96/96, one `<h1>` each, no heading-level skip, every image alt'd, every anchor carries `scroll-mt-header`, **0 lead-photo leaks into a gallery** — plus in-browser checks at **1280 + 375**: no horizontal scroll (the rail scrolls inside its own container), contrast 4.68–14.43 (**all ≥ AA**), an anchor jump landing at exactly **77px** (the header's bottom edge), and the 2px navy focus ring measured on real elements. `npm run build` (115 pages, all 96 seasons SSG) + `npm run lint` clean; all six routes 200 and an unknown slug 404s. **`results` became content-verified mid-phase**: it was empty on all 96 at the start, the Cowork pass published the first **10 seasons** (1930-31…1940-41), and the section plus its jump-link appeared on those pages by themselves via ISR — no code change, no redeploy. Decisions **D-3.04-1…-12**. ⚠️ **Two findings handed on, neither introduced here:** a repo-wide `cn()` bug that silently drops `brand.md`'s custom `text-*` size tokens (D-3.04-12) and 3 duplicate photo documents from the 2.09 ingestion (D-3.04-10). Ships via a PR (`phase-3.04-season-redesign` → `main`).
- **[3.03 — Homepage + footer + navbar redesign COMPLETE] The front door was rebuilt to lead with the club's identity and its legends — the design quality that sank V1 is now the first thing you see.** The homepage follows the **seven-section flow** (Hero → Story → Legends → Numbers → Decade explore → Moment → Quick links + footer), all from **real Sanity content** (ISR 60): a photo-forward hero (crest + wordmark over the deterministic most-recent `teamPhoto`, the 2025/26 squad); the verified `siteSettings.description`; a legends marquee (5 players — 3 vintage B&W portraits lead, 2 navy-initials tiles follow, portraits-first, D-3.03-2); a **navy** records band from the 7 `clubRecord` docs (championship featured, D-3.03-3); an 11-tile decade grid with real counts deep-linking into `/arhiva`; a full-bleed 1993-Cup **moment** photo (deterministic, D-3.03-4); and cleaned-up quick-link cards. The page reads as one system via a **navy/paper rhythm** and the recurring **orange-marker** motif — it beats the „better than a WordPress theme?" bar. **Footer redesigned**: the old fake demo email/phone/socials (PL-9) are gone, now visible `[PLACEHOLDER]` chips; the „неофицијална архива" line + statement come from `src/lib/facts.ts`. **Navbar refreshed** (sticky navy, 6 items, orange active-underline, mobile menu intact; hamburger now a 44px tap target — header still 77px; a non-color hover underline added). Key craft call: a new `SectionOverline` **`onPhoto`** variant (orange **marker** + PAPER text) — pixel-sampling showed orange text over the hero photo is only ~3.7:1, below AA, so paper carries the text and orange stays a marker (D-3.03-5); orange text survives only on the solid-navy records band (4.68:1). `DecadeTimeline` retired → `DecadeExplore`; new `ClubRecords` + `DecadeExplore`. **Only `brand.md` tokens; no new token, no new dependency, no schema change.** Verified at 1280 + 375: no h-scroll; every text/bg ≥ AA (hero paper-over-photo ≥ 6.8:1, records overline 4.68:1, footer copyright 4.51:1); 2px navy keyboard focus ring; heading order clean; all images alt'd. `npm run build` (115 pages) + `npm run lint` clean; all six routes 200. Content dependency flagged: the legends set is thin (2 of 5 without portraits/years — honest placeholders), and `siteSettings.description` asserts founding „13 август 1922" which `facts.md` still lists UNVERIFIED (owner-authored CMS copy; recommend confirming into `facts.md`). Decisions **D-3.03-1…-6**. Ships via a PR (`phase-3.03-homepage-redesign` → `main`).
- **[3.02-Story — Season history-narrative fill COMPLETE] The archive now reads like the club's history book — real per-season `story` on the site for every documented season.** `season.story` (Приказна) filled for **66 seasons (1922-26 → 1995/96)**, transcribed **verbatim** from „ФК Беласица - гордоста на Струмица…docx" via 5 era-subagents (verbatim-only rule; every season's prose re-verified against the raw source — 66/66 pass; source OCR typos preserved per content-truth). Full **squad rosters** (apps/goals) added to `lineupAndStats` on 54 more seasons → `lineupAndStats` now on **80/96**. The 1992-93 demo `story` preserved (fill-empty-only). **This is the FIRST 3.02 change VISIBLE on the public site** — `story` renders via the existing SeasonStory component (verified live at `/arhiva/1982-83`: „Приказна за сезоната" + the championship narrative). Coverage stops at 1995/96 (the book's own end); 1996/97→2025/26 have no story (genuine source gap, empty). Guards hold (`clubRecord` 7, provenance 0 НЕПОТВРДЕНИ; the 2020s + 90-run photo/trainer fields untouched). **No schema/template/code change.** Known nuances flagged (D-3.02S-3): round-by-round results sit inside `story` for the 1920s–40s + 1980s–early-90s eras but not the others (the `results` field is empty — a uniform results pass is a recommended follow-on); 4 seasons carry trailing photo captions in `story`; OCR typos verbatim. Decisions **D-3.02S-1…-4**. Recommended follow-ons: a dedicated `results`-field pass + a light copy-edit of the preserved typos.
- **[3.02-Run — Content fill (remaining 90 seasons) COMPLETE] The owner approved the pilot; the same pattern is now applied across the whole archive.** Every `decade!=2020` season was processed: **`teamPhoto` on 77/90**, **`tablePhoto` on 82/90**, **`trainer` on 63/90**, **`lineupAndStats` (season top scorer) on 66/90**; `results` empty for all (no per-match source). Photos were classified by **8 parallel per-decade subagents** on the exact pilot rubric (846 images opened; every buildlineup/formation graphic, „FULL TIME" result graphic, table screenshot, portrait, roster form, action shot and webpage screenshot excluded); the write script **validated all 159 picked photo ids belong to their season (0 anomalies)** and a cross-era visual spot-check confirmed genuineness. Trainer + top-scorer text transcribed by hand from „Тренери на Беласица.docx" + „Најдобри стрелци на Беласица.docx". **The main 1922–1992 history narrative is `story` (Приказна) content — deliberately OUT of scope** (a separate owner-gated „narrative → story" phase; D-3.02R-3). The 6 pilot 2020s seasons untouched (`allSeasonsWithTeam == 83 == 77+6`); `clubRecord` still 7; provenance still 0 „НЕПОТВРДЕНИ". **No schema/template/code change** — content only; the 87-season write ran via a throwaway scratchpad script (text-JSON + picks-JSON, dry-run first; token, D-2.09R-5 precedent). Full-archive review report at `docs/ace-demo/Part-3-Phase-02-Run-review.html` (90 cards, 159 thumbnails). Decisions **D-3.02R-1…-4**. 3 borderline team images flagged (1950 youth squad, 1955-56 montage, 1988-89 two-team lineup); 13 seasons have no team photo, 8 no table (genuine source gaps). Nothing renders publicly yet — the redesign (3.04–3.06) surfaces these fields.
- **[3.02 — Content fill (2020s pilot) COMPLETE] The first real content is in the new season fields, on the six most recent seasons — the owner's pattern-approval gate before the archive-wide fill.** Each `decade==2020` season now has a genuine **`teamPhoto`** (a real squad photo — every buildlineup fan graphic, „FULL TIME" result graphic and table screenshot was **opened and rejected**; all 37 pilot photos were classified from contact sheets) and, where one exists, a **`tablePhoto`** (2025-26 has none — season in progress; 2022-23's team photo is a warm-up shot because no posed lineup exists in that folder — flagged). **`trainer`** set for 2020-21…2023-24 (from „Тренери на Беласица.docx"; 2024-25/2025-26 not documented → empty); **`lineupAndStats`** holds the documented season top scorer for 2020-21/2021-22/2022-23 (from „Најдобри стрелци…"; the history docx per-season coverage stops ~1995/96, so no 2020s squad/results source exists); **`results`** empty for all six (expected). **7 `clubRecord` docs** created + published, each cited: 4 honours (шампион 1982/83+1987/88, вицешампион, куп-полуфинале, југо-второлигаш ×5) + **Петар Андреев 555 наст./22 сезони** (appearances record) + **Мариќ 38** / **Мафков 27** single-season scorer records. **881 photo `provenance` strings rewritten** from „…НЕПОТВРДЕНИ…" to the `facts.md`-verified confirmed-rights wording (0 remain; each photo's source-folder attribution preserved) — **OV-10 cleared**. Guard verified: `count(season && decade!=2020 && (defined(teamPhoto)||defined(tablePhoto))) == 0` — the other 90 seasons untouched. **No schema/template/code change** (Sanity content only): the 6 seasons + 7 records went via Sanity MCP; the 881-doc rewrite ran via a throwaway scratchpad script (local write token, D-2.09R-5 precedent + OV-10's own prescribed method), never committed. Self-contained review report at `docs/ace-demo/Part-3-Phase-02-pilot-review.html` for owner approval. Decisions **D-3.02-1…-6**. ⚠️ **Owner sign-off wanted on the new provenance wording** (Claude's faithful transcription of the VERIFIED `facts.md` fact; not public-facing; re-runnable). Next: **3.02-Run** repeats Task A/B tagging+text for the other 90 seasons (Tasks C+D are already global/done).
- **[3.01 — Content model update COMPLETE · Part 3 begins] The Sanity model was reshaped so each season can hold a real team photo, a table image, a trainer name, a lineup/stats block and a results block — the invisible foundation for the crnobelanostalgija-style season redesign and the Statistics records page.** Nothing on the public site changed; this only prepares the model. Concretely, `src/sanity/schemaTypes/season.ts` gained five **optional** fields in Studio order right after `story`: **`teamPhoto`** + **`tablePhoto`** (`reference → photo`, each with an `options.filter` that scopes the picker to photos whose `relatedSeason` is the current season, D-3.01-3/-7), **`trainer`** (string — the new primary trainer field; the old `trainers` reference array stays for compatibility, D-3.01-4), **`lineupAndStats`** and **`results`** (portable text, the same `block` config as `story`). The legacy `finalTable`/`squad`/`trainers` are **unchanged in shape** — the league table is now meant to be shown as an **image** (`tablePhoto`), with structured `finalTable` retained as legacy (D-3.01-2); their descriptions now say „Задржано за компатибилност…". A new document type **`clubRecord`** (`src/sanity/schemaTypes/clubRecord.ts`: `label` req, `value` req, `category` radio [scorers/appearances/honours/other], `order`, preview) holds the curated all-time records for `/statistika` (D-3.01-5); person `careerStats` still backs the ranking tables. It is registered in `schemaTypes/index.ts` and exposed in `structure.ts` as „Клупски рекорди" (plural desk title over the singular schema title). **All 96 existing seasons remain valid** (additive + optional only, D-3.01-6); `match.ts` stays unregistered (D-2.01-2). Verified: `npm run build` (115 pages) + `npm run lint` clean; `sanity schema extract` shows the exact field order + optionality; `npx sanity schema deploy --workspace belasica-v2` succeeded and `get_schema` on the deployed Content-Lake schema lists the five new fields + `clubRecord`; `/studio` boots the updated schema with no error (the reference-`filter` runs from code — it is a function, so it is not serialized into the deployed tooling manifest, but is active in the code-bundled Studio; **no fallback to unfiltered was needed**). **No file under `src/app/` or `src/components/` changed; no `brand.md`/`globals.css`; no new dependency** — the public site is byte-for-byte identical (all seven routes 200). Decisions **D-3.01-1…-8**; owner-planning **D-0.00-13…-16** set Part 3's direction (redesign→restructure→fill→launch; league table as image; Statistics = curated records + person rankings; bulk fill by Claude, owner approves each). Ships via a PR from `phase-3.01-content-model-update` → `main`.
- **[2.08 — Homepage finalization COMPLETE] The homepage is a truthful front door to the finished archive — the last Part-2 build phase.** The homepage's selection logic, tuned when the site had 1 season + 8 photos, was rewritten for the 96-season / 889-photo archive. **`HOME_QUERY` photo ordering is now DETERMINISTIC** (D-2.08-3) — `order(select(defined(caption) && caption != "" => 0, 1) asc, coalesce(date, "9999") asc, _id asc)` on BOTH the featured season's `seasonPhotos` and the `gallery` (non-empty caption first → date asc, nulls last → `_id` asc as a stable total-order tiebreak). The old `order(coalesce(date,"9999") asc, caption asc)` was effectively arbitrary because all 881 ingested photos share null date + null caption; two cold reads now return an identical hero / moment-band / gallery (demonstrated readA≡readB against `production`). Gallery stays constrained in-query via `[0...10]` (one round trip; no JS slice). **Featured season stays automatic** (`order(decade desc, title desc)[0]` → today `2025-26`) and, having no `story`, its section **omits the teaser paragraph AND the `[PLACEHOLDER: приказна за сезоната]` chip** (D-2.08-2, verified in the DOM) — the title + „Деценија 2020" + photo + „Погледни ја сезоната" link still render. **Captions drive curation:** exactly **one** caption was published — `photo-f8662e1c` „Екипа на ФК Беласица" — on the genuine modern **FK Belasica squad photo** (2025-26, club crest plainly visible on the kit), which promoted it to `[0]` = **the hero**, replacing the buildlineup.com **fan-made lineup graphic** (`photo-64b01ef6`, `formacija.jpg`) the old arbitrary order had made the hero (recorded as the pre-change baseline). The fan graphic now fills the featured-season card (contextually apt — its own header reads „Беласица 2025 есен") + the moment band; ekipa also appears at gallery slot 9 (the page's pre-existing „featured photo repeats" property). Content-truth held: 3 other surfaced photos left uncaptioned (a fan graphic, a League-table screenshot, a vintage clipping — none captionable/surfaced truthfully). `DecadeTimeline` verified legible with all **11** decades at 1280 + 375 (scrolls internally; no page overflow) — no fix needed. **OV-RIGHTS closed** (D-2.08-1 — owner confirms rights to all 889 photos; stay public; `facts.md` VERIFIED entry added). Demo screenshots (`docs/ace-demo/screenshots/homepage-{desktop,mobile}.png`) regenerated from the finished page. `npm run build` (115 pages) + `npm run lint` clean; the only new visible text („Екипа на ФК Беласица" caption) renders Paper `#F7F4EC` on the navy gradient = **13.1:1** (AAA); `/` at 1280 + 375 no horizontal scroll; `/arhiva`, `/statistika`, `/legendi`, `/za-nas`, `/kontakt` all 200. **The only `src/` change is `src/app/(site)/page.tsx`** — no schema (`src/sanity/` untouched), no new dependency, no `brand.md` token, `globals.css` unchanged. New owed items **OV-10 / OV-11 / OV-12**. Decisions **D-2.08-1…-5**. Ships via **[PR #20](https://github.com/DinovLazar/belasica-v2/pull/20)** (`phase-2.08-homepage-finalization` → `main`) — **preview verified before requesting merge** (gate not waived): `https://belasica-v2-git-phase-208-homepage-811363-dinovlazars-projects.vercel.app/` renders `/` correctly at 1280 + 375 (hero = the real squad photo, featured no story chip, 11 decades, no h-scroll), and matched the local prod render exactly. PR open for Lazar's diff-review + merge.
- **[2.09-Run — Content ingestion COMPLETE] The archive is populated: 96 seasons + 889 published photos.** The mirror was placed at `~/belasica-ingest-source/` (611 MB · **1163 images** · 17 docs · **119 folders** — larger than the ~915/97 estimate, not short) and an Editor write token used locally (never committed, never on Vercel). Dry run: **0 collisions, 0 unmatched**, five locked mappings correct, `1992-93` already-present. **The slash-folder `1950/51` arrived on disk as `1950-51` (hyphen) → `RE_SPAN` unchanged** (D-2.09R-1; resolves D-2.09-7's owed check). **Wave 0** created **95 season shells** (→ 96 total, all with a decade); **Waves 1–8** uploaded **881 photos**, each wave's created count == source, public count held at **8**, idempotent (re-run Wave 8 → 0 created). ⚠️ **Then, by informed owner decision (D-2.09R-2), all 881 were PUBLISHED** — Lazar directed „publish, not only draft … settle rights later" after being told this makes ~881 mostly-third-party screenshots public on the live site and that `facts.md` still flags screenshot rights unsettled; public photo count is now **889**. The publish ran via a throwaway scratchpad script (D-2.09R-5) so the committed ingestion tooling stays drafts-only. **OV-4 cleared** — season `1982-83` „Прва македонска" 1st-place row (P30 W16 D11 L3, 45:19, 43 pts) transcribed from `Беласица 1926-2025-финал…xlsx`; `/statistika` balance now renders (summary band + coverage line + season row). **OV-7 cleared** — two players from the history docx: **Роберт Христовски** (175 наст., 7 гол.) + **Љупчо Мафков** (115 гол., appearances null); both render in `/statistika` rankings and their Кариера tiles; **the no-`0` rule is confirmed against real data** (Мафков shows one tile „ГОЛОВИ 115", never „0 настапи"). **OV-6 cleared** — `SiteFooter` now reads `UNOFFICIAL_ARCHIVE_LABEL`/`_STATEMENT` from `src/lib/facts.ts` (no hardcoded copy; byte-identical to `facts.md` by script) and the Studio `siteSettings.footerUnofficialArchiveText` field was corrected to the verified string + republished. Three reports committed to `docs/ingestion/` (dry-run, run, editorial hand-off). `npm run build` + `npm run lint` clean; **the only `src/` change is `SiteFooter.tsx`** (Task 8) — no `src/app/`, no schema, no `brand.md`/`globals.css`. Decisions **D-2.09R-1…-5**. Ships via a PR (`phase-2.09-run-ingestion` → `main`).
- **[2.09 — Content ingestion, prior partial pass — SUPERSEDED by 2.09-Run above] At that time the script was built and verified but the ingestion was blocked on a missing source mirror.** The deterministic, resumable, **dry-run-first** ingestion script lives at **`scripts/ingest/`** — `classify.mjs` (pure folder→`{kind,slug,title,decade,flags}` per `content-ingestion-plan.md` §3), `run.mjs` (walk → classify → report → `--commit` writes), `verify-classify.mjs` (**23/23** locked-mapping self-test). It was exercised at every path reachable without the archive: the classifier proof, a full dry run + report against a synthetic mirror (collision detection, `1992-93`→already-present against live Sanity, by-wave counts), and all three guards (missing mirror, `--commit` without token, `--commit` without `--wave` — each exits non-zero, clearly). **What did NOT run: Tasks 4–8** (real dry run, Wave 0, Waves 1–8, and Task 8's OV-4/OV-7 content transcription) — the mirror `~/belasica-ingest-source/` **does not exist on disk** (searched thoroughly; largest image folder found held 28 files vs. the expected ~915). Per the brief, this was **not** worked around: no Drive-API fallback, no fabricated tree (D-2.09-3). Lazar chose „build the non-blocked work now". Photos, when they land, are created as **drafts** — the public read still returns **8** published photos (verified); the rights gate holds mechanically (plan §5). One new **devDependency**: `@sanity/client` 7.23.1 (exact, D-2.09-5). `.env.example` gained an empty `SANITY_API_WRITE_TOKEN=` (secret, local-only; never valued in the repo, never on Vercel). Decisions D-2.09-1…-7 logged (incl. the verification-gate waiver and the 2.09-before-2.08 reorder). **Also this phase: the 2.05 documentation gap is closed** — D-2.05-1/-2/-3 back-filled and `Part-2-Phase-05-Completion.md` filed. `npm run build` + `npm run lint` clean; **no file under `src/app/`, `src/components/`, `src/sanity/schemaTypes/`, `brand.md` or `globals.css` changed.** Ships via **[PR #18](https://github.com/DinovLazar/belasica-v2/pull/18)** (`phase-2.09-content-ingestion` → `main`) — **partial**: safe-to-merge tooling + docs (no runtime change), the ingestion run tracked as OV-9. **Preview verified: all six routes 200** (`/`, `/arhiva`, `/statistika`, `/legendi`, `/za-nas`, `/kontakt`); published photo count still 8.
- **[2.07 — Contact page] The last nav 404 is gone — the six-item nav is complete.** **`/kontakt`** ships as a **server component**: breadcrumb → H1 „Контакт" + intro → provisional banner (D-2.05-1) → two-column body (**form left**, direct-contact block right with `border-l border-mist`; at 375 single column, **form first**, direct block below `border-t border-mist`). The new **`ContactForm`** (`src/components/contact/`) is the repo's **second client component** (after `StatTable`) and carries the full `idle → submitting → success | error` machine — plain `fetch` POST, `FormData` body, `Accept: application/json`, **no form/validation library and no new dependency**: idle „Испрати"; submitting „Се испраќа…" with the button disabled at a reserved min-width (**no layout shift**) and inputs disabled; success replaces the form with a `role="status"` `aria-live="polite"` panel („Ви благодариме!"); error puts a `role="alert"` banner **above** the form, **retains the user's input**, and the button reads „Испрати повторно". Copy is **Macedonian, not the handover's Serbian** (D-2.07-1) — „Пошаљи"/„Шаље се…"/„Хвала!"/„Пошаљи поново" appear nowhere in the diff. **The important state is the unset endpoint — the current production state:** `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is a **public** form action (not a secret, D-2.07-2), unset until 3.03, so the form renders inside a **disabled `<fieldset>`** with a notice + `PlaceholderChip` (PL-14) and **no `<form>` element at all** — submission is impossible two independent ways, the rest of the page renders normally, nothing throws (D-2.07-3). The direct-contact block is **all placeholders** — email chip (PL-3, now **visible**) + socials chip (PL-15) — the footer's demo values (PL-9) deliberately **not** propagated (D-2.07-4). `.env.example` added with an empty value and committed via a `!.env.example` `.gitignore` negation; **not** set in `.env.local` or Vercel — **no secret in the repo** (D-2.07-2). ⚠️ **All four live states are fixture-verified only (OV-8):** proven against a temporary in-file mock (stripped before commit, per the D-2.06-6 precedent) — a real idle→submitting→error transition **with input retained**, a real →success transition, and the submitting/success static states, all in-browser at 1280; **the disabled/unset state IS content-verified** at 1280 + 375, since that is what renders with no endpoint. `npm run build` + `npm run lint` clean; **every new text/bg pair ≥ 4.94:1** (AA needs 4.5:1; navy/paper 13.1, neutral-700/white 11.4, error/white 6.6). **No schema change; `SiteFooter`, `src/lib/facts.ts` and every other route untouched; no new `brand.md` token; no new dependency.** Ships via **[PR #17](https://github.com/DinovLazar/belasica-v2/pull/17)** (`phase-2.07-contact-page` → `main`) — **preview verified before merge** (gate not waived): `https://belasica-v2-git-phase-207-contact-page-dinovlazars-projects.vercel.app/kontakt` returns 200 and renders the disabled-form state at 1280 + 375, with `/`, `/arhiva`, `/statistika`, `/legendi`, `/za-nas` all 200. PR open for Lazar's diff-review + merge.
- **[2.06 — People & pages] The person links across the site stop 404-ing.** Three routes ship from live Sanity with ISR `revalidate = 60`: **`/legendi`** (portrait roster in fixed bands Играчи → Тренери → Раководство), **`/legendi/<slug>`** (every published person — `generateStaticParams` over all 5 slugs, so **trainer and president slugs resolve**, not just players), and **`/za-nas`** (provisional). Every squad name, trainer chip and `/statistika` player link now lands on a real page. **Placement (D-2.05-2) is live-exercised:** Петар Андреев is `["player","president"]`, so he sits **once** under Играчи carrying **both** chips — which leaves **Раководство empty, so it self-omits and only two bands render**. That is the correct result, and it contradicts the brief's DoD („renders three bands"). **Биографија is content-verified** — three published people have real `bio` blocks and it renders via `SeasonStory`; the handover §3 and the brief both claim „the demo people have none yet", which is **stale**. ⚠️ **Кариера is fixture-verified only (D-2.06-6 / OV-7)** — no published person has `careerStats`, so the tiles have never rendered from real content; all four states (both / appearances-only / genuine `0` / both-null → section omits) were proven against a temporary in-file fixture, since stripped. The no-`0` rule is exact: a **null** metric omits its tile, a **real 0** renders „0". **Merged via [PR #15](https://github.com/DinovLazar/belasica-v2/pull/15) (2026-07-18) and confirmed live on production** — `https://belasica-v2.vercel.app/legendi`, `/legendi/<slug>` and `/za-nas` all return 200 with the real content (Играчи + Тренери bands, no Раководство; OV-3 verbatim), and `/`, `/arhiva`, `/statistika` unregressed. Production lagged the merge by ~2 min (merge ≠ deploy, as at 2.04), then caught up on its own. Preview gate was verified before merge — **not waived, unlike D-2.04-7:** `https://belasica-v2-9uhscvnhp-dinovlazars-projects.vercel.app` returns 200 and the real content for `/legendi`, `/legendi/<slug>` and `/za-nas`, with `/`, `/arhiva` and `/statistika` unregressed. New: `src/components/legends/` (4 components) + `src/lib/people.ts` + `src/lib/facts.ts`. **No new `brand.md` token** (§9's expected outcome) and **no schema change**; `/kontakt` deliberately not built. ⚠️ **The 2.05 handover this phase builds to was never in the repo** — Lazar supplied it in-session and it was landed here (**D-2.06-1**); the 2.05 design phase remains unclosed. Nine conflicts with it were resolved the repo's way per its own §0: the biggest is that its UI copy is **Serbian** („Сезоне"/„Фотографије"/„Све легенде") against a Macedonian-only repo (**D-2.06-2**). `npm run build` + `npm run lint` clean; verified at 1280 + 375; **all 22 new text/bg pairs ≥ 4.94:1** (AA needs 4.5:1); focus ring measured on a real keyboard tab. ⚠️ **OV-6 opened:** `siteSettings.footerUnofficialArchiveText` holds **different, unverified, never-rendered** copy than the VERIFIED OV-3 string — a live trap for whoever wires it up (D-2.06-9).
- **[2.04 — Statistics page] The last empty top-level nav link is filled.** **`/statistika`** (previously 404) renders breadcrumb → **Најдобри стрелци** → **Најмногу настапи** → **Севкупен биланс на клубот**, from live published Sanity content with ISR `revalidate = 60`. Both player rankings read **only `person.careerStats`** — the authoritative career total; no code path sums `season.squad` into a career total (D-2.01-3), and a player whose total was never entered is **omitted rather than shown as 0**. The balance sums **only Belasica's own final-table row** (`isBelasicaRow`) across seasons that have a `finalTable`, into a 10-figure summary band + a season-by-season table, under a **coverage line** that states how many seasons the aggregate is based on — so a partial aggregate is never read as complete. New: `src/components/stats/` (3 components) + `src/lib/stats.ts` (presentation-free aggregation). The sortable `StatTable` is plain React state — **no table library, no new dependency** — with `<button>` headers, `aria-sort` on the active column, and **unknowns sorting last in both directions** (D-2.04-5) so a missing stat never tops a ranking. ⚠️ **The 2.04 brief cites a 2.02 handover section that does not exist** — handover §15 explicitly defers Statistics („Not designed: Legends, Statistics, About…"); the page was built from the handover's shared conventions + §6.4 table spec + `brand.md`, adding **no token** (**D-2.04-2** — the page therefore ships without a design review). Two further conflicts resolved: the Plan's per-competition balance is impossible on the locked model and became one all-time balance (**D-2.04-1**), and §15's „goal difference must not be computed" was scoped to the standings table, so GD/win % appear in the aggregate band only (**D-2.04-4**). Sections keep their heading + an empty notice instead of self-omitting (**D-2.04-3**) — with today's data **all three show notices, which is the correct expected result**. ⚠️ **Fixture-verified, not content-verified (D-2.04-6)** — no published season has a `finalTable` and neither published player has `careerStats`, so no table has ever rendered from real content. `npm run build` + `npm run lint` clean; verified at 1280 + 375; every new text/bg pair ≥ 10.37:1 (AA needs 4.5:1). Ships via **[PR #14](https://github.com/DinovLazar/belasica-v2/pull/14)**. Merged without the Vercel preview gate (D-2.04-7, owner-directed) during a third-party Vercel/GitHub outage — **no preview URL exists for this phase**, so `localhost` was the only pre-merge verification. **The outage has since cleared and production is confirmed live (OV-5 resolved 2026-07-17):** `https://belasica-v2.vercel.app/statistika` returns 200 with all three sections + their three placeholder chips, and `/` + `/arhiva` still return 200. The waived gate cost nothing in the end, but the waiver stands as a one-off, not a precedent.
- **[2.03 — Archive & Season templates] The archive is now real and navigable.** Two new routes read live published Sanity content with ISR `revalidate = 60`: **`/arhiva`** (decade-grouped season cards, newest decade first, real „N сезони" counts with correct Macedonian pluralisation, a sticky decade jump-nav offset by `top-header`, 3/2/1-col grid) and **`/arhiva/<slug>`** (breadcrumb → hero → Приказна → Конечна табела → Состав → Тренери → Фотографии → back-links, with `generateStaticParams` over every published slug; unknown slug → 404). **Every data section omits itself when empty** (D-2.02-3) — verified live: „Сезона 1992/93" has no `finalTable`, and „Конечна табела" is genuinely absent. A season with all five sections empty renders one archive notice + five chips (D-2.02-8); a photoless season gets a solid navy title band, never a greybox hero (D-2.02-2). 11 new components in `src/components/archive/` + `src/lib/archive.ts` / `src/lib/focus.ts`. `PhotoFrame` gained an **additive** `fit="cover"|"contain"` prop (D-2.02-7) so the archival grid mats whole scans instead of cropping them — no existing caller changed. **No `brand.md` token was added or needed; nothing hardcoded.** ⚠️ **The 2.03 brief was written against a superseded handover draft** and its "resolved decisions" would have re-introduced three WCAG AA failures fixed in `faaf8ba`; on Lazar's in-session call the phase was built to the **merged repo handover + `brand.md`** instead (**D-2.03-1** — the brief should be regenerated). One dependency added with Lazar's approval: **`@portabletext/react` 6.2.0** (D-2.03-2), already in the tree via `sanity`. `npm run build` + `npm run lint` clean. Ships via **[PR #13](https://github.com/DinovLazar/belasica-v2/pull/13)** — preview verified at 1280 + 375: `https://belasica-v2-lp0z9ycdo-dinovlazars-projects.vercel.app`.
- **[2.01 — content model lock] The schema is no longer a draft.** The content model is **locked** against the real archive (P0.1: ≈915 photos, ≈74 season folders 1922→2025-26, ≈23 thematic folders, ≈0.4 GB, free tier). Four launch types: **`siteSettings`, `season`, `person`, `photo`**. Changes: (1) the photo↔season/person relationship is now **single-direction** — the link lives only on the photo (`relatedSeason`/`relatedPerson`) and is read by GROQ back-reference; the `season.photos`/`person.photos` forward arrays are **gone** (D-2.01-1). (2) **`match` is retired** from the launch model — no match-level source exists in the Drive, so statistics come from `season.finalTable` + `season.squad`; `match.ts` stays in-repo, unregistered, with a deferral header (D-2.01-2). (3) **`person.careerStats` is the authoritative career total** (per-season detail stays in `season.squad`; never summed to fake a total) (D-2.01-3). (4) **`season.decade` is now required** and `season.slug`/`person.slug` **enforce uniqueness** per type via a new `isUniqueSlug` helper (D-2.01-6). (5) `photo.provenance` stays **required** and carries the rights-gate comment. **Verify-before-remove was run and did *not* come back clean** — one season („Сезона 1992/93") *did* populate `season.photos`, but both photos were **dual-linked** via `relatedSeason`, so removal dropped no data; the orphaned array was unset on that document (D-2.01-5). Locked schema **deployed to Sanity `production`** (workspace `belasica-v2`); `npm run build` + `npm run lint` clean; homepage verified still rendering photos + portraits from the back-references. **No new dependencies.** Also new: **`docs/content-ingestion-plan.md`** — the decided ingestion approach for 2.09 (D-2.01-4). Ships via PR `phase-2.01-content-model-lock` → main — pending Lazar's diff-review + Vercel preview check. ⚠️ **This phase opened with 1.06 still open** (Ace sit-down uncaptured, OV-3 unresolved) by owner decision (D-2.01-7) — the model has **not** been checked against Ace's stated needs.
- **[1.06b — visual polish]** A visual pass on top of Phase 1.05 (branch `phase-1.06b-visual-polish`), no content or data-model changes: (1) the site **header is now sticky** (`sticky top-0 z-40` + a `border-paper/10` hairline) — verified it stays fixed on scroll and the mobile menu still opens beneath the bar; (2) the **footer is filled out** — the brand block now sits beside three columns (**Навигација** reusing `NAV_ITEMS` / **Контакт** / **Следете нѐ**) with a copyright bottom bar, styled in brand tokens; the contact/social values are **demo placeholders** (PL-9, D-1.06b-1), not real facts; (3) the **gallery is now an asymmetric editorial mosaic** — the first photo is a 2×2 feature and the rest tile around it; `PhotoFrame` gained an optional **fill mode** and captions overlay on a navy gradient (D-1.06b-3); (4) light polish only — section rhythm (`py-16 md:py-24` + `border-t border-mist`) and card-lift hovers were already consistent from 1.05.2, so nothing was forced. **Task 2 (crest logo + favicon) was already shipped by D-crest-1**, so it was a no-op this pass — `src/app/favicon.ico` was kept deliberately (it is the crest, not the Next default the brief assumed; D-1.06b-2). `npm run build` + `npm run lint` clean; verified in-browser at desktop (1280) and mobile (375). No new dependencies. Ships via **[PR #10](https://github.com/DinovLazar/belasica-v2/pull/10)** (`phase-1.06b-visual-polish` → main) — pending Lazar's diff-review + Vercel preview check before merge.
- **The homepage is now the full 8-section content page.** `src/app/(site)/page.tsx` renders, top→bottom: Hero (season team photo) → Intro (club description) → Featured season („Сезона 1992/93" + story teaser) → **Decades timeline** (1920-ти→2020-ти, 1990-ти highlighted) → Legends → **Moment band** (full-bleed 1993 cup photo) → Gallery (8 photos) → **Explore grid** (4 nav cards) → Footer. All from **live published Sanity content** via the read client (ISR `revalidate = 60`). Three sections are new this phase (D-1.05.2-3); the page is ~35% taller on desktop (more on mobile).
- **Legend portraits now render — no new content was needed.** The 1.06 snapshot concluded the portraits were a content gap needing Ace-owned uploads; that was a **misdiagnosis** (see D-1.05.2-2). The portraits for **Васо Цветков and Панче Пантазиев are already linked** via `photo.relatedPerson`; the old query just read the nonexistent `person.photos`. The rewritten `HOME_QUERY` (D-1.05.2-1) reads photos by **back-reference** (`relatedSeason`/`relatedPerson`), so both player portraits render with the real linked images. Legends is now filtered to **players only**, ordered by name; meta = role + `playingYears`.
- **Crest is on `main` and deploying.** PR #7 (`chore-crest-logo`, crest header/footer logo + favicon, D-crest-1) is **merged** to `origin/main`; PR #8 (`phase-1.06-verification`, the 1.06 docs/state) is **merged** too. This phase (1.05.2) stacks on both and ships via **PR #9** (`phase-1.05.2-homepage-content-sync`). Once #9 merges, production shows the crest + the full 8-section homepage.
- **Published content (live `production` dataset) — after the 2.09-Run ingestion:** `siteSettings` (title „ФК Беласица" + full description; footer field now the verified string); **96 seasons** 1922→2025-26 (all with title + decade; every season has ≥1 photo; **1** — `1982-83` — has a real `finalTable`, **1** has a `story`, **1** has a `squad`; the rest need story/table/squad/trainers via the editorial pass); **889 published photos** (881 ingested at 2.09-Run, each with a resolved `relatedSeason`; rights confirmed by the owner at 2.08 (OV-RIGHTS **resolved**), and **at 3.02 all 881 `provenance` strings were rewritten from НЕПОТВРДЕНИ to the confirmed-rights wording — OV-10 cleared** (0 „НЕПОТВРДЕНИ" remain). **2.08 added exactly one `caption`:** the 2025-26 squad photo `photo-f8662e1c` → „Екипа на ФК Беласица", the homepage hero-curation lever, D-2.08-5 — **9 photos carry a `caption` in total**: 8 demo + this one); **7 persons** (the 5 demo + **Роберт Христовски** 175/7 and **Љупчо Мафков** 115 gol. — the 2 with real `careerStats`). *(Pre-2.09 baseline was: 1 season, 5 persons, 8 photos.)* **3.01 added five optional `season` fields (`teamPhoto`/`tablePhoto`/`trainer`/`lineupAndStats`/`results`) and the `clubRecord` type; 3.02 (2020s pilot) + 3.02-Run filled them across the WHOLE 96-season archive: `teamPhoto`, `tablePhoto`, `trainer` and `lineupAndStats` were filled across the WHOLE 96-season archive, **3.02-Story** filled `season.story` on 66/96 (1922→1995/96) and enriched `lineupAndStats` to 80/96, and **3.04-Cowork** then re-verified every season's photos against source and filled the first `results`. **Live counts, verified against the published dataset on 2026-07-26: `teamPhoto` 83/96 · `tablePhoto` 88/96 · `trainer` 68/96 · `lineupAndStats` 80/96 · `results` 10/96 · `story` 66/96 · `clubRecord` 7 · photos 889 · provenance „НЕПОТВРДЕНИ" 0.** 3.04-Cowork's changes (18 published season docs): +`tablePhoto` on 1943 and 1951, +`teamPhoto` on 2007-08, 1999-2000 and 2000-01 repointed to the true final table, `teamPhoto` **cleared** on 1952 (it was the 1951 squad reused) and `tablePhoto` **cleared** on 1991-92 (only a mid-season table exists), `results` on 10 seasons (1930-31…1940-41, extracted verbatim from `story`), and `trainer` 1957-58 = „Коста Ефински" (D-3.04C-1…-6). `results` stays empty on the other 86 — those seasons' scores sit inside `story` prose and were deliberately not reformatted into lists (D-3.04C-5). Legacy `finalTable`/`squad`/`trainers` remain as before (1 season each). Empty slots are genuine source gaps.**
- **Lighthouse baseline (3.04d, v13.4.1, local prod build):** Home mobile Perf **92** (lantern-simulated) / **96 (`--throttling-method=devtools`)** · LCP 3.3s sim / 2.3s devtools · A11y **100** · SEO **100**. Season page (`/arhiva/1982-83`) mobile Perf **92–93**. Desktop Perf **99**. BP 96 locally only because Vercel Analytics 404s off-platform. The remaining simulated-LCP gap is the lantern estimator, not bytes (the hero variant is a 39 KB webp; observed load phases sum ~147ms). **Confirm on the Vercel preview at the 3.04d PR** (HTTP/2 + CDN). *(Prior baseline 2.08, homepage `/`, v13.4.1, on the Vercel preview):* Mobile Perf **88** · A11y **100** · BP **100** · SEO 63\*. Desktop Perf **99** · A11y **100** · BP **100** · SEO 63\*. \*SEO is depressed only because preview deployments are `noindex` (Lighthouse `is-crawlable`=0) — a preview artifact, not a regression; production is indexable (prior baseline SEO 100). Mobile Perf 88 (vs 1.06's 93 — the hero is now a full photograph). Record only — the ≥95 gate is 3.02. *(Prior 1.06 baseline, v13.4.0: Mobile Perf 93 · A11y 100 · BP 96 · SEO 100; Desktop Perf 99 · A11y 100 · BP 96 · SEO 100.)*
- **Ace demo kit** at `docs/ace-demo/` (from 1.06): `walkthrough.md` (Macedonian demo script incl. the OV-3 footer question), `feedback.md` (**captured 2026-07-16** — Ace's reaction + the 2.02 direction), `screenshots/homepage-{desktop,mobile}.png` (still to retake — the page has three new sections + real portraits + the crest now).
- OV-2 resolved (wordmark **ФК Беласица** VERIFIED). **OV-3 resolved** (2026-07-16) — Ace confirmed the current footer wording as correct at the sit-down; VERIFIED in `facts.md`, no code change needed. **OV-1 resolved** (2026-07-16) — Ace confirmed rights to all Drive photos (see register for the screenshot caveat).
- Stubbed / not wired: **the entire six-item nav now lands on a real page** — `/kontakt` (2.07) was the last 404 and is built. `/arhiva` + `/arhiva/<slug>` (2.03), `/statistika` (2.04), `/legendi` + `/legendi/<slug>` + `/za-nas` (2.06) and `/kontakt` (2.07) are all live. The **contact form is fully built and, since 3.03b, LIVE** — `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is set in `.env.local` + Vercel (Prod + Preview), so `/kontakt` renders the real `<form>` and messages reach Formspree (D-3.03b-1, PL-14 cleared, OV-8 cleared). The disabled-fieldset fallback (D-2.07-3) is still the behaviour whenever the variable is unset — e.g. a fresh clone. No domain or OG-image work yet.
- Current phase: **3.01 — Content model update** (this phase; ships via PR → `main`) — the first phase of **Part 3**. Part 1 and Part 2 are complete and on `main`. Part 3's shape (owner-set, D-0.00-13…-16): **foundation → fill → redesign → launch** — 3.01 is the foundation (schema), 3.02 is the content fill/photo-tagging, later phases redesign the season/statistics pages (crnobelanostalgija-style) and launch. The **second Ace sit-down has still not happened**, so `/za-nas` is still provisional and still carries its own „Страницата е во подготовка" banner (D-2.05-1, D-3.10-9); **`/kontakt` is no longer marked provisional — the owner had its banner removed at 3.10 (D-3.10-1)**. PL-1/PL-2 (About) render as visible chips; on Contact **only PL-15 (socials) is left** — PL-14 went with 3.03b (the Formspree endpoint, D-3.03b-1) and PL-3 with 3.07 (the email, D-3.07-5). That sit-down still gates the About copy and any Contact/Legends revision.

## Current stack

See `src/_project-state/00_stack-and-config.md` (only source; exact pins). **3.01 added NO dependency** — a schema/Studio-only change (no npm package, no `brand.md`/`globals.css` touch). **2.03 added exactly one dependency: `@portabletext/react` 6.2.0** (D-2.03-2) — already present transitively via `sanity`, so the lockfile gained one line. Otherwise unchanged since 1.05.2, which added none — reuses 1.03–1.05 (`next-sanity`/`@sanity/image-url` reads, `lucide-react` for the arrow icons). Reveal = CSS + IntersectionObserver (no new lib). Unchanged: Next.js 15.5.20, React 19.2.4, Tailwind CSS 4.3.2, `sanity` 4.22.0, `next-sanity` 11.6.13, `@sanity/image-url` 2.1.1, `@vercel/analytics` 2.0.1, Motion 12.42.2, Lucide 1.24.0, shadcn/ui (neutral base, repointed to brand).

## Built pages / components

- `src/app/layout.tsx` — bare root layout: `<html lang="mk">`/`<body>`, fonts, `globals.css`, `<Analytics/>`, site metadata, crest favicon set (`icon.png`/`apple-icon.png`/`favicon.ico`, on main via PR #7).
- `src/app/(site)/layout.tsx` — site chrome: pre-paint `.js` flag script (D-1.05-5) + skip link + `SiteHeader` + `<main>` + `SiteFooter`.
- `src/app/(site)/page.tsx` — **the homepage**, **rebuilt at 3.03** to the seven-section heritage-and-legends-led flow (Hero → Story → Legends → Numbers → Decade explore → Moment → Quick links); one `HOME_QUERY` round trip; ISR `revalidate = 60`. Hero = the most-recent season's `teamPhoto` (`order(decade desc, title desc)`, fallback newest photo); legends = players sorted portraits-first then name (D-3.03-2); records = `clubRecord` docs; decades = per-decade counts reduced in JS; moment = the deterministic captioned+season-anchored+landscape photo (oldest era, widest crop, D-3.03-4). Legend cards equalised to full height via a homepage-scoped grid variant (LegendCard untouched). The old 8-section page (featured season, gallery mosaic, dot-rail timeline) was **replaced**. **3.10 — the hero is pinned to the 1982/83 squad photograph**, in code rather than as a Sanity field: `HERO_SEASON_SLUG`/`HERO_PHOTO_ID` constants feed the query as parameters and a new `heroPinned` lookup sits *beside* the two older sources, so resolution is pinned → newest-season → newest-photo and unpublishing the pinned season silently falls back instead of showing a placeholder (exercised, D-3.10-4). Alt resolves in that same order („Тимска фотографија — Сезона 1982/83", derived from the fetched title). Hero box `4/5 → 3/2` at base, `objectPosition 32% → 38%`; `moment` now excludes the hero photo by `_id` (D-3.10-5). **3.19 — the badge is three pennants, not the crest:** a `ZNAMENCA` module table drives three plain `<img loading="lazy">` from `public/znamenca/`, normalised by **height** (`h-16/20/28` + `w-auto`; all three are 520px tall) and wrapped in **one** `role="img"` + `aria-label`, with `alt=""` on each — one accessible object, not three (D-3.19-4). **2026-08-17 — all three are now background-free** (D-crest-3…-5): 6 and 9 were replaced in place by owner-supplied cut-outs, and slot 1 now points straight at `/crest.svg` instead of a second copy of the same pennant, so the „no white panel" the lockup has claimed since 3.06a is finally true of the files themselves. Widths changed 368/476/418 → **364/446/438**; the 520 height and every accessibility decision are unchanged. `/crest.svg` is untouched and still the header's mark — the hero now shares it rather than duplicating it. **Also 3.19 — `HOME_QUERY`'s `legends` projection gained `careerStats { appearances }`**, without which this band showed a count on 2 of its 10 cards while `/legendi` showed one on all it could — the same ten people described differently, which `HomeData`'s own type comment forbids (D-3.19-3). Now 8 of 10; the two blanks (Ефтимов, Пантазиев) hold neither field and are blank on both pages. **3.24 changed two of the seven sections.** **§6 „Момент од историјата" is pinned:** `HOME_QUERY` gained `momentPinned` (`_id == $momentPhotoId`, `MOMENT_PHOTO_ID` = `39b358c0-…` — „Младата екипа на Беласица со Купот на Македонија, 1993", season `1992-93`), with the old deterministic `moment` projection kept beneath it as the fallback, mirroring the hero's chain since D-3.10-4. The section now carries a `u-link` to **`/razno/mladinska-skola`** labelled „Прочитај повеќе за младинската школа", **rendered only when the pin resolved** — the fallback picture is not the 1993 side, so the link goes away rather than becoming false (D-3.24-2). **§7 „Каде понатаму" has five boxes, not four:** „Разно" → `/razno`, sub-label „Теми од историјата", placed between „Статистика" and „За нас" to match `src/lib/nav.ts`; the grid went `1/2/4` → **`1/2/5`** and the last tile takes `sm:col-span-2 lg:col-span-1` (derived from the array length) so it closes the two-track row between `sm` and `lg` (D-3.24-3).
- `src/app/studio/[[...tool]]/page.tsx` — embedded Sanity Studio at `/studio`.
- `src/app/(site)/arhiva/page.tsx` — **Archive index** (2.03): decade-grouped season cards from live content, sticky jump-nav, real counts. ISR 60.
- `src/app/(site)/arhiva/[slug]/page.tsx` — **Season detail** (2.03): `generateStaticParams` over every published slug, self-omitting sections, all-empty notice, photo/photoless hero. ISR 60. **3.13 — two of the five sections were renamed:** `story` is now rail „Преглед" / heading **„Преглед"**, and `staff` is rail „Играчи" / heading **„Играчи и тренер"** (D-3.13-1). The section **`id`s are unchanged — `prikazna` and `trener`** — so `#prikazna`/`#trener` links already shared still resolve; the `id` deliberately no longer matches the label and a comment on each says so (D-3.13-3). The `<h3>` „Тренер: <име>" inside the staff section is untouched. The `SECTIONS` doc comment’s stale „every section renders on every page" claim (reversed at 3.06a) was corrected at the same time (D-3.13-2). Verified: the rendered `<section id>` set is **byte-identical to `main` across all 96 season pages**.
- `src/components/archive/` — **new in 2.03**: `Breadcrumb`, `SectionHeading`, `DecadeSectionHeader`, `SeasonCard`, `DecadeJumpNav`, `StandingsTable`, `SquadTable`, `PersonChip`, `SeasonStory` (Portable Text), `PhotoGrid`, `SeasonEmptyNotice`. Reusable by 2.04/2.05. **+ 3.04:** `MattedPhoto`, `SeasonAnchorNav`, `SeasonRecordList`, `SeasonNeighbourNav`; **+ 3.04b:** `SeasonSectionEmpty`. **+ 3.05b: `PhotoLightbox`** — the repo's **fifth client component**, the season gallery's overlay (provider + trigger + dialog). **+ 3.17:** `DecadeJumpNav` became a thin wrapper over the new shared **`src/components/JumpNav.tsx`**, and `SeasonRecordList`'s `roster` variant gained the after-eleven divider; the new **`src/components/BackToTop.tsx`** (the repo's **seventh** client component) is mounted once in the `(site)` layout. `PhotoGrid` gained an **opt-in `lightbox` prop** and stays a server component, building every overlay URL server-side; the person page does not pass it and is unchanged (D-3.05b-3). **+ 3.20:** `PhotoLightboxProvider` takes an optional **`label`** prop for the dialog's accessible name (defaulting to „Фотографии од сезоната"), the same move `SeasonAnchorNav` made at 3.19 — „Разно" passes `Фотографии — <наслов>` (D-3.20-3); **the overlay is otherwise unchanged and is reused by `RaznoPhotoGrid` as-is**, which is what D-3.05b-3's server/client split bought. **+ 3.19:** `SeasonAnchorNav` takes an optional **`label`** prop (defaulting to „Скок низ сезоната") so „Разно" can reuse the rail under a name that is true on a topic page — the season page's call site and rendered `aria-label` are byte-identical (D-3.19-7); and **`PhotoLightbox` was re-laid-out** — arrows and figure now share one flex row with a real gutter, replacing the absolute `left-4`/`right-4` positioning under which a wide scan ran beneath both controls; below `sm` the row wraps and the arrows fall under the photograph via `order`, on one set of buttons (D-3.19-8). **+ 3.24:** the overlay **scales the scan to its usable area** instead of rendering it at the file's own pixel size — the row is `flex-1 min-h-0` and `sm:flex-nowrap`, the figure `sm:flex-1 sm:self-stretch`, and the image itself is the growing item (`sm:flex-1` + `w-full` + `object-contain`), so it lands at ~84 % of viewport height on desktop and at full usable width on a phone, upscaling where the file is smaller (D-3.24-1). **The `sm:max-w-[calc(100vw-11rem)]` cap is gone** — the gutter is now structural, produced by `shrink-0` buttons beside a `flex-1` figure, and `11rem` survives only inside `sizes` as a width hint. `sm:flex-nowrap` is load-bearing: with the wrap left on, the figure measured 1 258px tall in an 800px viewport (D-3.24-1b).
- `src/lib/archive.ts` (2.03) — decade labels/anchors, count pluralisation (D-2.02-12), `isBelasicaRow`, `statCell` (`—` for null, `0` preserved). `src/lib/focus.ts` (2.03) — shared focus rings; the homepage's two inline copies were left alone (out of scope).
- `src/app/(site)/statistika/page.tsx` — **Statistics** (2.04): three sections from live content, one GROQ round trip, empty notices wired. ISR 60. **Both player tables are now bounded by a threshold, not a row cap:** `SCORER_MIN_GOALS = 21` (D-3.12-5, **35 rows** today) and, new at 3.24, **`APPEARANCE_MIN = 46`** (D-3.24-4, **116 rows**, down from 119). Each states its cut in one line under its heading — „Најмногу настапи" gained „Листата ги опфаќа играчите со 46 или повеќе внесени првенствени настапи за Беласица." (D-3.24-5) — and each empty notice matches, since a section can now be empty because nobody clears the cut. `defined(careerStats.…)` was dropped from the appearances query as redundant, not as a loosening: `>= n` is false for an undefined field, and a genuine recorded `0` is still a number (`statCell` renders it, `— ` is reserved for unknown). ⚠️ **The 46 is under review — see OV-57**; it removes three players, including Горан Пандев.
- `src/app/(site)/legendi/page.tsx` — **Legends index** (2.06): three fixed-order role bands from live content; one person → exactly one band by priority (D-2.05-2), all role chips shown; name-sorted via `localeCompare("mk")`; empty band fully omitted. ISR 60. **3.10 — the search moved into the navy header:** the populated path now returns `<LegendsBrowser bands={bands} />`, and that component renders `PageHeader` itself (breadcrumb → H1 → intro → „160 личности" → the field as `children`), so the empty band between header and first orange rule is gone; the page keeps `PageHeader` only on its empty-roster branch (D-3.10-2/-3). Still statically prerendered — all 160 people are in the server HTML with JS off. **3.13 — Тренери and Раководство are no longer alphabetical:** both bands are ordered **newest-first by most recent service**, from a year derived at build time — trainers from the latest `season.trainer` naming them (exact trimmed name, never a substring), officials from the latest four-digit year in their biography’s opening line (D-3.13-4). **No new field, no schema change, no Sanity write, and the year is never rendered** — it is a sort key only. The roster and the season list arrive in **one round trip** (D-3.13-5), and the bands are handed to `LegendsBrowser` as the **five fields `LegendBand` declares**, projected by name rather than spread, so `bioLead`/`sortYear` — and `legendRank`/`careerStats`, which had crossed since 3.12 — stay out of the client bundle (D-3.13-6). **The Играчи band is untouched and byte-identical to `main`.** **3.19 — `careerStats.appearances` now crosses that client boundary too**, projected as a single number (never by spreading `careerStats`, so `goals` stays server-side), because the card renders it as the appearance count's fallback (D-3.19-2). `bioLead` and `sortYear` still do not cross. ⚠️ **The live band split is now `98 · 34 · 28 = 160`, not the 86 · 46 · 28 this file recorded through 3.17** — twelve people gained a `player` role in Studio since then; content drift, not a code change (OV-49). **3.22 — four tabbed categories**, and a person is in **every** one he qualifies for rather than only his highest (D-2.05-2 withdrawn); the fourth, Репрезентативци, is `INTERNATIONAL_SLUGS` and corresponds to no schema role. **3.27 — „Играчи" is `legendRank`, not the `player` role** (D-3.27-3): the tab holds only people the book ranks, taking it **153 → 138** and dropping 15 men, all of whom remain in another tab (10 Тренери, 5 Репрезентативци) so the distinct roster is unchanged. **The band split measured on `production` at 3.27 is `153 · 69 · 29 · 10`, distinct `211` — the `98 · 34 · 28 = 160` above is stale by 51 people and OV-49 should be re-read against the new figures** (D-3.27-10). **`project()` is now category-aware** (D-3.27-4): each card is built for the tab it will appear in and carries only that tab's fields, so `nationalStats` crosses the client boundary on 10 cards, `trainerYears` on 69, `officialYears` on 29 and `legendRank`/`legendAppearances`/`careerStats` on 138 — `sourceNote` and `bioLead` on **none**, verified in the built HTML. The query gained `trainerYears`, `officialYears` and `nationalStats{appearances, goals}`; `PersonRow` is now stated in full rather than derived from `LegendCardData`, because the row is what the query returns and the card is what one tab renders.
- `src/app/(site)/legendi/[slug]/page.tsx` — **Person detail** (2.06): `generateStaticParams` over **every** published person (trainers/officials included — no 404); breadcrumb → hero → Биографија → Кариера → Сезони → Фотографии → back-link, each self-omitting. `careerStats` read directly, never summed (D-2.01-3). ISR 60. **3.27 — „Кариера" is now up to three labelled, self-omitting groups** (D-3.27-8), rendered by a local `FigureGroup`: **„Беласица"** (`careerStats`, with the book's printed `legendAppearances` taking precedence as since 3.23), **„Цела кариера"** (`nationalStats`, with `sourceNote` rendered beneath the figures), and **„Периоди"** (`trainerYears` as „Тренер", `officialYears` as „Раководство"). The two figure sets are **never summed and neither falls back to the other** — they are different scopes from different sources, and an unlabelled grid of four figures is the OV-47 ambiguity this removes. `playingYears` stays in the hero and is deliberately not repeated here; the hero is otherwise untouched. Verified with injected values and reverted (D-3.27-13): a recorded **`0` renders** („Голови 0"), because every test is `!= null` and not falsiness.
- `src/app/(site)/pravni-informacii/page.tsx` — **Правни информации** (**new 3.07**): the archive's legal statement in ten sections, reachable only from the footer's bottom bar (D-3.07-2). **Static server component — no Sanity read, no client component, and no `revalidate`** (D-3.07-8; it builds as `○ (Static)` with an empty Revalidate column, unlike every neighbour's `1m`). Navy `PageHeader` (H1 + „Последно ажурирање: 16 август 2026" in the meta slot) → one `max-w-measure` column of ten `Reveal`-wrapped `<section>`s, each `u-h3` navy heading + `text-body` neutral-700 prose, two square-marker lists, and §10's `mailto:` from `CONTACT_EMAIL`. Copy verbatim from a module-scope `SECTIONS` array (D-3.07-7) — **41/41 rendered lines `diff`-identical to the brief**. Zero chips in `<main>`; indexable; built from existing primitives only — **no new component file and no new token**.
- `src/app/(site)/razno/page.tsx` — **Разно index** (**new 3.16**): navy `PageHeader` (H1 + intro + „7 теми“, the 7 derived from `RAZNO_TOPICS.length`) over a 3/2/1 grid of seven photo-less cards. **Static — no Sanity read, no `revalidate`.**
- `src/app/(site)/razno/[slug]/page.tsx` — **Разно detail** (**new 3.16**): seven pages prerendered from `RAZNO_TOPICS`; unknown slug → `notFound()`. One `max-w-measure` column of `para` prose / hairline-separated `record` runs / one `closing` block, closing on the book's source credit, then prev/next + „Сите теми“. **3.19 — the topic splits into „Преглед" + „Рекорди" and gains the archive's rail** („Скок низ темата"). The cut is at the **first `record` block, never by filtering kinds**, so the transcription's source order is preserved intact (D-3.19-5). Where it splits, both sections are `aria-labelledby` their own headings, narrowing D-3.16-8 rather than reversing it (D-3.19-6). **3.20 — a third section, „Фотографии", appears wherever the topic has photographs, always last** (D-3.20-4). ⚠️ **This supersedes 3.19's „three of the seven split, the other four show no rail": all seven topics now hold at least „Преглед" + „Фотографии", so every one carries the rail.** The `isSplit` guard stays because the condition is a property of the data, not a constant. The gallery renders **outside** `max-w-measure` (the reading measure is for the chapter's text) via `RaznoPhotoGrid`, and the source credit tracks the last **prose** section rather than the last section, so it still closes the book's own text rather than hanging under the photographs (D-3.20-5).
- `src/content/razno.ts` — **new 3.16**, the first file under `src/content/`. The seven topics and their **101 transcribed blocks**, generated from `data/book/razno-source.md` and re-diffed against it (101/101 byte-identical). **Not in Sanity by decision** (D-3.16-2).
- `src/components/razno/RaznoNeighbourNav.tsx` — **new 3.16**: prev/next topic cards; a deliberate copy of `SeasonNeighbourNav`'s pattern rather than a widening of it (D-3.16-7).
- `src/app/(site)/za-nas/page.tsx` — **About** (2.06), **provisional** (D-2.05-1): banner + OV-3 verbatim + PL-1/PL-2 chips. No Sanity read; hero omitted for want of a field (D-2.06-4). ISR 60.
- `src/app/(site)/kontakt/page.tsx` — **Contact** (2.07), **no longer provisional — the banner was removed at owner request in 3.10, retiring D-2.05-1 for this page** (D-3.10-1): **server component** — breadcrumb → H1 → two-column form + direct-contact block (`border-l`/`border-t border-mist`). Reads `NEXT_PUBLIC_FORMSPREE_ENDPOINT` and passes it to `ContactForm` — **set since 3.03b, so the live form renders** (D-3.03b-1). **3.07: the direct block's email is now the real `mailto:` from `CONTACT_EMAIL`** (PL-3 cleared on both its surfaces at once — D-3.07-5); footer demo values not propagated (D-2.07-4). **3.03b and 3.07 landed within hours of each other, and between them left PL-15 (socials) as the page's only open placeholder — `<main>` carries exactly 1 chip, not 3.** **3.10 deleted the banner outright** rather than rewriting it a third time; the form, the `mailto:` link and the PL-15 chip are untouched and all still render. No Sanity read. ISR 60. **The last nav 404 — nav now complete.**
- `src/components/contact/ContactForm.tsx` — **new in 2.07**, the repo's **second client component** (after `StatTable`): the `idle → submitting → success | error` machine (plain `fetch`, `FormData`, no library). Macedonian copy (D-2.07-1). `role="alert"` error (above form, input retained) / `role="status"` success (form replaced); no layout shift on submit. **Unset endpoint → disabled `<fieldset>` + notice + `PlaceholderChip`, no `<form>` — cannot submit** (D-2.07-3); that is now the fresh-clone path only, since **3.03b set the endpoint** (D-3.03b-1) and the live form is what renders. **All states verified against the real Formspree service — OV-8 and PL-14 both cleared.**
- `src/components/legends/` — **new in 2.06**: `LegendCard` (portrait or navy initials tile, D-2.06-7), `RoleBandGrid` (self-omitting band), `PersonHero` (portrait hero / navy monogram band), `RoleChips` (shared by both). `src/lib/people.ts` (2.06) — role labels/bands/priority, `orderedRoles`, `initials`, Cyrillic `compareByName`, per-band pluralisation. **+ 3.12:** `compareByLegendRank` (the book’s appearance ranking). **+ 3.13:** `seasonStartYear`, `buildTrainerYearIndex`, `tenureEndYear` and `compareByRecency` — the derived newest-first order for the two staff bands; all four are pure, and `compareByRecency` sends anyone with no derivable year to the **end** of the band via `compareByName` (D-3.13-4). `src/lib/facts.ts` (2.06) — the VERIFIED OV-3 strings as constants (see OV-6). **+ 3.19:** `LegendCard`'s appearance count is **no longer gated on the rank** and takes a second source — `legendAppearances` (the book's printed string) else `careerStats.appearances` (D-3.19-2), `!= null` so a genuine recorded `0` survives. **80 of 98 players now show a count, up from 32**; the 18 with neither source show nothing — never a zero, a dash or a chip. **+ 3.23:** `RoleBandGrid` was renamed **`CategoryGrid`** — it had stopped rendering a role band at 3.22. **+ 3.27:** `LegendCard` takes a **`category` prop** and renders the facts belonging to that tab and no others — rank + `playingYears` + appearance count under Играчи, `trainerYears` under Тренери, `officialYears` under Претседатели, and `nationalStats` under Репрезентативци as „Цела кариера: … настапи, … голови" (D-3.27-4). **The prop defaults to `"player"`, which keeps the homepage marquee byte-identical and out of scope** (D-3.27-12). **Role chips render on every card in every tab** — Ace was explicit that a coach who played keeps his player *tag* and only loses the player *detail*. `CategoryGrid` passes its own category down. `src/lib/people.ts` gained **`appearanceCountLabel`/`goalCountLabel`** (the D-2.02-12 singular rule, only 1 takes the singular) for the whole-career line. `LegendCardData`'s fields below `role` are now **optional**, because a Тренери card genuinely has no rank on it rather than a null.
- `src/components/stats/` — **new in 2.04**: `StatTable` (the only client component — sortable headers, `aria-sort`, keyboard-operable, no table library), `BalanceSummary`, `StatsEmptyNotice`. `src/lib/stats.ts` (2.04) — `aggregateClubBalance` + `formatGoalDifference` / `formatWinRate`; presentation-free, sums only `isBelasicaRow` rows and tracks per-metric coverage.
- `src/components/home/` — `PlaceholderChip`, `PhotoFrame` (matted frame + `next/image`; `ratio` **optional** for fill mode, 1.06b; **`fit="cover"|"contain"` added 2.03, D-2.02-7**), `Reveal` (scroll reveal), `SectionOverline` (**gained the `onPhoto` variant at 3.03** — orange marker + paper text over photos, D-3.03-5), **`ClubRecords`** (3.03 — the records strip from `clubRecord` docs; **3.05b: capped at six by an explicit label whitelist**, D-3.05b-1/-2), **`DecadeExplore`** (3.03 — the decade-tile grid with real counts). **`DecadeTimeline` was removed at 3.03** (replaced by `DecadeExplore`, D-3.03-6).
- `src/components/` — `SiteHeader.tsx` (**sticky** navy bar — `sticky top-0 z-40` + `border-paper/10`; **refreshed 3.03**: hamburger is now a 44px tap target via `p-2.5` — header still 77px; inactive nav items gain a non-color `hover:border-paper/40` underline), `SiteFooter.tsx` (**redesigned 3.03**: brand block + **Навигација / Контакт / Следете нѐ** columns + copyright — the old demo contact/social values are **removed**, now visible `[PLACEHOLDER]` chips for email + socials; the „неофицијална архива" label + statement come from `src/lib/facts.ts`; dynamic copyright year; crest on a white tile — D-crest-1), `Container.tsx`; plus `fonts.ts`, `lib/nav.ts`, `lib/utils.ts`, `lib/facts.ts`, `lib/focus.ts`. `globals.css` has the reveal + placeholder-hatch utilities.
- `src/sanity/env.ts` · `client.ts` · `image.ts` · `structure.ts` — connection, read client, image builder, Studio desk structure.
- `src/sanity/schemaTypes/` — the model (2.01 lock, **re-opened additively for Part 3 at 3.01**, re-locked after 3.06): `index.ts` + `siteSettings`, `season`, `person`, `photo`, **`clubRecord`** (new at 3.01). `match.ts` is in-repo but **unregistered** (deferred, D-2.01-2). **`season` (3.01, D-3.01-1..7):** after `story` it now has five **optional** fields — `teamPhoto` + `tablePhoto` (`reference → photo`, picker filtered to the same season via `options.filter`), `trainer` (string, the new primary trainer field), `lineupAndStats` + `results` (portable text, same `block` config as `story`) — then the **legacy** `finalTable`/`squad`/`trainers` (unchanged in shape; descriptions now say „Задржано за компатибилност…"). **`clubRecord`** (`clubRecord.ts`, D-3.01-5): curated Statistics records — `label` (req), `value` (req), `category` (radio: scorers/appearances/honours/other), `order`; shown in `/studio` as „Клупски рекорди" (plural desk title set in `structure.ts`). Photos link **one direction only**: `photo.relatedSeason` / `photo.relatedPerson`, read by GROQ back-reference — the `season.photos`/`person.photos` arrays no longer exist (D-2.01-1). `season.decade` required; `season.slug`/`person.slug` unique per type via `src/sanity/lib/isUniqueSlug.ts` (D-2.01-6). All 3.01 changes are additive/optional (D-3.01-6) — the 96 existing seasons stay valid. Schema deployed to workspace `belasica-v2` (never the stray `default`, D-2.01-8). **`person` (3.27, D-3.27-1/-2/-7): three new OPTIONAL fields**, so all 211 existing documents stay valid — **`trainerYears`** („Години како тренер") and **`officialYears`** („Години во раководството"), both strings like `playingYears` because the sources give spans, open-ended terms and single years; and **`nationalStats`** („Статистика од целата кариера") — an object of `appearances`, `goals` and **`sourceNote`**, holding the **whole career across every club and the national team**. ⚠️ **`nationalStats` is deliberately NOT merged with `careerStats` and neither ever falls back to the other** — `careerStats` is Belasica-only and authoritative (D-2.01-3), these come from public records, and substituting one for the other is the OV-47 defect. `careerStats` was retitled **„Кариерна статистика (Беласица)"** so the two cannot be confused in the form (D-3.27-7); its name, shape and data are unchanged. **No `isNationalTeamPlayer` was added** — Репрезентативци membership stays `INTERNATIONAL_SLUGS`, whose order is Ace's own numbering (D-3.27-1). All three fields ship **empty across all 211 people** (D-3.27-5). Verified `optional: true` via `sanity schema extract`, and `sanity schema list` shows exactly one deployed schema, workspace `belasica-v2`.
- `public/crest.png` — crest artwork (on main); since 2026-07-30 the white outside the pennant outline is transparent (D-crest-2 — dimensions still 864×1220, so the OG metadata in `layout.tsx` is unchanged; OG scrapers now composite the crest on their own background). `public/znamenca/` — **new 3.19, cut out 2026-08-17**: the marks the homepage hero renders in place of the crest, numbers 1, 6 and 9 of a larger set in the owner's order; the rest are not in the repo. `zname-06.webp` (446×520, 57 KB) and `zname-09.webp` (438×520, 44 KB) are now owner-supplied **background-free** replacements of the original scans; the hero's slot 1 is `/crest.svg`, so ⚠️ **`zname-01.webp` (368×520, 61 KB) is orphaned but deliberately kept** as the archival scan (D-crest-3…-5). ⚠️ **What they depict is recorded nowhere** — no `facts.md` entry (OV-46). `sanity.config.ts` / `sanity.cli.ts` (repo root) — Studio + CLI config.

## Tracked data & scripts — NOT application code (3.11)

> Nothing under `src/` imports any of this. It adds **zero** bytes to every bundle and cannot affect the build — `npm run build` is 271/271, unchanged.

- **`data/book/`** — the tracked **source drop** from „ФК Беласица – гордоста на Струмица" (Аце Стојанов, финална верзија **04.10.2025**). Seven files, ~6,3 MB: `seasons.json` (96 сезони — наслов, приказна, натпревари, состав, топ-стрелци, официјален биланс), `matches.json` (**2.267** натпревари — домаќин, гостин, резултат, натпреварување, есен/пролет, коло, исход), `master-table.json` (the book's own „БЕЛАСИЦА НИЗ ИСТОРИЈАТА", 107 rows — the **independent control** on the extraction, and OV-14's book side), `index.json` (lightweight per-season index), `season-content.json` (what the fill script reads — **2.563** result lines, **167** story paragraphs, plus `resultsProvenance` per season), `README.md` and `extraction-report.md` (Macedonian, written for Аце). **Every match and squad row carries `sourceLine`**, the book's original line verbatim. **Owed: a native read of the two Markdown files by Lazar and Аце.**
- **`scripts/fill-season-content.mjs`** — fills the **existing** `season.story` and `season.results` `block[]` fields from `season-content.json`. **No schema change, and none is needed.** Plain Node ESM, no new dependency. **Every write is `setIfMissing`** — a season that already has content keeps it, and a re-run changes nothing; Portable Text keys are a deterministic SHA-1 of slug+field+index+text, committed with `autoGenerateArrayKeys: false`, so the patch is idempotent. **Dry run is the default** and writes nothing to Sanity (no token needed, read access only); `SANITY_API_WRITE_TOKEN` is required **only** with `--commit`, and without it `--commit` exits non-zero before touching anything. Flags: `--commit`, `--only <slugs>`, `--field story|results|both`, `--report <path>`. ✅ **RUN 2026-08-02 on the owner's instruction, after 3.11 shipped it unfired.** `--commit --field both`: **86 of 96** seasons patched, exit 0, 0 drafts at run time. Live dataset `story` **66 → 96**, `results` **10 → 95**. Because the season template runs `revalidate = 60`, it appeared **without a redeploy**. Verified across all 96 live pages: **2.563/2.563 result lines render**. ⚠️ Eleven seasons keep a pre-existing 3.02-Story story rather than the book's — correct `setIfMissing` behaviour; `1992-93` is the one real gap (human-step 21). ⚠️ **Known latent edge, inert today:** the script's query does not exclude drafts, so with a write token an open Studio draft for a season could take that slug's place in the lookup and the patch could land on `drafts.season-<slug>`, where the site would never show it. **Verified against production with the real token: 96 season documents, 0 drafts** — safe to run as-is; re-check any season left open in Studio. Hardening if ever needed: `*[_type=="season" && !(_id in path("drafts.**"))]`.
- **`docs/ingestion/season-content-report.md`** — the committed **dry-run** report. 96 seasons processed, 86 would be patched, 10 skipped (the 1930-31…1940-41 run 3.04 already filled). Regenerated on every run.

## Integrations wired

- **Sanity** — wired and **serving real content**. Project **`belasica`** (id `f8rmnfry`), dataset **`production`**, **public-read, no token** (D-1.04-1/2). Homepage reads published content server-side (ISR 60s). `cdn.sanity.io/images/**` allowed for `next/image`. Env vars in `.env.local` (git-ignored) + Vercel (Prod + Preview): the three `NEXT_PUBLIC_SANITY_*` values, no token.
  - **Locked schema deployed (2.01)** via `npx sanity schema deploy` → workspace **`belasica-v2`** on `production` (`_.schemas.belasica-v2`). `/studio` is embedded in Next and bundles its schema **from code**, so it shows the locked model as soon as the PR deploys — the Content-Lake manifest is for tooling. Deploy writes `dist/static/create-manifest.json`; `/dist` is now git-ignored.
  - ⚠️ **Two schema manifests exist on this dataset.** A stray **Studio-deployed `default`** workspace (hosted studio app `q93d4lfwpetxz05s17ulprtb`) carries a *completely different, non-project* model (`page`, `story`, `season{label,startYear}`, `person{fullName}`, `source`/`verified`) — not from this repo, predates it. **Schema-aware tooling defaults to `workspaceName: "default"` and will read the WRONG model** — always pass `workspaceName: "belasica-v2"`. Left in place deliberately (D-2.01-8); zero runtime impact.
- **Vercel** — connected; production `https://belasica-v2.vercel.app` (D-0.00-6). `origin/main` now includes the crest (PR #7) + 1.06 docs (PR #8); the 8-section homepage lands when **PR #9** merges. Preview deploy per PR (public, D-1.01-5); Deployment Protection off. PRs this stretch: [#7](https://github.com/DinovLazar/belasica-v2/pull/7) crest (merged), [#8](https://github.com/DinovLazar/belasica-v2/pull/8) 1.06 (merged), [#9](https://github.com/DinovLazar/belasica-v2/pull/9) 1.05.2 (this — preview verified 200, 8 sections, portraits load: `https://belasica-v2-git-phase-1052-homepage-72474b-dinovlazars-projects.vercel.app`).
- Vercel Web Analytics — `<Analytics/>` in root layout (cookieless).
- Claude Code GitHub Action — **not installed** (D-1.01-4); Part-1 review-gate item **formally waived** (D-1.06-1). The Vercel PR preview is the one automated safety rail.

## Owed-verification register

> Items the executor could not verify and owes to Lazar/Ace. At 3+ items, the next phase is a verification phase.

**NEW at 3.27 — five items. OV-58 is the consequential one: 15 men leave the Играчи tab, and who counts as a player is the owner's call, not the executor's.**

- **OV-58 (content / editorial — for the owner) · Confirm the 15 men who drop out of „Играчи".** The tab is now Ace's ranked list (`legendRank`) rather than the `player` role, taking it from **153 to 138**. **Nobody leaves the site** — all 15 remain in another tab and the distinct roster is unchanged at 211 — but the owner must confirm the list before merge. **Still in Тренери (10):** Аце Стојанов, Дервиш Хаџиосмановиќ, Звонко Тодоров, Илија Матеничаров, Јордан Николов, Југослав Тренчовски, Ристо Божинов, Томе Ефтимов, Чедо Хаџиски, Шефки Арифовски. **Still in Репрезентативци (5):** Горан Пандев, Ацо Стојков, Горан Попов, Игор Ѓузелов, Тони Бандулиев. ⚠️ Note that **Аце Стојанов**, the author of the book the whole ranking comes from, is himself among the 15 — he is not in his own ranked list. If any of these should stay, the fix is a `legendRank` typed in Studio, not a code change (D-3.27-3).
- **OV-59 (copy — for Lazar) · Native read of the new Studio field titles and descriptions, plus three new visible labels.** Studio: **„Години како тренер"**, **„Години во раководството"**, **„Статистика од целата кариера"** (with sub-fields „Настапи (цела кариера)", „Голови (цела кариера)", „Извор на бројките"), and the retitled **„Кариерна статистика (Беласица)"** (D-3.27-7). On the site: the Кариера group labels **„Беласица"**, **„Цела кариера"**, **„Периоди"** (with „Тренер" / „Раководство"), and the card line **„Цела кариера: 38 настапи, 16 голови"**. Full description strings are quoted in the completion report.
- **OV-60 (content — for Ace and Cowork) · All three new fields are empty across all 211 people and need the content pass to fill them.** `trainerYears` and `officialYears` are the years a man **coached** and **served**, never his playing span. `nationalStats` is his **whole career** — every club plus the national team — and is **not** the Беласица figure, which stays in „Кариерна статистика (Беласица)". ⚠️ **`sourceNote` should be filled whenever a number is**, because it is what lets a reader check a figure the club's own records do not back; it renders on the person page beside the numbers.
- **OV-61 (visual — for Ace) · Confirm that separating „Беласица" from „Цела кариера" on the person page reads clearly.** It cannot be judged on the live site yet — every whole-career field is empty, so today only the „Беласица" group renders. It was verified with injected test values and reverted (D-3.27-13); the shape is „Кариера → Беласица (Настапи/Голови) → Цела кариера (Настапи/Голови + извор) → Периоди (Тренер/Раководство)". ⚠️ Also for Ace: the **Репрезентативци card deliberately shows no year span at all** (D-3.27-6), because `playingYears` is a Беласица span; say if a span is wanted there.
- **OV-63 (owner decision — for Lazar and Ace) · Squad names do not link, in practice: 1 of 1.982 rendered rows.** The policy is an exact whole-string match, chosen deliberately because the looser options assert identities the book does not make (D-3.11-2): **121 surnames carry more than one distinct book string** — Митев has ten, Василев eight, Николов seven — so a surname match would put the wrong man behind a link. The result is one underlined name (Давид Стојков) among 1.981 plain ones, which reads as a rendering fault rather than a feature. **The honest choices are to keep it, or to drop squad linking entirely until a verified alias map exists.** Kept for now, per instruction. **How to check:** open any season with a squad table and look at whether one lone link looks broken.
- **OV-64 (content — for Ace) · 27 matches whose scorer figures read as goal counts, not minutes.** The extraction report calls every figure a minute; **1.323 of the 1.435 numbered matches contain a number above 12 and certainly are** — but the book also prints „Беласица – Локомотива (Гевгелија) 5:0 (Иванов 4, Чулевски)“, where 4+1 = 5 goals. **112 matches carry only low numbers and 27 of them sum exactly to Беласица's goals when read as counts.** The site prints the figures with **no unit and no marker**, so it is correct either way and asserts neither (D-3.28-5). **How to check:** Аце reads those 27 against the book and says which reading is right; only then can a minute marker ever be added.
- **OV-65 (owner awareness — for Ace) · These tables are NOT editable in Studio.** Exactly the „Разно“ trade (D-3.16-2): the rows live in `src/content/season-tables.ts`, generated from `data/book/*.json`, so a correction is a code change, a regeneration and a deploy — not a Studio edit. ⚠️ **His original text is untouched and recoverable**: `results` still carries 2.579 blocks across 96 documents and `lineupAndStats` 2.433 across 84, none of them written to by this phase. **How to check:** Аце is told before he tries to fix a scoreline in Studio and finds it does nothing.
- **OV-66 (visual — for Ace) · Read the tables on real season pages and confirm the grouping and the columns are what was meant.** Specifically: (a) that a season's **qualification and playoff ties belong in their own table** below the league table, which is how 1987/88, 1950, 1954/55 and 1938/39 now read — the brief assumed a Куп grouping, and **the book's season section records no cup ties at all** (D-3.28-3); (b) that **Натпревар is left as home — away** and never reordered to put Беласица first, since home/away is a fact; (c) that the есен/пролет label uses the book's own wording („Есенски дел 1988“) rather than a bare „Есен“. ⚠️ Also owed: a **native read of the new Macedonian column heads** — „Коло“, „Натпревар“, „Резултат“, „Стрелци“, „№“, „Име“, „Настапи“, „Голови“.
- **OV-62 (verification — for Lazar) · The three new Studio fields were confirmed from the deployed schema, not by opening a document in `/studio`.** The DoD asks for three documents opened in Studio; the in-app browser cannot authenticate to Studio, so this was verified instead by `sanity schema extract` (all three fields and all three `nationalStats` sub-fields report `optional: true`) and by `sanity schema list` showing exactly one deployed schema, workspace **`belasica-v2`**, dataset `production`. Since all three are optional and absent from every document, **no existing document can fail validation** — but the eyeball is still owed. **How to check:** open one player, one coach and one president in `/studio` and confirm the new fields appear with no validation error.

**NEW at 3.24 — five items, and one closure (OV-52). OV-57 is the consequential one: the appearance threshold this phase was told to ship does not do what it was meant to do, and it removes Горан Пандев from the table.**

- **OV-57 (content / editorial — for the owner) · „Најмногу настапи" now cuts at 46 appearances, which removes 3 players of 119 — not the ~24 intended — and the three are Горан Пандев (38), Ацо Стојков (35) and Горан Попов (26).** The brief set the cut believing the table held **71** rows and expecting **47** to survive; it holds **119**, of which **116** clear 46. The threshold was shipped as instructed and the real number reported (D-3.24-4). ⚠️ The three men it drops are among the club's best known, with small Беласица totals precisely because they left young — and **Пандев still appears on „Најдобри стрелци" directly above**, so one page now lists him as a scorer and omits him from appearances. **What is owed:** the owner's actual number. For his decision: no threshold yields exactly 47 today — **≥131 gives 46 rows, ≥130 gives 48** (Коцев and Џорлев are tied on 130). It is one constant, `APPEARANCE_MIN` in `src/app/(site)/statistika/page.tsx`.
- **OV-53 (copy — for Lazar) · Three new Macedonian strings need a native read on screen.** „Прочитај повеќе за младинската школа" (the homepage moment link), „Теми од историјата" (the „Разно" quick-link sub-label), and „Листата ги опфаќа играчите со 46 или повеќе внесени првенствени настапи за Беласица." (the appearances cut). All three are structural/navigation copy making no factual claim; the third mirrors the scorers' sentence word for word apart from the noun and the number.
- **OV-54 (verification — for Lazar) · Keyboard activation of the statistics sort headers was not exercised.** Sorting itself is verified — a real mouse click toggles it, both directions, 116 rows preserved, and the orange focus ring is visibly on the header button. But `Enter`/`Space` could not be tested: the in-app browser's key delivery to this pane timed out repeatedly. The headers are native `<button type="button" onClick>` elements, so activation is browser-native behaviour, and **`StatTable.tsx` is not in this phase's diff** — the risk is inherited, not introduced. **How to check:** Tab to a column heading on `/statistika` and press Enter.
- **OV-55 (visual — for Lazar and Ace) · Eyeball the enlarged lightbox on a real phone and confirm the softness of an upscaled scan is acceptable.** D-3.24-1 accepts that a scan smaller than the box now looks softer; photo 5 of `/arhiva/1985-86` upscales 1,38×. Most scans are unaffected (the median published photograph is 1 066px tall and is now *downscaled* into the bigger box), so this is about the minority. If it reads as too soft, the lever is the `max-h-[62vh]`/flex sizing in `PhotoLightbox.tsx`, not a filter.
- **OV-56 (editorial — for Ace) · Confirm the pinned „Момент од историјата" photograph is the right one for the front page.** It is now fixed in code rather than resolved by ordering (D-3.24-2): „Младата екипа на Беласица со Купот на Македонија, 1993". This phase pinned **what was already resolving** — it did not choose a new picture — and the link beneath it now depends on that choice being right.

✅ **Nothing is owed from the Sanity `vercel.app` sweep.** It read **2 443 published documents** and found **0** occurrences of `vercel.app` or `belasica-v2` in any field, so there is nothing for Ace to fix in Studio (D-3.24-6).

**NEW at 3.20 — three items. OV-51 is the consequential one: 64 photographs are now public with no provenance record of any kind.**

- **OV-50 (content — for Ace) · Thirty-five of the 64 „Разно" photographs have no caption.** 29 do. The rest arrived with machine-generated filenames (`FB_IMG_…`, `Messenger_creation_…`) carrying no information about the picture, so they render with no caption and `alt=""` — a screen-reader user gets no description of them (D-3.20-6). Nothing was invented and no placeholder chip was reintroduced. **How to fix:** Ace describes any he recognises; each is one `caption` line in `src/content/razno-photos.ts`. **The files are named `<slug>-NN.webp` in his own folder order**, so „mladinska-skola-14" identifies one unambiguously.
- **OV-51 (rights / provenance — for the owner and Ace) · The 64 new photographs carry no provenance record at all.** Every one of the 881 ingested photos is a Sanity document holding a `provenance` string, rewritten at D-3.02-5 to the confirmed-rights wording. **These 64 are static files under `public/` — not documents — so they have no such field, and nothing anywhere in the repo says where they came from** (D-3.20-1). `facts.md` records the owner's confirmation for „**all archive photos**" and „all 889 published photos"; whether that statement was meant to cover this later batch has not been asked. The original filenames — which hinted at Facebook/Messenger origins — **were replaced by the `<slug>-NN` renaming**, so that hint is no longer recoverable from the repo. **Two things owed:** confirm the rights cover these 64, and decide whether a provenance note should live beside them (a header comment in `razno-photos.ts` would do, since there is no document to hold one).
- **OV-52 — ✅ CLOSED at 3.24.** The overlay was opened in a real browser on **both** call sites — `/arhiva/1985-86` and `/razno/mladinska-skola` — at **1280×800 and 375×812**, and stepped through with the arrows. The per-topic accessible name is **confirmed live**: the „Разно" dialog announces `Фотографии — Младинска школа на Беласица` (D-3.20-3), the season one `Фотографии од сезоната`. The 3.19 arrow/gutter fix holds in both contexts and at both widths: **no control's rectangle intersects the painted photograph in any measured case**, and at 375 the arrows sit below the picture (measured: picture bottom 409px, arrows at y=651). Escape closes, focus returns to the triggering thumbnail, and the scroll lock releases. ⚠️ **One thing still owed, as its own item:** keyboard *activation* of the sort headers could not be exercised — see OV-54. *Superseded text: „The „Разно" lightbox has never been opened, and its dialog cannot be checked from the built output… `role=\"dialog\"` appears 0 times in the prerendered HTML."*

**NEW at 3.19 — five items. OV-46 and OV-47 are the consequential ones: the first puts three unidentified images at the top of the front page, the second is the cost of reversing a content-truth decision. The register is now well past the „next phase is a verification phase" threshold.**

- **OV-45 (native-speaker read — for Lazar/Ace) · Four new Macedonian strings have not been read on screen by a native speaker.** „Три знаменца на ФК Беласица" (the hero row's `aria-label` — heard, not seen, so it needs reading aloud rather than looking at), „Скок низ темата" (the „Разно" rail's landmark name), and the two section headings **„Преглед"** and **„Рекорди"**. „Преглед" already stands on the season page from 3.13, so the question for the topic pages is only whether it reads right over transcribed book prose. **How to check:** open any of `/razno/kup-na-uefa`, `/razno/partizan`, `/razno/tiverija` and read the rail and the two headings.
- **OV-46 (content truth — for Ace) · What are the three znamenca, and are these the right three?** The homepage's most prominent mark is now three pennant scans, and **`facts.md` holds no entry for them** — no date, no occasion, no provenance. The `aria-label` therefore says only „Три знаменца на ФК Беласица" and deliberately dates nothing (D-3.19-4). Two questions: **(1)** is „знаменца" the right word, and is it true that all three are Беласица's? **(2)** the files are numbered **1, 6 and 9 of a larger set** and only those three are in the repo — is that the selection and the order he wanted? Changing either is one edit to the `ZNAMENCA` table in `page.tsx`. **How to check:** load the homepage and look at the three pennants above „ФК БЕЛАСИЦА".
- **OV-47 (content truth — for Ace) · The appearance number on a legend card now comes from one of two differently-sourced fields, and the card does not say which.** `legendAppearances` is the book's printed figure, transcribed; `careerStats.appearances` was „entered here from the source docs" (D-2.01-3). Until 3.19 only the first was ever rendered, precisely so the number's provenance was single and known (D-3.15-12). The owner's instruction overrode that for display, and **nothing was written into the book's field** — but a reader now cannot tell a transcribed figure from a compiled one. **OV-39's underlying question is still open and is now the way to close this**: does the book print appearance counts for ranks 3–50? If yes, transcribing them makes the two sources one and the ambiguity disappears.
- **OV-48 (content truth — for Ace) · Eighteen players show no appearance count at all, and two of them are on the homepage.** The instruction was „кај сите играчи треба да има бројка на натпревари"; 80 of 98 now have one. The remaining 18 hold **neither** field. Two are in the homepage's own top ten — **Томче Ефтимов (#8)** and **Панче Пантазиев (#9)** — both `legendAppearances: null` and `careerStats.appearances: null` in `production`, verified by query. Пантазиев is a known case: D-3.12-2 records that the book ranks fifteen of its eighty on a count printed only as a **range**, which no number field holds. **If Ace can supply a figure (or a range) for these two, they are the highest-value ones to fix** — they sit on the front page. Rendering a zero or a dash was refused (content truth; the no-`0` rule from 2.04).
- **OV-49 (content drift — for the owner) · The Играчи/Тренери split moved from 86 · 46 to 98 · 34 through Studio edits, and nobody has reviewed it.** Measured in this phase's build and confirmed by query: **98 играчи · 34 тренери · 28 претседатели = 160**. The total is unchanged, so nobody was added or removed — **twelve people gained a `player` role** since 3.17, which moves them from Тренери to Играчи under the priority rule (D-2.05-2). **This phase did not cause it** and no code changed; it is live content drift, surfaced here because the snapshot asserted the old split. Consequence: those twelve are now sorted by `legendRank`/appearances rather than by the derived newest-first chronology (D-3.13-4), and each now shows an appearance count if one exists. **How to check:** open `/legendi` and confirm the three band counts are intended.

**NEW at 3.10 — three items, one of them an owner decision that changes the site's most prominent image:**

**NEW at 3.17 — two items, both readings rather than decisions. Neither blocks the merge; both should happen before Ace sees the site as finished.**

- **OV-43 (native-speaker read — for Lazar/Ace) · Three new Macedonian strings have not been read on screen by a native speaker.** They are the only copy this phase wrote: the two rail names „**Скок по улога**" (`/legendi`) and „**Скок низ статистиката**" (`/statistika`), and the back-to-top button's accessible name „**Назад на врвот**", which is `sr-only` — it is never seen, only *heard*, so it wants judging as speech rather than as a label. All three follow the pattern of the two rails already live („Скок по деценија", „Скок низ сезоната"), so the risk is low and the check is quick.
- **OV-44 (content judgement — for Ace) · The roster divider groups the first eleven of a numbered list, and only he can say whether the book's numbering means what that implies.** On 51 season pages a hairline now falls after line 11. The rule is deliberately silent — no label, no `<hr>`, `aria-hidden` — precisely because the archive cannot assert that those eleven started (D-3.17-4). **But a rule in that position still suggests it.** If the book's numbering is a squad list in some other order, the divider is drawing a line through nothing and should come out. Worth checking against two or three seasons he knows.

**NEW at 3.16 — three items. OV-40 is the consequential one: it is a visible layout break at tablet widths, and it needs a decision about a component this phase was not allowed to edit.**

- ✅ **OV-40 · RESOLVED 2026-08-14 (3.23, D-3.23-1).** The burger breakpoint moved `md` → `lg`, so the horizontal nav appears only at ≥1024 px and the burger panel — which already listed all seven items at 48 px targets — covers everything below. **Re-measured after the change at 375, 768, 810, 820, 899, 1024, 1280 and 1408 px: the header is 78 px at all eight, delta 0** against the `--spacing-header` token. Anchor offsets at 810 px are now `/arhiva` **+5 px**, `/legendi` **+5**, `/statistika` **+5** (the `3.25rem` reserve is 52 px for a 47 px rail) and a season page **0 px** flush (its own rail is not sticky) — all non-negative, where before they were −18 to −23. ⚠️ **Owed the owner:** this changes the header on every route at tablet widths. ⚠️ Also caught in passing: moving the nav without moving the toggle left 768–1023 px with **no navigation at all** — found by measuring, not by reading. *Original text:* **The seven-item header nav wraps to two rows between roughly 769 and 898 px.** Measured with „Разно“ toggled off in the same DOM to get a true pre-phase baseline: at **768 px the six-item nav already wrapped** — pre-existing — and at **790 / 820 / 860 px six items fit on one row and seven do not**. At **899 px and above** (and at 900 / 1280 / 1408) seven fit on one row. At **375 px** the desktop nav is hidden and the burger panel lists all seven cleanly (48 px targets, orange left bar on „Разно“). **No horizontal overflow at any width tested.** The cost is not only cosmetic: in the wrap band the sticky header is **101 px tall while `--spacing-header` still claims 78 px**, so every `scroll-mt-header` anchor lands ~23 px under it and the archive's decade rail sticks 23 px too high — across the whole site, on any route, at iPad-portrait widths (810, 820). **Not fixed here:** the brief states that `nav.ts` and `sitemap.ts` are the only shared files this phase edits, and `SiteHeader.tsx` is neither (D-3.16-11). Smallest likely fix: `gap-7` → `gap-5` in the `md` range. Better fix, which also clears the pre-existing 768 px break: move the burger breakpoint from `md` to `lg`. **Both change the header on every route — the owner's call.**
- **OV-41 (native-speaker read — for Lazar/Ace) · The eight new pages have not been read on screen by a native speaker.** Sixteen new Macedonian strings: the `/razno` intro, the seven card summaries, the source line and the eight meta descriptions. *(The 101 transcribed paragraphs are the book's own words and are deliberately unedited — a reader should know that before flagging „додеака“ or „својотклуб“ as mistakes. They are the author's, D-3.16-3.)*
- **OV-42 (naming — for Ace) · Do the seven topic titles and the section name „Разно“ read right to him?** The titles are the brief's, not the book's chapter headings — the book's numbered headings („16. Младинска школа на Беласица“, „22 Навивачи „Ајдуци ““) are deliberately not rendered, because their chapter numbers are meaningless on the site.

**NEW at 3.15 — two items. OV-39 is the consequential one: it decides whether 48 of the 80 ranked legends ever show an appearance count.**

- **OV-39 (content truth — for Ace) · Does the book print appearance counts for ranks 3–50?** `legendAppearances` was populated for **32 of the 80** ranked players — ranks **1–2** (the owner's own figures, 555 and 383) and ranks **51–80** (the only ones `data/book/legends.json` carries). **Ranks 3–50 render a rank and a name with no count**, which is a visible gap in the middle of the list (D-3.15-5). **The archive almost certainly already knows those numbers:** 43 of those 48 people carry `careerStats.appearances`, and the values decrease monotonically with rank — 366, 336, 328, 262, 260, 235, 235, 232, 230, 229 … — which is what a transcription of the same ranked list looks like. It was **not** backfilled, because `legendAppearances` is documented in Studio and in the schema as *the book's printed value* and `careerStats` has a different recorded provenance (D-2.01-3); copying one into the other would assert a provenance nobody has checked (D-3.15-12). **What's needed:** Ace confirms whether the book prints a count for ranks 3–50 and whether those are the same numbers as `careerStats`. If yes, the backfill is a one-line change to the same script and all 80 gain a count. **Cross-check already done and clean:** across all 32 written, the book and `careerStats` **never disagree** (0 conflicts), and the owner's two values are independently corroborated by `careerStats` (555 and 383 exactly).
- **OV-38 (native-speaker read — for Lazar/Ace) · The new Macedonian copy has not been read by a native speaker on screen.** Three new strings: the footer's copy-permission line, and the two „За нас" paragraphs. The About copy is **the author's own writing, rendered byte-for-byte** and explicitly not to be edited — so this is a *proofreading and rendering* check (does it read right on the page, do the „…" quotes and the en dash land correctly at every width), not an invitation to rewrite. The permission line is the owner's wording and is the one genuinely new claim the site makes about itself.

**NEW at 3.14 — three items. Two are content-truth questions the renames created; one is corroboration for a fact the owner supplied. None blocks the PR — but OV-35 and OV-36 are claims the site now makes.**

- **OV-35 (content truth — for Ace) · „Претседатели" asserts that all 28 people in that band were club presidents.** The old label „Раководство" claimed only that they were officials; the new one, and the count „28 претседатели", claim the specific role (D-3.14-1). Nobody has checked the band's membership against the book. **If it holds a vice-president, a secretary or another official, the label is now wrong in a way the old one was not** — and the fix would be either a different label or splitting the `president` role in the schema. Ace to confirm the 28 are all presidents.
- **OV-36 (content truth — for the owner) · „Резултати и стрелци" over-promises on 19 season pages.** Measured across the built HTML: of the **95** season pages that render a results section, **76 name at least one scorer and 19 do not** — `1922-26`, `1926-1930`, `1930-31`, `1931-32`, `1932-33`, `1933-34`, `1934-35`, `1935-36`, `1936-37`, `1937-38`, `1938-39`, `1939-40`, `1940-41`, `1942`, `1943`, `1944`, `1945-48`, `1948-49` and `2012-13`. The book records only scores for those years. The heading is one shared constant, so it says „и стрелци" on all 95 (D-3.14-3). Owner to decide: accept it, or make the „и стрелци" half conditional on the season carrying a scorer.
- **OV-37 (corroboration — for Ace) · Петар Андреев's trainer service is uncorroborated.** The role change to `["player","trainer"]` was made on the owner's instruction (D-3.14-4), which is the source of record for `person.role`. But the book records him **primarily as a player from 1974**, and no source in the repo names him as a trainer. Ace to confirm he coached the club, and ideally in which years — a trainer year would also place him correctly if a future phase ever ranks the Тренери band (D-3.13-4).

**NEW at 3.13 — three items. All three are questions of judgement for Ace and Lazar; none blocks the PR.**

- **OV-32 (question for Ace) · Officials are ordered by the END of their term, and one row disagrees with the brief.** The derived key is the **latest** year in a president's term, so „периодот 1991–1992" sorts as **1992**. That ties **Александар Оздоленовски** with four officials whose line reads „во 1992 година", and the specified tie-break (`compareByName`) puts him **first** of the five at **#11** — where the brief's own supplied table puts him **last, at #15**, which is only reachable by breaking ties on the term's **start** year (D-3.13-7). Two questions close this: is ordering officials by the end of their term how Ace thinks of the succession, and should a term that began earlier sort below one that did not? If start-year tie-breaking is wanted, it is a small change to `compareByRecency`. **The other 27 rows, and all 46 Тренери rows, match the brief exactly.**
- **OV-33 (question for Ace) · Seasons with several coaches separate only by name.** A trainer's key is the **latest season** their name appears in, so a coach sacked mid-season and the one hired to replace him both carry that season's year and are then ordered alphabetically — Благој Гуцев and Шефки Арифовски both sit on 2022, and Андреј Чернишов / Ване Милков / Ѓоре Јовановски all on 2020. Ace should confirm that reads correctly, or say which of each pair should lead.
- **OV-34 (native read) · five heading strings have had no native-speaker check. EXTENDED at 3.14.** „Преглед" stands from 3.13; **„Играчи и тренер" was replaced at 3.14 and is no longer rendered anywhere**, so it drops off this item. The strings now owed a native read are **ПРЕГЛЕД** (3.13) and, new at 3.14, **ПРЕТСЕДАТЕЛИ**, **СТАТИСТИКА НА ИГРАЧИ** and **РЕЗУЛТАТИ И СТРЕЛЦИ** — plus the count noun **„28 претседатели"** under the band heading. All were supplied verbatim by their briefs and all render uppercase, but neither Lazar nor Ace has confirmed they read correctly as Macedonian section headings. Separate from OV-35/OV-36, which ask whether the new strings are *true*, not whether they read well.

**NEW at 3.12 — five items. Three are questions for Ace about his own numbers; none blocks the push.**

- ✅ **OV-27 · RESOLVED 2026-08-08 (3.15).** `legendRank` was written at 3.12 but never declared in the deployed schema, so it worked on the site and was invisible in Studio — Ace could not correct a rank. **The schema is now deployed** (`npx sanity schema deploy --workspace belasica-v2`, D-2.01-8) and both fields are confirmed present in the deployed `belasica-v2` manifest, read back through the Sanity MCP's `get_schema`: **`legendRank`** („Ранг по настапи (книга)", number) and the new **`legendAppearances`** („Настапи по книгата", string), both carrying Macedonian titles and descriptions and both positioned together immediately after „Години на играње". *(Verified via the deployed manifest rather than a Studio login — the in-app browser cannot complete Sanity's OAuth.)* Studio is embedded at `/studio`, so the same change also ships to the live Studio on merge.
- **OV-28 (question for Ace) · Two of his typed numbers disagree with the dataset, and were not changed.** He wrote **„4. Костадин Секулов 337“** where Sanity holds **336**, and **„6. Љупчо Мафков 262“** where the book itself puts **Дончо Георгиев** at #6 with 262 and Мафков at #7 with 260. His list omits Дончо Георгиев altogether, which reads like a slip while typing rather than a correction. The page follows the book. **One question closes this.**
- **OV-29 (question for Ace) · The book contradicts itself on Илија Андреев.** He is printed at **#33**, but his own entry states **167** appearances — one more than Коце Костадинов (#31) and Ристо Анчев (#32), both at 166. The printed rank was followed. Either the rank or the number is a typo in the source.
- **OV-30 · `Томе Ефтимов` and `Томче Ефтимов` may be one man in two documents.** `tomche-eftimov` (player + trainer, 1950–1967) received the rank-8 biography; `tome-eftimov` is a trainer-only record with no years. The book’s own trainer table uses both spellings for what looks like the same person. If they are one, one document should be merged away — **and rank 8 currently sits on the `tomche-` record**.
- **OV-31 · 126,297 characters have had no native-speaker read-through.** The words are Ace’s own, so the Macedonian is his — but the transcription touched roughly ninety places where the `.docx` had run words together, and nobody has read the sixty biographies end to end. A reading pass by Lazar or Ace is the honest next check.

- **OV-24 (owner decision) · The 375 px hero crop cuts the front row at both edges.** The pinned 1982/83 scan is 1.934:1; the mandated base box is `3/2` (1.5:1), so `object-cover` shows only **11.2 %–88.8 %** of the picture's width and the outermost kneeling player on each side is sliced. This is arithmetic, not tuning — `objectPosition` has no vertical effect at that width and can only trade one edge for the other. To show the row whole on a phone the base ratio must be ≈1.93 or wider (`aspect-[2/1]` shows the full width and crops height instead). **One line changes it** if Lazar wants the whole team over the taller box (D-3.10-6).
- **OV-25 · Native read of the new header meta wording, „160 личности".** New Macedonian string, same singular rule as the band counts (only 1 takes the singular). Needs a native ear to confirm it reads naturally above the search field rather than as a database count.
- **OV-26 · Owner confirmation that the 1982/83 photograph is one Ace has publishing rights to.** **OV-1/OV-RIGHTS covered „all Drive photos" generally, but this picture is now the single most prominent image on the site** — it opens every visit. Worth an explicit yes on this one frame before launch.

**Prior — NEW at 3.09 — three items, one of them BLOCKING for 3.11:**

- **OV-19 (BLOCKING for 3.11) · Re-measure `/`, `/arhiva` and `/legendi` on a deployed URL from a clean, wired connection.** The Vercel preview scored **87** and **88** where local scored 94 and 95 — outside the DoD's ±3 tolerance. The fixes demonstrably landed (`resourceLoadDelay` on `/arhiva` **1243 → 15 ms**, `fetchpriority=high` false → true, measured against the pre-fix commit still live on production), but `elementRenderDelay` ~2000 ms now dominates — and it is identical on the homepage, which was never reveal-wrapped, both before and after. **Eliminated by test:** cold Vercel image cache (warmed to `x-vercel-cache: HIT`, no change) and a regression from this phase (the pre-fix control scores 87/90 on the same path). **Untested suspect:** this machine's network — TTFB to Vercel **170–190 ms** vs 3–67 ms on loopback, over a VPN tunnel on a phone hotspot, with a ±7-point run-to-run spread on a single route. Until this is settled, the local numbers are the honest measure of *this phase's changes*, and the Vercel numbers are unverified for absolute launch conformance.
- **OV-20 (owner decision) · One mobile route stays at 94.** `/arhiva`, one point short, reaching 95 in one of three runs. Closing it means dropping `next/font`'s preload, which was built and measured: `/arhiva` **98**, `/` **95**, at the cost of a homepage first paint that doubles and becomes unpredictable (**760 / 1518 / 1512 ms** across three runs, against a stable 759–809 ms). Shipped the stable paint; **one line flips it** if Lazar prefers the points (D-3.09-8).
- **OV-21 · VoiceOver spot-check of landmark roles on `/arhiva`.** Per HTML-AAM only the sticky `SiteHeader` is a `banner` — the other 12 `<header>` elements are scoped to `main`/`section` and are `generic` — and axe reports zero failures with `landmark-one-main` passing. But Chrome's `computedRole` and `getComputedAccessibleNode()` are both unavailable in the in-app browser, so this is **spec + axe verified, not computed-role verified**.

**Open items (14 active + owed spot-checks).** **3.07 added OV-22 (native read-through of the legal page) and OV-23 (the takedown promise is operational — someone must watch the inbox).** ⚠️ **At 3+ items the next phase is a verification phase** — and six of these (OV-13…OV-18) are **content questions for Ace**, not code work, recorded by 3.02F-Code without resolving any of them. OV-RIGHTS + OV-9 resolved; OV-6 resolved at 2.09-Run; OV-10 resolved at 3.02; **OV-4 + OV-7 CLOSED at 3.02F-Code** (✅ inline below — content-verified at scale, no longer fixture-verified). Still open: OV-8, OV-11, OV-12, and the six new content items. What remains open:

- **OV-22 (NEW at 3.07, copy — blocks nothing, but it is the page's whole point)** · **The legal page's Macedonian has never been read on screen by a native speaker.** The ten sections were supplied by the owner as final, native-reviewed text and are rendered **verbatim** — proven by diffing the rendered output against the brief (41/41 lines identical, D-3.07-7) — so the executor introduced no wording. What has *not* happened is anyone reading the finished page in the browser, at size, in context. On a page that makes legal commitments, a transcription that is provably faithful is not the same as prose that reads right. **How it clears:** Lazar and Ace open `/pravni-informacii` on the preview at desktop and on a phone and read it end to end. One specific thing to check: the two quoted phrases render „ФК Беласица“ and „како што е“ with a U+201C closing quote (Macedonian convention, and the repo's existing 4 instances vs 0 of U+201D) — if U+201D was intended, that is a one-character fix (D-3.07-9).
- **OV-23 (NEW at 3.07, operational — not a code item)** · **§5 commits the archive to unconditional takedown on request, and nobody has confirmed the inbox is watched.** The page now tells any rights holder that their material will be removed „без одлагање и без прашања" and that no legal process is needed, naming `info@belasicahistory.mk` as the channel. That is a public promise the site can now be held to; the code is correct and the `mailto:` resolves, but a promise is only kept by a human reading the mail. **How it clears:** confirm the mailbox exists, that someone receives it, and agree who acts on a takedown request and how fast. Related but separate: the Formspree endpoint for `/kontakt`'s form is **still unset** (PL-14 / OV-8), so the contact form is not a second channel yet — right now the `mailto:` is the only working way to reach the archive.
- **OV-10 · ✅ RESOLVED 2026-07-21 (3.02, D-3.02-5)** — all **881** ingested-photo `provenance` strings were rewritten from „…НЕПОТВРДЕНИ…" to the `facts.md`-verified confirmed-rights wording (each photo's source-folder attribution preserved; verified **0** „НЕПОТВРДЕНИ" remain and **881** now carry the confirmed-rights text). ⚠️ **One owner-approval note:** `facts.md` records the rights as VERIFIED prose but holds **no** ready-made Macedonian provenance string, so the new wording is Claude's **faithful transcription** of that verified fact (swapping only the rights clause). It is **not** rendered on the public site and is fully re-runnable — the exact before→after is in §3 of `docs/ace-demo/Part-3-Phase-02-pilot-review.html`; if the owner prefers different phrasing, one script re-run changes all 881. *Original issue (2.08, D-2.08-1): the per-document `provenance` lagged the owner's confirmed rights.*
- **OV-11 (active, from 2.08 / D-2.08-5)** · **Season `2025-26` — the auto-featured newest — holds only 2 images, so the homepage's hero/card/band are content-thin.** The two are a **buildlineup.com fan-made lineup graphic** (`formacija.jpg`) and the genuine **modern FK Belasica squad photo** (`ekipa.jpeg`). This phase captioned the squad photo to promote it to the hero; the fan graphic necessarily fills the featured-season card (its own header reads „Беласица 2025 есен", so contextually apt) and the full-bleed moment band („Момент од историјата"), which is cosmetically weak. **How Lazar/Ace clear it:** add one or more genuine 2025-26 photos in Studio and caption the preferred lead (a caption promotes it, D-2.08-3); and/or decide whether the buildlineup fan graphic should stay published.
- **OV-12 (active, EXTENDED at 3.04-Cowork — a spot-check)** · **Some photos are filed under the wrong era.** The original 2.08 example is now **resolved**: the „BELASICA-FUDBALSKI BUTIK" clipping in `1982-83` was re-examined and is **genuine and correctly filed** (a 1982/83 promotion photo — it reads „Najzad je i Strumica dobila drugoligaša"). But the 3.04-Cowork re-verification found **five newly-identified era-misfiled photos**, none of them used as a team/table image: `1952` (`photo-f1f5c9cd…` — actually the 1951 squad), `1959-60` (`photo-ce4e219b…` — captioned „ФК Беласица 1962"), `1987-88` (`photo-799f4a3e…` — a 1978 Cup-final photo), `1999-2000` (`photo-e8f96cde…`) and `2000-01` (`photo-3ce1d7b6…`). They still appear in the **wrong season's „Фотографии" grid** on the redesigned page. **How Lazar clears it:** re-`relatedSeason` those five in Studio — a photo-document edit, deliberately out of 3.04-Cowork's season-field scope (D-3.04C-3).
- **Owed spot-check, NEW at 3.04-Cowork (not blocking, human-eye):** **four „team photos" in use are not posed squads**, kept deliberately because no posed squad exists for those seasons (D-3.04C-4): `1950` (the youth „подмладок" squad), `1955-56` (a composite portrait montage), `1988-89` (a two-team Bokelj–Belasica pre-match photo) and `2007-08` (an on-pitch goal celebration). Each is a genuine Belasica image; the owner may supply a posed squad instead, and clearing the field self-omits the photo cleanly on the redesigned page.
- **Owed spot-checks (not blocking, human-eye):** (a) spot-check the **10 named season pages** against their source folders — list in `docs/ingestion/2.09-editorial-handoff.md` §7 (era ranges, lone years, slash-form, century rollover, the demo season); (b) confirm the **9 `manual-title` seasons** (`1922-26`, `1926-1930`, `1945-48`, `1942`, `1943`, `1944`, `1950`, `1951`, `1952`) — what each represents. `manual-review` folders: **0** (nothing to resolve).
- **OV-13 (NEW at 3.02F-Code, content — the most visible one)** · **`/statistika` now shows two different all-time totals on one screen.** The `clubRecord` „Вкупна екипна статистика низ историјата" states **2275 натпревари, 1030-421-824, гол-разлика 3907:2976, 2885 бода** (the history book's figure). Directly below it, „Севкупен биланс на клубот" sums the **92** entered `finalTable` rows to **2240, 1018-411-811, 3851:2928, 2849**. Both are faithfully rendered content; neither is computed to look complete. Rendered rather than suppressed on the D-3.04-10 principle (D-3.02F-C-6), and the coverage line already scopes the band to „92 сезони со внесена конечна табела" — but a careful reader will still find the page disagreeing with itself. **How Ace clears it:** decide which all-time total is authoritative and either correct the `clubRecord` or restate it so it reads as the book's claim rather than the archive's own count. *(This is a superset of OV-14 below: the book-vs-spreadsheet gap, now visible on the live page.)*
- **OV-14 (active — content; NEW at 3.02F-Code, HALF-VERIFIED at 3.11)** · **The history book and the spreadsheet disagree on the club's all-time totals.** The book: **2275** matches, 1030-421-824, 3907:2976, **2885** pts. `Беласица 1926-2025-финал.xlsx`: **2265**, 1026-421-824, 3895:2970, **2871**. The site's own balance is a third number (see OV-13) because it sums only the 92 seasons with an entered table. **Ace to reconcile the two sources.** ✅ **3.11 settled one side of the dispute.** The book's own summary table („БЕЛАСИЦА НИЗ ИСТОРИЈАТА", 107 rows) was extracted separately to `data/book/master-table.json` and its totals **reproduce exactly** — `2275 · 1030–421–824 · 3907:2976 · 2885` — read straight from the source rather than transcribed by hand. Re-derived from the file at 3.11, not quoted from the report. So the figure OV-14 attributes to „the book" **is** the book's, and the remaining gap is real and precisely bounded: **10 matches, 4 wins, 12:6 goals, 14 points**, all of it on the spreadsheet's side. The extraction is a third, independent check — it reads **2.267 matches** from the book's *text*, within ±2 of the table on **92 of 96 seasons**; the four divergences (`1950`, `1950/51`, `1954/55`, `1987/88`) are cup and qualification ties the text counts and the table does not, not extraction errors (`data/book/README.md`). **What is still owed is unchanged and is Ace's:** decide which source is authoritative. Nothing was applied — no `clubRecord` was edited and no Sanity write was made.
- **OV-15 (active — content; NEW at 3.02F-Code, a third voice available since 3.11)** · **The spreadsheet contradicts itself on two seasons' points.** `1992/93` reads **34** in the chronological column and **46** in the per-competition block; `1993/94` reads **26** vs **33**. The chronological column was the one transcribed into `season.finalTable`, so those are the numbers the site renders and sums. **Ace to confirm which column is correct** — both seasons currently feed the all-time balance. ℹ️ **3.11 supplies a tie-breaker but does not use it:** `data/book/master-table.json` now holds the book's own row for both seasons and can serve as the third voice. **Deliberately not applied** — which source wins is Ace's decision, not a script's, and this phase made no Sanity write.
- **OV-16 (NEW at 3.02F-Code, content)** · **Four unresolved name variants between the Drive folders and the book.** `Митко Џорлев` / `Д. Џорлев`; `Димитар Босиљанов` / `М. Босиљанов`; `Томче Ефтимов` / `Томе Ефтимов`; `Митко Георгиев - Шеки` / `Димитар Георгиев–Шеки` / `Мите Георгиев-Шеки`. **The Drive spelling was used throughout**, so these are the names now rendering on `/legendi` and in the `/statistika` rankings. **Ace to confirm the correct form of each.**
- **OV-17 (NEW at 3.02F-Code, content)** · **Two people who may be one person.** The book does not say whether `Ацо Стојанов` and `Александар Стојанов` are the same man. They exist as **two separate `person` documents** with two separate pages. **Ace to confirm** — if they are one person, the two documents must be merged or one unpublished.
- **OV-18 (NEW at 3.02F-Code, content)** · **Two known gaps in the people roster.** (a) **Eleven presidents** in `Претседатели на ФК Беласица.docx` carry no dates and were left out of the archive entirely. (b) The book's **fifth-highest all-time scorer, `М. Костовски` (73 goals)**, has **no `person` document** — he is absent from the 80-name appearances list the roster was built from, so other high scorers may be missing for the same reason. He does appear inside the „Топ 5 голгетери" `clubRecord` value, so the site names a player it has no page for. **Ace to decide** whether to add the eleven presidents without dates, and to check the scorer list against the roster.
- **OV-8** · **RESOLVED 2026-07-31 (3.03b, D-3.03b-1).** The Contact form's states no longer rest on a fixture — both halves of the check ran against the real service, in-browser at 1280. **(a)** With the live endpoint (`https://formspree.io/f/mojgzzep`): `idle → „Се испраќа…" → success panel`, observed as a state sequence, not a screenshot. Formspree returned **2xx** — the handler throws on non-ok, so the success panel is itself the proof of a real accepted POST. One test message („ТЕСТ", 2026-07-31) is sitting in the owner's Formspree inbox and **should be deleted**. **(b)** Against a deliberately dead endpoint (`https://formspree.io/f/zzzznotreal`): `idle → „Се испраќа…" → error`, with the `role="alert"` banner **above** the form (verified by document position, not by eye), **all three typed values retained**, the fields re-enabled and the button reading „Испрати повторно". **Confirmed on the Vercel preview too**, so the build-time-inlining caveat is closed rather than carried: `https://belasica-v2-ljvyzwojx-sunset-services-team.vercel.app/kontakt` returns **200**, its HTML contains the endpoint, and „Формуларот сѐ уште не е активен" and the Formspree chip are both **absent** from it — the Vercel variable is picked up correctly at build.
- **OV-6 · ✅ RESOLVED 2026-07-20 (2.09-Run, Task 8)** — the Studio `footerUnofficialArchiveText` field was corrected to the verified `facts.md` string and republished, and `SiteFooter` now reads both strings from `src/lib/facts.ts` (no hardcoded copy; byte-identical to `facts.md` verified by script). *Original issue (2.06, D-2.06-9):* **A populated Sanity field contradicted the VERIFIED OV-3 copy, and nothing rendered it.** `siteSettings.footerUnofficialArchiveText` is populated in live `production` with „Ова е неофицијална архива на историјата на ФК Беласица, создадена од љубов кон клубот. Сајтот не е официјална страница на клубот и не е поврзан со него." — **different** from the string `facts.md` records as VERIFIED („Ова е неофицијална архива посветена на ФК Беласица. Не е официјалната страница на клубот."). `facts.md` verifies the wording „exact, **as rendered**", and what renders is `SiteFooter`'s **hardcoded** copy — so Ace was shown the hardcode and **never saw the CMS field**. The field is therefore unverified copy that looks authoritative in Studio. `/za-nas` (2.06) renders the verified string from `src/lib/facts.ts` and does **not** read the field. **The risk:** anyone who reasonably wires the CMS field up ships copy Ace never confirmed, on the one statement the site's honesty rests on. **How Lazar clears it:** decide which wording is right. If the `facts.md` one stands → **delete or correct the field in Studio**. If Ace prefers the CMS wording → get it confirmed, update `facts.md`, then point **both** `src/lib/facts.ts` and `SiteFooter` at one source. Either way the OV-3 string should end up in exactly one place.
- **OV-7 · ✅ CLOSED 2026-07-30 (3.02F-Code)** — was „resolved" at 2.09-Run on **two** players; it is now **content-verified at scale**: 71 people render an appearances tile and 55 a goals tile, and a sweep of **all 160** person pages found **0** wrong values, **0** missing tiles and **0** rendered `0` for an unrecorded metric. The no-`0` rule is exercised in both directions by real data — 19 people have appearances but no goals, 3 have goals but no appearances. *(⚠️ The brief stated „15 people who have goals but no appearance count"; the measured split is **3** goals-only and **19** appearances-only.)* *Prior status: RESOLVED 2026-07-20 (2.09-Run, D-2.09R-4)* — two players published with real `careerStats` from the history docx (Роберт Христовски 175 наст./7 гол.; Љупчо Мафков 115 гол., appearances null); Кариера tiles render, both appear in `/statistika` rankings, and the no-`0` rule is confirmed against real data (Мафков shows one tile „ГОЛОВИ 115", never „0 настапи"). *Original issue (2.06, D-2.06-6):* **The Кариера section had never rendered from real content.** No published person has `careerStats` (all five are `null`), so the career tiles on `/legendi/<slug>` are **fixture-verified only** — all four states were proven against a temporary in-file fixture (since stripped), and the live pages correctly omit the section. Same shape as OV-4/D-2.04-6, and it clears with the same content step: **OV-4's fix also clears this** — publishing one player with `careerStats` populates both `/statistika`'s rankings and that person's Кариера tiles. Verify the no-`0` rule survives real data: a person with `appearances` but no `goals` must show **one** tile, not „0 голови".
- **OV-4 · ✅ CLOSED 2026-07-30 (3.02F-Code)** — was „resolved" at 2.09-Run on **one** season's `finalTable`; the balance is now **content-verified at scale** across **92** seasons (band + 92 rows + the „Составено од 92 сезони" coverage line), with sorting proven on real data to put unknowns last in both directions (D-2.04-5). D-2.04-6's „fixture-verified only" no longer applies to any part of `/statistika`. *Prior status: RESOLVED 2026-07-20 (2.09-Run, D-2.09R-3)* — season `1982-83` published with a real `finalTable` ФК Беласица row (1st, P30 W16 D11 L3, 45:19, 43 pts) transcribed from `Беласица 1926-2025-финал…xlsx`; `/statistika` balance renders (summary band + coverage line + season row), verified live. *Original issue (2.04, D-2.04-6):* **The Statistics tables had never rendered from real content.** Live `production` has no season with a `finalTable` and no player with `careerStats`, so `/statistika`'s tables, summary band, coverage lines and sorting are **fixture-verified only** — correct in code and in-browser, but unproven against published documents. The balance aggregate is the one place this page could produce a confidently wrong number. **How Lazar clears it:** in `/studio`, publish **one** player with `careerStats` (goals + appearances) and **one** season with a `finalTable` containing a Беласица row; reload `/statistika` twice ~60s apart (ISR 60). Expect the player in both rankings, and the balance notice replaced by the band + coverage line + one season row. Clears naturally at **2.09** — worth doing early in that phase, before ~74 seasons land. ⚠️ **2.09 ran but Task 8 (the content transcription that would clear this) was blocked by the missing source mirror (D-2.09-3), so OV-4 did NOT clear** — it clears in the follow-up ingestion run (or sooner, by hand in Studio).

> **OV-1's caveat → OV-RIGHTS → RESOLVED (2.08).** The bulk publish happened (D-2.09R-2), so „settle before publish" became „settle now that it's public"; the owner then confirmed rights to all 889 photos (D-2.08-1), closing OV-RIGHTS. The document-level `provenance` lag it leaves behind is now tracked as **OV-10** above.

**Resolved**

- **OV-RIGHTS** · **Photo publishing rights for the live public archive.** Resolved 2026-07-21 (2.08, D-2.08-1). 2.09-Run had published 881 mostly-third-party-screenshot photos (D-2.09R-2) ahead of a written settlement, opening this as the urgent top item. The owner confirmed the project holds the rights to all archive photos and that all **889** published photos may remain public; recorded as a dated VERIFIED entry in `facts.md`. Un-publishing the 881 and restricting the homepage to the 8 Ace-owned demo photos were both offered and declined. ⚠️ The document-level `provenance` strings still read НЕПОТВРДЕНИ and lag this confirmation — carried forward as **OV-10** (a bulk `provenance` rewrite is separate ingestion tooling, not this phase).

- **OV-9** · **The 2.09 content ingestion ran and completed.** Resolved 2026-07-20 (2.09-Run). Mirror placed at `~/belasica-ingest-source/`; dry run clean (0 collisions / 0 unmatched); Wave 0 created 95 season shells (96 total); Waves 1–8 uploaded 881 photos (each wave created==source, public count held at 8, idempotent). All then **published** by owner decision (D-2.09R-2) → 889 public photos. `/arhiva` (96 seasons · 11 decades), `/statistika` and `/legendi` are populated from real content. Reports in `docs/ingestion/`. ⚠️ The publish's rights exposure is tracked as the active **OV-RIGHTS** item above.

- **OV-5** · **`/statistika` deployed to production — the Vercel webhook outage cleared itself.** Resolved 2026-07-17. The 2.04 merge went in without the preview gate during a third-party Vercel/GitHub outage (D-2.04-7), and `/statistika` still 404'd on production ~4 min after merging, so `main` and the live site were out of sync. **Verified resolved by direct check:** `https://belasica-v2.vercel.app/statistika` returns **200** and serves the real page — all three section headings (`Најдобри стрелци`, `Најмногу настапи`, `Севкупен биланс на клубот`) plus the three expected `[PLACEHOLDER]` chips (`голови по играч`, `настапи по играч`, `конечни табели по сезони`). `/` and `/arhiva` also return 200, so no route regressed. **No action was needed** — Vercel recovered and deployed `main` on its own; no redeploy was triggered. The D-2.04-7 concern (a Vercel-only failure in env vars / image domains / ISR surfacing first in production) **did not materialise**: the page renders on their runtime exactly as it did on `localhost`. Note this clears the *deployment* gap only — **OV-4 still stands**: the tables have still never rendered from real content, since the live page correctly shows empty notices.
- **OV-1** · **Photo publishing rights (P0.2).** Resolved 2026-07-16 — at the sit-down Ace confirmed he **has the right to use all photos in the Drive**; VERIFIED in `facts.md` (D-1.06-3). ⚠️ **Caveat kept on record:** the P0.1 audit found most archive photos are third-party screenshots (Facebook / Messenger / newspaper), for which "right to use" is legally nuanced. This is **not a launch blocker anymore**, but the specifics (ideally a written confirmation from Ace) should be nailed down before the bulk **2.09** ingestion is published — `docs/content-ingestion-plan.md` §5 already keeps every ingested photo **unpublished** until publish is explicitly cleared, so the gate holds procedurally regardless. *(The 8 already-published demo photos are Ace's own and were never in question.)*
- **OV-3** · Exact Macedonian footer wording. Resolved 2026-07-16 — Ace confirmed the current wording as correct at the sit-down (label „неофицијална архива"; line „Ова е неофицијална архива посветена на ФК Беласица. Не е официјалната страница на клубот."); VERIFIED in `facts.md`. **No code change needed** (confirmed as-is, not corrected).
- **OV-2** · Cyrillic wordmark **ФК Беласица**. Resolved 2026-07-15 — owner confirmed spelling (chat); VERIFIED in `facts.md`.

## Placeholder register

> Every visible `[PLACEHOLDER]` on the site. Must be empty before cutover — launch blocker checked at 3.05.

> ## ✅ **THE REGISTER NOW HOLDS NO VISIBLE CHIP. This launch blocker is cleared (3.15).**
>
> **Proof, not assertion:** `grep -rl "\[PLACEHOLDER" .next/server/app --include="*.html"` returns **0**, and the same over `--include="*.rsc"` returns **0** — across all **544** built `.html`/`.rsc` artefacts of the **279**-page build (re-verified at 3.16; it was 264 HTML files of the 271-page build. The last two emitters, `/za-nas` (4 chips) and `/kontakt` (1 chip), were cleared this phase.
>
> ⚠️ **„Zero chips" does NOT mean „3.08 is done".** **PL-4 is still open** — but purely as a **metadata** item: `metadataBase` (`src/app/layout.tsx`), `robots.ts` and `sitemap.ts` still hardcode `https://belasica-v2.vercel.app`. That is invisible on the page and is exactly what the next phase, 3.08, exists to fix.
>
> ⚠️ **`PlaceholderChip` deliberately remains in the codebase**, imported by nine components as a defensive fallback for states the model still permits (a person published with no name, a season with no title, an unset form endpoint, an empty roster). A `grep` over `src/` therefore still finds it — that is the component's source, not a render. Deleting working error handling to satisfy a text search was explicitly rejected (D-3.15-9); the requirement is that **none renders**, and that is what is verified above.

| # | Placeholder | Page | Registered | Status / Cleared in |
|---|---|---|---|---|
| PL-1 | Ace's public name + About text | За нас | 2026-07-14 | **CLEARED 2026-08-08 (3.15)** — the owner supplied the „За нас" copy in Ace's own words. **Аце Стојанов** is VERIFIED in `facts.md` as author of the site's texts, of the FB profile *FK Belasica 1956-2006* and of the book. Both chips („јавно име на авторот на архивата", „текст за архивата — кој ја создал и зошто") are gone; the copy renders verbatim from `ABOUT_AUTHOR_PARAGRAPH`. *Superseded text: „Open — now VISIBLE (2.06)".* |
| PL-2 | Ace's father: name + playing years | За нас | 2026-07-14 | **CLEARED 2026-08-08 (3.15)** — **Томе Стојанов (1953–2020)**, 328 првенствени натпревари во 14 сезони, VERIFIED in `facts.md`. Both chips („име на таткото…", „години на играње") are gone; his name renders as a link to `/legendi/tome-stojanov` (exactly one published person carries that name). ✔️ The copy's figures cross-check against the archive's own data: `careerStats.appearances` = 328, `playingYears` = 1976–1990 (14 seasons). *Superseded text: „Open — now VISIBLE (2.06)".* |
| PL-3 | Contact email | Контакт + footer (+ Правни информации) | 2026-07-14 | **CLEARED 2026-07-31 (3.07)** — `info@belasicahistory.mk` is VERIFIED in `facts.md` (owner, chat) and renders as a real `mailto:` on **all three** surfaces from the single `CONTACT_EMAIL` constant (D-3.07-6). Both chips are gone: the footer's („е-пошта за контакт") and `/kontakt`'s („адреса за е-пошта") — the latter cleared in the same phase because PL-3 was open on two surfaces and half-clearing it would have made `/kontakt` contradict its own footer (D-3.07-5). Verified against the built HTML: `/kontakt`'s `<main>` now carries **2** chips (PL-14, PL-15), not 3. ⚠️ **Not the same thing as the form destination** — the Formspree endpoint is still unset (PL-14 / OV-8) |
| PL-4 | Domain | site-wide metadata | 2026-07-14 | ✅ **CLOSED IN CODE 2026-08-14 (3.23, D-3.23-2)** — the origin is now a single constant, `SITE_URL` in `src/lib/site.ts`, read from `NEXT_PUBLIC_SITE_URL` with the Vercel domain as the fallback; `layout.tsx` (`metadataBase`), `robots.ts` and `sitemap.ts` all import it, and `grep -rn "belasica-v2.vercel.app" src/` now returns **`src/lib/site.ts` only**. The variable is set **nowhere** — not in `.env.local`, not on Vercel — and that is deliberate. **What remains is Ops, not code:** point DNS at Vercel, set `NEXT_PUBLIC_SITE_URL=https://www.belasicahistory.mk`, **redeploy** (build-time inlining), and hand-edit `public/llms.txt`, the one place that cannot import the constant. *Superseded text:* ⚠️ **THE ONLY PL STILL OPEN — and it is a METADATA item, not a visible chip.** It renders no `[PLACEHOLDER]` anywhere; „the register holds no visible chip" and „PL-4 is open" are both true at once, and reading the first as „3.08 is done" is the mistake to avoid. **Partly cleared 2026-07-31 (3.07)** — the domain **`www.belasicahistory.mk`** is now VERIFIED in `facts.md` (owner, chat) and renders as plain text in §1 of `/pravni-informacii`. ⚠️ **The cutover has NOT happened and is explicitly the next phase:** `metadataBase` in `src/app/layout.tsx`, plus `robots.ts` and `sitemap.ts` (which repeat it because they cannot import from a layout module), all still hardcode `https://belasica-v2.vercel.app` — three places, kept in sync by comment only |
| PL-5 | Intro description (`siteSettings.description`) | Почетна | 2026-07-15 | **CLEARED** — description published + rendering |
| PL-6 | Featured season (image, title, story) | Почетна | 2026-07-15 | **CLEARED / degrades cleanly (2.08)** — featured is now „Сезона 2025/26" (auto, newest); image + title render; with no `story` the section **self-omits the teaser and shows NO placeholder chip** (D-2.08-2), replacing the „[PLACEHOLDER: приказна за сезоната]" chip that was visible at the pre-phase baseline |
| PL-7 | Legend portraits | Почетна | 2026-07-15 | **CLEARED 1.05.2** — the read path (`relatedPerson`) now renders the linked portraits for both players shown (Васо, Панче); no content step was needed. See D-1.05.2-2. |
| PL-8 | Gallery photos | Почетна | 2026-07-15 | **CLEARED** — 8 photos rendering |
| PL-9 | Footer contact (email + phone) + social links | site-wide (footer) | 2026-07-15 | **RETIRED 3.03; email half fully CLOSED 2026-07-31 (3.07)** — the fake demo email/phone/socials (D-1.06b-1) were **removed** at 3.03 and replaced by chips; at 3.07 the email chip became the real VERIFIED address. **Note for the record: the brief's „replace the footer's demo contact email" premise was already stale** — no demo email survived in `src/` to replace; 3.03 had removed it. A sweep of the built output confirms the only email-shaped string anywhere is `ime@example.com`, the contact form input's `placeholder` **attribute** (a typing hint on the IANA-reserved `example.com`, not an archive address — deliberately kept). The footer still shows no phone (never a verified fact); socials remain open under PL-15. |
| PL-10 | Season detail — team photo / table image / trainer & stats / results / story / photos, per season | Архива (season pages) | 2026-07-16 | **Largely cleared by 3.02 + 3.04** — the redesigned page (3.04) renders the new fields, and the chips inside `SeasonEmptyNotice` were re-worded to match them (D-3.04-7). Live coverage: teamPhoto 83/96 · tablePhoto 87/96 · trainer 67/96 · lineupAndStats 80/96 · story 66/96 · results 10/96 and rising. **No chip renders on any published season** — every gap self-omits its section (D-2.02-3); the chips only appear in the all-empty notice, which no published season currently reaches |
| PL-11 | Season lead photo (Archive card greybox) | Архива (index) | 2026-07-16 | **Largely cleared 2.09-Run** — 881 photos published + linked via `relatedSeason`; every season's lead photo now resolves (no card chip on `/arhiva`). ⚠️ Published under unsettled rights (OV-RIGHTS). A *curated* lead still needs a `date`/order field (D-2.03-3, model-locked) |
| PL-12 | Statistics — goals/appearances per player (`person.careerStats`) | Статистика | 2026-07-17 | **CLEARED 2026-07-30 (3.02F-Code)** — the 3.02F content pass took `careerStats` to **71 players with appearances** and **55 with goals** (52 have both, 19 appearances-only, 3 goals-only). Verified against the built output: the rankings render **71** and **55** rows, no section shows an empty notice, and no chip renders. Remaining players are a genuine source gap, not a pending pass |
| PL-13 | Statistics — final tables per season (`season.finalTable`) | Статистика | 2026-07-17 | **CLEARED 2026-07-30 (3.02F-Code)** — `finalTable` now on **92 of 96** seasons, and all 92 carry a Беласица row. The balance renders band + **92** season rows + the coverage line „Составено од 92 сезони со внесена конечна табела.". The 4 seasons without one (`1922-26`, `1936-37`, `1942`, `2025-26`) are deliberate source gaps |
| PL-14 | Formspree endpoint not configured (form disabled) | Контакт | 2026-07-18 | **CLEARED 2026-07-31 (3.03b, D-3.03b-1)** — `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is set in `.env.local` and on **Vercel (Production + Preview)**. `/kontakt` now renders the **live** form: real `<form>`, no disabled `<fieldset>`, no „Формуларот сѐ уште не е активен" notice, and the Formspree chip is gone — the only chips left on the page are PL-3 and PL-15, which are a different channel. **Takes effect in production on the redeploy** this branch's merge triggers (`NEXT_PUBLIC_*` is inlined at build time) |
| PL-15 | Social profiles | Контакт + footer | 2026-07-18 | **FULLY CLEARED 2026-08-08 (3.15, D-3.15-2)** — on **both** surfaces at once. „Следете нѐ" now renders **Фејсбук** and **Инстаграм** as icon links (`https://www.facebook.com/share/1FK4bKq9wx/`, `https://www.instagram.com/belasica1956.2006`, both VERIFIED in `facts.md`), from the shared `SocialLinks` component, in the footer **and** in `/kontakt`'s direct-contact block. Both are `target="_blank"` + `rel="noopener noreferrer"`, both ≥44×44, accessible names „Беласица на Фејсбук" / „Беласица на Инстаграм". The Instagram tracking query string was stripped (D-3.15-3). The 3.12 „фан-страница" framing is **retired**: the „За нас" copy states that Аце Стојанов authors both profiles, so calling them somebody else's fan page would contradict the About page. *Superseded text: „Half CLEARED 2026-08-02 (3.12, D-3.12-6) … Still OPEN on `/kontakt`".* |

> **On a season page after the 3.04 redesign there are NO visible `[PLACEHOLDER]` chips.** Every missing field self-omits its section instead (D-2.02-3 / D-3.01-6), which is the deliberate design: a sparse season must read as deliberate, not as broken. The only chips the template can emit are the H1 fallback (fires only if a season is published without a `title` — impossible today, the field is required) and the six chips inside the all-empty notice (D-3.04-7), which no published season reaches. Verified across all 96 rendered pages.

> **`/statistika` now shows ZERO `[PLACEHOLDER]` chips in `<main>`** (3.02F-Code). All four sections carry real content — 30 curated records, 55 scorers, 71 appearances, 92 balance rows — so no empty notice renders and `StatsEmptyNotice` currently appears on no section at all. The only two chips on the page are the **footer's** PL-3 (е-пошта) and PL-15 (социјални мрежи), which are site-wide. *Superseded text: „shows three visible chips … no player has `careerStats` and no season has a `finalTable`" — both cleared by the 3.02F content pass.*

> **On `/` there are now ZERO visible `[PLACEHOLDER]` chips** (3.15). *Superseded text: „the visible chips are: two in the Legends band + two in the footer … Љупчо Мафков and Роберт Христовски have no `playingYears`".* All four are gone and by three different routes: the footer's PL-3 chip became the real `mailto:` at 3.07; its PL-15 chip became the two social icon links at 3.15; and **the two legend-band year chips were already gone before this phase** — every published player now has `playingYears` (measured: 0 players missing it; Љупчо Мафков carries `1972–1983`). `LegendCard`'s year placeholder branch was removed anyway (D-3.15-6), so a future gap **self-omits the line** rather than emitting a chip, matching every other gap on the site. A missing portrait still gets the navy-initials **tile** — a deliberate brand element, **not** a chip. The hero H1 falls back to the verified wordmark `ФК Беласица`, never a placeholder. **No featured-season or gallery section exists in the redesign** (the seven-section flow dropped them), so PL-6/PL-8 no longer render on `/`. ⚠️ The Story section renders `siteSettings.description` verbatim, which asserts founding „13 август 1922" — owner-authored CMS copy that `facts.md` still lists as **UNVERIFIED** (Club founding year); it renders because the brief designates the description as the section-2 source, but it should be confirmed into `facts.md`.

## Carryovers

- ✅ **[3.10 → CLOSED at 3.15] Does `/za-nas` keep its provisional banner?** **No — it is deleted.** The owner supplied the real „За нас" copy on 2026-08-08, so the banner's own claim (that the text was still being written) stopped being true, and PL-1/PL-2 closed with it (D-3.15-7). `ProvisionalBanner` was a local function inside `za-nas/page.tsx`, not a shared component, so it went with the page edit — **there is no component file to delete**, and no other page imported it. **D-2.05-1 is now fully retired** (its `/kontakt` half went at D-3.10-1), and **D-3.10-9's unmet DoD line finally passes**: `grep -rn "Страницата е во подготовка" src/app src/components src/lib src/sanity` returns **0**. No route ships provisional any more.
- **[3.10] The hero photograph is upscaled.** The pinned scan is 1079 px wide and the hero requests 2400, so it is slightly soft on a wide desktop. Accepted for an archival photograph; a sharper scan of the same frame would be the only real fix.
- **[DONE] Load + publish demo content** in `/studio` — done (15 published docs; homepage renders them).
- **[DONE] Register the Studio host** — not relevant; the homepage read is server-side (no CORS).

## Remaining human steps (Lazar)

> The Ace sit-down (2026-07-16) closed the Part-1 human work. **2.02 and 2.05 are unblocked.** What's left:

1. **[DONE 2026-07-16] Ace sit-down.** Captured in `docs/ace-demo/feedback.md`. Direction for 2.02: Archive/Season pages like crnobelanostalgija.com.
2. **[DONE 2026-07-16] OV-3** — footer wording confirmed as-is, VERIFIED in `facts.md`. No code change needed.
3. **[DONE 2026-07-16] OV-1 / P0.2 photo rights** — Ace confirmed rights to all Drive photos; VERIFIED in `facts.md` (D-1.06-3). ⚠️ Not a launch blocker anymore, but settle the screenshot-provenance specifics (ideally in writing) before **2.09** publishes ingested photos; the §5 unpublished-until-cleared gate holds meanwhile.
4. **[DONE 2026-07-16] Locked model checked against Ace's asks** — Ace named no content need the 4 types cannot hold; the lock stands (D-1.06-4). Clears the D-2.01-7 risk.
5. **[OPEN] Second Ace sit-down (~1 week)** — to gather Legends / About / Contact input; **2.05 reads that** before it opens. 2.02 does **not** wait on it.
6. **[DONE 2026-07-21 · 2.08]** Re-took the demo screenshots (`docs/ace-demo/screenshots/homepage-{desktop,mobile}.png`) from the finished homepage at 1280 + 375 (full-page, reveal forced via `prefers-reduced-motion`). Note: `/` is not fully placeholder-free — the Legends `[0]` (Мафков) shows portrait + years chips (both still correct, kept per Task 4).
7. *(Optional)* **Add the remaining portraits** (Петар Андреев renders on `/legendi` later; Гоце/Илија are trainers, excluded from Legends) — not needed for the homepage, which is placeholder-free as-is.
8. **[DONE] Part 1 formally closed** — all Part-1 owed items resolved; the project is in Part 2.
9. **[OPEN · 3.05b] Eyeball the new crest** at desktop and on a phone — header, hero, and the browser tab. It is a repaired rebuild of the same artwork (clipped right edge restored, pennant point closed), now vector and sharp at every size; the palette is untouched.
10. **[OPEN · 3.05b] Confirm the six homepage records are the right six** — Шампион · Вицешампион · Полуфинале-Куп · Југословенски второлигаш · Најдобар стрелец · Рекордер по настапи. Changing the set is a one-line edit to `HOMEPAGE_RECORD_LABELS` in `src/components/home/ClubRecords.tsx` (D-3.05b-1).
11. **[OPEN · 3.05b] Decide whether to source portrait photographs for the eight legends who have none** — the homepage band is currently **2 photographs and 8 monogram tiles** (Андреев and Т. Стојанов have portraits; Василев, Панов, Секулов, Д. Георгиев, Мафков, М. Георгиев-Шеки, Сулев, Атанасов do not). The monogram is the specified treatment, not a defect — no stand-in face was substituted and nobody was dropped for lacking one.
12. **[OPEN · 3.07] Read `/pravni-informacii` end to end on the preview**, desktop and phone, with Ace — the copy is yours and is rendered verbatim, but nobody has read the finished page on screen (OV-22).
13. **[OPEN · 3.07] Confirm `info@belasicahistory.mk` is a real, monitored mailbox and agree who handles takedown requests** — §5 now publicly promises removal „без одлагање и без прашања" (OV-23).
14. **[PARTLY DONE 2026-08-02 · 3.11 — the git half is closed, the security half is NOT]** ✅ `_to_delete/` **is gone**: the 11 junk files were removed in `7ef3547` and verified absent from the tree and from `git ls-files`. ⚠️ **Still open, and it is the part that matters:** `sanity_token_transfer.tmp` was **never tracked by git**, so no commit could have removed it — it is absent from the working tree now, but whether it was **shredded or merely unlinked** is unknown, and its name suggests it held a live token. **Rotate the Sanity write token if you cannot confirm it was shredded.** Original wording: *Inspect and shred `_to_delete/sanity_token_transfer.tmp` in your working tree, rotate the token if it held a live one, then `git rm -r _to_delete/`* — 11 junk files are tracked on `main` in a public repo, and the untracked stray beside them is not git-ignored, so `git add -A` would stage it.
15. **[OPEN · 3.07 → 3.08] The domain is verified but not cut over.** `www.belasicahistory.mk` is VERIFIED in `facts.md` and named on the legal page; `metadataBase`, `robots.ts` and `sitemap.ts` still point at `belasica-v2.vercel.app` in three hardcoded places. That is the next phase.
16. **[OPEN · 3.11 — Lazar + Аце] Read `data/book/README.md` and `data/book/extraction-report.md` natively.** Both are written in Macedonian **for Аце**: what was extracted, how it was checked against the book's own summary table, the four seasons that diverge, and what was deliberately left unchanged. No machine can sign these off — the whole drop rests on Аце recognising his book in them.
17. **[OPEN · 3.11 — Аце] Confirm the four seasons where the extracted match count differs from the book's own table** — `1950` (16 vs 12), `1950/51` (10 vs 14), `1954/55` (25 vs 21), `1987/88` (38 vs 34). **Expected cause, not a defect:** the table counts league matches only while the text also lists cup and qualification ties (`1950` + `1950/51` reconcile exactly at **26 = 26** once taken together). 92 of 96 seasons already agree within ±2.
18. **[OPEN · 3.11 — Аце] Approve the twelve assembled result lines for `1926-1930` and the two notes on `1922-26` and `1936-37`.** These are the only three seasons whose `results` are **not** copied from a printed list: `1926-1930` was assembled from the book's own sentences, and the other two carry an explanatory note because the book prints no results for them. Each is marked in the data as `resultsProvenance` (D-3.11-4). **Until Аце approves, they are extracted-but-unverified** — and `scripts/fill-season-content.mjs` would publish them on the next `--commit` run.
19. **[OPEN · 3.11 — owner decision] Reopen `match`, or leave it deferred.** D-2.01-2 dropped the type because there was „no per-match source"; `data/book/matches.json` now holds **2.267 matches** with result, opponent, competition and scorers with minutes. **Nothing was changed** — the schema is byte-identical and `match` stays unregistered, because this is a content-model decision, not a script's (D-3.11-1). If you reopen it: re-add `match` to `schemaTypes/index.ts`, decide `match ↔ season`, then ingest. If you don't, the file stays a tracked source for derived figures.
20. **[DONE 2026-08-02 · owner-authorised, run on Lazar's instruction] `node scripts/fill-season-content.mjs --commit` was run.** The owner chose the full scope (`--field both`) after being shown that it would publish the 14 lines still owed Аце's approval at items 17–18 — **those are now live and still unapproved.** Result: **86 of 96** seasons patched, exit 0, 0 drafts present at run time. Live dataset went `story` **66 → 96** and `results` **10 → 95**; `2025-26` has no `results` field, as intended. **Verified on the live site, all 96 pages fetched:** every one of the **2.563** result lines renders on its own page, byte-for-byte against `season-content.json` — **96/96 seasons correct on results**. ⚠️ **Eleven seasons render a story that is not the book's** — `1932-33`, `1938-39`, `1966-67`, `1972-73`, `1974-75`, `1976-77`, `1980-81`, `1987-88`, `1991-92`, `1992-93`, `1994-95`. This is `setIfMissing` behaving correctly, not a fault: all eleven already carried the **3.02-Story** transcription, so the extraction never touched them. Ten differ only in paragraph splitting and wording (within ~10 % of the book's character count). **`1992-93` is the real gap — see item 21.**
21. **[OPEN · 3.11 — owner decision, needs an overwrite] `1992-93` still shows the old 2.x demo summary, not Аце's text.** The page carries **768 characters**; the book's own narrative for that season is **2.243** — the site is missing roughly two-thirds of it, and what it does show is a **condensed paraphrase**, making this the only season page whose story is not the author's own writing. It survived because 3.02-Story deliberately preserved the demo text and `setIfMissing` cannot replace it. **Fixing it requires an explicit overwrite**, which `fill-season-content.mjs` cannot do by design — a deliberate one-document `set` (not `setIfMissing`) on `1992-93` only. Owner's call, because it destroys published content.

## Known issues

- **⚠️ A SECOND local build-failure mode, unrelated to the Sanity CDN one (found 3.09).** On **Node 26.3.0 / Next 15.5.20**, an *incremental* `npm run build` intermittently dies **before compiling** with `TypeError: Cannot read properties of undefined (reading 'length')` at `WasmHash._updateWithBuffer` in webpack's bundled `bundle5.js`. **Fix: `rm -rf .next/cache` — not the whole `.next`**; both were tested and the corruption lives entirely in the webpack persistent cache. It never fires on a cold build, so **Vercel is unaffected** — it is a local tax on both operators when iterating, and it cost two wasted debugging detours before the pattern was clear. The log is ~2 MB because webpack dumps its minified bundle into the stack trace: filter it with `awk 'length($0) < 300' build.log | tail -40` before reading.
- **⚠️ 3.06a shipped code but filed no housekeeping, and the gate was waived (3.09).** `f8de548`, `4b333e8` and `bcd6196` merged — the crest re-crop, the season-page reorder, `SeasonRecordBoard`, `LegendsBrowser` — with **no completion report**, **no `D-3.06a-*` decisions** (the log jumps `D-3.05b-7` → `D-3.09-1`), a `current-state.md` still reading `NEXT: 3.06`, and `file-map.md` missing `LegendsBrowser.tsx`. 3.09 asked the owner, who waived the precondition, and this snapshot now describes 3.06a's changes **reconstructed from its diff** rather than written by its executor. Its decisions are lost unless someone reconstructs them. **Fourth occurrence of the phase-hygiene pattern** already recorded for 2.03/2.04/2.05 below. **Also recorded at 3.07:** the 3.07 brief asserted these state files still read „as if Phase 1.01 just closed“ when they were in fact current through 3.05b — the **fourth** brief written against repo state that did not match the repo (cf. D-2.03-1, D-2.04-2, D-2.06-1). 3.07 reconstructed 3.06a’s shipped surface from its diff and commits but deliberately did **not** invent its rationale (D-3.07-10).
- **⚠️ NEW at 3.07 — 11 files of git plumbing junk are tracked on `main`, and an untracked file beside them looks like it held a token.** `_to_delete/` carries `HEAD.lock`, `index.lock`, `index.lock.2`, `index.lock.stale` and 7 × `tmp_obj_*`, committed during 3.06a. Harmless to the build, but **this is a public repo**, and two *untracked* strays sit in the same directory in Lazar's working tree: `git-index.lock.leftover` and **`sanity_token_transfer.tmp`**. Because `_to_delete/` is a tracked directory and neither stray is git-ignored, a single `git add -A` would stage a file whose name suggests a **Sanity write token**. 3.07 did not open or delete it (destructive, out of scope, and reading a suspected secret is the wrong move). **Action for Lazar: inspect and shred `sanity_token_transfer.tmp` — and if it ever held a live token, rotate it — then `git rm -r _to_delete/`.**
- **✅ The build's Sanity reads are now FULLY hardened (closed at 3.23, C3).** All four remaining bare `client.fetch` call sites — `/arhiva`, `/legendi`, `/statistika` and `sitemap.ts` — now go through `src/sanity/fetch.ts`, so **every** Sanity read in the app is retried five times with exponential backoff before it is allowed to fail. **Each site's existing failure behaviour was preserved deliberately:** `/arhiva` and `/statistika` still have no try/catch and still fail loudly (an index that quietly renders „нема сезони" is worse than a build that stops); `/legendi` and `sitemap.ts` keep their catches, so the first degrades to its honest empty notice and the second to the static paths. `grep -rn "client.fetch" src/app` now returns nothing. *Superseded text:* **⚠️ The build's Sanity reads are only PARTLY hardened (3.02F-Code; narrowed at 3.05b).** `src/sanity/fetch.ts` covers the two fan-out templates — season and person, 262 of the 270 pages — with 5 bounded retries and a loud, slug-named failure, and `next.config.ts` adds `staticGenerationRetryCount: 2` on top. **3.05b added the homepage** — the call site 3.02F observed failing first — keeping its graceful placeholder fallback rather than the templates' throw (D-3.05b-4). **Still NOT covered:** `/arhiva`, `/legendi`, `/statistika` and `sitemap.ts` call `client.fetch` directly, so a `ConnectTimeoutError` on any one of them still fails the whole build with no retry. Scoped out deliberately each time, and it is a ~5-line change per call site whenever a phase touches those files. **Note for whoever does it:** `/arhiva` and `/legendi` have no try/catch at all, so they already fail loudly — wrapping them adds retry, not error handling.
- **The build flake is characterised, not eliminated (D-3.02F-C-2).** The failure is always `Connect Timeout Error … f8rmnfry.apicdn.sanity.io:443, timeout: 10000ms`, and it fires on a *different* random page each time. During this phase's three passing builds the helper absorbed **125** such failures. ⚠️ **Measured on a machine whose default route is a VPN tunnel (`utun5`) over a phone hotspot** — 63–95 ms to the CDN with 12 ms jitter — so the local frequency is probably worse than a Vercel builder's. A concurrency probe found the CDN serving 16 parallel requests cleanly (connect 0.08→0.44 s, zero timeouts), which is why the fix is retry rather than reduced build concurrency. If Vercel deploys still fail on this, the next lever is `experimental.staticGenerationMaxConcurrency`, not more retries.

- **⚠️ The 2.04 brief cites a handover section that does not exist (D-2.04-2).** The brief instructs the executor to read "the section covering the Statistics page" in the 2.02 handover. There is none — handover **§15 explicitly defers it**: „Not designed: Legends, Statistics, About, Contact, and the person detail page — 2.04/2.05." So `/statistika` was derived from the handover's shared conventions (§4.3–4.6), its Stats-table spec (§6.4) and `brand.md`, adding no token. **Consequence: the page ships without a design review** — Lazar sees it first on the PR preview. This is the **second brief in a row** whose spec references don't match the repo (cf. D-2.03-1); both should be regenerated before they are kept as instruction history. If 2.05 produces a Statistics design that disagrees, only the components change — `src/lib/stats.ts` is presentation-free.
- **⚠️ The 2.05 design phase is unclosed, and its handover only reached the repo via 2.06 (D-2.06-1).** The file Lazar supplied in-session is now committed at `docs/design-handovers/Part-2-Phase-05-Handover.md`, but 2.05 filed **no completion report and no decisions** — so **D-2.05-1 (About/Contact provisional), D-2.05-2 (multi-role priority) and D-2.05-3 (Contact/Formspree) are cited by the handover, by the 2.06 brief and by this snapshot, yet exist nowhere in `decisions.md`.** Those three IDs are dangling references. 2.06 logged its own work under `D-2.06-*` rather than back-filling another phase's namespace. **Third occurrence of the same pattern** (D-2.03-1 stale draft → D-2.04-2 absent section → this): a brief written against spec text the repo did not contain.
- **⚠️ The 2.05 handover carries known errors — do not follow it literally.** Resolved the repo's way per its own §0, and each logged: its UI copy is **Serbian** in a Macedonian-only repo (D-2.06-2); it cites **D-2.02-2/-3** for the photoless-tile rule, but those are the season-ordering and section-heading decisions — the real rule is 2.02 **§6.2b**, and `current-state.md` had been repeating the same mis-citation (D-2.06-7); it specs a card-lift **shadow** and an **amber** placeholder chip, neither of which has a `brand.md` token (D-2.06-5); it calls the person's field `roles[]` when the locked schema says `role`; and its §3 „read from `season.squad`" would leave every trainer's Сезони empty (D-2.06-3). Its **mockup was never supplied**, so the build worked from prose alone — the one part of the design that was never seen.
- **~~Кариера is fixture-verified, not content-verified (D-2.06-6 / OV-7).~~ RESOLVED at 3.02F-Code.** The tiles are now content-verified across **all 160** person pages: 71 render an appearances tile, 55 a goals tile, **0** render a `0` for an unrecorded metric, **0** are missing a tile the data supports. Both no-`0` directions are exercised by real people (19 appearances-only, 3 goals-only). *Superseded text:* **The Кариера section had never rendered from real content.**
- **~~Statistics is fixture-verified, not content-verified (D-2.04-6).~~ RESOLVED at 3.02F-Code.** Every part of `/statistika` now renders from real content and was verified against the **built output**: 30 records, 55 scorers, 71 appearances, 92 balance rows, the „92 сезони" coverage line, and sorting proven on live data to place unknowns last in both directions. The balance aggregate — „the one place this page could produce a confidently wrong number" — is now exercised by 92 seasons, and the number it produces is itself an owed question (**OV-13**). *Superseded text:* **every table, the summary band and the sorting on `/statistika` have never rendered from real content.**
- **⚠️ The 2.03 brief contradicts the merged 2.02 handover (D-2.03-1).** The brief was written against a superseded handover draft (different palette, different mockup filenames, and a "resolved decisions" list quoting `D-2.02-1…6` that conflicts with the logged `D-2.02-1…17`). Following it literally would re-introduce three WCAG AA failures fixed in `faaf8ba`. 2.03 was built to the **repo handover + `brand.md`** on Lazar's in-session call. **The brief should be regenerated before it is kept as instruction history**, or the next reader hits the same contradiction.
- **⚠️ Three shipped states are fixture-verified, not content-verified (D-2.03-4).** The **standings table**, the **photoless navy hero** and the **fully-empty season notice** have never rendered from real Sanity content — only one season is published and it has no `finalTable`, no missing photo and no empty sections beyond the table. Correct in code and in-browser. Clears naturally at **2.09**; sooner if one photoless season + one season with a `finalTable` are published.
- **~~Curated lead photo — modelled but not wired.~~ RESOLVED at 3.04b (D-3.04b-2).** `season.teamPhoto` is now read by **both** surfaces: the season page mats it whole inside the navy title band (3.04), and the **archive index reads it for the card lead** (3.04b) — closing a gap that had stood since 3.01, where 69 of 96 cards showed an arbitrary photo. The old back-reference survives only as the fallback for the 13 seasons with no `teamPhoto`. *Superseded text:* `season.teamPhoto` is the intended curated lead. Since 3.04 the **season page** reads it (matted whole inside the navy title band, D-3.04-3/-4), so the wrong-photo problem is fixed there. **`/arhiva` still uses the old „earliest free-text `date`" back-reference for its card lead (D-2.03-3)**, so the index and the season page can show different photos for the same season. Out of scope at 3.04; it is a one-clause GROQ change on the index whenever a phase touches it.

- **⚠️ „Резултати" and „Приказна за сезоната" show the same match list twice on 10 season pages (found at 3.04, live now).** The 3.04-Cowork pass filled `results` on `1930-31` … `1940-41` by extracting the **leading blocks of `story` verbatim** (its own D-3.04C-5 records this and anticipates the consequence) and left `story` untouched. Measured against live content: **72 of the 83 published `story` blocks on those 10 seasons are repeated verbatim in `results`**, and on **9 of the 10** „Приказна за сезоната" is the whole match list again followed by **exactly one** paragraph of narrative (verified on the preview at `/arhiva/1938-39`: Резултати = 16 rows, Приказна = the same 16 rows + 1 paragraph). **This is a content overlap, not a template bug** — the season page renders each field faithfully and both sections self-omit correctly; de-duplicating in the template was **deliberately rejected** (same reasoning as D-3.04-10: a template that suppresses one of two published fields is overruling the record, and a prefix-match heuristic would misfire the moment two fields legitimately share an opening line). **Fix = trim the leading result blocks from `story` on those 10 seasons**, so `results` holds the list and `story` holds the narrative; **ISR 60 corrects the pages within a minute, no code change, no redeploy.** Worth doing before the remaining 86 seasons are filled the same way.
- **~~`cn()` silently drops `brand.md`'s custom `text-*` size tokens (D-3.04-12).~~ RESOLVED at 3.04d (D-3.04d-1).** `src/lib/utils.ts` now builds its merger via `extendTailwindMerge` with the eight custom sizes registered under `font-size` (+ `tracking-overline` under `tracking`); all 8 affected call sites render the brand scale, verified in-browser at 1280 + 375 (overlines 12px, chips/nav 14px). **Residual maintenance duty:** a future font-size token added to `@theme` must ALSO be added to the list in `utils.ts`, or it regresses to being dropped as a colour conflict — the in-file comment says so; nothing enforces it mechanically.
- **⚠️ Three seasons render the same photograph twice — duplicate `photo` documents from the 2.09 ingestion (D-3.04-10, found at 3.04).** `1969-70`, `1982-83` and `1992-93` each have **two distinct published `photo` documents sharing one uploaded image asset** (3 pairs out of 889 photos). The season page dedupes by document `_id` as specified, so this is upstream content, not a template bug. **`1992-93` is the one that matters:** its `teamPhoto` points at the **bare** duplicate (`photo-6ce62638057327612b0c9b65c126e25602c064ae`) while the copy left in the gallery (`32991331-8812-437f-9529-5a89fd201c57`) carries the **only** caption and date that photograph has („Екипа на ФК Беласица, есен 1992" / „1992"). Deduping by asset instead was rejected precisely because it would delete that record (D-3.04-10). **Fix = two content edits, no code:** re-point `1992-93`'s `teamPhoto` at `32991331-…` (which also gives the lead its caption) and unpublish the three bare duplicates.
- **Two schema manifests on `production` — tooling reads the wrong one by default (2.01).** Besides our `belasica-v2` workspace, a stray **Studio-deployed `default`** workspace holds a non-project model (`page`/`story`/`fullName`/`startYear`/`source`/`verified`). Any schema-aware tool that omits `workspaceName` resolves **`default`** and gets a schema that is not this project's (this bit the 2.01 verification on the first attempt). **Always pass `workspaceName: "belasica-v2"`.** Not deleted — out of scope and not ours to remove (D-2.01-8). Cleanup is an owner call; it matters most for **2.09**.
- **~~The locked model was never checked against Ace's needs (2.01).~~ RESOLVED 2026-07-16.** The Ace sit-down happened and is captured in `docs/ace-demo/feedback.md`. Ace named **no** content need the four types (`siteSettings`/`season`/`person`/`photo`) cannot hold — nothing missing, and the only page-specific ask (Archive/Season like crnobelanostalgija.com) is a design/layout direction, not a new content shape. The D-2.01-7 risk is cleared; the lock stands (D-1.06-4).
- **`--spacing-header` (77px) mirrors the header, it does not drive it (D-2.02-17).** Both new archive routes now depend on it (sticky jump-nav + every decade anchor offset). Changing `SiteHeader`'s padding or crest size silently breaks those offsets. The hardening (`h-header` on the `<header>` element) is still unclaimed — handover §12 MT-2 suggests it.
- **Crest club-blue vs. locked navy.** The crest's blue samples at ≈ **`#125C9A`**, brighter than the locked navy **`#12294F`** (D-crest-1). Palette **unchanged** (logo/favicon only). A future owner-gated palette refinement should use `#125C9A` as the crest blue but keep a **dark navy for text/links** (a brighter blue risks failing WCAG AA on paper, where navy passes at ~13:1).
- **Pre-existing hydration warning (dev only):** the `.js` class added to `<html>` by the reveal pre-paint script (D-1.05-5) produces a React hydration-attribute warning in dev. Benign, predates 1.05.2 (main/crest show it too); a future fix is `suppressHydrationWarning` on `<html>` or setting the flag differently. Not a runtime bug.
- **"Empty homepage" (1.06 phase-open blocker) — RESOLVED.** Stale ISR/CDN caching of the pre-content-load state; self-healed on revalidation. Env correct. See D-1.06-2. If it recurs after a content change, check ISR/CDN staleness first (wait ~2 min or redeploy), not env.
- **Lighthouse re-baselined at 3.04d** — home mobile 92 simulated / 96 devtools-throttled, desktop 99 (see the baseline bullet above). The lantern-simulated number sits under the ≥95 line for simulator reasons, not byte reasons; confirm on the Vercel preview at the 3.04d PR before treating it as a gap.
- **`@sanity/image-url` deprecation warning at build** — benign, pre-existing (`src/sanity/image.ts`).
- **Studio pinned to 4.x** for Next 15 (D-1.04-4); Next 16 later unlocks sanity 6 / next-sanity 13.
- The Phase 1.02 design handover + mockups were delivered in-prompt and are still **not** in `docs/design-handovers/`. Flagged for the orchestrator.

## What's now possible

- **The archive backbone exists.** Every season published in Studio gets a real, linkable page within ~a minute — so **2.09 can ingest ~74 season shells and watch the archive populate itself**, with no page work per season. The self-omitting sections and the all-empty notice mean a shell season looks deliberate, not broken, on day one.
- **The statistics layer is built ahead of its data, so 2.09 populates it for free.** `/statistika` needs no page work per player or per season: publish a player with `careerStats` and they enter the rankings; publish a season with a `finalTable` and the whole balance section (band + coverage line + season row) appears within ~a minute. The reference site's statistics layer is the thing this archive was measured against, and it now exists as a shape waiting for content rather than a phase still to come.
- **2.05 has a person page to build into.** `/statistika` links every ranked player to `/legendi/<slug>` (404 until 2.05 — expected), so the moment 2.05 ships, the statistics tables become an entry point into the person pages rather than a dead end.
- **2.04 and 2.05 have a page to link into.** Season pages already link squad names and trainers to `/legendi/<slug>` (404 until 2.05 builds it; the slugs are live now, e.g. `petar-andreev`), and `src/components/archive/` (`StandingsTable`, `SquadTable`, `PersonChip`, `Breadcrumb`, `SectionHeading`) is reusable for the statistics and person templates.

- **The content model is fixed, so Part 2 can build on a shape that won't move.** The Archive/Season, Legends and Statistics templates (2.02→2.05) can be designed and coded against four settled types, with the back-reference read pattern (`relatedSeason`/`relatedPerson`) as the template for every season/person page. 2.04 no longer has to decide where aggregate numbers come from — `person.careerStats` is authoritative, `season.squad` is the per-season detail (D-2.01-3).
- **2.09 has a deterministic spec to build against.** `docs/content-ingestion-plan.md` fixes the Drive-folder→season mapping (grounded in the real audited folder names — hyphen `1992-93`, slash `1950/51`, single-year `1942`, era ranges `1922-26`/`1945-48 Илинден`, numbered thematic `NNN. Title`, and Ace's own `- не` exclude marker), the by-decade wave plan, and the provisional-provenance/rights gate. The script can be written and dry-run before a single document is written.
- The homepage is the redesigned **seven-section** heritage-and-legends-led front door (3.03), reading real Sanity content. The home components (`PlaceholderChip`, `PhotoFrame`, `Reveal`, `SectionOverline` incl. its `onNavy`/`onPhoto` overlay variants, `ClubRecords`, `DecadeExplore`) and the reveal/placeholder CSS are reusable by the remaining redesign phases; the navy-band + orange-marker + photo-forward system is the template the 3.04–3.06 pages should extend. The `onPhoto` overline pattern (orange marker + paper text) is the reusable answer for any text-over-photo surface.
- **Phase 2.02 (Archive & Season templates) and Phase 2.05 (People & pages) MUST read `docs/ace-demo/feedback.md` before they open** — now captured (2026-07-16). For 2.02 it carries the concrete direction: model the Archive/Season pages closely on **crnobelanostalgija.com** (modern, readable). For 2.05, feedback.md notes Ace has **not** given Legends/About/Contact input yet — that comes from a **second sit-down (~1 week)**, so 2.05 should wait for the updated feedback.md; 2.02 does not.
- **2.02 is unblocked and is the next phase.** It is a design phase, so it runs sketch-first (rough direction in chat → Lazar iterates → then the brief), per D-0.00-2.
