# file-map.md — Belasica-V2

> Location in repo: `src/_project-state/file-map.md`. Every meaningful file/folder, one line each: what it's for. Updated by the executor on **every** add, rename, or delete — a stale map lies. Real tree as of Phase 1.05 (node_modules, .next, and other gitignored paths omitted).

## Docs & rules (repo root)

- `CLAUDE.md` — Code's standing rules (behavioral contract; under 150 lines)
- `README.md` — public-facing repo overview; "unofficial archive" disclaimer; stack + layout
- `facts.md` — verified business facts; the only legal source for factual claims on the site
- `brand.md` — design tokens + brand rules; the only token source (confirmed 1.02 tokens written back in Phase 1.03; zero SEED)
- `briefs/` — every phase brief, saved by Lazar; versioned instruction history (`.gitkeep` placeholder — the Phase 1.01 brief was not filed per owner decision, D-1.01-3). Seven briefs filed as of 3.11; most phases ran from a brief pasted into the session rather than committed
- `data/book/` — **new 3.11, extended 3.12**, the tracked **source drop** extracted from „ФК Беласица – гордоста на Струмица" (Аце Стојанов, финална верзија 04.10.2025). Seven files, ~6,3 MB. **Data, not code — nothing in `src/` imports it**, so it adds zero bytes to any bundle. Every match and squad row carries `sourceLine`, the book's original line verbatim (D-3.11-2, D-3.11-3) **3.12 adds `data/book/legends.json`** — the people half of the same extraction: 86 records, 80 carrying the book’s all-time appearance rank, 60 carrying the full biography paragraph by paragraph with a `line` on each. **The only file in this folder the site’s content depends on** — written into Sanity by `scripts/ingest/bios.mjs`; the site reads Sanity, never this file
  - `README.md` — provenance, the ±2-match accuracy check against the book's own summary table, the four known divergences, and what was deliberately left unchanged. **Owed: a native read by Lazar and Аце**
  - `extraction-report.md` — the Macedonian coverage report written for Аце
  - `seasons.json` — **96 seasons** (1922/26 → 2025/26): title, story, matches, squad, top scorers, official balance. 3,6 MB
  - `matches.json` — flat list of **2.267 matches**: home, away, result, competition, autumn/spring, round, outcome. 2,0 MB. **Nothing can accept this yet** — `match` is unregistered (D-2.01-2, premise voided at D-3.11-1)
  - `master-table.json` — the book's own „БЕЛАСИЦА НИЗ ИСТОРИЈАТА" table, 107 rows, used as an **independent control** on the extraction. Its totals reproduce the book exactly: `2275 · 1030–421–824 · 3907:2976 · 2885` — the book's side of OV-14, now machine-read from source
  - `index.json` — lightweight index: season, league, placement, match count
  - `season-content.json` — **what the fill script reads**: per-season `story` and `results` plus a `resultsProvenance` string. 96 seasons · **2.563** result lines · **167** story paragraphs; provenance splits 92 printed lists / 1 assembled from sentences / 2 notes / 1 none (D-3.11-4)
  - `razno-source.md` — **new 3.16**, the seventh through thirteenth chapters, which 3.11 and 3.12 did not touch: УЕФА · младинска школа · Виареџо · Тиверија · Партизан · стадион · Ајдуци. **109 paragraphs, each preceded by `<!-- L#### -->`** — its line number in the whole-document read, so every sentence on the site traces to one line of the book. **This is the one file in `data/book/` that a `src/` module is generated FROM** (never imported — `src/content/razno.ts` is its transcription, re-diffed against it out of band, 101/101 byte-identical). ⚠️ Its own `Slug:` annotations are stale for three topics; the module owns the slugs (D-3.16-1)
  - `statistika-source.md` — **new 3.34**, the verbatim transcription of Аце's statistics document. **232 non-empty paragraphs, each preceded by `<!-- L#### -->`**, its index in the whole-document read. Produced by a stdlib `zipfile` + `ElementTree` read of `word/document.xml` (the file has no tables, footnotes or endnotes); **empty paragraphs dropped, nothing else changed** — his run-together words, trailing periods and two-digit period spans are all preserved. **Verified 232/232 byte-identical to the `.docx`.** Like `razno-source.md` this is the one kind of file in `data/book/` a `src/` module is generated FROM, never imported
  - `season-2025-26.json` — **new at 3.36**, and ⚠️ **the only file in this directory that is NOT from the book.** The book's `matches.json` holds **0** matches for 2025/26 — its extract ends with spring 2025 — so this season's 30 results and 35-player squad come from the published Sanity `season` document (`results` + `lineupAndStats`, entered August 2026 from Аце's material). Written by `scripts/extract-season-2025-26.mjs`; carries a `provenance` object stating the source in Macedonian, so it cannot be mistaken for book data. Row shapes deliberately mirror the book's (`matches[]`, `squad[]`, both with `sourceLine`), plus `derivedRecord` (**P30 W18 D4 L8 57:21**, asserted equal to the document's own `finalTable` row) and `staffAndNotes` (the 9 `lineupAndStats` lines that are not numbered player rows — the стручен штаб, the претседател and two headers, preserved here because the table switch stops displaying them: OV-89)
  - `sources/belasica-statistika-razno.docx` — **new 3.34**, Аце's original „БЕЛАСИЦА - статистика и разно" (22.000 bytes, SHA-256 `7cf8ab94…f2d955d0`). Committed as provenance so the transcription above can be re-verified from the repo alone. ⚠️ **It was not in the repo when the phase began** — located on disk and committed under the brief's own path (D-3.34-1)
- `docs/data-shapes.md` — **new at 3.29**, the read-only answer to „what is actually in the data". Every field of `person` (210 docs), `season` (96), `photo` (1 054) and the six `data/book/*.json` files, each with a real count beside it — plus the enumerable values (`role` 152·68·29, `competitionType`, `stage`, `decade`), the four „Легенди" bands as the site computes them (**138 · 68 · 29 · 10**, distinct **210**), and the full appearances distribution for any threshold decision. **Nothing imports it**; it exists to be read before a brief is written, after three consecutive briefs carried premises the data contradicted. Its §5 lists the six traps that have each cost a phase — chief among them that **`get_schema` defaults to the stray `default` workspace** and returns a pre-2.02 manifest (D-2.01-8), which is how 3.25-Cowork came to report the Part-3 fields undeployed when they were live. Corrects three recorded claims: `resultsProvenance` is **not a Sanity field at all**, `season.squad`/`season.trainers` are **1 of 96** each, and **no squad row carries a `position` field** (D-3.29-4)
- `docs/design-handovers/` — Design's handover files; Code reads the matching one before any UI work
  - `Part-1-Phase-05-Homepage.md` — homepage §Layout spec, archived in-repo (Phase 1.05)
  - `Part-2-Phase-02-Handover.md` + `Part-2-Phase-02-mockups/` — Archive & Season templates: shared conventions, §5–§7 specs, mockups (Phase 2.02)
  - `Part-2-Phase-05-Handover.md` — **People & pages** design spec (Legends / Person / About / Contact). Landed into the repo by Phase **2.06** — the 2.05 design phase never committed it (D-2.06-1). Its §0 „the repo wins" rule governs; several of its citations are wrong or stale — see D-2.06-2/-5/-7. The mockup it names (`People & Pages.dc.html`) was never supplied
  - `Part-3-Phase-05a-Directions.md` — **Phase 3.05a direction exploration**: the three homepage proposals (А „Спортски весник" / Б „Клупски музеј" / В „Трибина"), each with intent, its exact token deltas vs `brand.md`, and what adopting it site-wide would touch. **No recommendation** — the pick is the owner's. Read together with the three `/predlog-*` routes
- `docs/accessibility-report.md` — **new on `a11y-remediation`**, the WCAG 2.2 AA audit and remediation report, written for the owner rather than for a developer: what was found, what changed, the full contrast table, the keyboard walkthrough per page, the two items needing an owner decision, and an explicit statement of what an automated audit cannot certify. Its §7 explains the 91 pa11y errors that are deliberately left (a checker bug, reproduced in twelve lines)
- `docs/a11y-scan-before/` + `docs/a11y-scan-after/` — the raw evidence behind that report, so every number in it can be re-checked rather than trusted. axe-core over all **324** public URLs at 1280 and 375 (`axe-desktop.json`, `axe-mobile.json`), pa11y WCAG2AA over the same 324 (`pa11y-all.json` — every error in full, warnings collapsed to one row per code per page), the rendered-DOM contrast and target-size sweeps, Lighthouse accessibility scores (`lighthouse.json`, slimmed of its embedded screenshots), the keyboard-walk transcripts (`after/keyboard/`), and the before/after screenshot pairs. `after/pa11y-H43-tbody-repro.html` is a standalone twelve-line page with no site code in it that reproduces the checker bug §7 describes
- `docs/ace-demo/` — Ace demo kit (Phase 1.06); Phases 2.02 & 2.05 read `feedback.md` before they open
  - `walkthrough.md` — demo script (spoken lines in Macedonian; incl. the OV-3 footer question)
  - `feedback.md` — capture template for Ace's feedback (**still the empty template** — the sit-down had not happened as of 2.01; D-2.01-7)
  - `screenshots/homepage-desktop.png` (1280-wide) · `homepage-mobile.png` (375-wide) — full-page captures of the finished homepage, **regenerated at 2.08** from the local prod build (reveal forced via `prefers-reduced-motion`)
  - `Part-3-Phase-02-pilot-review.html` — **self-contained** visual review report (Phase 3.02): the 6 pilot seasons' chosen team+table thumbnails, trainer/stats text, per-season gaps, the 7 `clubRecord`s + sources, and the 881-provenance-rewrite before→after. For the owner to approve the pilot pattern before the 90-season run.
  - `Part-3-Phase-02-Run-review.html` — **self-contained** full-archive review report (Phase 3.02-Run): all 90 `decade!=2020` seasons over 10 decade sections, each with team+table thumbnails, trainer + top scorer, gaps, and 3 review-flags (77 team / 82 table photos). 2.3 MB.
  - `Part-3-Phase-04-content-review.html` — full-archive **content**-review page (Phase 3.04-**Cowork**): all 96 seasons by decade with their `teamPhoto` + `tablePhoto` thumbnails, plus the flagged borderline and era-misfiled photos. Thumbnails are linked from `cdn.sanity.io` (needs a connection to view), not embedded. Macedonian; a review artifact for Lazar, not part of the site build
- `docs/content-ingestion-plan.md` — **how the ~915 photos / ~74 seasons get into Sanity** (Phase 2.01, D-2.01-4): the hybrid approach (scripted shells+assets / manual editorial), the deterministic Drive-folder→season mapping rules, the by-decade wave plan, and the provisional-provenance / P0.2-rights gate. Phase 2.09 builds and runs against this.
- `docs/ingestion/` — Phase 2.09 ingestion reports + hand-off:
  - `README.md` — run procedure + „verify no draft leaked" check
  - `2.09-classifier-verification.md` — classifier self-test proof (23/23)
  - `2.09-dry-run-report.md` — **2.09-Run**: machine classification of the real mirror (119 folders → 96 seasons / 21 thematic / 2 skip; 0 collisions, 0 unmatched)
  - `2.09-run-report.md` — **2.09-Run**: the actual run (Wave 0 + Waves 1–8, the owner-directed publish D-2.09R-2, OV-4/OV-7 cleared)
  - `2.09-editorial-handoff.md` — **2.09-Run**: manual-pass worklist (per-season content, 9 manual-title seasons, person folders, 10 spot-check pages, source docs)
  - `README.md` — what the reports are, the run procedure (dry run → Wave 0 → Waves 1–8), and the **mirror-missing status** (the three run reports are pending the mirror + a write token — D-2.09-3)
  - `2.09-classifier-verification.md` — **done**: output of `verify-classify.mjs` proving `classify()` matches the locked §3 mappings (no mirror needed)
  - `2.09-dry-run-report.md` · `2.09-run-report.md` · `2.09-editorial-handoff.md` — **pending the mirror**; generated by the dry run / commit waves
  - `season-content-report.md` — **new 3.11**, written by `scripts/fill-season-content.mjs`. One row per season: status, what `story` and `results` would receive, and the season's `resultsProvenance`. The committed copy is now the **real run** — „**ЗАПИШАНО ВО SANITY**", 2026-08-02 04:42, 96 seasons processed and **86 patched** (the dry-run copy 3.11 filed was overwritten by it, as designed). Regenerated on every run

## Project state (`src/_project-state/`)

- `current-state.md` — live repo snapshot; NEXT line; owed-verification + placeholder registers
- `file-map.md` — this file
- `00_stack-and-config.md` — append-only stack/config log with exact pinned versions
- `decisions.md` — append-only decision log, IDs `D-<phase>-<n>`
- `completions/_TEMPLATE.md` — completion-report template
- `completions/Part-1-Phase-01-Completion.md` — Phase 1.01 completion report
- `completions/Part-1-Phase-03-Completion.md` — Phase 1.03 completion report
- `completions/Part-1-Phase-04-Completion.md` — Phase 1.04 completion report
- `completions/Part-1-Phase-05-Completion.md` — Phase 1.05 completion report
- `completions/Part-1-Phase-05-2-Completion.md` — Phase 1.05.2 completion report (homepage content-sync)
- `completions/Part-1-Phase-06-Completion.md` — Phase 1.06 completion report (draft — pending the Ace sit-down + portraits before close)
- `completions/Part-2-Phase-01-Completion.md` — Phase 2.01 completion report (content model lock)
- `completions/Part-2-Phase-02-Completion.md` — Phase 2.02 completion report (Archive & Season design)
- `completions/Part-2-Phase-03-Completion.md` — Phase 2.03 completion report (Archive & Season templates)
- `completions/Part-2-Phase-04-Completion.md` — Phase 2.04 completion report (Statistics page)
- `completions/Part-2-Phase-05-Completion.md` — Phase 2.05 completion report — **BACK-FILLED at 2.09** (the design phase filed none; records the handover-only artifact + the nine 2.06 conflicts; D-2.05-1/-2/-3 back-filled in `decisions.md`)
- `completions/Part-2-Phase-06-Completion.md` — Phase 2.06 completion report (People & pages build)
- `completions/Part-2-Phase-07-Completion.md` — Phase 2.07 completion report (Contact page `/kontakt`)
- `completions/Part-2-Phase-09-Completion.md` — Phase 2.09 completion report (content ingestion — script built; waves blocked on the missing mirror, D-2.09-3)
- `completions/Part-2-Phase-09-Run-Completion.md` — Phase 2.09-**Run** completion report (the actual ingestion: 96 seasons + 889 published photos; OV-4/-6/-7/-9 cleared; the owner-directed publish D-2.09R-2)
- `completions/Part-3-Phase-01-Completion.md` — Phase 3.01 completion report (content model update: 5 optional `season` fields + `clubRecord` type; schema deployed)
- `completions/Part-3-Phase-02-Completion.md` — Phase 3.02 completion report (content fill — 2020s pilot: 6 seasons tagged + trainer/top-scorer text, 7 `clubRecord`s, 881 photo `provenance` rewritten → OV-10 cleared)
- `completions/Part-3-Phase-02-Run-Completion.md` — Phase 3.02-**Run** completion report (the remaining 90 seasons: 77 team + 82 table photos, 63 trainers, 66 top-scorers; photos classified by 8 parallel subagents + validated; main narrative→`story` deferred, D-3.02R-3)
- `completions/Part-3-Phase-02-Story-Completion.md` — Phase 3.02-**Story** completion report (`season.story` filled for 66 seasons 1922→1995/96 from the history narrative, verbatim via 5 era-subagents; +squad rosters in `lineupAndStats`; first 3.02 change visible on the live site, D-3.02S-1…-4)
- `completions/Part-3-Phase-04-Code-Completion.md` — Phase 3.04 **Code** completion report (the season-page rebuild: all 96 pages restructured around the Part 3 fields; decisions `D-3.04-1…-12`; the `cn()` type-scale bug and the results/story overlap handed on)
- `completions/Part-3-Phase-04-Cowork-Completion.md` — Phase 3.04-**Cowork** completion report, the parallel **Sanity-content** phase that ran alongside the code rebuild (all 96 seasons' photos re-verified against source, 2 wrong images cleared + 2 repointed, the first `results` fill on 10 seasons, trainer 1957-58 recovered). Its counts were re-verified against the live published dataset before it was committed. Decisions `D-3.04C-1…-6`
- `completions/Part-2-Phase-08-Completion.md` — Phase 2.08 completion report (homepage made truthful to the 96-season archive: deterministic photo ordering, featured teaser self-omits, the real 2025-26 squad photo promoted into the hero; OV-RIGHTS closed, D-2.08-1…-3)
- `completions/Part-3-Phase-02F-Code-Completion.md` — Phase 3.02F-**Code** completion report (build hardened at 270 pages — Sanity reads retry ×5 and fail loudly instead of silently becoming a 404 — plus the 30 curated `clubRecord`s surfaced on `/statistika`)
- `completions/Part-3-Phase-03-Completion.md` — Phase 3.03 completion report (homepage rebuilt as the photo-forward seven-section front door, footer redesigned with the fake demo contact/socials replaced by visible `[PLACEHOLDER]` chips, navbar refresh). **Did not wire the Formspree endpoint it was assigned — see 3.03b**
- `completions/Part-3-Phase-03b-Completion.md` — Phase 3.03b completion report (**configuration only**: `NEXT_PUBLIC_FORMSPREE_ENDPOINT` set in `.env.local` + Vercel Prod/Preview, so the `/kontakt` form goes live; PL-14 and OV-8 both cleared against the real service; `D-3.03b-1, -2`)
- `completions/Part-3-Phase-04d-Completion.md` — Phase 3.04d completion report (owner-directed audit-fix phase, no written brief: the `cn()` type-scale bug fixed at source, ≥24px tap targets, OG/robots/sitemap launch metadata, LCP priority hints)
- `completions/Part-3-Phase-05-Completion.md` — Phase 3.05 completion report („Трибина" adopted site-wide from `/predlog-c`, and all three proposal routes deleted)
- `completions/Part-3-Phase-05a-Completion.md` — Phase 3.05a completion report (three full homepage directions at `/predlog-a|b|c` on identical live content, `noindex` and unlinked, plus the comparison handover — **no recommendation**, the pick was the owner's)
- `completions/Part-3-Phase-05b-Completion.md` — Phase 3.05b completion report (homepage narrowed back to a front door — ten most-capped legends and six records instead of all 160 people / 30 records — repaired vector crest, and the season photo lightbox)
- `completions/Part-3-Phase-09-Completion.md` — Phase 3.09-**Code** completion report (performance & accessibility measured, fixed and re-measured: desktop 100 on all nine routes, mobile ≥95 on eight of nine; the `Reveal`-at-`opacity:0` LCP defect; `D-3.09-1…-8`)
- `completions/Part-3-Phase-10-Completion.md` — Phase 3.10-**Code** completion report (the `/kontakt` provisional banner removed, the legends search moved into the navy header and given Latin-input matching, the homepage hero pinned to 1982/83; `D-3.10-1…-9`, and the 375 px hero crop escalated as **OV-24**)
- `completions/Part-3-Phase-12-Completion.md` — Phase 3.12-**Code** completion report (biographies from the book, ranking by appearances, the 21-goal scorer threshold, four photographs, the footer fan-page link). Also records the branch-divergence incident and its recovery (D-3.12-8)
- `completions/Part-3-Phase-11-Completion.md` — Phase 3.11-**Code** completion report (the book extraction landed as tracked data in `data/book/`, plus the `setIfMissing`-only fill script; **no schema change, no Sanity write, no file under `src/` outside `_project-state/`**; `D-3.11-1…-6`, OV-14's book side machine-verified, and two DoD items reported unmet because the brief was written against a repo state that no longer holds)
- `completions/Part-3-Phase-13-Completion.md` — Phase 3.13-**Code** completion report (two season sections renamed + Тренери/Претседатели ordered newest-first). ⚠️ **Indexed retroactively at 3.14** — 3.13 filed the report but never added this line
- `completions/Part-3-Phase-14-Completion.md` — Phase 3.14-**Code** completion report (four owner-requested corrections: the officials band renamed to „Претседатели" with its count noun, two season `<h2>`s renamed, and Петар Андреев's role corrected to `["player","trainer"]` as a live Sanity write)
- `completions/Part-3-Phase-16-Completion.md` — Phase 3.16-**Code** completion report („Разно“: seven topics from the book as an index + seven static detail pages, „Разно“ in the main nav and the sitemap; **zero new dependencies, no Sanity anything, 271 → 279 pages**; `D-3.16-1…-14`, and **OV-40**, the header-nav wrap that a seventh item widens)
- `completions/Part-3-Phase-25-Cowork-Completion.md` — Phase 3.25-**Cowork** completion report, **filed retroactively at 3.30** from the text Lazar supplied. The Sanity-only content phase: 29 `officialYears` + 45 `trainerYears`, the Ефтимов duplicate merged, the 2022/23 lead photo repointed. ⚠️ **It wrote nothing to the repo by design**, and its report went missing for two phases (**OV-69**, D-3.29-1, closed by D-3.30-6). Its six decisions are `D-3.25-1…-6`
- `completions/Part-3-Phase-30-Completion.md` — Phase 3.30-**Code** completion report (the internationals' figures relabelled to „За репрезентацијата", `APPEARANCE_MIN` 46 → **130** giving 48 rows, the derived **2025/26** table row, and **11 authorised Sanity writes**; `D-3.30-1…-7`; **OV-57/-69/-70/-71 resolved**, **OV-72…OV-80** opened)
- `completions/Part-3-Phase-32-Completion.md` — Phase 3.32-**Code** completion report (the determined part of Аце's extended tail: Играчи **138 → 142** and the roster **210 → 212**, via **four Sanity writes** — ranks 154 and 161 onto two existing men, and two new people at 160 and 162. **Zero application-code changes**; the four render on presence-gated fields alone. `D-3.32-1…-3`; **OV-79 partially resolved**, **OV-81…OV-84 opened**. ⚠️ Records that the brief's named single source, `Part-3-Phase-31-Dataset-Note.md`, **does not exist anywhere** — the fourth stale-brief incident — and that ranks **139–159 remain blocked on 20 missing first names**)

- `completions/Part-3-Phase-RANKS-Completion.md` — the **`distinct-legend-ranks`** branch (not a numbered phase): the owner-reported „three players at rank 135" was **four**, and one of **29 tie groups covering 70 of the 162 ranked players** — Аце's competition-style numbering, internally perfect, not a data fault. At the owner's instruction every ranked player now holds a **unique** rank, each taken from within the span his group already owned, ordered alphabetically. **41 documents changed, no player's ladder position moved.** `D-RANKS-1`. ⚠️ Records that the within-tie order is **DERIVED, not Аце's word**, and is flagged as such in `facts.md`)

> ⚠️ **This list is behind the directory.** `completions/` holds **50** reports plus the template; **40** are indexed here (both counted on disk at 3.32 — the previous figures, „48 and 39", were themselves one short). The gap predates 3.30 and covers phases between 3.16 and 3.29. 3.30 added only its own two lines rather than guessing at descriptions for reports it did not write — see **OV-80**.

## Application (`src/`)

- `src/app/layout.tsx` — **bare** root layout: `<html lang="mk">`/`<body>`, fonts, `globals.css`, Vercel Analytics, site metadata. **3.04d:** `metadataBase` (Vercel origin, hardcoded + sync comment), Open Graph (siteName/locale `mk_MK`/crest image 864×1220) + Twitter `summary` card (D-3.04d-3). Site chrome moved to the `(site)` group so `/studio` can escape it (Phase 1.04, D-1.04-3)
- `src/app/not-found.tsx` — **new at 3.23 (B3)**, the site's 404, „Страницата не постои": navy `PageHeader` (breadcrumb → H1 → one line of body copy) then a paper block with three onward links („Почетна", „Архива", „Легенди") in the footer nav column's anatomy at paper values (D-3.23-6). ⚠️ **At the app ROOT, not in `(site)`, and it mounts the chrome itself** — skip link, `SiteHeader`, `<main>`, `SiteFooter`, `BackToTop`. Built in `(site)` first and measured: a route-group `not-found.tsx` is reached only by an explicit `notFound()`, so an unmatched URL fell through to Next's English default (D-3.23-8). **If `(site)/layout.tsx` gains a chrome element, add it here too.** Emits no canonical and no `BreadcrumbList` — the visible trail stays as a way back. Verified on the production build: unmatched paths and all three `notFound()` routes return **404** and render this page.
- `src/app/robots.ts` — robots file-convention route: allow all, disallow `/studio`, sitemap pointer (Phase 3.04d, D-3.04d-4)
- `src/app/sitemap.ts` — sitemap file-convention route: **7** static routes (`/razno` added at 3.16) + the **seven `/razno/<slug>` topics**, read from `RAZNO_TOPICS` so a topic cannot be prerendered without appearing here, and emitted with **no `lastModified`** because they have no document and therefore no revision time (D-3.16-14) + every published season/person slug via the public read client (`defined(slug.current)`, `_updatedAt` as `lastModified`, `revalidate = 60`, static-only fallback on a failed read) (Phase 3.04d, D-3.04d-4)
- `src/app/(site)/error.tsx` — **new at 3.23 (B4)**, the segment error boundary (`"use client"`, as Next requires). Exists so a Sanity read failing during an **ISR revalidation** shows the archive's own page rather than a raw Next error screen — `fetch.ts` deliberately throws on the season and person templates after five attempts (D-3.02F-C-1), which is right at build time and was a raw error screen at request time. Same chrome and tone as the 404, heading „Нешто тргна наопаку", and a retry control wired to `reset`. **Renders no `error.message`, no `digest` and no stack.** It sits inside `(site)` so the layout still wraps it — an error boundary replaces the segment's page, not the layout above it. ⚠️ **Never made to fire**; verified structurally only.
- `src/app/(site)/layout.tsx` — public-site chrome: pre-paint `.js` flag script + skip link + `SiteHeader` + `<main>` + `SiteFooter` (relocated verbatim from root layout in 1.04; `.js` script added 1.05 for the reveal progressive-enhancement, D-1.05-5) **3.17: + `BackToTop`**, mounted once immediately after `SiteFooter` — the single mount point for the whole public site, and the reason `/studio` never gets the button. It renders `null` until the reader is 1.5 viewports down, so it adds nothing to any prerendered page
- `src/app/(site)/page.tsx` — **the homepage**, **rebuilt again at 3.05** into the „Трибина" seven-block page: a matchday-poster hero (photograph full-bleed, crest as a white block capped by the 6px orange bar and pinned over the picture's lower edge, wordmark on solid navy at 14.95:1 — D-3.05a-10) then alternating paper/navy full-bleed blocks (Story → Legends → scoreboard Records → Decades → Moment → Quick links). Same `HOME_QUERY` and ISR as 3.03 — this phase re-dressed the page, it did not re-source it. Prior note, **3.03**: to the seven-section heritage-and-legends-led flow (Hero → Story → Legends → Numbers → Decade explore → Moment → Quick links) from live published Sanity content; ISR `revalidate = 60`. One `HOME_QUERY`: hero = most-recent `teamPhoto` (`order(decade desc, title desc)`, newest-photo fallback); legends = players sorted portraits-first then name (D-3.03-2) via the reused `LegendCard` (equal-height via a homepage-scoped grid variant); records = `clubRecord` docs → `ClubRecords`; decades reduced to per-decade counts → `DecadeExplore`; moment = deterministic captioned+season-anchored+landscape photo (D-3.03-4). Imports `focusOnPaper`/`focusOnNavy` from `@/lib/focus`. Graceful placeholders when empty. Replaced the 1.05→2.08 eight-section page (featured season, gallery mosaic, dot-rail). **3.05b — the front door stops showing the whole archive:** the crest `<Image>` now points at `/crest.svg` (header does too); `HOME_QUERY`'s `legends` projection ranks `coalesce(careerStats.appearances, -1) desc, name asc` and slices **`[0...10]`** (the `-1` keeps an unrecorded player below one with a single appearance — D-2.01-3's total is authoritative, never summed); the records strip is capped at **six** by `ClubRecords`' own label whitelist (D-3.05b-1); and `client.fetch` is wrapped in `fetchOrThrow` while **keeping** the graceful `EMPTY` fallback, unlike the season/person templates' loud throw (D-3.05b-4). The display sort (portraits first, then Cyrillic name, D-3.03-2) now only arranges the chosen ten (Phase 3.03, D-3.03-1..6; 3.05b, D-3.05b-1/-4/-7). **3.10 — the hero is pinned to 1982/83 in code:** `HERO_SEASON_SLUG` + `HERO_PHOTO_ID` module constants feed `HOME_QUERY` as parameters; a new `heroPinned` lookup sits **beside** `heroSeason`/`heroFallbackPhoto` so resolution is pinned → newest-season → newest-photo (the fallback is exercised, not assumed — D-3.10-4). `heroAltFor()` resolves the alt in that same order, so alt and image can never disagree; with no caption on the pinned photo it renders the season title with Studio's „…“ quotes stripped. The hero box goes `4/5 → 3/2` at base (`sm:16/10`, `lg:21/8` unchanged) with `objectPosition` `32% → 38%` — **at 375 px this still crops 11.2 % off each side of a 1.934:1 scan, a known limitation escalated to the owner (D-3.10-6)**. The `moment` filter now excludes the hero photograph explicitly by `_id` (D-3.10-5) **3.12: the legends band ranks on `coalesce(legendRank, 9999) asc`** instead of `careerStats.appearances`, so the homepage’s ten and `/legendi`’s order cannot disagree — Томче Ефтимов and Панче Пантазиев in, Зоран Митевски and Ванчо Костов out (D-3.12-3)
- `src/app/studio/[[...tool]]/page.tsx` — embedded Sanity Studio at `/studio` (`NextStudio`); renders on the bare root (no site chrome) (Phase 1.04)

> **`src/app/(predlozi)/` was DELETED at Phase 3.05** (D-3.05-9). The three homepage proposals А/Б/В lived there from 3.05a; the owner picked **В „Трибина"**, it was adopted site-wide, and the whole route group went with the pick. The designs survive in git history and, as intent + token deltas, in `docs/design-handovers/Part-3-Phase-05a-Directions.md`. The three exploration routes leave the build with them.

- `src/app/fonts.ts` — **Oswald + Golos Text** via `next/font/google`, Cyrillic subsets, two weights each (Oswald 600/700 · Golos 400/700). Replaced Inter + Source Serif 4 at 3.05 when direction В was adopted; the faces were promoted here out of the deleted `predlog-c/fonts.ts` (Phase 1.03; replaced 3.05, D-3.05-2/-7/-9)
- `src/app/globals.css` — Tailwind 4 `@theme` driven by brand.md tokens; **rewritten at 3.05** to the „Трибина" layer: two navies (`navy` `#0D1F3C` + `navy-2` `#12294F`), `orange` `#EE7A16` as a block fill, clamp type scale + `--text-stat`/`--text-stat-lead`/`--text-wordmark`, `--spacing-section`, page 1248, `--spacing-header` **78px**, all radii `0`. Adds the **role classes** in `@layer components` (`u-display`/`u-h1`/`u-h2`/`u-h3`/`u-stat`/`u-label`/`u-cap`/`u-tile`/`u-card`/`u-btn`/`u-link`), which carry the direction's uppercase + tracking that a `--text-*` token cannot (D-3.05-3). ⚠️ `.u-focus` / `.u-focus--on-navy` sit **outside every `@layer` on purpose** — inside `components` a `@layer utilities` rule in the vendored shadcn sheet silently disabled every focus ring (D-3.05-4). Also: reduced-motion baseline; reveal-on-scroll (`.js [data-reveal]`, re-timed to 260ms); `.placeholder-hatch` + `.placeholder-hatch--on-navy` (Phase 1.03/1.05; rewritten 3.05) **3.35:** `.rail-fade` — the scroll-rail edge-fade affordance (two `@property` lengths driving a `mask-image` on the scroller, `animation-timeline: scroll(self inline)`, all inside `@supports`), with `animation-duration: auto !important` to survive the reduced-motion reset above it (D-3.35-1, D-3.35-3).
- `src/app/favicon.ico` — club-crest favicon (16/32/64), **re-generated from the repaired vector master at 3.05b** — it no longer embeds the pre-swap opaque render (chore-crest-logo; rebuilt 3.05b)
- `src/app/icon.png` — 512² crest favicon (Next file-convention `<link rel=icon>`), re-generated at 3.05b (chore-crest-logo; rebuilt 3.05b)
- `src/app/apple-icon.png` — 180² crest apple-touch-icon (Next file-convention), re-generated at 3.05b (chore-crest-logo; rebuilt 3.05b)
- `src/components/SiteHeader.tsx` — **sticky** navy header/nav, **restyled at 3.05** to „Трибина": opened by the 6px orange bar that also closes the footer, condensed caps nav over a 3px underline carrying the state, 48px burger, `navy-2` mobile panel with a 3px orange left bar on the current row. Restyle not rewrite — still `sticky top-0`, same burger, same `isActivePath` — so `--spacing-header` survived with a **re-measure to 78px** and every `top-header`/`scroll-mt-header` consumer still lands right (verified: anchor jump = 78px). Prior note: (`sticky top-0 z-40` + `border-paper/10`, 1.06b); client component; active-state (orange underline + `aria-current`) + accessible mobile menu (opens beneath the sticky bar); crest on a white tile beside the wordmark. **3.03 refresh:** hamburger is a 44px tap target (`p-2.5`); inactive nav items get a non-color `hover:border-paper/40` underline. **3.05b:** the crest `<Image>` `src` moved `/crest-ui.webp` → **`/crest.svg`**, keeping `unoptimized` and `width={400} height={565}` (the rebuild's 0.7079 ratio matches, so `h-10 w-auto` renders identically); the white tile and the 6px orange bar stay, because the crest's left half is white. Re-measured after the swap: header is **still 78px** at both 1280 and 375, so `--spacing-header` was **not** changed and an anchor jump still lands flush at its bottom edge (Phase 1.03; sticky 1.06b; refresh 3.03; crest chore-crest-logo; 3.05b D-3.05b-7)
- `src/components/SiteFooter.tsx` — **navy** footer since 3.05, closed by the same 6px orange bar that opens the header so the page reads as one bounded object. Four columns (brand/OV-3 statement · Навигација from `nav.ts` · Контакт · Следете нѐ) over a bottom bar. **3.15:** „Следете нѐ" swapped its single fan-page text link for `SocialLinks` (Фејсбук + Инстаграм icon links, D-3.15-2), and the bottom bar gained the **copy-permission line** on its own row beside the © and „Правни информации" — its own row, not a third flex item, because at ~150 characters it would otherwise wrap the © line awkwardly at every width; capped at the reading measure. **The footer now renders no placeholder chip at all** — its last one (PL-15) closed here. Every string comes from `src/lib/facts.ts`.
- `src/components/SocialLinks.tsx` — **NEW 3.15.** The archive's two social profiles (Фејсбук, Инстаграм) as icon links, rendered identically on both surfaces that carry them: the footer's „Следете нѐ" column and `/kontakt`'s direct-contact block. `onNavy` prop picks the surface, exactly like `LegendCard`/`PlaceholderChip`. Hrefs and labels come from `SOCIAL_LINKS` in `src/lib/facts.ts` — one source, so the two surfaces cannot drift (which is how PL-15 stayed half-open from 3.12). Full **44×44** tap target (`min-h-11`), unlike the footer's other links at 29px (D-3.15-10); accessible name „Беласица на Фејсбук" via an `sr-only` prefix, with the visible word a substring of it so WCAG 2.5.3 holds. Focus ring from `src/lib/focus.ts`. **Closes PL-15 on both surfaces.**
- `src/components/icons/SocialIcons.tsx` — **NEW 3.15.** `FacebookIcon` + `InstagramIcon`, hand-authored inline SVG on a 24×24 artboard — **no icon library and no new dependency**. `brand.md` names no icon set, so the pair is drawn to this direction: both are outlined containers with a solid mark inside, at the same 2px ring weight, so they read as one set rather than as two glyphs from two libraries. Single `evenodd` path each (ring/body punched out by an inner subpath), `fill="currentColor"` so the footer's ink and hover carry through, `aria-hidden="true"` + `focusable="false"` — the accessible name always comes from the link, never the SVG.
- `src/components/JumpNav.tsx` — **NEW 3.17.** The sticky in-page jump rail, generalised out of `DecadeJumpNav` so three long pages share one rail: `/arhiva` by decade, `/legendi` by role, `/statistika` by section. Takes `items: {id,label}[]` + `ariaLabel`. Markup, classes and vertical rhythm are lifted verbatim — `sticky top-header z-30 bg-navy-2`, `overflow-x-auto px-5 py-1 md:px-8`, `py-2` links, `focusOnNavy`, `tabular-nums` — and the **container+link vertical total must stay 12px per side**, because every call site's `scroll-mt-[calc(var(--spacing-header)+3.25rem)]` is derived from it (measured rail height **47px**). Returns `null` below two items (`SeasonAnchorNav`'s rule, now shared — D-3.17-6). A **server component**; `/legendi` pulls it into the client bundle because its bands are filtered in the browser (+270 B on that route only) **3.35:** the scroller carries `rail-fade` — mask only, so the 47px height and every derived `scroll-mt` are unchanged, measured (D-3.35-1, OV-88).
- `src/components/BackToTop.tsx` — **NEW 3.17.** „Назад на врвот" — the site's one fixed control and the repo's **seventh** client component. Mounted once in `src/app/(site)/layout.tsx`, so `/studio` (outside the route group) never gets it. Appears above `scrollY > innerHeight * 1.5`, read **once on mount** (a restored or deep-linked position must not wait for a scroll event — verified at `/arhiva#d1930`) and thereafter on a `passive` scroll listener coalesced through `requestAnimationFrame`. **Hidden means unmounted, not `opacity:0`** — it is in **zero** prerendered HTML files. Click scrolls to `top: 0` with `behavior` read from `prefers-reduced-motion` **at click time**, then hands focus to the skip link (D-3.17-8). 48×48 `rounded-full` navy circle, `bottom-5 right-5 md:bottom-8 md:right-8`, hover swaps to **orange fill / navy ink** at 160 ms, `focusOnPaper` ring, hand-authored 20×20 inline SVG arrow (**no icon library**), `sr-only` name, no `title`. **`z-30` deliberately** — below the header (40), the skip link (50) and `PhotoLightbox` (50), so an open lightbox covers it (hit-tested). Its `rounded-full` is brand.md's single named radius exception (D-3.17-2)
- `src/components/Container.tsx` — max-width (**1248px** since 3.05) + page-gutter primitive (20px / 32px md). Wraps a block's _content_; the block's colour runs full-bleed on the `<section>` (Phase 1.03; 3.05)
- `src/components/PageHeader.tsx` — **new at 3.05** (D-3.05-6): the navy block every inner page opens with — breadcrumb → H1 → intro → counted meta. Used by `/arhiva`, `/statistika`, `/legendi`, `/za-nas`, `/kontakt`; the season and person pages keep their richer heroes but sit on the same navy block with the breadcrumb pulled inside
- `src/components/home/PlaceholderChip.tsx` — placeholder chip (dashed border, hatch α 0.18, mono `[PLACEHOLDER: …]`); the only legal way to show a missing display fact. **3.05:** gained an `onNavy` variant, because this direction puts real content inside navy blocks where a paper chip would punch a hole; both states are contrast-checked against their own hatch **stroke**, the worst case a sweep otherwise skips (4.73 / 5.84:1 — D-3.05a-8) (Phase 1.05; onNavy 3.05)
- `src/components/home/PhotoFrameView.tsx` — **new at 3.09 (D-3.09-1)**: the frame's _markup only_, with **no Sanity dependency**. Takes an already-resolved `FramedImage = { src, objectPosition }`. **This is the half a `"use client"` component may import** — `LegendsBrowser → CategoryGrid → LegendCard` reaches it, and importing `PhotoFrame` there instead dragged the whole `@sanity/image-url` builder (17.3 KB) into `/legendi`'s browser bundle
- `src/components/home/PhotoFrame.tsx` — **server-only since 3.09**: resolves the Sanity asset via `framedImage()` and delegates the markup to `PhotoFrameView`; its public API is unchanged. Photo frame where **`fit` now selects the surround as well as the crop** (D-3.05-5): `cover` = a hard-edged block on navy (presentation: hero, moment, card leads), `contain` = the mist mat + hairline that the archival sets need for the mixed-quality rule. Prior note: `next/image` via `urlFor`; greybox + chip when no image; `ratio` optional — omit for fill mode (`h-full`) used by the gallery mosaic (Phase 1.05; fill mode 1.06b, D-1.06b-3) · **2.03:** gained an additive `fit="cover"|"contain"` prop (default `cover`, no existing caller changed) so the archival photo grid can mat whole scans rather than crop them (D-2.02-7)
- `src/components/home/Reveal.tsx` — client reveal-on-scroll wrapper. **3.09:** gained `immediate`, which omits `data-reveal` entirely so the element is never hidden — set on the LCP card of `/arhiva` and `/legendi`, where the reveal was holding the largest-paint element at `opacity: 0` for a measured 370 ms / 331 ms (D-3.09-3). Prior: IntersectionObserver toggles `.is-visible`; 60ms stagger via `delayIndex`. **3.05:** gained `as` (`li`/`tr`/`section`/`figure`) so it can wrap a grid item without breaking a `<ul>`'s content model, and the motion re-timed to 260ms in `globals.css` (Phase 1.05; 3.05)
- `src/components/home/SectionOverline.tsx` — the `u-label` section label: a solid orange **bar** + tracked caps, with the text following its surface (navy on light, paper on navy). **Rewritten at 3.05** — the accent is now always the bar and never a letterform, so all three variants share one shape; before, `onNavy` dropped the rule and turned the text orange. Prior note: `onPaper` = orange rule + navy text; `onNavy` = orange text on SOLID navy (4.6:1); **`onPhoto` (added 3.03, D-3.03-5)** = orange rule marker + PAPER text, for overlays on a photo+gradient (hero, moment band) where orange text fails AA (Phase 1.05; onPhoto 3.03)
- `src/components/home/ClubRecords.tsx` — the „Клубот во бројки" scoreboard strip (homepage only — `/statistika` renders its 30 through `ClubRecordList`). **3.05b:** selection is now an **explicit six-label whitelist** (`HOMEPAGE_RECORD_LABELS`), so a 31st `clubRecord` in Studio cannot change the front page; the array _is_ the render order and `category`/`order` are no longer consulted here — `CATEGORY_PRIORITY`/`sortRecords` deleted, superseding D-3.03-3 for this surface (D-3.05b-1). „Шампион на Македонија" is pinned as the featured orange cell and is never back-filled from another record; a `lastCellSpan` helper widens the final cell so the strip's last row ends flush instead of showing an empty orange track (D-3.05b-2). Prior note (3.03): full-width championship feature, orange ground with 2px gaps as rules, paper text, link to `/statistika` (Phase 3.03; 3.05b)
- `src/components/home/DecadeExplore.tsx` — **new 3.03**: the „Разгледај по децении" grid — one tile per decade with a real `seasonCountLabel` count, deep-linking to `/arhiva#d{decade}` (D-3.03-6). Replaces the removed `DecadeTimeline` (Phase 3.03)
- `src/lib/nav.ts` — single source for nav items + `isActivePath()` (Phase 1.03). **7 items since 3.16** — „Разно“ sits between „Статистика“ and „За нас“. ⚠️ The seventh item widens a pre-existing header wrap: see D-3.16-11 / OV-40
- `src/lib/site.ts` — **new at 3.23 (B1, closing PL-4)**: the single `SITE_URL`, `process.env.NEXT_PUBLIC_SITE_URL` with the Vercel origin as the fallback, trailing slash stripped. Imported by `src/app/layout.tsx` (`metadataBase`), `src/app/robots.ts`, `src/app/sitemap.ts` and `src/components/archive/Breadcrumb.tsx` (absolute `BreadcrumbList` URLs) — so `grep -rn "belasica-v2.vercel.app" src/` returns **this file only**. The default is deliberately the Vercel origin: canonical tags and a sitemap advertising a host DNS does not resolve are worse than none. ⚠️ Its doc comment names **`public/llms.txt` as a fourth place** that hardcodes the origin and cannot import this constant.
- `src/lib/utils.ts` — shadcn `cn()` class-merge helper; **3.04d:** built via `extendTailwindMerge` with the eight custom `@theme` font-size tokens + `tracking-overline` registered, so size and colour merge correctly (D-3.04d-1; a new `@theme` type token must also be added here)

## Sanity (`src/sanity/`) — created Phase 1.04; model **LOCKED** at 2.01, **re-opened additively at 3.01** (re-locks after 3.06)

- `src/sanity/env.ts` — reads `NEXT_PUBLIC_SANITY_PROJECT_ID` / `_DATASET`, pinned `apiVersion`, `useCdn: true`
- `src/sanity/client.ts` — read-only client (published perspective, no token)
- `src/sanity/fetch.ts` — **new at 3.02F-Code**: `fetchOrThrow(query, params, label)`, the bounded-retry read used by the two fan-out templates (season, person). 5 attempts, exponential backoff 1/2/4/8 s, every retry `console.warn`ed, then a **throw naming the slug** with the underlying error unwrapped from `cause` (D-3.02F-C-1 / -2). A query matching nothing still returns `null` and still 404s — only a thrown read is retried. ⚠️ The other reads (`/`, `/arhiva`, `/legendi`, `/statistika`, `sitemap.ts`) still use `client.fetch` directly and are **not** covered
- `src/sanity/image.ts` — `@sanity/image-url` builder (`urlFor`) for `next/image`
- `src/sanity/frame.ts` — **new at 3.09 (D-3.09-1)**: `framedImage(image, width) → { src, objectPosition } | null`, plus the hotspot→`object-position` resolution moved out of `PhotoFrame`. **The single seam that keeps `@sanity/image-url` out of the browser** — call it on the server and pass the result as props, the pattern D-3.05b-3 set for the lightbox
- `src/sanity/structure.ts` — Studio desk structure; `siteSettings` pinned as a singleton (D-1.04-5). season/person/photo are auto-listed via `documentTypeListItems()` (so retiring `match` from `index.ts` drops it with no change here); **`clubRecord` is listed explicitly so its desk entry reads the plural „Клупски рекорди"** (schema title is the singular „Клупски рекорд") — 3.01, D-3.01-5
- `src/sanity/lib/isUniqueSlug.ts` — `isUniqueSlugPerType`: slug-uniqueness check scoped to the document type, wired into `season.slug` / `person.slug` via `options.isUnique`; excludes the doc's own draft/published pair (Phase 2.01, D-2.01-6)
- `src/sanity/schemaTypes/index.ts` — the schema `types` array: `siteSettings`, `season`, `person`, `photo`, **`clubRecord`** (added 3.01). `match` is deliberately **not** registered (D-2.01-2)
- `src/sanity/schemaTypes/siteSettings.ts` — singleton: site title, description, footer archive statement
- `src/sanity/schemaTypes/season.ts` — season: title, slug (**unique**), decade (**required**), story (PT), **then five 3.01 optional fields (D-3.01-1..7): `teamPhoto` + `tablePhoto` (`reference → photo`, picker filtered to the same season via `options.filter`), `trainer` (string), `lineupAndStats` + `results` (PT, same `block` config as `story`)**, then legacy `finalTable`/`squad`/`trainers` (unchanged shape; descriptions now note „Задржано за компатибилност…", D-3.01-2). **No `photos`** — photos attach via `photo.relatedSeason` and are read by GROQ back-reference (D-2.01-1)
- `src/sanity/schemaTypes/clubRecord.ts` — **new at 3.01 (D-3.01-5)**: curated Statistics records. `label` (**req**), `value` (**req**), `category` (string, radio list: scorers/appearances/honours/other), `order` (number), preview (label/value). Empty until 3.02+; person `careerStats` still backs the ranking tables
- `src/sanity/schemaTypes/match.ts` — **DEFERRED / unregistered** (D-2.01-2): kept in-repo with a deferral header, absent from `index.ts`, so Studio does not list „Натпревар". No match-level Drive source exists (P0.1); stats come from season aggregates. Re-add via a future phase if that changes
- `src/sanity/schemaTypes/person.ts` — person: name, slug (**unique**), roles, playing years, `legendRank`, `legendAppearances`, bio (PT), `careerStats` (**authoritative** career total — D-2.01-3). **No `photos`** — the portrait link is single-direction via `photo.relatedPerson` (D-2.01-1). **3.15 adds `legendAppearances`** — the book's printed appearance count, **a string, not a number**, because nine of the eighty ranked players are printed as a **range** („120–135") that no number field can hold (D-3.15-4); it sits directly beside `legendRank` so Ace finds the pair together. **The schema is DEPLOYED as of 3.15** (`--workspace belasica-v2`, D-2.01-8), which closes **OV-27**: `legendRank` had existed in the data since 3.12 but was invisible in Studio.
- `src/sanity/schemaTypes/photo.ts` — photo: image (hotspot, required), caption, provenance (**required** — the rights paper-trail; carries the P0.1/P0.2 rights-gate comment), date, `relatedSeason` / `relatedPerson` — **the single, canonical direction** of both relationships (D-2.01-1)

## Archive templates (Phase 2.03)

- `src/app/(site)/arhiva/page.tsx` — **Archive index** `/arhiva`: decade-grouped season cards from live Sanity content, newest decade first; page header + real „N сезони · M децении" counts, sticky decade jump-nav, 3/2/1-col card grid. ISR `revalidate = 60`. Zero seasons → one placeholder chip (handover §5.6) **3.04b (D-3.04b-2): the card lead is now the curated `season.teamPhoto`** — it previously took `[0]` of the season photos by a `date` key that 886 of 889 photos lack, so 69 of 96 cards showed an arbitrary image; the old back-reference remains only as the fallback for the 13 seasons without one, on the deterministic 2.08 key.
- `src/app/(site)/arhiva/[slug]/page.tsx` — **Season detail** `/arhiva/<slug>`, **rebuilt at 3.04**: breadcrumb → navy title band with the matted `teamPhoto` mounted inside it (D-3.04-4) → in-page anchor rail → **Табела** (`tablePhoto` as an image, D-3.01-2) → **Статистика на играчи** (`trainer` in its own heading + `lineupAndStats`; „Тренер и статистика" → „Играчи и тренер" at 3.13, → „Статистика на играчи" at 3.14) → **Резултати и стрелци** (`results`; renamed at 3.14) → **Преглед** (`story`; renamed from „Приказна за сезоната" at 3.13) → **Фотографии** → prev/next season + back-links. One GROQ round trip: dereferences `teamPhoto`/`tablePhoto` **with the asset's intrinsic dimensions**, fetches the gallery by back-reference **excluding both lead photos by `_id`** (D-3.04-2), and resolves both neighbours by `decade asc, slug.current asc` (D-3.04-1). The legacy `finalTable`/`squad`/`trainers` are no longer read (D-3.04-6). `generateStaticParams` over every published season slug; unknown slug → `notFound()`. Every data section omits itself when empty (D-2.02-3 / D-3.01-6); no lead photo **and** no section → the archive notice (D-2.02-8). ISR `revalidate = 60` **3.02F-Code:** every read on this route (`generateStaticParams`, `generateMetadata`, page body) now goes through `fetchOrThrow` — the old `catch → notFound()` is gone, because a transient CDN timeout used to emit a **silent 404 for a season that exists** (D-3.02F-C-1). **3.05b:** the `gallery` projection also returns each asset's intrinsic `width`/`height`, and „Фотографии" passes `lightbox` to `PhotoGrid` — the only call site that does (D-3.05b-3). **3.13: two sections were renamed** — `story` to „Преглед"/**„Преглед"** and `staff` to „Играчи"/**„Играчи и тренер"**, rail label and `<h2>` together (D-3.13-1). **3.14 renamed two `<h2>`s again, labels untouched this time** — `staff.heading` to **„Статистика на играчи"** (D-3.14-2, superseding that half of D-3.13-1) and `results.heading` to **„Резултати и стрелци"** (D-3.14-3). The section **`id`s are deliberately unchanged** (`prikazna`, `trener`) so links already shared still resolve, and no longer match their labels (D-3.13-3); the rendered `<section id>` set is byte-identical to `main` across all 96 pages.
- `src/components/archive/Breadcrumb.tsx` — `Почетна / Архива / <season>`; navy links, mist `/` separators, current crumb `aria-current`; unknown crumb → placeholder chip (D-2.02-5)
- `src/components/archive/SectionHeading.tsx` — orange rule marker + serif H2; the archive templates' section label (D-2.02-3). Replaces `SectionOverline` on these pages only — the homepage's is untouched
- `src/components/archive/DecadeSectionHeader.tsx` — serif decade + real „N сезони" count (neutral-500) + orange rule → mist hairline
- `src/components/archive/SeasonCard.tsx` — 3:2 lead photo (or Mist greybox + chip), **neutral-500** decade overline (D-2.02-1), serif navy title; whole card is one `<a>`; 2px lift on hover. Wraps its own `Reveal` **inside** the `<li>`
- `src/components/archive/DecadeJumpNav.tsx` — **since 3.17 a thin wrapper over `src/components/JumpNav.tsx`**: it maps `decades` → `{id: decadeAnchor(d), label: decadeLabel(d)}` and names the rail „Скок по деценија“. The markup, the classes and the sticky offset now live in `JumpNav`; `/arhiva`'s **rendered DOM is byte-identical to a `main` baseline build** (291.164 bytes, proved against an isolation build with `BackToTop` unmounted) and its CSS bundle hash is unchanged (D-3.17-7). Prior note: only decades with ≥1 published season; mobile scrolls the rail, never the page body; offset by `top-header` (D-2.02-17). Scroll-spy deliberately not built (optional per handover §5.3)
- `src/components/archive/MattedPhoto.tsx` — **new at 3.04 (D-3.04-3)**: a solo archival scan matted at **its own** aspect ratio — mist ground, hairline border, real padding — sized from the asset's intrinsic `metadata.dimensions`. Never wider than the asset's native pixel width (a low-res scan is matted, never upscaled); optional `maxHeightRem` cap (40rem on the lead photo, uncapped on the standings image so it stays readable); `tone` switches the caption to paper on navy. Falls back to `PhotoFrame ratio="3/2" fit="contain"` when an asset has no dimension metadata. Used for `teamPhoto` + `tablePhoto` only — the „Фотографии" grid still uses fixed frames so a mixed set lines up
- `src/components/archive/SeasonAnchorNav.tsx` — **new at 3.04 (D-3.04-5)**: in-page rail („Табела · Тренер · Резултати · Приказна · Фотографии"), listing **only sections that rendered** and suppressing itself below two entries. **Deliberately not sticky**, unlike `DecadeJumpNav`, so a single `scroll-mt-header` on each section is the exact offset (verified: an anchor jump lands the section top at 77px, the header's bottom edge). Mobile scrolls the rail, never the page body
- `src/components/archive/SeasonResultsTable.tsx` — **new at 3.28**. „Резултати" as one table per competition or stage: navy header band, a 6px orange bar above each group, hairline row separators, the score as the strongest cell. Columns Коло · Натпревар · Резултат · Стрелци, where **„Коло" renders only for a group that holds a round** — 12 of 2.267 matches do, so it is absent on 94 of 96 season pages (D-3.28-4). **Below `sm` each `<tr>` becomes a grid** and the scorers drop to their own line; one `<table>`, one DOM, and **zero horizontal scroll at 375 px** (D-3.28-6). A match with no scorers renders an empty cell — no dash, no zero — and the cell is still present from `sm` up so every row matches the header's column count. Server component
- `src/components/archive/SeasonSquadTable.tsx` — **new at 3.28**. „Состав и статистика" as № · Име · Настапи · Голови, navy header band. **Imports `RosterDivider` from `SeasonRecordList`** rather than redrawing the after-eleven rule, and applies 3.17's two conditions unchanged (a row numbered 11, and a row numbered 12 after it); the rule spans the table in its own `aria-hidden` row, since it holds no data and states nothing (D-3.17-4). Names link to `/legendi/<slug>` **only on an exact whole-string match** — 1 of 1.982 rendered rows (D-3.28-7) — via a plain inline link, not the legacy `PersonChip` (D-3.28-8). Server component
- `src/components/archive/SeasonRecordList.tsx` — **new at 3.04 (D-3.04-9)**: Portable Text for the two **record** fields. `variant="roster"` (`lineupAndStats`) = a tight line list; `variant="results"` (`results`) = one hairline-separated row per match. Both `tabular-nums`; nothing is reformatted (source OCR quirks ship verbatim, D-3.02S-3). `SeasonStory` is untouched and still renders prose. **3.17: the `roster` variant draws one hairline after the line numbered 11** — `rosterDividerKey` walks the blocks, finds the first `normal` block matching `/^\s*(\d{1,2})\s*[.)]?\s*(?=\D)/` with a captured `11`, and returns its `_key` only if a later block captures `12`; the `normal` renderer emits `<div aria-hidden className="mt-4 mb-1 h-px bg-mist" />` after that paragraph. **No text, no `<hr>`, hidden from the accessibility tree** — the book prints a numbered list, not the words „стартна единаесторка“ (D-3.17-4). Live counts: **51 of 96 seasons render one divider, 45 render none, none renders two**; the `results` variant keeps the module-scope config built with a `null` key and is provably unaffected (D-3.17-9). **3.28: `RosterDivider` is now exported** (and takes an optional `className`) so `SeasonSquadTable` reuses the same rule instead of drawing a second one
- `src/components/archive/SeasonRecordBoard.tsx` — **new at 3.06a**: the season page's league-record strip — a `grid-cols-2 sm:grid-cols-4` scoreboard on the orange ground with 2px gaps as rules (`border-y-[3px] border-orange`), rendering that season's record as label/value cells. Same scoreboard vocabulary as the homepage's `ClubRecords`, scoped to one season. ⚠️ Phase 3.06a filed **no completion report and logged no decisions** (`D-3.06*` count is 0), so this entry is reconstructed from the diff and commits `f8de548`/`b637b5e` — the _why_ behind it is not recorded anywhere (D-3.07-10)
- `src/components/archive/SeasonNeighbourNav.tsx` — **new at 3.04 (D-3.04-1)**: prev/next season cards (overline label + serif title, arrow, 2px lift, no shadow). A missing neighbour renders **nothing** rather than a disabled control; „Следна" keeps the right-hand column even when „Претходна" is absent. Titles fall back to the slug — never to an invented „Сезона …"
- `src/components/archive/StandingsTable.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** „Конечна табела": navy `<thead>`, `<th scope="col">` with abbreviation + `sr-only` full label (D-2.02-14), zebra body, ФК Беласица row = `bg-highlight` + inset 2px orange left marker + navy rank (D-2.02-4), unknown cells „—". Mobile keeps **all nine columns** and scrolls inside the frame with `#`+`Клуб` sticky-left (D-2.02-10). Kept because `season.finalTable` still holds a real row (`1982-83`) that `/statistika` aggregates; **delete together with the legacy fields when the model re-locks after 3.06**
- `src/components/archive/SquadTable.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** „Состав": light table in `max-w-measure` (D-2.02-11); names link to `/legendi/<slug>`; unresolved player → chip, unknown numbers „—". Superseded on the page by `lineupAndStats` + `SeasonRecordList`
- `src/components/archive/PersonChip.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** Trainer chip → `/legendi/<slug>`; orange dot marker, navy label. Superseded on the page by the `season.trainer` string in the staff section — a section renamed „Играчи и тренер" at 3.13, so this file's own comment still names the old heading (left untouched on purpose, D-3.13-8)
- `src/components/archive/SeasonStory.tsx` — Portable Text renderer for `season.story` (and `person.bio`) via `@portabletext/react` (D-2.03-2); blockquote uses orange as a left rule only. **Unchanged at 3.04** — the two record fields got their own renderer instead (D-3.04-9)
- `src/components/archive/PhotoGrid.tsx` — „Фотографии": matted `PhotoFrame fit="contain"` (D-2.02-7); caption below the frame = orange rule marker + neutral-500 date overline + unclamped description (D-2.02-9). `provenance` never rendered. **3.04:** the season page now passes a gallery with `teamPhoto`/`tablePhoto` already excluded in GROQ (D-3.04-2 supersedes D-2.02-6). **3.05b:** gained an **opt-in `lightbox` prop** (default `false`, set only by the season page — the person page is unchanged, D-3.05b-3). Still a **server** component: when the prop is on it builds every overlay URL here with `urlFor(...).width(2400).fit("max")` (`max` so a small scan is never upscaled — brand.md's mixed-quality rule) and passes plain serialisable data into `PhotoLightbox`; a photo whose asset is missing stays inert rather than offering a button onto nothing
- `src/components/archive/PhotoLightbox.tsx` — **new 3.05b**, the repo's **fifth client component**: the season gallery's photo overlay. Exports `PhotoLightboxProvider` (owns the open index, the overlay and the page's scroll lock) and `PhotoLightboxTrigger` (the thumbnail `<button>`, which wraps the server-rendered frame as `children` so the photo never becomes client code). `role="dialog"` + `aria-modal` + `aria-label`, navy/96 backdrop, date/caption/`N / M` counter in an `aria-live="polite"` region below. **Sizing (3.24, D-3.24-1):** the scan is `object-contain` **scaled to the overlay's usable area** rather than shown at its own pixel size — the image is the growing flex item (`sm:flex-1` + `w-full`), so it fills whatever the arrows, the close button's `pt-20` band and the caption leave it (~84 % of viewport height on desktop), upscaling where the file is smaller; below `sm` it is `w-full h-auto max-h-[62vh]`. The 3.19 arithmetic cap (`sm:max-w-[calc(100vw-11rem)]`, `max-h` 62/76vh) is **gone** — the gutter is structural now — and `11rem` survives only inside `sizes`. `sm:flex-nowrap` on the row is load-bearing (D-3.24-1b): a wrapping flex line cannot be capped, and with it left on the figure measured 1 258px tall in an 800px viewport. Three 48×48 orange/navy controls; navigation **wraps**; both arrows absent for a one-photo season. `ArrowLeft`/`ArrowRight`/`Escape`/backdrop-click, focus trapped and returned to the triggering thumbnail. **Contains no Sanity import.** Scroll lock is on `documentElement`, not `<body>` (D-3.05b-5); focus restoration and the entry motion deliberately avoid `requestAnimationFrame` (D-3.05b-6) — the motion is `animate-in fade-in slide-in-from-bottom-1` from the already-pinned `tw-animate-css`
- `src/components/archive/SeasonSectionEmpty.tsx` — **new at 3.04b (D-3.04b-1)**: one section's empty state on a season page — a bordered white card with a single line of structural copy („Во архивата сѐ уште нема…“). Since 3.04b every section renders on every season page, so this is what a section with no content shows instead of vanishing. **Deliberately carries no `PlaceholderChip`** (unlike `StatsEmptyNotice`): always-on sections would emit ~170 chips across the 96 pages and swamp the placeholder register, which is a launch blocker
- `src/components/archive/SeasonEmptyNotice.tsx` — the fully-empty season notice (D-2.02-8); structural copy only. **3.04:** chips re-worded to the redesigned page's six sections (D-3.04-7), and the state is now a **guard** — after the 3.02 fill no published season is fully empty
- `src/lib/archive.ts` — archive display helpers: `decadeLabel`, `decadeAnchor`, `seasonCountLabel`/`decadeCountLabel` (pluralisation, D-2.02-12), `isBelasicaRow`, `statCell` (`—` for null, `0` preserved)
- `src/lib/focus.ts` — shared `focusOnPaper` / `focusOnNavy` focus-ring strings. **3.03:** the redesigned homepage now imports these (the previously-inlined copies were folded in); `DecadeTimeline`'s copy went away with the component
- `src/app/(site)/statistika/page.tsx` — **Statistics** `/statistika` (Phase 2.04, **records section added 3.02F-Code**): breadcrumb → **a sticky `JumpNav`** („Скок низ статистиката“) → **Најдобри стрелци** → Најмногу настапи → Севкупен биланс на клубот → **Клупски рекорди**. One GROQ round trip, now **four** reads: the 30 `clubRecord` docs lead the page (D-3.02F-C-4/-5); scorers + appearances rank **only** `person.careerStats` (D-2.01-3 — `season.squad` is never summed into a career total); seasons with `count(finalTable) > 0` feed the balance. The three original queries are **unchanged in shape and ordering**. The three table sections keep their heading and show an empty notice when empty (D-2.04-3); the records section instead **self-omits entirely** when no group survives (D-2.02-3). **3.17 reordered the four sections** at the owner's instruction — the page now opens with the table readers come for and the curated records close it (D-3.17-3) — so the hairline logic inverted: `#strelci` leads and takes **no** `border-t` (its top edge is already the navy rail's colour change), while `#nastapi`, `#bilans` and `#rekordi` each carry one, the last now unconditionally. The `cn()` call that guarded the old conditional rule is gone, and with it the file's only `cn` import. Each `<section>` carries an `id` and `scroll-mt-[calc(var(--spacing-header)+3.25rem)]`; the rail's Клупски рекорди item is added only when that section renders. `STATS_QUERY`, `SCORER_MIN_GOALS`, every column, caption and empty notice are byte-identical. ISR `revalidate = 60` **3.12: „Најдобри стрелци“ stops at `SCORER_MIN_GOALS = 21`** — 28 rows, ending on Ристо Панов, at the owner’s instruction; a line under the heading states the threshold, and the empty notice was rewritten because the section can now be empty for a second reason (D-3.12-5)
- `src/components/stats/StatTable.tsx` — **client** sortable table: `<button>` headers with `aria-sort` on the active column, unknown values sort last in both directions (D-2.04-5), `—` for null / `0` preserved via `statCell`. Styled as the archive Stats table (navy `<thead>`, zebra body, abbreviation + `sr-only` full label, D-2.02-14); mobile keeps every column and scrolls inside the frame with the first column sticky-left (D-2.02-10). Plain React state — no table library
- `src/components/stats/BalanceSummary.tsx` — the all-time balance band: сезони · одиграни · победи · нерешени · порази · ДГ · ПГ · гол-разлика · бодови · победи % (D-2.04-1). Unrecorded columns render „—", never `0`; GD/win % computed here only, never as a table column (D-2.04-4)
- `src/components/stats/StatsEmptyNotice.tsx` — a Statistics section's empty state: one structural sentence + a placeholder chip (D-2.04-3). Never a zero row, never an empty table shell. ⚠️ **Renders on no section today** — all three now have real content
- `src/components/stats/ClubRecordList.tsx` — **new at 3.02F-Code**: „Клупски рекорди" as a label/value **register**, not the brand scoreboard (D-3.02F-C-4 — the longest `value` is a 481-character 17-name roster, which `u-stat` cannot carry). One white `u-cap` block per category (h3 + `<dl>`), rows divided by `mist` hairlines; label = the scoreboard's tracked-caps `dt` vocabulary in navy, value = `text-body` ink capped at `max-w-measure`; two columns `minmax(0,15rem)/minmax(0,1fr)` from `md`, stacked below. No new `brand.md` token
- `src/app/(site)/legendi/page.tsx` — **Legends index** `/legendi` (Phase 2.06): breadcrumb → H1 + intro → three fixed-order role bands (Играчи → Тренери → Претседатели). **Since 3.10 it renders `PageHeader` only on the empty-roster branch** — the populated path returns `<LegendsBrowser bands={bands} />`, which owns the header so the search field can live inside it (D-3.10-2); the three header strings are therefore written in both files, a contained duplication on a defensive path (D-3.10-3). Reads every published person + portrait by `photo.relatedPerson` back-reference. Placement happens here, not in the band: each person lands in exactly one band by highest-priority role (player > trainer > president, D-2.05-2) and shows **all** their role chips; a person with no recognised role is placed in no band. Name-sorted with `localeCompare("mk")` (GROQ cannot collate Cyrillic). Empty band → fully omitted; zero people → heading + one chip. ISR `revalidate = 60` **3.12: Играчи is no longer alphabetical** — it sorts on `compareByLegendRank` (book rank → recorded appearances → name), at the owner’s instruction (D-3.12-2). **3.13: Тренери and Претседатели stop being alphabetical too** — both now sort on `compareByRecency`, newest-first by most recent service, from a year derived at build time (trainers from the latest `season.trainer` naming them, officials from the term in their biography) with **no new field and no Sanity write** (D-3.13-4). The year is never rendered. The query now reads `legendRank`, `careerStats.appearances` and `bioLead`, and also returns the season/trainer list in the **same round trip** (D-3.13-5); the bands are handed over as the five fields `LegendBand` declares, projected by name so no sort input reaches the client bundle (D-3.13-6). **Играчи is unchanged and byte-identical to `main`.** **3.22 rewrites placement:** a person is now in **every** category they qualify for, not just the highest-priority one (D-2.05-2 withdrawn, D-3.22-2), and the page builds **four** categories — Играчи / Тренери / Претседатели / Репрезентативци — from `CATEGORY_ORDER`. Both sort years (`trainerYear`, `presidentYear`) are derived for every person holding the matching role, not just for the one band they used to be filed under. The header count is the **distinct** slug set, never the sum of the categories. Passes `total` to `LegendsBrowser`.
- `src/app/(site)/legendi/[slug]/page.tsx` — **Person detail** `/legendi/<slug>` (Phase 2.06): breadcrumb → `PersonHero` → Биографија → Кариера → Сезони → Фотографии → back-link. `generateStaticParams` over **every** published person, so trainer/official slugs never 404; unknown slug → `notFound()`. Every section self-omits when empty. `careerStats` read directly, never summed from `season.squad` (D-2.01-3); a missing metric omits its tile, never renders `0`, both missing → section omits. Сезони matches `squad[]` **or** `trainers[]` (D-2.06-3). ISR `revalidate = 60` **3.02F-Code:** all three reads now go through `fetchOrThrow`; the old `catch → notFound()` is gone (D-3.02F-C-1). `generateStaticParams` covers all **160** published people — every slug verified 200 against the built output.
- `src/app/(site)/pravni-informacii/page.tsx` — **Legal information** `/pravni-informacii` (**new at Phase 3.07**): the archive's legal statement — unofficial-archive declaration, purpose, the AI-assisted-build disclosure, data-accuracy caveat, the photo/copyright takedown-on-request posture (D-3.07-3), club marks, liability limit, external links, amendments, contact. Navy `PageHeader` (H1 + „Последно ажурирање" meta) then one `max-w-measure` column of ten `<section>`s. **Static server component — no Sanity read, no client component and deliberately NO `revalidate`** (D-3.07-8), so it builds as `○ (Static)` with an empty Revalidate column. The copy is held verbatim in a module-scope `SECTIONS` array rendered through one `LegalBlock` (D-3.07-7), which is what makes the character-for-character diff against the brief mechanical (41/41 lines identical). §10's address is a `mailto:` from `CONTACT_EMAIL` in a lowercase variant of the `u-link` anatomy (D-3.07-4); §1's domain is plain text. Indexable, and reachable **only** from the footer's bottom bar — absent from `src/lib/nav.ts` and `SiteHeader` by design (D-3.07-2). Zero placeholder chips in `<main>`
- `src/app/(site)/za-nas/page.tsx` — **About** `/za-nas`. **No longer provisional as of 3.15** (D-3.15-7 retires D-2.05-1): the banner and all four placeholder chips are gone, and the page renders the owner-supplied copy **verbatim** from `ABOUT_AUTHOR_PARAGRAPH` / `ABOUT_DEDICATION_PARAGRAPH` — H1 (navy `PageHeader`) → two paragraphs → the OV-3 unofficial-archive block, which now sits **after** the copy so the author's words open the page. Reads Sanity for the first time: one small query for a published person named „Томе Стојанов", and his name becomes a link to `/legendi/<slug>` **only if exactly one exists** (count + slug, never `[0]`, so a future duplicate degrades to plain text rather than pointing at an arbitrary document). The paragraph is split on the name so the rendered text stays byte-identical to the constant whether or not the link resolves. `fetchOrThrow` in a try/catch: a CDN wobble degrades the link, never the page. `ProvisionalBanner` was a local function here, not a shared component — it went with this edit and no file remains.
- `src/components/legends/LegendCard.tsx` — 4:5 portrait (`PhotoFrame fit="cover"`) **or** a solid navy initials tile when photo-less (D-2.06-7). One real `<a>` wrapping the whole card. **3.15 rewrites the title line to `1. Петар Андреев   555`:** rank as a list index at the name's size in the quiet ink, name on `flex-1`, appearance count pushed to the end of the line in `tabular-nums` at `text-small`; baseline-aligned, so a name that wraps to two lines keeps the count level with the first. Both numbers carry an `sr-only` qualifier („Ранг ", „ настапи") so the card never announces a bare digit. Shows the count **only** when both `legendRank` and `legendAppearances` are present — a ranked player with no printed figure gets rank + name and nothing else: no chip, no zero, no dash (D-3.15-5). Anyone unranked (Тренери, Претседатели, unranked players) renders exactly as before. **A missing `playingYears` now self-omits the meta line** instead of emitting a placeholder chip (D-3.15-6) — the last chip that could reach a production page. **3.19 decoupled the count from the rank** (`legendAppearances` else `careerStats.appearances`, `!= null` so a real `0` survives), superseding D-3.15-5's both-or-nothing rule. **3.27 makes the card ROLE-SCOPED:** it takes a **`category` prop** and renders only that tab's facts — rank + `playingYears` + count under Играчи, `trainerYears` under Тренери, `officialYears` under Претседатели, `nationalStats` under Репрезентативци as „Цела кариера: … настапи, … голови"; never the Belasica figures on that last one (D-3.27-2/-4). Defaults to `"player"` so the homepage marquee is unchanged (D-3.27-12). **Role chips stay on every card in every tab.** `LegendCardData`'s fields below `role` are optional — the server projects per category, so a coaching card has no rank on it at all.
- `src/components/legends/CategoryGrid.tsx` — **renamed from `RoleBandGrid.tsx` at 3.23 (D-3.22-7's open follow-up, closed)**; one category panel: serif H2 + real count + orange rule → mist hairline (the `DecadeSectionHeader` pattern) over a 3/2/1 grid of `LegendCard`. **Renders nothing when empty** — so „0 играчи" can never appear. Takes `role` (not the handover's `title`) so title/priority/count-noun all read from `@/lib/people`. **3.17: + a required `anchorId`** — it goes on the `<section>` together with `scroll-mt-[calc(var(--spacing-header)+3.25rem)]`, so the page's role rail can land on it; `headingId` is unchanged and stays on the `<h2>` (D-3.17-5) **3.22: takes a `LegendCategory`, not a `PersonRole`** — a superset, since „Репрезентативци“ corresponds to no schema value. Markup unchanged; title and count noun now read from `CATEGORY_TITLE` / `categoryCountLabel`. `anchorId` still lands on the `<section>`, which now sits _inside_ the tab panel. **3.23: renamed to `CategoryGrid.tsx`, export renamed to `CategoryGrid`, all imports updated and the doc comment rewritten to describe what it does now.** Proven inert: `/legendi`'s rendered DOM is byte-identical to the `main` baseline apart from React `useId` values (identical length, ids internally consistent), which shift only because the `(site)` segment gained an `error.tsx` boundary.
- `src/components/legends/LegendsBrowser.tsx` — **client**; **since 3.10 it renders the whole of `/legendi` below the site header** — the navy `PageHeader` (breadcrumb → H1 → intro → `personCountLabel` meta → the search field as `children`) _and_ the bands under it, so the input and the filtered list share state in one component and no empty band sits between them (D-3.10-2). Receives the bands already built + name-sorted on the server and filters them in the browser (whole roster is one read, so no request per keystroke; the page still lists everyone with JS off — `/legendi` is still statically prerendered). Matching moved to `matchesName` from `@/lib/translit`, so **Latin spellings find Cyrillic names**; the local `normalise` now only answers „is the user searching at all". Field chrome is on the navy scale — `text-paper` label, `text-paper/80` count and clear button, `focusOnNavy` ring, white input surface kept. A band with no match self-omits via `CategoryGrid`; zero matches overall renders one notice, on paper, above the bands. **3.17: it renders the role rail** — a `JumpNav` („Скок по улога“) between `</PageHeader>` and the `<Container>`, built from `visible` so it can only ever point at a band that is actually on the page, with labels read from `BAND_TITLE` and ids from `BAND_ANCHOR`. A search that narrows the page to one band leaves one item, so the rail declines to render and the content below shifts up **46px** — measured, and always below the reader's focus point, since the search field sits above the rail (D-3.17-6). `aria-live` result count + clear button **3.22 replaces the jump rail with a TAB RAIL** (D-3.22-1): four `role="tab"` buttons over four `role="tabpanel"` divs, one visible at a time, the other three `hidden` **but still in the HTML** so the page ships whole to a crawler. Takes a new `total` prop — the distinct-people count, which cannot be summed from `bands` now that a player-coach is in two of them. **The active tab is derived, never stored:** state holds only the tab the user _asked for_; the tab shown is that one if it still has matches, else the first that does — so typing „пинда“ moves the reader to Претседатели on its own. Tab labels carry a `(n)` count while searching and a category with no matches is `disabled` rather than removed. Roving tabindex (one Tab stop for the rail) + ←/→ with wrap. Reuses `JumpNav`'s exact surface — no new token. **Post-3.23: pressing a tab scrolls back up to it** — the panel swap alone left the reader's scroll where it was, so someone 100 cards down Играчи who pressed Тренери arrived 100 cards down Тренери. A press (click _or_ ←/→) now lands the opened category's 6px orange bar directly under the sticky rail, the same place the old jump links landed — measured 130px, rail bottom 125px. **Up only** (`window.scrollY > top` or nothing happens), so a reader still at the page header is not pushed past the header and the search field. Keyed on a `{category, nth}` press value, not on `active`: `active` also moves on its own when a search empties the open category, and a typist must not be thrown up the page mid-word; the `nth` counter makes re-pressing the _same_ tab count as a second press. The offset is read off the section's own computed `scroll-mt-[calc(var(--spacing-header)+3.25rem)]`, never restated, and `behavior` reads `prefers-reduced-motion` at press time (`BackToTop`'s rule). ⚠️ **This rail is a second copy of `JumpNav`'s surface, not an import of it** (D-3.35-2); it took the same `rail-fade` class at 3.35, and any future change to either rail must be made twice.
- `src/lib/translit.ts` — **new at 3.10 (D-3.10-7)**: Latin-keyboard matching for the Cyrillic roster. A per-letter table of accepted Latin spellings (each letter also accepts itself, so Cyrillic queries are untouched) plus `matchesName(name, query)`, a backtracking substring matcher that consumes whichever spelling the query used — longest variant first. Variant sets **overlap on purpose** (`c` → ц/ч/ќ, `z` → з/ж/ѕ): over-matching is the correct failure direction for a 160-name filter. Characters outside the table (space, hyphen, en dash, parentheses, full stop — all present in roster names) match only themselves. Deliberately **not** in `people.ts`: that file is display helpers, this is input handling
- `src/components/legends/PersonHero.tsx` — person hero: portrait-left (300px) / text-right at desktop, stacked at mobile. Photo-less → a **navy band** carrying a monogram plate (2.02 handover §6.2b, as the season hero does). Name is the page's single `<h1>`
- `src/components/legends/RoleChips.tsx` — a person's roles as chips, shared by `LegendCard` + `PersonHero`. Priority-ordered: `[0]` (the role that placed them) is the filled navy chip, the rest muted outlines; `onNavy` inverts both for the hero band. Role name always spelled out — never colour-only
- `src/lib/people.ts` — people helpers: `ROLE_LABEL` / `BAND_TITLE` / `ROLE_PRIORITY` (band order **and** placement priority in one list), `orderedRoles` (drops unrecognised values), `primaryRole`, `initials`, `compareByName` (`localeCompare("mk")`), `BAND_ANCHOR` (**added 3.17** — the `<section id>` each band renders on `/legendi` and therefore the target of its rail link: `igraci` · `treneri` · `pretsedateli`, Latin slugs like every other anchor on the site, kept beside `BAND_TITLE` so a band's title, count noun and anchor read from one source — D-3.17-5), `bandCountLabel` (per-band pluralisation, the D-2.02-12 rule) and — **added 3.10** — `personCountLabel` (the `/legendi` header's archive total; neutral „личност/личности" because it counts across all three bands, same singular rule). NB: the homepage's previously-inlined `ROLE_LABEL` went away at 3.03 — the legends band now uses `LegendCard`, which reads these helpers **Added 3.12: `compareByLegendRank`** (+ the `RankedPerson` shape) — the Играчи band’s three-tier order: book rank, then recorded appearances, then name (D-3.12-2) **Added 3.13: `seasonStartYear`, `buildTrainerYearIndex`, `tenureEndYear`, `compareByRecency`** (+ the `DatedPerson` shape) — the derived newest-first order for Тренери and Претседатели (D-3.13-4). All four are pure and none of them renders anything: the year is a sort key only. `buildTrainerYearIndex` keys on the **exact** trimmed name and never substring-matches (a loose match would credit „Благој Истатов" with seven other men's seasons), and `compareByRecency` sends anyone with no derivable year to the **end** of the band via `compareByName` **3.22 replaces the `BAND_*` role layer with a category layer** (D-3.22-1): `LegendCategory` (= `PersonRole | "international"`), `CATEGORY_ORDER` / `CATEGORY_TITLE` / `CATEGORY_TAB_LABEL` / `CATEGORY_ANCHOR`, and `categoryCountLabel` replacing `bandCountLabel`. `ROLE_PRIORITY` survives as chip order only — it is no longer a placement priority. New `tenureSortYear` reads a president-only term out of `playingYears`, returning `POSITIVE_INFINITY` for an open-ended „2015–“ (D-3.22-4). `buildTrainerYearIndex` now returns an **order-aware sort key** (`year + position/(n+1)`), not a year — position within a season's coach string is real information (D-3.22-5).
- `src/lib/facts.ts` — VERIFIED copy from `facts.md` as constants: the OV-3 `UNOFFICIAL_ARCHIVE_LABEL` + `UNOFFICIAL_ARCHIVE_STATEMENT` and `CONTACT_EMAIL`. **3.15 adds:** `CONTENT_PERMISSION_NOTICE` (the footer's copy-posture line — asks **permission**, does **not** claim ownership, D-3.15-1); `FACEBOOK_URL`, `INSTAGRAM_URL` and `SOCIAL_LINKS` (**replacing** `FACEBOOK_FAN_PAGE`/`FACEBOOK_FAN_PAGE_LABEL`, whose „фан-страница" framing is retired at D-3.15-2 — the Instagram tracking query string is stripped, D-3.15-3); and the „За нас" body, `ABOUT_AUTHOR_PARAGRAPH` / `ABOUT_DEDICATION_PARAGRAPH` / `ABOUT_FATHER_NAME`. ⚠️ Every string here is transcribed byte-for-byte and carries a comment saying why it must not be „improved" — the About copy especially: it names the book (superseding a standing rule, D-3.15-8) and mixes „веб сајт"/„веб страна" because the author did.
- `src/lib/stats.ts` — `aggregateClubBalance` (sums **only** `isBelasicaRow` rows across seasons with a `finalTable`; per-metric `contributing` counts make a partial aggregate detectable), `formatGoalDifference`, `formatWinRate`; **+ 3.02F-Code:** `RECORD_GROUPS` (the four Studio category titles verbatim, in page order honours → scorers → appearances → other) and `groupClubRecords` (drops a record missing `label`/`value`, files an unknown/absent category under „Друго" rather than hiding it, sorts `order` asc with unset last then `label` by `mk` collation, drops an empty group). Presentation-free (D-2.04-1 / D-2.04-4 / D-3.02F-C-5)

## „Разно“ — seven topics from the book (Phase 3.16)

- `src/content/razno.ts` — **new at 3.16**; the first file under `src/content/`. The seven topics in the **owner's order** (УЕФА → Младинска школа → Виареџо → Партизан → Тиверија → Стадион → Ајдуци — neither alphabetical nor chronological), each with `slug`, `title` (the H1), `navTitle` (the card/spine title where the H1 is too long for a card column), `summary`, `metaDescription` and `blocks: { kind: "para" | "record" | "closing"; line; text }[]`. **101 blocks, ~27.000 characters, every one byte-identical to its `L####` paragraph in `data/book/razno-source.md`** (D-3.16-3) bar two recorded extraction repairs: the L6934+L6935 join (D-3.16-4) and the markdown bullet stripped from five lines (D-3.16-5). `line` is **never rendered** — it is the audit trail, the same rule `data/book/*.json` follows via `sourceLine`. Also exports `RAZNO_SOURCE_CREDIT` (the attribution every detail page ends with, D-3.16-12), `RAZNO_TITLE` / `RAZNO_INTRO` / `RAZNO_META_DESCRIPTION`, and the `raznoTopic()` / `raznoNeighbours()` lookups. **This array is the single ordering source** — the index grid, the prev/next spine, `generateStaticParams` and the sitemap all read it, and the „7 теми“ count is its `length`. **Copy lives here rather than in Sanity by decision (D-3.16-2): Ace cannot edit these pages in Studio.**
- `src/components/stats/SourceTable.tsx` — **new at 3.34**, the **fixed** (non-sortable) statistics table, a **server component**. Same visual contract as `StatTable` — navy header, `zebra` striping, mist hairlines, `—` for an unknown cell, and a keyboard-reachable `role="region"` scroll frame — with the sorting and the client boundary removed, because Аце's ranking and chronology ARE the content (D-3.34-3). Cells are `ReactNode`, which is what lets a tied season stack two names in one cell. **Never links a name** (D-3.28-7, OV-63)
- `src/components/stats/AceTables.tsx` — **new at 3.34**, the render layer for the three transcribed sections: `SeasonScorersTable` (block A), `RankedSourceTable` (blocks B–E) and `PlainRankedSourceTable` (the two Прва-македонска tables). **Column headers are Аце's own words**, split from his column line, plus a `#`/`sr-only` „Ранг" column for the numerals he prints without a heading. A season he left blank renders as an em dash with an `sr-only` „нема податок" — never a guessed scorer
- `src/content/legendi.ts` — **new 3.22**; owner-supplied data for `/legendi` that has no home in the Sanity model, and the machine-readable form of two `facts.md` entries rather than a second source. `INTERNATIONAL_SLUGS` — the fourth category's membership **and order**, taken from Ace's numbered Drive folder `010. Репрезентативци на Македонија`; **by slug, never by name**, so a rename in Studio cannot drop a man silently, and a slug matching nobody is dropped rather than rendered as a gap (D-3.22-3). `COACH_YEAR_OVERRIDE` — a coach's latest year where the archive holds no season to derive it from; currently one entry, Мартин Алаѓозовски → 2026, which **must be removed the moment a 2026/27 season is published** (D-3.22-6). ⚠️ Ace cannot edit either list in Studio; both change by code edit in step with `facts.md`.
- `src/content/season-tables.ts` — **new at 3.28**, **GENERATED — do not hand-edit.** Written by `scripts/build-season-tables.mjs` from `data/book/matches.json` + `data/book/seasons.json` and, **since 3.36**, `data/book/season-2025-26.json`. **93 seasons with structured matches (2.297 in total), 74 with a structured squad**, keyed by season slug. ⚠️ **One of the 93 is not from the book: `2025-26`** — the book's extract ends with spring 2025 and holds zero matches for it, so its 30 results and 35-player squad come from the published Sanity season document (D-3.36-5). The module header names the split; every other season is the book's. Exports the `SeasonMatch` / `SeasonMatchPart` / `SeasonMatchGroup` / `SeasonSquadRow` / `SeasonTables` types and the `seasonTablesFor(slug)` lookup, which returns `null` for a season the book does not cover — and that `null` is the whole switch between the table and Аце's prose (D-3.28-2). ⚠️ **1,2 MB, and server-only by construction**: the season page is a server component, `/arhiva/[slug]` First Load JS is **123 kB, identical to `/legendi/[slug]` and `/razno/[slug]`**, so none of it reaches the browser. `sourceLine` is deliberately **not** carried across — it would roughly double the file for a string nothing renders; it stays in `data/book/*.json`. ⚠️ **Listed in `.prettierignore`** — the generator owns its formatting (D-3.28-10). ⚠️ **Not editable in Studio** (D-3.28-1)
- `src/content/statistika-extra.ts` — **new at 3.34**, **GENERATED — do not hand-edit.** Written by `scripts/build-statistika-extra.mjs` from `data/book/statistika-source.md`. Аце's club statistics as three page sections: `SEASON_SCORERS` (**76 seasons**, 1950 → 2025/26, with **4 he left blank** and **5 ties**), `YUGOSLAV_TABLES` (**4 ranked tables, 75 rows**, each with his heading, intro, column line and tail/footnote lines) and `MACEDONIAN_LEAGUE` (his narrative plus **25 + 16** ranked rows). Exports the `SeasonScorerRow` / `RankedRow` / `RankedTable` / `PlainRankedTable` / `MacedonianLeague` types, plus `SEASON_SCORERS_INTRO` and `SEASON_SCORERS_COLUMN_LINE` so no wording is retyped at the call site. `line` traces every figure to one paragraph of the source and is **never rendered**. ⚠️ **Listed in `.prettierignore`** — the generator owns its formatting (D-3.34-7). ⚠️ **Not editable in Studio** (D-3.16-2). ⚠️ **Three of his rank numerals are internally inconsistent and are carried unchanged** (D-3.34-5, OV-85)
- `src/app/(site)/razno/page.tsx` — **`/razno` index** (new at 3.16). Navy `PageHeader` (breadcrumb → H1 „Разно“ → intro → „7 теми“, the 7 derived from the array length) over a 3/2/1 grid of seven cards. Each card is one real `<a>` in the `SeasonCard`/`LegendCard` anatomy **minus the photograph** — white on paper, `u-h3` navy title, `text-body` neutral-700 summary, 6px orange bar wiping in along the bottom edge on hover plus the standard 4px lift (D-3.16-10), no shadow. **No empty photo frame and no placeholder chip** where the photographs will later go (brief decision 3). **Static server component — no Sanity read, no `revalidate`** (the D-3.07-8 pattern), so it builds as `○ (Static)` **3.22: each card now opens on a 3:2 `cardPhoto`** (owner feedback 11.08.2026). Padding moved off the link onto the text block so the photo sits flush to the card edges — the `SeasonCard` anatomy. `object-cover` (the topic-page grid uses `object-contain`: that is the archive view and must not crop a scan; the card is a teaser). `alt=""` — decorative, the heading beneath is the link's accessible name. First three eager, the rest lazy. A topic naming no `cardPhoto` still renders a text-only card: no empty frame, no placeholder chip.
- `src/app/(site)/razno/[slug]/page.tsx` — **`/razno/<slug>`**, seven pages prerendered from `RAZNO_TOPICS` (`● (SSG)`, empty Revalidate). Unknown slug → `notFound()`. Navy `PageHeader` with the topic's H1 and the three-crumb trail, then one `max-w-measure` editorial column: `toRuns()` groups **consecutive `record` blocks into one hairline-separated list** (the `SeasonRecordList variant="results"` cadence, rendered directly because these are plain strings — D-3.16-6), `para` runs keep `SeasonStory`'s `mt-6` prose rhythm at `text-body-l`, and the single `closing` block („НАПРЕД БЕЛАСИЦА!“) sits in the white `u-cap` block the season page and `/za-nas` use for a set-apart statement. Ends with `RAZNO_SOURCE_CREDIT` above a mist hairline in the `neutral-500` provenance register — **exactly once per page, seven across the built output**. Then prev/next + „Сите теми“. The prose `<section>` is deliberately **unnamed**, so it is not a duplicate landmark over the H1 (D-3.16-8)
- `src/components/razno/RaznoNeighbourNav.tsx` — prev/next topic cards, walking `RAZNO_TOPICS` in array order. **A deliberate copy of `SeasonNeighbourNav`'s pattern, not a generalisation of it** (D-3.16-7): identical markup, classes and behaviour, with `/razno/<slug>` and „Претходна тема“ / „Следна тема“. A missing neighbour — topic 1's previous, topic 7's next — renders **nothing**, never a disabled card

## Contact page (Phase 2.07)

- `src/app/(site)/kontakt/page.tsx` — **Contact** `/kontakt` (Phase 2.07) — **no longer provisional: the banner was removed at owner request in 3.10, retiring D-2.05-1** (and with it D-3.03b-2/-3, D-3.10-1). Now breadcrumb → H1 „Контакт" + intro → two-column body (form left, direct-contact block right with `border-l border-mist`; single column at 375, form first, direct block below `border-t border-mist`). **Server component**; reads `NEXT_PUBLIC_FORMSPREE_ENDPOINT` and passes it to `ContactForm` (D-2.07-2). **3.07:** the direct block's email is now the real `mailto:` from `CONTACT_EMAIL` — PL-3 was open on _two_ surfaces and clearing only the footer would have left this page contradicting its own footer (D-3.07-5); the socials chip (PL-15) and the Formspree chip (PL-14) are unchanged, so `<main>` now carries **2** chips, not 3. Prior note: direct block was all placeholders, the footer's demo values deliberately not propagated (D-2.07-4). No Sanity read; the title is a direct `<h1>` not `SectionHeading` (D-2.07-6). ISR `revalidate = 60`. **The last 404 in the nav — the six-item nav is now complete.**
- `src/components/contact/ContactForm.tsx` — **client** component (the repo's second, after `StatTable`): the `idle → submitting → success | error` state machine (D-2.05-3). Plain `fetch` POST to the endpoint with `Accept: application/json`, `FormData` body — no form/validation library. Macedonian copy „Испрати" / „Се испраќа…" / „Ви благодариме!" / „Испрати повторно" (D-2.07-1). Labelled inputs, `type=email` + `required` native validation, `role="alert"` (error, above the form, input retained) / `role="status"` `aria-live="polite"` (success, form replaced), `focusOnPaper` ring, submit min-width reserves space (no layout shift). **Endpoint unset → the whole form renders inside a disabled `<fieldset>` with a notice + `PlaceholderChip` (PL-14); no `<form>`, so no submission is possible** (D-2.07-3). Reuses `PlaceholderChip`, not an „amber" chip (D-2.07-5)

## Ingestion script (Phase 2.09) — `scripts/ingest/`

- `scripts/ingest/classify.mjs` — **pure**, I/O-free folder-name classifier: `classify(name) → { kind, slug, title, decade, flags }` implementing `content-ingestion-plan.md` §3 (top-down, first match wins). Also `provenance()` (the locked §5 string), `decadeOf`, `decadeToWave`, `WAVE_LABELS`. Titles use „…“ marks (D-2.09-4); span separators include slash look-alikes ／ ⁄ but not underscore (D-2.09-7). No Sanity, no fs
- `scripts/ingest/run.mjs` — walks the mirror, classifies, writes the report, and (only with `--commit`) creates season shells (Wave 0) + uploads season-folder photos as **drafts** (Waves 1–8). Plain Node ESM. Dry-run default (writes nothing); `--source`/`--wave`/`--include-thematic`/`--report` flags; loads `.env.local` itself (no dotenv dep); reads `SANITY_API_WRITE_TOKEN` and exits clearly if unset on `--commit`. Deterministic idempotent IDs (`season-<slug>`, `drafts.photo-<sha1(relPath)>`); script-side slug-collision detection (D-2.09-6); never publishes, invents nothing beyond slug/title/decade/relatedSeason/image/provenance
- `scripts/ingest/extract-legends.py` — **(3.12)** chapters 9 + 10 of the book `.docx` → `data/book/legends.json`. Chapter geometry is line numbers, not patterns (the source headings are inconsistent). Restores spaces the `.docx` lost between run-together words via a vocabulary-driven splitter plus an explicit override table; changes nothing else, and leaves the author’s own typos standing
- `scripts/ingest/bios.mjs` — **(3.12)** `data/book/legends.json` → `person.bio` + `person.legendRank`, one Portable Text block per source paragraph, verbatim. Dry-run default; `--commit`; `--only <slug,…>`. Writes the **published** document (no rights gate on text, unlike the 2.09 photo drafts); block `_key`s are `sha1(slug:line:chunk)` so re-runs are byte-identical; refuses to write at all if any slug is missing from the dataset. Paragraphs over 900 chars are re-broken at sentence boundaries (D-3.12-4)
- `scripts/ingest/photos-owner.mjs` — **(3.12)** hand-delivered photographs → **published** `photo` documents, from a small JSON manifest (`file`, `caption`, `provenance`, one of `personSlug`/`seasonSlug`). References are resolved before any upload, so a slug typo fails before it costs a transfer; IDs are `photo-<sha1(basename)>`, so a re-run replaces rather than duplicates (D-3.12-7)
- `scripts/ingest/verify-classify.mjs` — pure unit check of `classify()` against the locked mappings; no mirror/Sanity/token; exits non-zero on any mismatch. The classifier's correctness evidence while the mirror is unavailable

## Content fill script (Phase 3.11) — `scripts/`

- `scripts/build-season-tables.mjs` — **new at 3.28**, generates `src/content/season-tables.ts` from the book extract **and, since 3.36, from `data/book/season-2025-26.json` as a second input** keyed to slug `2025-26` (D-3.36-5). **READ-ONLY AND OFFLINE: it opens no Sanity client, holds no token and makes no network call** — there is no `--commit` path to fire. Proven at 3.36 by sabotaging `globalThis.fetch` to throw: `--check` still passes, while the extractor under the same stub fails on its first call. Before merging the second input it **asserts the book contributes nothing to that slug** and exits non-zero if it ever does, so one season can never be described by two sources at once. `--check` regenerates in memory and diffs against the committed file, exiting non-zero if it is stale, which is the reviewer's proof the module matches the data. Also writes `docs/ingestion/season-tables-coverage.md`. Holds the book-id → slug map: the rule is „/" → „-" for 93 of 96 seasons, with three listed exceptions (`1926/30`→`1926-1930`, `1943/44`→`1944`, `1999/00`→`1999-2000`). Grouping is by `stage`, falling back to `competition` (D-3.28-3), and a squad with no appearances or goals is dropped so the richer prose keeps the section (D-3.28-9)
- `scripts/extract-season-2025-26.mjs` — **new at 3.36**, the one-time extraction behind `data/book/season-2025-26.json`: parses the 2025/26 season document's `results` and `lineupAndStats` Portable Text into the book's own row shapes. **Read-only and token-free** — one `fetch` against the **public** query API of a public-read dataset, no Sanity client, no credential, no write path, and it is the _only_ piece of this pipeline that touches the network. `--check` re-fetches and byte-diffs the committed output (needs network; **not** part of the build — the build's proof is `build-season-tables.mjs --check`, which is offline). **Asserts before writing** and exits non-zero on any failure: 30 matches, 15 home / 15 away, both half-seasons present, 35 squad rows, and the re-derived record **equal to the document's `finalTable` row** field by field — so a transcription slip fails the run rather than shipping. The only arithmetic is `apps = starts + subs` on Аце's own two figures (D-3.36-3); every team name, scoreline, scorer, figure and player name is verbatim, including the one-line „Спироски"/„Спиркоски" spelling split (D-3.36-4) and the birth year kept inside the name string (D-3.36-1). The only character removed anywhere is the trailing colon on the two half-season labels (D-3.36-2)
- `scripts/a11y/` — **new on `a11y-remediation`**, five scanners written for the WCAG 2.2 AA audit, plus a `README.md` giving the exact invocation for each. **They add no dependency**: all five drive the `puppeteer-core` and `axe-core` that already arrive under `node_modules` with the `lighthouse` devDependency, and point at the machine's own Chrome (D-a11y-6). Nothing in `src/` imports them and they are never run by the build
  - `scan-axe.mjs` — axe-core over a path list on stdin at one viewport, one browser for the whole run (the CLI relaunches Chrome per URL, ~20 min over 324 routes). Scrolls each page first so nothing is audited mid-reveal
  - `scan-contrast.mjs` — SC 1.4.3/1.4.11 from the **rendered** DOM: composites alpha, resolves the ancestor background, takes the threshold from the computed size and weight, and **reports what it cannot resolve** (text over a background image) instead of passing it silently — the hole D-3.05a-8 already records. `A11Y_ALL_PAIRS=1` dumps every pair the site renders, which is the report's contrast table
  - `scan-targets.mjs` — SC 2.5.8 including the spacing exception: it draws the 24px circle the criterion describes rather than only measuring the box, and separates real failures from targets the exception rescues
  - `keyboard-walk.mjs` — Tabs (or Shift+Tabs) through a page and records per stop what got focus, whether a ring is painted, and — by **hit-testing** the focused rectangle — whether the sticky chrome covers it. This is what caught the SC 2.4.11 failure. `A11Y_NO_SCROLL_PADDING=1` reproduces the pre-fix state on a fixed build, which is how the report's before/after numbers were taken with one instrument
  - `visual-diff.mjs` — screenshots a fixed page set at fixed **absolute** scroll offsets and pixel-diffs two runs, writing a map with changed pixels in red. Absolute offsets on purpose: `scrollIntoView` is itself moved by `scroll-padding-top`, so it would compare two different parts of the document
- `scripts/build-statistika-extra.mjs` — **new at 3.34**, generates `src/content/statistika-extra.ts` from `data/book/statistika-source.md`. **READ-ONLY: no Sanity client, no token, no `--commit` path.** `--check` regenerates in memory and byte-diffs the committed module. Prints **two proofs**: a **round-trip** (every parsed row re-serialised from its own fields and compared to its source line with whitespace removed — **197/197 identical**; whitespace is the only thing the parser may change, because Аце glues fields together) and a check of **his own stated row counts** („следниве 30 играчи", „Седум играчи", „следниве 25 играчи" — 30/7/25, all matching). Segments the six blocks by detecting his ALL-CAPS headers and his `Играч/период/голови` column lines rather than by hardcoded line numbers
- `scripts/fill-season-content.mjs` — **new 3.11**, fills the **existing** `season.story` and `season.results` `block[]` fields from `data/book/season-content.json`. **No schema change, and none is needed.** Plain Node ESM, no new dependency. **Every write is `setIfMissing`**, so a season that already has content keeps it and a re-run changes nothing; Portable Text `_key`s are a deterministic SHA-1 of slug+field+index+text, committed with `autoGenerateArrayKeys: false`, so the patch is idempotent. **Dry run is the default** — it writes nothing to Sanity, needs no token, and emits `docs/ingestion/season-content-report.md`. **`SANITY_API_WRITE_TOKEN` is required only with `--commit`** (env or `.env.local`, parsed by the script itself — the 2.09 precedent); without it `--commit` exits non-zero before touching anything. Flags: `--commit`, `--only <slugs>`, `--field story|results|both`, `--report <path>`. **✅ Run for real on 2026-08-02** on the owner's instruction, after 3.11 shipped it unfired — 86 of 96 seasons patched, `story` 66 → 96 and `results` 10 → 95 live. ⚠️ It **cannot** replace existing content by design, which is why `1992-93` still shows the old 2.x demo summary

## One-off content correction (Phase 3.14) — `scripts/`

- `scripts/fix-andreev-role.mjs` — **new 3.14**, a single-document, single-field role correction: `person` `f69a1977-…837c2` (`petar-andreev`) from `["player","president"]` to `["player","trainer"]` (owner correction, D-3.14-4). Modelled on `ingest/bios.mjs` — same `.env.local` parser, same `@sanity/client` setup (`perspective: "published"`, `useCdn: false`), dry-run default, `--commit` to apply, exits 1 if `--commit` is passed without `SANITY_API_WRITE_TOKEN`. **Idempotent:** already-corrected is a no-op exit 0, not a failure (D-3.14-6). Guards before any write — document exists, `name === "Петар Андреев"`, no unknown role value, current role is the known before-state, computed result deep-equals `["player","trainer"]`, and (with a token) no draft shadows the document. ⚠️ **Never run with `--commit`** — the patch was applied through the authenticated Sanity MCP instead, since this machine has no write token (D-3.14-5), so the script's write path is unexercised. Kept as the record of the change and as a re-runnable check that it still holds

## Build & tooling config (repo root)

- `package.json` / `package-lock.json` — dependencies, all pinned exact; scripts (dev/build/start/lint)
- `tsconfig.json` — TypeScript config; `@/*` path alias
- `next.config.ts` — Next.js config; **`images.formats: ["image/avif","image/webp"]` (3.09, D-3.09-4 — a format change only; no photograph's `quality` was reduced)**; `images.remotePatterns` allows `cdn.sanity.io/images/**` for `next/image` (Phase 1.04); `experimental.staticGenerationRetryCount: 2` (3.02F-Code, D-3.02F-C-3) — the second layer of build resilience under `src/sanity/fetch.ts`: the helper retries a flaky _read_, this re-runs the _page_; **`async redirects()` (3.35)** — one permanent (308) rule, `/legendi/tomche-eftimov` → `/legendi/tome-eftimov`, for the link Аце shared before the Ефтимов merge unpublished the old page (D-3.25-2). The survivor slug was read from the published data, not assumed.
- `sanity.config.ts` — embedded Studio config (projectId/dataset via env, schema types, structure + vision plugins); read by `/studio` and the Sanity CLI (Phase 1.04)
- `sanity.cli.ts` — Sanity CLI config (projectId/dataset from env; tolerant of missing values) (Phase 1.04)
- `.env.local` — **git-ignored** (`.env*`); `NEXT_PUBLIC_SANITY_PROJECT_ID` / `_DATASET` / `_API_VERSION`. No token, not committed (Phase 1.04). `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is **not** set here — 3.03's step (D-2.07-2)
- `.env.example` — **committed** template (Phase 2.07): `NEXT_PUBLIC_FORMSPREE_ENDPOINT=` (empty) + a one-line comment. Trackable because `.gitignore` carries `!.env.example` after `.env*`; every other `.env*` stays ignored (D-2.07-2). No secret. **2.09** added `SANITY_API_WRITE_TOKEN=` (empty) with a comment — the ingestion write token, documented but never valued here; the real value lives only in `.env.local` (D-2.09-5)
- `.gitignore` — Next default + `/dist` (2.01) + **`!.env.example`** negation (2.07, D-2.07-2) + **`/.claude/worktrees/`** (added with the book drop) — Claude Code worktrees are local scratch and carry a full build output each; verified at 3.11 that `git ls-files .claude/worktrees` returns nothing
- `eslint.config.mjs` — ESLint flat config (Next 15 `FlatCompat`: next/core-web-vitals + next/typescript). **3.09:** ignore globs anchored with `**/` (`**/.next/**`, `**/out/**`, `**/build/**`, `.claude/**`, `_to_delete/**`) — the root-only patterns let a git worktree's own `.next` be parsed in full and `npm run lint` died with a V8 OOM before reaching a source file
- `postcss.config.mjs` — PostCSS config (`@tailwindcss/postcss`)
- `components.json` — shadcn/ui config (style new-york, base color neutral, Lucide icons)
- `public/crest.svg` — **new 3.05b**, the crest's **scalable master** (916×1294 viewBox, 79 KB, pure vector paths — no embedded raster, no script, no external refs). A traced, repaired rebuild of the same artwork, not a new crest: the source scan was clipped on its right edge and its pennant point cut off flat; the rebuild closes the point on the artwork's own measured diagonals and evens the margins. Club blue unchanged at `#125C9A` (D-crest-1). **This is what the header and the hero render**, via `next/image` + `unoptimized` — Next refuses to optimize SVG without `dangerouslyAllowSVG`, and an SVG has no raster variants worth generating (D-3.05b-7)
- `public/crest.png` — official FK Belasica club crest, **re-exported from the vector master at 3.05b** (still **864×1220**, so `layout.tsx`'s Open Graph block stays correct and untouched). Transparent outside the pennant outline (D-crest-2). Canonical asset for the Open Graph card and the favicon lineage; not rendered on-page (chore-crest-logo; rebuilt 3.05b)
- `public/crest-ui.webp` — 400×565 raster UI derivative, re-exported at 3.05b. Retained as the raster fallback and as the `/crest.png` lineage's source; **nothing references it** since the header and hero moved to `/crest.svg` (D-3.05-11; D-3.05b-7)
- `public/og-default.png` — **new at 3.23 (B5)**, the site's share card: **1200×630**, 76 494 bytes, confirmed with `sips` against the file on disk. Navy ground, the 6px orange bar on the top edge, the crest at 340px, „ФК БЕЛАСИЦА" in Oswald 700 and „НЕОФИЦИЈАЛНА АРХИВА" in Golos 700 tracked caps. Replaces `/crest.png` (864×1220 — a **portrait** image in a landscape slot) as `openGraph.images`; `twitter.card` became `summary_large_image` in the same change. **A static asset, not `next/og`** — composed in a throwaway route so it inherited the real self-hosted fonts and tokens, screenshotted with headless Chrome at that exact viewport, and the route deleted (D-3.23-4). Regenerating it means recreating that route.
- `public/razno/<slug>/<slug>-NN.webp` — **new 3.20**, the „Разно" topics' photographs: **64 files, 9,3 MB**, one folder per topic — `tiverija` 3 · `viaredzo-kup` 5 · `kup-na-uefa` 6 · `stadion-blagoj-istatov` 6 · `partizan` 7 · `ajduci` 8 · `mladinska-skola` 29. Optimised to WebP ahead of time at the size the grid shows them, and rendered by plain `<img>` — never `next/image`, so none of the 64 reaches the Vercel optimiser (D-3.20-7). ⚠️ **Not Sanity assets**: no `photo` document, no `provenance` field, so **Ace cannot manage these in Studio** and nothing in the repo records where they came from (D-3.20-1, OV-51)
- `src/content/razno-photos.ts` — **new 3.20**, **GENERATED**: `RAZNO_PHOTOS` keyed by topic slug, each entry `src` + intrinsic `width`/`height` + optional `caption`. Written once from the optimisation run's manifest and checked against `public/razno/`; **the manifest is scratch and is not a build dependency** — nothing imports JSON at runtime, and it is deliberately not committed. Regenerate rather than hand-edit. `caption` is **absent for 35 of the 64** because the archive holds no description of them (D-3.20-6)
- `src/components/razno/RaznoPhotoGrid.tsx` — **new 3.20**, the topics' „Фотографии" gallery. A **server** component: three up, `3/2` matted frames, captions in the archive's anatomy. Deliberately **not** `PhotoGrid`, which is Sanity-shaped end to end; the **lightbox is reused unchanged**, because D-3.05b-3 had already split it so the server passes plain `url · width · height · caption` (D-3.20-2)
- `public/znamenca/` — **new 3.19, cut out 2026-08-17**, the marks the homepage hero renders in place of the single crest, **numbers 1, 6 and 9 of a larger set in the owner's own order** (the other numbers are not in the repo). All three are now **background-free**, where all three used to be scans on white: `zname-06.webp` (446×520, 57 KB) and `zname-09.webp` (438×520, 44 KB) were **replaced in place** by owner-supplied cut-outs (D-crest-3), and **slot 1 no longer lives in this folder at all** — the `ZNAMENCA` table points at `/crest.svg`, which was always the same pennant with its background already gone (D-crest-4). ⚠️ **`zname-01.webp` (368×520, 61 KB) is therefore ORPHANED** — still committed, referenced by nothing; kept as the archival scan rather than deleted, since it is owner-supplied source material (D-crest-5). The shared **520px height survived the swap** and is still what lets one `h-*` + `w-auto` normalise three shapes into a set; `/crest.svg` has no pixels, so the table carries its viewBox reduced to that height (364×520). Cut-outs are **lossy webp q88 + `alphaQuality:100`**, not lossless — lossless cost 154/172 KB for art that renders at 112 px (D-crest-3). Rendered as plain `<img loading="lazy">` (never `next/image`) for the same reason the crest was — the page's LCP is the hero photograph behind them (D-3.09-2); verified still un-preloaded in the built HTML after the swap. ⚠️ **What they depict, and from when, is still recorded nowhere** — `facts.md` has no entry, so the `aria-label` names the group and dates nothing (D-3.19-4, OV-46)
- `public/llms.txt` — **new at 3.23 (B7)**, a plain-text description of the archive for AI answer engines, in Macedonian. States the unofficial-archive status in its **first two lines**, then what the archive holds with counts taken from **live data** (96 сезони 1922–2026, **231** личности (211 → 212 at 3.35, **212 → 231 at 3.33** when Аце's tail landed, each time re-checked against `count(*[_type == "person"])`), 7 теми — not the brief's stale „~160"), the main section URLs, the copy-permission line matching the footer's `CONTENT_PERMISSION_NOTICE` verbatim, and the sitemap URL. ⚠️ **Hardcodes the origin** — `public/` is static and cannot import `SITE_URL`, so this is the fourth place to edit at cutover; `src/lib/site.ts` says so.
- ~~`public/*.svg` — create-next-app default static assets (next/vercel/file/globe/window)~~ — **DELETED at 3.23 (C5).** All five (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`) were Phase-1.01 scaffolding with **0 references** anywhere in `src/`, `public/`, `docs/` or `scripts/`, confirmed by grep before removal.

## Audits (`docs/audits/`) — new at 3.09

- `docs/audits/Part-3-Phase-09-baseline.md` — the **pre-change** Lighthouse numbers for all nine sampled routes × {mobile, desktop}, plus the exact method (tool version, exact CLI command, median-of-3 rule). **Re-run this method at 3.11** to verify the launch; it is the contract, the harness script was scratch
- `docs/audits/Part-3-Phase-09-results.md` — the same table post-fix, side by side with the baseline and a note per row on what moved it; the two routes still at 94 on mobile with the measured tradeoff that was refused (D-3.09-8); First Load JS per route; the woff2 preload count and which four faces they are; and the full manual WCAG 2.2 AA evidence (contrast, target size, focus-not-obscured, reflow at 375/320, text spacing, the lightbox's eight behaviours, `StatTable`, reduced-motion and no-JS, and each of the six WCAG 2.2 criteria recorded pass or N/A)

## ✅ Committed by accident — REMOVED at 3.11 (found at 3.07)

- ~~`_to_delete/`~~ — **gone.** The 11 tracked files of git plumbing junk (`HEAD.lock`, `index.lock`, `index.lock.2`, `index.lock.stale`, 7 × `tmp_obj_*`), committed to `main` during Phase 3.06a and left in place by 3.07, were deleted in commit `7ef3547` alongside the book drop. Verified at 3.11: absent from the working tree **and** from `git ls-files`. ⚠️ **The two untracked strays are a separate matter and are NOT confirmed handled** — `git-index.lock.leftover` and `sanity_token_transfer.tmp` lived only in Lazar's working tree, never in git, so no commit could have removed them; neither is present in the tree now, but whether `sanity_token_transfer.tmp` was **shredded or merely deleted** is unknown to this phase. Its name suggests it held a **token** — carried forward as a human step
- `.prettierignore` — **new at 3.28.** The repo had none. Excludes `src/content/season-tables.ts` (D-3.28-10) and, since 3.34, `src/content/statistika-extra.ts` (D-3.34-7) — each generator owns its file's formatting, because both scripts' `--check` byte-diffs the committed module. Also `.next/` and `node_modules/`. Nothing hand-written is excluded
- `.claude/launch.json` — dev-server config for the in-app browser preview (added during 3.06a). Harmless and useful; noted here only because it was never mapped

## Not present (deliberately)

- `.github/workflows/` — no CI/review workflow; the Claude review Action was dropped for this project (D-1.01-4)
