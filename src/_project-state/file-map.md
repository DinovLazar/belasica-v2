# file-map.md — Belasica-V2

> Location in repo: `src/_project-state/file-map.md`. Every meaningful file/folder, one line each: what it's for. Updated by the executor on **every** add, rename, or delete — a stale map lies. Real tree as of Phase 1.05 (node_modules, .next, and other gitignored paths omitted).

## Docs & rules (repo root)
- `CLAUDE.md` — Code's standing rules (behavioral contract; under 150 lines)
- `README.md` — public-facing repo overview; "unofficial archive" disclaimer; stack + layout
- `facts.md` — verified business facts; the only legal source for factual claims on the site
- `brand.md` — design tokens + brand rules; the only token source (confirmed 1.02 tokens written back in Phase 1.03; zero SEED)
- `briefs/` — every phase brief, saved by Lazar; versioned instruction history (`.gitkeep` placeholder — the Phase 1.01 brief was not filed per owner decision, D-1.01-3). Seven briefs filed as of 3.11; most phases ran from a brief pasted into the session rather than committed
- `data/book/` — **new 3.11, extended 3.12**, the tracked **source drop** extracted from „ФК Беласица – гордоста на Струмица" (Аце Стојанов, финална верзија 04.10.2025). Seven files, ~6,3 MB. **Data, not code — nothing in `src/` imports it**, so it adds zero bytes to any bundle. Every match and squad row carries `sourceLine`, the book's original line verbatim (D-3.11-2, D-3.11-3) **3.12 adds `data/book/legends.json`** — the people half of the same extraction: 86 records, 80 carrying the book’s all-time appearance rank, 60 carrying the full biography paragraph by paragraph with a `line` on each. **The only file in this folder the site’s content depends on** — written into Sanity by `scripts/ingest/bios.mjs`; the site reads Sanity, never this file
  - `README.md` — provenance, the ±2-match accuracy check against the book's own summary table, the four known divergences, and what was deliberately left unchanged. **Owed: a native read by Lazar and Аце**
  - `extraction-report.md` — the Macedonian coverage report written for Аце
  - `seasons.json` — **96 seasons** (1922/26 → 2025/26): title, story, matches, squad, top scorers, official balance. 3,6 MB
  - `matches.json` — flat list of **2.267 matches**: home, away, result, competition, autumn/spring, round, outcome. 2,0 MB. **Nothing can accept this yet** — `match` is unregistered (D-2.01-2, premise voided at D-3.11-1)
  - `master-table.json` — the book's own „БЕЛАСИЦА НИЗ ИСТОРИЈАТА" table, 107 rows, used as an **independent control** on the extraction. Its totals reproduce the book exactly: `2275 · 1030–421–824 · 3907:2976 · 2885` — the book's side of OV-14, now machine-read from source
  - `index.json` — lightweight index: season, league, placement, match count
  - `season-content.json` — **what the fill script reads**: per-season `story` and `results` plus a `resultsProvenance` string. 96 seasons · **2.563** result lines · **167** story paragraphs; provenance splits 92 printed lists / 1 assembled from sentences / 2 notes / 1 none (D-3.11-4)
- `docs/design-handovers/` — Design's handover files; Code reads the matching one before any UI work
  - `Part-1-Phase-05-Homepage.md` — homepage §Layout spec, archived in-repo (Phase 1.05)
  - `Part-2-Phase-02-Handover.md` + `Part-2-Phase-02-mockups/` — Archive & Season templates: shared conventions, §5–§7 specs, mockups (Phase 2.02)
  - `Part-2-Phase-05-Handover.md` — **People & pages** design spec (Legends / Person / About / Contact). Landed into the repo by Phase **2.06** — the 2.05 design phase never committed it (D-2.06-1). Its §0 „the repo wins" rule governs; several of its citations are wrong or stale — see D-2.06-2/-5/-7. The mockup it names (`People & Pages.dc.html`) was never supplied
  - `Part-3-Phase-05a-Directions.md` — **Phase 3.05a direction exploration**: the three homepage proposals (А „Спортски весник" / Б „Клупски музеј" / В „Трибина"), each with intent, its exact token deltas vs `brand.md`, and what adopting it site-wide would touch. **No recommendation** — the pick is the owner's. Read together with the three `/predlog-*` routes
- `docs/ace-demo/` — Ace demo kit (Phase 1.06); Phases 2.02 & 2.05 read `feedback.md` before they open
  - `walkthrough.md` — demo script (spoken lines in Macedonian; incl. the OV-3 footer question)
  - `feedback.md` — capture template for Ace's feedback (**still the empty template** — the sit-down had not happened as of 2.01; D-2.01-7)
  - `screenshots/homepage-desktop.png` (1280-wide) · `homepage-mobile.png` (375-wide) — full-page captures of the finished homepage, **regenerated at 2.08** from the local prod build (reveal forced via `prefers-reduced-motion`)
  - `Part-3-Phase-02-pilot-review.html` — **self-contained** visual review report (Phase 3.02): the 6 pilot seasons' chosen team+table thumbnails, trainer/stats text, per-season gaps, the 7 `clubRecord`s + sources, and the 881-provenance-rewrite before→after. For the owner to approve the pilot pattern before the 90-season run.
  - `Part-3-Phase-02-Run-review.html` — **self-contained** full-archive review report (Phase 3.02-Run): all 90 `decade!=2020` seasons over 10 decade sections, each with team+table thumbnails, trainer + top scorer, gaps, and 3 review-flags (77 team / 82 table photos). 2.3 MB.
  - `Part-3-Phase-04-content-review.html` — full-archive **content**-review page (Phase 3.04-**Cowork**): all 96 seasons by decade with their `teamPhoto` + `tablePhoto` thumbnails, plus the flagged borderline and era-misfiled photos. Thumbnails are linked from `cdn.sanity.io` (needs a connection to view), not embedded. Macedonian; a review artifact for Lazar, not part of the site build
- `docs/content-ingestion-plan.md` — **how the ~915 photos / ~74 seasons get into Sanity** (Phase 2.01, D-2.01-4): the hybrid approach (scripted shells+assets / manual editorial), the deterministic Drive-folder→season mapping rules, the by-decade wave plan, and the provisional-provenance / P0.2-rights gate. Phase 2.09 builds and runs against this.
- `docs/ingestion/` — Phase 2.09 ingestion reports + hand-off:
  - `README.md` — run procedure + „verify no draft leaked" check
  - `2.09-classifier-verification.md` — classifier self-test proof (23/23)
  - `2.09-dry-run-report.md` — **2.09-Run**: machine classification of the real mirror (119 folders → 96 seasons / 21 thematic / 2 skip; 0 collisions, 0 unmatched)
  - `2.09-run-report.md` — **2.09-Run**: the actual run (Wave 0 + Waves 1–8, the owner-directed publish D-2.09R-2, OV-4/OV-7 cleared)
  - `2.09-editorial-handoff.md` — **2.09-Run**: manual-pass worklist (per-season content, 9 manual-title seasons, person folders, 10 spot-check pages, source docs)
  - `README.md` — what the reports are, the run procedure (dry run → Wave 0 → Waves 1–8), and the **mirror-missing status** (the three run reports are pending the mirror + a write token — D-2.09-3)
  - `2.09-classifier-verification.md` — **done**: output of `verify-classify.mjs` proving `classify()` matches the locked §3 mappings (no mirror needed)
  - `2.09-dry-run-report.md` · `2.09-run-report.md` · `2.09-editorial-handoff.md` — **pending the mirror**; generated by the dry run / commit waves
  - `season-content-report.md` — **new 3.11**, written by `scripts/fill-season-content.mjs`. One row per season: status, what `story` and `results` would receive, and the season's `resultsProvenance`. The committed copy is the **dry run** — „проба (dry run) — ништо не е запишано" — recording 96 seasons processed and 86 that would be patched. Regenerated on every run; a `--commit` run overwrites it with the real one

## Project state (`src/_project-state/`)
- `current-state.md` — live repo snapshot; NEXT line; owed-verification + placeholder registers
- `file-map.md` — this file
- `00_stack-and-config.md` — append-only stack/config log with exact pinned versions
- `decisions.md` — append-only decision log, IDs `D-<phase>-<n>`
- `completions/_TEMPLATE.md` — completion-report template
- `completions/Part-1-Phase-01-Completion.md` — Phase 1.01 completion report
- `completions/Part-1-Phase-03-Completion.md` — Phase 1.03 completion report
- `completions/Part-1-Phase-04-Completion.md` — Phase 1.04 completion report
- `completions/Part-1-Phase-05-Completion.md` — Phase 1.05 completion report
- `completions/Part-1-Phase-05-2-Completion.md` — Phase 1.05.2 completion report (homepage content-sync)
- `completions/Part-1-Phase-06-Completion.md` — Phase 1.06 completion report (draft — pending the Ace sit-down + portraits before close)
- `completions/Part-2-Phase-01-Completion.md` — Phase 2.01 completion report (content model lock)
- `completions/Part-2-Phase-02-Completion.md` — Phase 2.02 completion report (Archive & Season design)
- `completions/Part-2-Phase-03-Completion.md` — Phase 2.03 completion report (Archive & Season templates)
- `completions/Part-2-Phase-04-Completion.md` — Phase 2.04 completion report (Statistics page)
- `completions/Part-2-Phase-05-Completion.md` — Phase 2.05 completion report — **BACK-FILLED at 2.09** (the design phase filed none; records the handover-only artifact + the nine 2.06 conflicts; D-2.05-1/-2/-3 back-filled in `decisions.md`)
- `completions/Part-2-Phase-06-Completion.md` — Phase 2.06 completion report (People & pages build)
- `completions/Part-2-Phase-07-Completion.md` — Phase 2.07 completion report (Contact page `/kontakt`)
- `completions/Part-2-Phase-09-Completion.md` — Phase 2.09 completion report (content ingestion — script built; waves blocked on the missing mirror, D-2.09-3)
- `completions/Part-2-Phase-09-Run-Completion.md` — Phase 2.09-**Run** completion report (the actual ingestion: 96 seasons + 889 published photos; OV-4/-6/-7/-9 cleared; the owner-directed publish D-2.09R-2)
- `completions/Part-3-Phase-01-Completion.md` — Phase 3.01 completion report (content model update: 5 optional `season` fields + `clubRecord` type; schema deployed)
- `completions/Part-3-Phase-02-Completion.md` — Phase 3.02 completion report (content fill — 2020s pilot: 6 seasons tagged + trainer/top-scorer text, 7 `clubRecord`s, 881 photo `provenance` rewritten → OV-10 cleared)
- `completions/Part-3-Phase-02-Run-Completion.md` — Phase 3.02-**Run** completion report (the remaining 90 seasons: 77 team + 82 table photos, 63 trainers, 66 top-scorers; photos classified by 8 parallel subagents + validated; main narrative→`story` deferred, D-3.02R-3)
- `completions/Part-3-Phase-02-Story-Completion.md` — Phase 3.02-**Story** completion report (`season.story` filled for 66 seasons 1922→1995/96 from the history narrative, verbatim via 5 era-subagents; +squad rosters in `lineupAndStats`; first 3.02 change visible on the live site, D-3.02S-1…-4)
- `completions/Part-3-Phase-04-Code-Completion.md` — Phase 3.04 **Code** completion report (the season-page rebuild: all 96 pages restructured around the Part 3 fields; decisions `D-3.04-1…-12`; the `cn()` type-scale bug and the results/story overlap handed on)
- `completions/Part-3-Phase-04-Cowork-Completion.md` — Phase 3.04-**Cowork** completion report, the parallel **Sanity-content** phase that ran alongside the code rebuild (all 96 seasons' photos re-verified against source, 2 wrong images cleared + 2 repointed, the first `results` fill on 10 seasons, trainer 1957-58 recovered). Its counts were re-verified against the live published dataset before it was committed. Decisions `D-3.04C-1…-6`
- `completions/Part-2-Phase-08-Completion.md` — Phase 2.08 completion report (homepage made truthful to the 96-season archive: deterministic photo ordering, featured teaser self-omits, the real 2025-26 squad photo promoted into the hero; OV-RIGHTS closed, D-2.08-1…-3)
- `completions/Part-3-Phase-02F-Code-Completion.md` — Phase 3.02F-**Code** completion report (build hardened at 270 pages — Sanity reads retry ×5 and fail loudly instead of silently becoming a 404 — plus the 30 curated `clubRecord`s surfaced on `/statistika`)
- `completions/Part-3-Phase-03-Completion.md` — Phase 3.03 completion report (homepage rebuilt as the photo-forward seven-section front door, footer redesigned with the fake demo contact/socials replaced by visible `[PLACEHOLDER]` chips, navbar refresh). **Did not wire the Formspree endpoint it was assigned — see 3.03b**
- `completions/Part-3-Phase-03b-Completion.md` — Phase 3.03b completion report (**configuration only**: `NEXT_PUBLIC_FORMSPREE_ENDPOINT` set in `.env.local` + Vercel Prod/Preview, so the `/kontakt` form goes live; PL-14 and OV-8 both cleared against the real service; `D-3.03b-1, -2`)
- `completions/Part-3-Phase-04d-Completion.md` — Phase 3.04d completion report (owner-directed audit-fix phase, no written brief: the `cn()` type-scale bug fixed at source, ≥24px tap targets, OG/robots/sitemap launch metadata, LCP priority hints)
- `completions/Part-3-Phase-05-Completion.md` — Phase 3.05 completion report („Трибина" adopted site-wide from `/predlog-c`, and all three proposal routes deleted)
- `completions/Part-3-Phase-05a-Completion.md` — Phase 3.05a completion report (three full homepage directions at `/predlog-a|b|c` on identical live content, `noindex` and unlinked, plus the comparison handover — **no recommendation**, the pick was the owner's)
- `completions/Part-3-Phase-05b-Completion.md` — Phase 3.05b completion report (homepage narrowed back to a front door — ten most-capped legends and six records instead of all 160 people / 30 records — repaired vector crest, and the season photo lightbox)
- `completions/Part-3-Phase-09-Completion.md` — Phase 3.09-**Code** completion report (performance & accessibility measured, fixed and re-measured: desktop 100 on all nine routes, mobile ≥95 on eight of nine; the `Reveal`-at-`opacity:0` LCP defect; `D-3.09-1…-8`)
- `completions/Part-3-Phase-10-Completion.md` — Phase 3.10-**Code** completion report (the `/kontakt` provisional banner removed, the legends search moved into the navy header and given Latin-input matching, the homepage hero pinned to 1982/83; `D-3.10-1…-9`, and the 375 px hero crop escalated as **OV-24**)
- `completions/Part-3-Phase-12-Completion.md` — Phase 3.12-**Code** completion report (biographies from the book, ranking by appearances, the 21-goal scorer threshold, four photographs, the footer fan-page link). Also records the branch-divergence incident and its recovery (D-3.12-8)
- `completions/Part-3-Phase-11-Completion.md` — Phase 3.11-**Code** completion report (the book extraction landed as tracked data in `data/book/`, plus the `setIfMissing`-only fill script; **no schema change, no Sanity write, no file under `src/` outside `_project-state/`**; `D-3.11-1…-6`, OV-14's book side machine-verified, and two DoD items reported unmet because the brief was written against a repo state that no longer holds)

## Application (`src/`)
- `src/app/layout.tsx` — **bare** root layout: `<html lang="mk">`/`<body>`, fonts, `globals.css`, Vercel Analytics, site metadata. **3.04d:** `metadataBase` (Vercel origin, hardcoded + sync comment), Open Graph (siteName/locale `mk_MK`/crest image 864×1220) + Twitter `summary` card (D-3.04d-3). Site chrome moved to the `(site)` group so `/studio` can escape it (Phase 1.04, D-1.04-3)
- `src/app/robots.ts` — robots file-convention route: allow all, disallow `/studio`, sitemap pointer (Phase 3.04d, D-3.04d-4)
- `src/app/sitemap.ts` — sitemap file-convention route: 6 static routes + every published season/person slug via the public read client (`defined(slug.current)`, `_updatedAt` as `lastModified`, `revalidate = 60`, static-only fallback on a failed read) (Phase 3.04d, D-3.04d-4)
- `src/app/(site)/layout.tsx` — public-site chrome: pre-paint `.js` flag script + skip link + `SiteHeader` + `<main>` + `SiteFooter` (relocated verbatim from root layout in 1.04; `.js` script added 1.05 for the reveal progressive-enhancement, D-1.05-5)
- `src/app/(site)/page.tsx` — **the homepage**, **rebuilt again at 3.05** into the „Трибина" seven-block page: a matchday-poster hero (photograph full-bleed, crest as a white block capped by the 6px orange bar and pinned over the picture's lower edge, wordmark on solid navy at 14.95:1 — D-3.05a-10) then alternating paper/navy full-bleed blocks (Story → Legends → scoreboard Records → Decades → Moment → Quick links). Same `HOME_QUERY` and ISR as 3.03 — this phase re-dressed the page, it did not re-source it. Prior note, **3.03**: to the seven-section heritage-and-legends-led flow (Hero → Story → Legends → Numbers → Decade explore → Moment → Quick links) from live published Sanity content; ISR `revalidate = 60`. One `HOME_QUERY`: hero = most-recent `teamPhoto` (`order(decade desc, title desc)`, newest-photo fallback); legends = players sorted portraits-first then name (D-3.03-2) via the reused `LegendCard` (equal-height via a homepage-scoped grid variant); records = `clubRecord` docs → `ClubRecords`; decades reduced to per-decade counts → `DecadeExplore`; moment = deterministic captioned+season-anchored+landscape photo (D-3.03-4). Imports `focusOnPaper`/`focusOnNavy` from `@/lib/focus`. Graceful placeholders when empty. Replaced the 1.05→2.08 eight-section page (featured season, gallery mosaic, dot-rail). **3.05b — the front door stops showing the whole archive:** the crest `<Image>` now points at `/crest.svg` (header does too); `HOME_QUERY`'s `legends` projection ranks `coalesce(careerStats.appearances, -1) desc, name asc` and slices **`[0...10]`** (the `-1` keeps an unrecorded player below one with a single appearance — D-2.01-3's total is authoritative, never summed); the records strip is capped at **six** by `ClubRecords`' own label whitelist (D-3.05b-1); and `client.fetch` is wrapped in `fetchOrThrow` while **keeping** the graceful `EMPTY` fallback, unlike the season/person templates' loud throw (D-3.05b-4). The display sort (portraits first, then Cyrillic name, D-3.03-2) now only arranges the chosen ten (Phase 3.03, D-3.03-1..6; 3.05b, D-3.05b-1/-4/-7). **3.10 — the hero is pinned to 1982/83 in code:** `HERO_SEASON_SLUG` + `HERO_PHOTO_ID` module constants feed `HOME_QUERY` as parameters; a new `heroPinned` lookup sits **beside** `heroSeason`/`heroFallbackPhoto` so resolution is pinned → newest-season → newest-photo (the fallback is exercised, not assumed — D-3.10-4). `heroAltFor()` resolves the alt in that same order, so alt and image can never disagree; with no caption on the pinned photo it renders the season title with Studio's „…“ quotes stripped. The hero box goes `4/5 → 3/2` at base (`sm:16/10`, `lg:21/8` unchanged) with `objectPosition` `32% → 38%` — **at 375 px this still crops 11.2 % off each side of a 1.934:1 scan, a known limitation escalated to the owner (D-3.10-6)**. The `moment` filter now excludes the hero photograph explicitly by `_id` (D-3.10-5) **3.12: the legends band ranks on `coalesce(legendRank, 9999) asc`** instead of `careerStats.appearances`, so the homepage’s ten and `/legendi`’s order cannot disagree — Томче Ефтимов and Панче Пантазиев in, Зоран Митевски and Ванчо Костов out (D-3.12-3)
- `src/app/studio/[[...tool]]/page.tsx` — embedded Sanity Studio at `/studio` (`NextStudio`); renders on the bare root (no site chrome) (Phase 1.04)

> **`src/app/(predlozi)/` was DELETED at Phase 3.05** (D-3.05-9). The three homepage proposals А/Б/В lived there from 3.05a; the owner picked **В „Трибина"**, it was adopted site-wide, and the whole route group went with the pick. The designs survive in git history and, as intent + token deltas, in `docs/design-handovers/Part-3-Phase-05a-Directions.md`. The three exploration routes leave the build with them.

- `src/app/fonts.ts` — **Oswald + Golos Text** via `next/font/google`, Cyrillic subsets, two weights each (Oswald 600/700 · Golos 400/700). Replaced Inter + Source Serif 4 at 3.05 when direction В was adopted; the faces were promoted here out of the deleted `predlog-c/fonts.ts` (Phase 1.03; replaced 3.05, D-3.05-2/-7/-9)
- `src/app/globals.css` — Tailwind 4 `@theme` driven by brand.md tokens; **rewritten at 3.05** to the „Трибина" layer: two navies (`navy` `#0D1F3C` + `navy-2` `#12294F`), `orange` `#EE7A16` as a block fill, clamp type scale + `--text-stat`/`--text-stat-lead`/`--text-wordmark`, `--spacing-section`, page 1248, `--spacing-header` **78px**, all radii `0`. Adds the **role classes** in `@layer components` (`u-display`/`u-h1`/`u-h2`/`u-h3`/`u-stat`/`u-label`/`u-cap`/`u-tile`/`u-card`/`u-btn`/`u-link`), which carry the direction's uppercase + tracking that a `--text-*` token cannot (D-3.05-3). ⚠️ `.u-focus` / `.u-focus--on-navy` sit **outside every `@layer` on purpose** — inside `components` a `@layer utilities` rule in the vendored shadcn sheet silently disabled every focus ring (D-3.05-4). Also: reduced-motion baseline; reveal-on-scroll (`.js [data-reveal]`, re-timed to 260ms); `.placeholder-hatch` + `.placeholder-hatch--on-navy` (Phase 1.03/1.05; rewritten 3.05)
- `src/app/favicon.ico` — club-crest favicon (16/32/64), **re-generated from the repaired vector master at 3.05b** — it no longer embeds the pre-swap opaque render (chore-crest-logo; rebuilt 3.05b)
- `src/app/icon.png` — 512² crest favicon (Next file-convention `<link rel=icon>`), re-generated at 3.05b (chore-crest-logo; rebuilt 3.05b)
- `src/app/apple-icon.png` — 180² crest apple-touch-icon (Next file-convention), re-generated at 3.05b (chore-crest-logo; rebuilt 3.05b)
- `src/components/SiteHeader.tsx` — **sticky** navy header/nav, **restyled at 3.05** to „Трибина": opened by the 6px orange bar that also closes the footer, condensed caps nav over a 3px underline carrying the state, 48px burger, `navy-2` mobile panel with a 3px orange left bar on the current row. Restyle not rewrite — still `sticky top-0`, same burger, same `isActivePath` — so `--spacing-header` survived with a **re-measure to 78px** and every `top-header`/`scroll-mt-header` consumer still lands right (verified: anchor jump = 78px). Prior note: (`sticky top-0 z-40` + `border-paper/10`, 1.06b); client component; active-state (orange underline + `aria-current`) + accessible mobile menu (opens beneath the sticky bar); crest on a white tile beside the wordmark. **3.03 refresh:** hamburger is a 44px tap target (`p-2.5`); inactive nav items get a non-color `hover:border-paper/40` underline. **3.05b:** the crest `<Image>` `src` moved `/crest-ui.webp` → **`/crest.svg`**, keeping `unoptimized` and `width={400} height={565}` (the rebuild's 0.7079 ratio matches, so `h-10 w-auto` renders identically); the white tile and the 6px orange bar stay, because the crest's left half is white. Re-measured after the swap: header is **still 78px** at both 1280 and 375, so `--spacing-header` was **not** changed and an anchor jump still lands flush at its bottom edge (Phase 1.03; sticky 1.06b; refresh 3.03; crest chore-crest-logo; 3.05b D-3.05b-7)
- `src/components/SiteFooter.tsx` — **navy** footer since 3.05, closed by the same 6px orange bar that opens the header so the page reads as one bounded object; the light `footer` surface token was retired with it. Prior note (light footer): brand block (crest + wordmark + mandatory unofficial-archive label/statement from `@/lib/facts`) beside **Навигација / Контакт / Следете нѐ** columns + copyright bottom bar (dynamic year). **Redesigned 3.03:** the old fake demo email/phone/socials (PL-9) are **removed** — Контакт shows a „Контакт формулар" link + a `[PLACEHOLDER]` email chip (PL-3), „Следете нѐ" shows a `[PLACEHOLDER]` socials chip (PL-15). **3.07:** the Контакт chip became the real `mailto:info@belasicahistory.mk` from `CONTACT_EMAIL` (clears PL-3 + the email half of PL-9), and the copyright bottom bar gained a **„Правни информации"** link to `/pravni-informacii`, grouped with the © in a flex row — placed here rather than in `NAV_ITEMS` or as a fourth column so it reaches every page without becoming a seventh destination (D-3.07-2). „Следете нѐ" still shows its chip (PL-15, still unverified). Reuses the existing `footerLink` recipe, so the new link carries the footer's own hover underline and the shared `focusOnNavy` ring (measured: 3px solid orange, 2px offset) (Phase 1.03; columns 1.06b; redesign 3.03; crest chore-crest-logo; 3.07) **3.12: „Следете нѐ“ is a real link** — the Facebook fan page from `FACEBOOK_FAN_PAGE`, opened in a new tab with `rel=noopener noreferrer`, the site’s only external link. `PlaceholderChip` is no longer imported here; PL-15 stays open on `/kontakt` (D-3.12-6)
- `src/components/Container.tsx` — max-width (**1248px** since 3.05) + page-gutter primitive (20px / 32px md). Wraps a block's *content*; the block's colour runs full-bleed on the `<section>` (Phase 1.03; 3.05)
- `src/components/PageHeader.tsx` — **new at 3.05** (D-3.05-6): the navy block every inner page opens with — breadcrumb → H1 → intro → counted meta. Used by `/arhiva`, `/statistika`, `/legendi`, `/za-nas`, `/kontakt`; the season and person pages keep their richer heroes but sit on the same navy block with the breadcrumb pulled inside
- `src/components/home/PlaceholderChip.tsx` — placeholder chip (dashed border, hatch α 0.18, mono `[PLACEHOLDER: …]`); the only legal way to show a missing display fact. **3.05:** gained an `onNavy` variant, because this direction puts real content inside navy blocks where a paper chip would punch a hole; both states are contrast-checked against their own hatch **stroke**, the worst case a sweep otherwise skips (4.73 / 5.84:1 — D-3.05a-8) (Phase 1.05; onNavy 3.05)
- `src/components/home/PhotoFrameView.tsx` — **new at 3.09 (D-3.09-1)**: the frame's *markup only*, with **no Sanity dependency**. Takes an already-resolved `FramedImage = { src, objectPosition }`. **This is the half a `"use client"` component may import** — `LegendsBrowser → RoleBandGrid → LegendCard` reaches it, and importing `PhotoFrame` there instead dragged the whole `@sanity/image-url` builder (17.3 KB) into `/legendi`'s browser bundle
- `src/components/home/PhotoFrame.tsx` — **server-only since 3.09**: resolves the Sanity asset via `framedImage()` and delegates the markup to `PhotoFrameView`; its public API is unchanged. Photo frame where **`fit` now selects the surround as well as the crop** (D-3.05-5): `cover` = a hard-edged block on navy (presentation: hero, moment, card leads), `contain` = the mist mat + hairline that the archival sets need for the mixed-quality rule. Prior note: `next/image` via `urlFor`; greybox + chip when no image; `ratio` optional — omit for fill mode (`h-full`) used by the gallery mosaic (Phase 1.05; fill mode 1.06b, D-1.06b-3) · **2.03:** gained an additive `fit="cover"|"contain"` prop (default `cover`, no existing caller changed) so the archival photo grid can mat whole scans rather than crop them (D-2.02-7)
- `src/components/home/Reveal.tsx` — client reveal-on-scroll wrapper. **3.09:** gained `immediate`, which omits `data-reveal` entirely so the element is never hidden — set on the LCP card of `/arhiva` and `/legendi`, where the reveal was holding the largest-paint element at `opacity: 0` for a measured 370 ms / 331 ms (D-3.09-3). Prior: IntersectionObserver toggles `.is-visible`; 60ms stagger via `delayIndex`. **3.05:** gained `as` (`li`/`tr`/`section`/`figure`) so it can wrap a grid item without breaking a `<ul>`'s content model, and the motion re-timed to 260ms in `globals.css` (Phase 1.05; 3.05)
- `src/components/home/SectionOverline.tsx` — the `u-label` section label: a solid orange **bar** + tracked caps, with the text following its surface (navy on light, paper on navy). **Rewritten at 3.05** — the accent is now always the bar and never a letterform, so all three variants share one shape; before, `onNavy` dropped the rule and turned the text orange. Prior note: `onPaper` = orange rule + navy text; `onNavy` = orange text on SOLID navy (4.6:1); **`onPhoto` (added 3.03, D-3.03-5)** = orange rule marker + PAPER text, for overlays on a photo+gradient (hero, moment band) where orange text fails AA (Phase 1.05; onPhoto 3.03)
- `src/components/home/ClubRecords.tsx` — the „Клубот во бројки" scoreboard strip (homepage only — `/statistika` renders its 30 through `ClubRecordList`). **3.05b:** selection is now an **explicit six-label whitelist** (`HOMEPAGE_RECORD_LABELS`), so a 31st `clubRecord` in Studio cannot change the front page; the array *is* the render order and `category`/`order` are no longer consulted here — `CATEGORY_PRIORITY`/`sortRecords` deleted, superseding D-3.03-3 for this surface (D-3.05b-1). „Шампион на Македонија" is pinned as the featured orange cell and is never back-filled from another record; a `lastCellSpan` helper widens the final cell so the strip's last row ends flush instead of showing an empty orange track (D-3.05b-2). Prior note (3.03): full-width championship feature, orange ground with 2px gaps as rules, paper text, link to `/statistika` (Phase 3.03; 3.05b)
- `src/components/home/DecadeExplore.tsx` — **new 3.03**: the „Разгледај по децении" grid — one tile per decade with a real `seasonCountLabel` count, deep-linking to `/arhiva#d{decade}` (D-3.03-6). Replaces the removed `DecadeTimeline` (Phase 3.03)
- `src/lib/nav.ts` — single source for nav items + `isActivePath()` (Phase 1.03)
- `src/lib/utils.ts` — shadcn `cn()` class-merge helper; **3.04d:** built via `extendTailwindMerge` with the eight custom `@theme` font-size tokens + `tracking-overline` registered, so size and colour merge correctly (D-3.04d-1; a new `@theme` type token must also be added here)

## Sanity (`src/sanity/`) — created Phase 1.04; model **LOCKED** at 2.01, **re-opened additively at 3.01** (re-locks after 3.06)
- `src/sanity/env.ts` — reads `NEXT_PUBLIC_SANITY_PROJECT_ID` / `_DATASET`, pinned `apiVersion`, `useCdn: true`
- `src/sanity/client.ts` — read-only client (published perspective, no token)
- `src/sanity/fetch.ts` — **new at 3.02F-Code**: `fetchOrThrow(query, params, label)`, the bounded-retry read used by the two fan-out templates (season, person). 5 attempts, exponential backoff 1/2/4/8 s, every retry `console.warn`ed, then a **throw naming the slug** with the underlying error unwrapped from `cause` (D-3.02F-C-1 / -2). A query matching nothing still returns `null` and still 404s — only a thrown read is retried. ⚠️ The other reads (`/`, `/arhiva`, `/legendi`, `/statistika`, `sitemap.ts`) still use `client.fetch` directly and are **not** covered
- `src/sanity/image.ts` — `@sanity/image-url` builder (`urlFor`) for `next/image`
- `src/sanity/frame.ts` — **new at 3.09 (D-3.09-1)**: `framedImage(image, width) → { src, objectPosition } | null`, plus the hotspot→`object-position` resolution moved out of `PhotoFrame`. **The single seam that keeps `@sanity/image-url` out of the browser** — call it on the server and pass the result as props, the pattern D-3.05b-3 set for the lightbox
- `src/sanity/structure.ts` — Studio desk structure; `siteSettings` pinned as a singleton (D-1.04-5). season/person/photo are auto-listed via `documentTypeListItems()` (so retiring `match` from `index.ts` drops it with no change here); **`clubRecord` is listed explicitly so its desk entry reads the plural „Клупски рекорди"** (schema title is the singular „Клупски рекорд") — 3.01, D-3.01-5
- `src/sanity/lib/isUniqueSlug.ts` — `isUniqueSlugPerType`: slug-uniqueness check scoped to the document type, wired into `season.slug` / `person.slug` via `options.isUnique`; excludes the doc's own draft/published pair (Phase 2.01, D-2.01-6)
- `src/sanity/schemaTypes/index.ts` — the schema `types` array: `siteSettings`, `season`, `person`, `photo`, **`clubRecord`** (added 3.01). `match` is deliberately **not** registered (D-2.01-2)
- `src/sanity/schemaTypes/siteSettings.ts` — singleton: site title, description, footer archive statement
- `src/sanity/schemaTypes/season.ts` — season: title, slug (**unique**), decade (**required**), story (PT), **then five 3.01 optional fields (D-3.01-1..7): `teamPhoto` + `tablePhoto` (`reference → photo`, picker filtered to the same season via `options.filter`), `trainer` (string), `lineupAndStats` + `results` (PT, same `block` config as `story`)**, then legacy `finalTable`/`squad`/`trainers` (unchanged shape; descriptions now note „Задржано за компатибилност…", D-3.01-2). **No `photos`** — photos attach via `photo.relatedSeason` and are read by GROQ back-reference (D-2.01-1)
- `src/sanity/schemaTypes/clubRecord.ts` — **new at 3.01 (D-3.01-5)**: curated Statistics records. `label` (**req**), `value` (**req**), `category` (string, radio list: scorers/appearances/honours/other), `order` (number), preview (label/value). Empty until 3.02+; person `careerStats` still backs the ranking tables
- `src/sanity/schemaTypes/match.ts` — **DEFERRED / unregistered** (D-2.01-2): kept in-repo with a deferral header, absent from `index.ts`, so Studio does not list „Натпревар". No match-level Drive source exists (P0.1); stats come from season aggregates. Re-add via a future phase if that changes
- `src/sanity/schemaTypes/person.ts` — person: name, slug (**unique**), roles, playing years, bio (PT), `careerStats` (**authoritative** career total — D-2.01-3). **No `photos`** — portraits attach via `photo.relatedPerson` (D-2.01-1) **3.12: `legendRank` (number, 1–80)** — the book’s all-time appearance ranking, the sort key for the Играчи band and the homepage’s ten (D-3.12-2/-3). Deliberately not derived from `careerStats.appearances`: fifteen of the eighty are ranked on a count the book gives only as a range
- `src/sanity/schemaTypes/photo.ts` — photo: image (hotspot, required), caption, provenance (**required** — the rights paper-trail; carries the P0.1/P0.2 rights-gate comment), date, `relatedSeason` / `relatedPerson` — **the single, canonical direction** of both relationships (D-2.01-1)

## Archive templates (Phase 2.03)
- `src/app/(site)/arhiva/page.tsx` — **Archive index** `/arhiva`: decade-grouped season cards from live Sanity content, newest decade first; page header + real „N сезони · M децении" counts, sticky decade jump-nav, 3/2/1-col card grid. ISR `revalidate = 60`. Zero seasons → one placeholder chip (handover §5.6) **3.04b (D-3.04b-2): the card lead is now the curated `season.teamPhoto`** — it previously took `[0]` of the season photos by a `date` key that 886 of 889 photos lack, so 69 of 96 cards showed an arbitrary image; the old back-reference remains only as the fallback for the 13 seasons without one, on the deterministic 2.08 key.
- `src/app/(site)/arhiva/[slug]/page.tsx` — **Season detail** `/arhiva/<slug>`, **rebuilt at 3.04**: breadcrumb → navy title band with the matted `teamPhoto` mounted inside it (D-3.04-4) → in-page anchor rail → **Табела** (`tablePhoto` as an image, D-3.01-2) → **Тренер и статистика** (`trainer` in the heading + `lineupAndStats`) → **Резултати** (`results`) → **Приказна за сезоната** (`story`) → **Фотографии** → prev/next season + back-links. One GROQ round trip: dereferences `teamPhoto`/`tablePhoto` **with the asset's intrinsic dimensions**, fetches the gallery by back-reference **excluding both lead photos by `_id`** (D-3.04-2), and resolves both neighbours by `decade asc, slug.current asc` (D-3.04-1). The legacy `finalTable`/`squad`/`trainers` are no longer read (D-3.04-6). `generateStaticParams` over every published season slug; unknown slug → `notFound()`. Every data section omits itself when empty (D-2.02-3 / D-3.01-6); no lead photo **and** no section → the archive notice (D-2.02-8). ISR `revalidate = 60` **3.02F-Code:** every read on this route (`generateStaticParams`, `generateMetadata`, page body) now goes through `fetchOrThrow` — the old `catch → notFound()` is gone, because a transient CDN timeout used to emit a **silent 404 for a season that exists** (D-3.02F-C-1). **3.05b:** the `gallery` projection also returns each asset's intrinsic `width`/`height`, and „Фотографии" passes `lightbox` to `PhotoGrid` — the only call site that does (D-3.05b-3).
- `src/components/archive/Breadcrumb.tsx` — `Почетна / Архива / <season>`; navy links, mist `/` separators, current crumb `aria-current`; unknown crumb → placeholder chip (D-2.02-5)
- `src/components/archive/SectionHeading.tsx` — orange rule marker + serif H2; the archive templates' section label (D-2.02-3). Replaces `SectionOverline` on these pages only — the homepage's is untouched
- `src/components/archive/DecadeSectionHeader.tsx` — serif decade + real „N сезони" count (neutral-500) + orange rule → mist hairline
- `src/components/archive/SeasonCard.tsx` — 3:2 lead photo (or Mist greybox + chip), **neutral-500** decade overline (D-2.02-1), serif navy title; whole card is one `<a>`; 2px lift on hover. Wraps its own `Reveal` **inside** the `<li>`
- `src/components/archive/DecadeJumpNav.tsx` — sticky decade rail, offset by `top-header` (D-2.02-17); only decades with ≥1 published season; mobile scrolls the rail, never the page body. Scroll-spy deliberately not built (optional per handover §5.3)
- `src/components/archive/MattedPhoto.tsx` — **new at 3.04 (D-3.04-3)**: a solo archival scan matted at **its own** aspect ratio — mist ground, hairline border, real padding — sized from the asset's intrinsic `metadata.dimensions`. Never wider than the asset's native pixel width (a low-res scan is matted, never upscaled); optional `maxHeightRem` cap (40rem on the lead photo, uncapped on the standings image so it stays readable); `tone` switches the caption to paper on navy. Falls back to `PhotoFrame ratio="3/2" fit="contain"` when an asset has no dimension metadata. Used for `teamPhoto` + `tablePhoto` only — the „Фотографии" grid still uses fixed frames so a mixed set lines up
- `src/components/archive/SeasonAnchorNav.tsx` — **new at 3.04 (D-3.04-5)**: in-page rail („Табела · Тренер · Резултати · Приказна · Фотографии"), listing **only sections that rendered** and suppressing itself below two entries. **Deliberately not sticky**, unlike `DecadeJumpNav`, so a single `scroll-mt-header` on each section is the exact offset (verified: an anchor jump lands the section top at 77px, the header's bottom edge). Mobile scrolls the rail, never the page body
- `src/components/archive/SeasonRecordList.tsx` — **new at 3.04 (D-3.04-9)**: Portable Text for the two **record** fields. `variant="roster"` (`lineupAndStats`) = a tight line list; `variant="results"` (`results`) = one hairline-separated row per match. Both `tabular-nums`; nothing is reformatted (source OCR quirks ship verbatim, D-3.02S-3). `SeasonStory` is untouched and still renders prose
- `src/components/archive/SeasonRecordBoard.tsx` — **new at 3.06a**: the season page's league-record strip — a `grid-cols-2 sm:grid-cols-4` scoreboard on the orange ground with 2px gaps as rules (`border-y-[3px] border-orange`), rendering that season's record as label/value cells. Same scoreboard vocabulary as the homepage's `ClubRecords`, scoped to one season. ⚠️ Phase 3.06a filed **no completion report and logged no decisions** (`D-3.06*` count is 0), so this entry is reconstructed from the diff and commits `f8de548`/`b637b5e` — the *why* behind it is not recorded anywhere (D-3.07-10)
- `src/components/archive/SeasonNeighbourNav.tsx` — **new at 3.04 (D-3.04-1)**: prev/next season cards (overline label + serif title, arrow, 2px lift, no shadow). A missing neighbour renders **nothing** rather than a disabled control; „Следна" keeps the right-hand column even when „Претходна" is absent. Titles fall back to the slug — never to an invented „Сезона …"
- `src/components/archive/StandingsTable.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** „Конечна табела": navy `<thead>`, `<th scope="col">` with abbreviation + `sr-only` full label (D-2.02-14), zebra body, ФК Беласица row = `bg-highlight` + inset 2px orange left marker + navy rank (D-2.02-4), unknown cells „—". Mobile keeps **all nine columns** and scrolls inside the frame with `#`+`Клуб` sticky-left (D-2.02-10). Kept because `season.finalTable` still holds a real row (`1982-83`) that `/statistika` aggregates; **delete together with the legacy fields when the model re-locks after 3.06**
- `src/components/archive/SquadTable.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** „Состав": light table in `max-w-measure` (D-2.02-11); names link to `/legendi/<slug>`; unresolved player → chip, unknown numbers „—". Superseded on the page by `lineupAndStats` + `SeasonRecordList`
- `src/components/archive/PersonChip.tsx` — ⚠️ **LEGACY, unrendered since 3.04 (D-3.04-6).** Trainer chip → `/legendi/<slug>`; orange dot marker, navy label. Superseded on the page by the `season.trainer` string in the „Тренер и статистика" heading
- `src/components/archive/SeasonStory.tsx` — Portable Text renderer for `season.story` (and `person.bio`) via `@portabletext/react` (D-2.03-2); blockquote uses orange as a left rule only. **Unchanged at 3.04** — the two record fields got their own renderer instead (D-3.04-9)
- `src/components/archive/PhotoGrid.tsx` — „Фотографии": matted `PhotoFrame fit="contain"` (D-2.02-7); caption below the frame = orange rule marker + neutral-500 date overline + unclamped description (D-2.02-9). `provenance` never rendered. **3.04:** the season page now passes a gallery with `teamPhoto`/`tablePhoto` already excluded in GROQ (D-3.04-2 supersedes D-2.02-6). **3.05b:** gained an **opt-in `lightbox` prop** (default `false`, set only by the season page — the person page is unchanged, D-3.05b-3). Still a **server** component: when the prop is on it builds every overlay URL here with `urlFor(...).width(2400).fit("max")` (`max` so a small scan is never upscaled — brand.md's mixed-quality rule) and passes plain serialisable data into `PhotoLightbox`; a photo whose asset is missing stays inert rather than offering a button onto nothing
- `src/components/archive/PhotoLightbox.tsx` — **new 3.05b**, the repo's **fifth client component**: the season gallery's photo overlay. Exports `PhotoLightboxProvider` (owns the open index, the overlay and the page's scroll lock) and `PhotoLightboxTrigger` (the thumbnail `<button>`, which wraps the server-rendered frame as `children` so the photo never becomes client code). `role="dialog"` + `aria-modal` + `aria-label`, navy/96 backdrop, `object-contain` capped 92vw × 82vh, date/caption/`N / M` counter in an `aria-live="polite"` region below. Three 48×48 orange/navy controls; navigation **wraps**; both arrows absent for a one-photo season. `ArrowLeft`/`ArrowRight`/`Escape`/backdrop-click, focus trapped and returned to the triggering thumbnail. **Contains no Sanity import.** Scroll lock is on `documentElement`, not `<body>` (D-3.05b-5); focus restoration and the entry motion deliberately avoid `requestAnimationFrame` (D-3.05b-6) — the motion is `animate-in fade-in slide-in-from-bottom-1` from the already-pinned `tw-animate-css`
- `src/components/archive/SeasonSectionEmpty.tsx` — **new at 3.04b (D-3.04b-1)**: one section's empty state on a season page — a bordered white card with a single line of structural copy („Во архивата сѐ уште нема…“). Since 3.04b every section renders on every season page, so this is what a section with no content shows instead of vanishing. **Deliberately carries no `PlaceholderChip`** (unlike `StatsEmptyNotice`): always-on sections would emit ~170 chips across the 96 pages and swamp the placeholder register, which is a launch blocker
- `src/components/archive/SeasonEmptyNotice.tsx` — the fully-empty season notice (D-2.02-8); structural copy only. **3.04:** chips re-worded to the redesigned page's six sections (D-3.04-7), and the state is now a **guard** — after the 3.02 fill no published season is fully empty
- `src/lib/archive.ts` — archive display helpers: `decadeLabel`, `decadeAnchor`, `seasonCountLabel`/`decadeCountLabel` (pluralisation, D-2.02-12), `isBelasicaRow`, `statCell` (`—` for null, `0` preserved)
- `src/lib/focus.ts` — shared `focusOnPaper` / `focusOnNavy` focus-ring strings. **3.03:** the redesigned homepage now imports these (the previously-inlined copies were folded in); `DecadeTimeline`'s copy went away with the component
- `src/app/(site)/statistika/page.tsx` — **Statistics** `/statistika` (Phase 2.04, **records section added 3.02F-Code**): breadcrumb → **Клупски рекорди** → Најдобри стрелци → Најмногу настапи → Севкупен биланс на клубот. One GROQ round trip, now **four** reads: the 30 `clubRecord` docs lead the page (D-3.02F-C-4/-5); scorers + appearances rank **only** `person.careerStats` (D-2.01-3 — `season.squad` is never summed into a career total); seasons with `count(finalTable) > 0` feed the balance. The three original queries are **unchanged in shape and ordering**. The three table sections keep their heading and show an empty notice when empty (D-2.04-3); the records section instead **self-omits entirely** when no group survives (D-2.02-3), and the `border-t` on Најдобри стрелци is conditional so the page never draws a rule on the navy header's own colour change. ISR `revalidate = 60` **3.12: „Најдобри стрелци“ stops at `SCORER_MIN_GOALS = 21`** — 28 rows, ending on Ристо Панов, at the owner’s instruction; a line under the heading states the threshold, and the empty notice was rewritten because the section can now be empty for a second reason (D-3.12-5)
- `src/components/stats/StatTable.tsx` — **client** sortable table: `<button>` headers with `aria-sort` on the active column, unknown values sort last in both directions (D-2.04-5), `—` for null / `0` preserved via `statCell`. Styled as the archive Stats table (navy `<thead>`, zebra body, abbreviation + `sr-only` full label, D-2.02-14); mobile keeps every column and scrolls inside the frame with the first column sticky-left (D-2.02-10). Plain React state — no table library
- `src/components/stats/BalanceSummary.tsx` — the all-time balance band: сезони · одиграни · победи · нерешени · порази · ДГ · ПГ · гол-разлика · бодови · победи % (D-2.04-1). Unrecorded columns render „—", never `0`; GD/win % computed here only, never as a table column (D-2.04-4)
- `src/components/stats/StatsEmptyNotice.tsx` — a Statistics section's empty state: one structural sentence + a placeholder chip (D-2.04-3). Never a zero row, never an empty table shell. ⚠️ **Renders on no section today** — all three now have real content
- `src/components/stats/ClubRecordList.tsx` — **new at 3.02F-Code**: „Клупски рекорди" as a label/value **register**, not the brand scoreboard (D-3.02F-C-4 — the longest `value` is a 481-character 17-name roster, which `u-stat` cannot carry). One white `u-cap` block per category (h3 + `<dl>`), rows divided by `mist` hairlines; label = the scoreboard's tracked-caps `dt` vocabulary in navy, value = `text-body` ink capped at `max-w-measure`; two columns `minmax(0,15rem)/minmax(0,1fr)` from `md`, stacked below. No new `brand.md` token
- `src/app/(site)/legendi/page.tsx` — **Legends index** `/legendi` (Phase 2.06): breadcrumb → H1 + intro → three fixed-order role bands (Играчи → Тренери → Раководство). **Since 3.10 it renders `PageHeader` only on the empty-roster branch** — the populated path returns `<LegendsBrowser bands={bands} />`, which owns the header so the search field can live inside it (D-3.10-2); the three header strings are therefore written in both files, a contained duplication on a defensive path (D-3.10-3). Reads every published person + portrait by `photo.relatedPerson` back-reference. Placement happens here, not in the band: each person lands in exactly one band by highest-priority role (player > trainer > president, D-2.05-2) and shows **all** their role chips; a person with no recognised role is placed in no band. Name-sorted with `localeCompare("mk")` (GROQ cannot collate Cyrillic). Empty band → fully omitted; zero people → heading + one chip. ISR `revalidate = 60` **3.12: Играчи is no longer alphabetical** — it sorts on `compareByLegendRank` (book rank → recorded appearances → name), at the owner’s instruction (D-3.12-2). Тренери and Раководство keep `compareByName`; the query now also reads `legendRank` and `careerStats.appearances`
- `src/app/(site)/legendi/[slug]/page.tsx` — **Person detail** `/legendi/<slug>` (Phase 2.06): breadcrumb → `PersonHero` → Биографија → Кариера → Сезони → Фотографии → back-link. `generateStaticParams` over **every** published person, so trainer/official slugs never 404; unknown slug → `notFound()`. Every section self-omits when empty. `careerStats` read directly, never summed from `season.squad` (D-2.01-3); a missing metric omits its tile, never renders `0`, both missing → section omits. Сезони matches `squad[]` **or** `trainers[]` (D-2.06-3). ISR `revalidate = 60` **3.02F-Code:** all three reads now go through `fetchOrThrow`; the old `catch → notFound()` is gone (D-3.02F-C-1). `generateStaticParams` covers all **160** published people — every slug verified 200 against the built output.
- `src/app/(site)/pravni-informacii/page.tsx` — **Legal information** `/pravni-informacii` (**new at Phase 3.07**): the archive's legal statement — unofficial-archive declaration, purpose, the AI-assisted-build disclosure, data-accuracy caveat, the photo/copyright takedown-on-request posture (D-3.07-3), club marks, liability limit, external links, amendments, contact. Navy `PageHeader` (H1 + „Последно ажурирање" meta) then one `max-w-measure` column of ten `<section>`s. **Static server component — no Sanity read, no client component and deliberately NO `revalidate`** (D-3.07-8), so it builds as `○ (Static)` with an empty Revalidate column. The copy is held verbatim in a module-scope `SECTIONS` array rendered through one `LegalBlock` (D-3.07-7), which is what makes the character-for-character diff against the brief mechanical (41/41 lines identical). §10's address is a `mailto:` from `CONTACT_EMAIL` in a lowercase variant of the `u-link` anatomy (D-3.07-4); §1's domain is plain text. Indexable, and reachable **only** from the footer's bottom bar — absent from `src/lib/nav.ts` and `SiteHeader` by design (D-3.07-2). Zero placeholder chips in `<main>`
- `src/app/(site)/za-nas/page.tsx` — **About** `/za-nas` (Phase 2.06) — **provisional** (D-2.05-1): in-page banner → H1 → OV-3 framing block (navy left-rule, rendered verbatim from `@/lib/facts`) → PL-1 + PL-2 placeholder chips. No Sanity read. The handover's optional hero is omitted — the locked model has no field for it (D-2.06-4). ISR `revalidate = 60`
- `src/components/legends/LegendCard.tsx` — 4:5 portrait (`PhotoFrame fit="cover"`) **or** a solid navy initials tile when photo-less (D-2.06-7 — deliberately unlike `SeasonCard`'s greybox); serif navy name, role chips, `playingYears`. Whole card is one `<a>` (2px lift, no shadow — `brand.md` has no shadow token, D-2.06-5). Wraps its own `Reveal` **inside** the `<li>`. Missing years placeholder for **players only** (D-2.06-8)
- `src/components/legends/RoleBandGrid.tsx` — one role band: serif H2 + real count + orange rule → mist hairline (the `DecadeSectionHeader` pattern) over a 3/2/1 grid of `LegendCard`. **Renders nothing when empty** — so „0 играчи" can never appear. Takes `role` (not the handover's `title`) so title/priority/count-noun all read from `@/lib/people`
- `src/components/legends/LegendsBrowser.tsx` — **client**; **since 3.10 it renders the whole of `/legendi` below the site header** — the navy `PageHeader` (breadcrumb → H1 → intro → `personCountLabel` meta → the search field as `children`) *and* the bands under it, so the input and the filtered list share state in one component and no empty band sits between them (D-3.10-2). Receives the bands already built + name-sorted on the server and filters them in the browser (whole roster is one read, so no request per keystroke; the page still lists everyone with JS off — `/legendi` is still statically prerendered). Matching moved to `matchesName` from `@/lib/translit`, so **Latin spellings find Cyrillic names**; the local `normalise` now only answers „is the user searching at all". Field chrome is on the navy scale — `text-paper` label, `text-paper/80` count and clear button, `focusOnNavy` ring, white input surface kept. A band with no match self-omits via `RoleBandGrid`; zero matches overall renders one notice, on paper, above the bands. `aria-live` result count + clear button
- `src/lib/translit.ts` — **new at 3.10 (D-3.10-7)**: Latin-keyboard matching for the Cyrillic roster. A per-letter table of accepted Latin spellings (each letter also accepts itself, so Cyrillic queries are untouched) plus `matchesName(name, query)`, a backtracking substring matcher that consumes whichever spelling the query used — longest variant first. Variant sets **overlap on purpose** (`c` → ц/ч/ќ, `z` → з/ж/ѕ): over-matching is the correct failure direction for a 160-name filter. Characters outside the table (space, hyphen, en dash, parentheses, full stop — all present in roster names) match only themselves. Deliberately **not** in `people.ts`: that file is display helpers, this is input handling
- `src/components/legends/PersonHero.tsx` — person hero: portrait-left (300px) / text-right at desktop, stacked at mobile. Photo-less → a **navy band** carrying a monogram plate (2.02 handover §6.2b, as the season hero does). Name is the page's single `<h1>`
- `src/components/legends/RoleChips.tsx` — a person's roles as chips, shared by `LegendCard` + `PersonHero`. Priority-ordered: `[0]` (the role that placed them) is the filled navy chip, the rest muted outlines; `onNavy` inverts both for the hero band. Role name always spelled out — never colour-only
- `src/lib/people.ts` — people helpers: `ROLE_LABEL` / `BAND_TITLE` / `ROLE_PRIORITY` (band order **and** placement priority in one list), `orderedRoles` (drops unrecognised values), `primaryRole`, `initials`, `compareByName` (`localeCompare("mk")`), `bandCountLabel` (per-band pluralisation, the D-2.02-12 rule) and — **added 3.10** — `personCountLabel` (the `/legendi` header's archive total; neutral „личност/личности" because it counts across all three bands, same singular rule). NB: the homepage's previously-inlined `ROLE_LABEL` went away at 3.03 — the legends band now uses `LegendCard`, which reads these helpers **Added 3.12: `compareByLegendRank`** (+ the `RankedPerson` shape) — the Играчи band’s three-tier order: book rank, then recorded appearances, then name (D-3.12-2)
- `src/lib/facts.ts` — VERIFIED copy from `facts.md` as constants: the OV-3 `UNOFFICIAL_ARCHIVE_LABEL` + `UNOFFICIAL_ARCHIVE_STATEMENT`, transcribed byte-identically, **plus `CONTACT_EMAIL` (`info@belasicahistory.mk`, added 3.07, D-3.07-6)** — the address is imported by all three surfaces that render it (footer, `/kontakt`, `/pravni-informacii`) for both `href` and label, so the link target cannot drift from the text. NB: `siteSettings.footerUnofficialArchiveText` holds **different, unverified, unrendered** text — see **OV-6** / D-2.06-9 **Added 3.12: `FACEBOOK_FAN_PAGE` + `FACEBOOK_FAN_PAGE_LABEL`** — the one VERIFIED social link, rendered in the footer only and labelled as a **fan** page, not as this archive’s account (D-3.12-6)
- `src/lib/stats.ts` — `aggregateClubBalance` (sums **only** `isBelasicaRow` rows across seasons with a `finalTable`; per-metric `contributing` counts make a partial aggregate detectable), `formatGoalDifference`, `formatWinRate`; **+ 3.02F-Code:** `RECORD_GROUPS` (the four Studio category titles verbatim, in page order honours → scorers → appearances → other) and `groupClubRecords` (drops a record missing `label`/`value`, files an unknown/absent category under „Друго" rather than hiding it, sorts `order` asc with unset last then `label` by `mk` collation, drops an empty group). Presentation-free (D-2.04-1 / D-2.04-4 / D-3.02F-C-5)

## Contact page (Phase 2.07)
- `src/app/(site)/kontakt/page.tsx` — **Contact** `/kontakt` (Phase 2.07) — **no longer provisional: the banner was removed at owner request in 3.10, retiring D-2.05-1** (and with it D-3.03b-2/-3, D-3.10-1). Now breadcrumb → H1 „Контакт" + intro → two-column body (form left, direct-contact block right with `border-l border-mist`; single column at 375, form first, direct block below `border-t border-mist`). **Server component**; reads `NEXT_PUBLIC_FORMSPREE_ENDPOINT` and passes it to `ContactForm` (D-2.07-2). **3.07:** the direct block's email is now the real `mailto:` from `CONTACT_EMAIL` — PL-3 was open on *two* surfaces and clearing only the footer would have left this page contradicting its own footer (D-3.07-5); the socials chip (PL-15) and the Formspree chip (PL-14) are unchanged, so `<main>` now carries **2** chips, not 3. Prior note: direct block was all placeholders, the footer's demo values deliberately not propagated (D-2.07-4). No Sanity read; the title is a direct `<h1>` not `SectionHeading` (D-2.07-6). ISR `revalidate = 60`. **The last 404 in the nav — the six-item nav is now complete.**
- `src/components/contact/ContactForm.tsx` — **client** component (the repo's second, after `StatTable`): the `idle → submitting → success | error` state machine (D-2.05-3). Plain `fetch` POST to the endpoint with `Accept: application/json`, `FormData` body — no form/validation library. Macedonian copy „Испрати" / „Се испраќа…" / „Ви благодариме!" / „Испрати повторно" (D-2.07-1). Labelled inputs, `type=email` + `required` native validation, `role="alert"` (error, above the form, input retained) / `role="status"` `aria-live="polite"` (success, form replaced), `focusOnPaper` ring, submit min-width reserves space (no layout shift). **Endpoint unset → the whole form renders inside a disabled `<fieldset>` with a notice + `PlaceholderChip` (PL-14); no `<form>`, so no submission is possible** (D-2.07-3). Reuses `PlaceholderChip`, not an „amber" chip (D-2.07-5)

## Ingestion script (Phase 2.09) — `scripts/ingest/`
- `scripts/ingest/classify.mjs` — **pure**, I/O-free folder-name classifier: `classify(name) → { kind, slug, title, decade, flags }` implementing `content-ingestion-plan.md` §3 (top-down, first match wins). Also `provenance()` (the locked §5 string), `decadeOf`, `decadeToWave`, `WAVE_LABELS`. Titles use „…“ marks (D-2.09-4); span separators include slash look-alikes ／ ⁄ but not underscore (D-2.09-7). No Sanity, no fs
- `scripts/ingest/run.mjs` — walks the mirror, classifies, writes the report, and (only with `--commit`) creates season shells (Wave 0) + uploads season-folder photos as **drafts** (Waves 1–8). Plain Node ESM. Dry-run default (writes nothing); `--source`/`--wave`/`--include-thematic`/`--report` flags; loads `.env.local` itself (no dotenv dep); reads `SANITY_API_WRITE_TOKEN` and exits clearly if unset on `--commit`. Deterministic idempotent IDs (`season-<slug>`, `drafts.photo-<sha1(relPath)>`); script-side slug-collision detection (D-2.09-6); never publishes, invents nothing beyond slug/title/decade/relatedSeason/image/provenance
- `scripts/ingest/extract-legends.py` — **(3.12)** chapters 9 + 10 of the book `.docx` → `data/book/legends.json`. Chapter geometry is line numbers, not patterns (the source headings are inconsistent). Restores spaces the `.docx` lost between run-together words via a vocabulary-driven splitter plus an explicit override table; changes nothing else, and leaves the author’s own typos standing
- `scripts/ingest/bios.mjs` — **(3.12)** `data/book/legends.json` → `person.bio` + `person.legendRank`, one Portable Text block per source paragraph, verbatim. Dry-run default; `--commit`; `--only <slug,…>`. Writes the **published** document (no rights gate on text, unlike the 2.09 photo drafts); block `_key`s are `sha1(slug:line:chunk)` so re-runs are byte-identical; refuses to write at all if any slug is missing from the dataset. Paragraphs over 900 chars are re-broken at sentence boundaries (D-3.12-4)
- `scripts/ingest/photos-owner.mjs` — **(3.12)** hand-delivered photographs → **published** `photo` documents, from a small JSON manifest (`file`, `caption`, `provenance`, one of `personSlug`/`seasonSlug`). References are resolved before any upload, so a slug typo fails before it costs a transfer; IDs are `photo-<sha1(basename)>`, so a re-run replaces rather than duplicates (D-3.12-7)
- `scripts/ingest/verify-classify.mjs` — pure unit check of `classify()` against the locked mappings; no mirror/Sanity/token; exits non-zero on any mismatch. The classifier's correctness evidence while the mirror is unavailable

## Content fill script (Phase 3.11) — `scripts/`
- `scripts/fill-season-content.mjs` — **new 3.11**, fills the **existing** `season.story` and `season.results` `block[]` fields from `data/book/season-content.json`. **No schema change, and none is needed.** Plain Node ESM, no new dependency. **Every write is `setIfMissing`**, so a season that already has content keeps it and a re-run changes nothing; Portable Text `_key`s are a deterministic SHA-1 of slug+field+index+text, committed with `autoGenerateArrayKeys: false`, so the patch is idempotent. **Dry run is the default** — it writes nothing to Sanity, needs no token, and emits `docs/ingestion/season-content-report.md`. **`SANITY_API_WRITE_TOKEN` is required only with `--commit`** (env or `.env.local`, parsed by the script itself — the 2.09 precedent); without it `--commit` exits non-zero before touching anything. Flags: `--commit`, `--only <slugs>`, `--field story|results|both`, `--report <path>`. **⚠️ The `--commit` run is the owner's to trigger — this phase deliberately never ran it**

## Build & tooling config (repo root)
- `package.json` / `package-lock.json` — dependencies, all pinned exact; scripts (dev/build/start/lint)
- `tsconfig.json` — TypeScript config; `@/*` path alias
- `next.config.ts` — Next.js config; **`images.formats: ["image/avif","image/webp"]` (3.09, D-3.09-4 — a format change only; no photograph's `quality` was reduced)**; `images.remotePatterns` allows `cdn.sanity.io/images/**` for `next/image` (Phase 1.04); `experimental.staticGenerationRetryCount: 2` (3.02F-Code, D-3.02F-C-3) — the second layer of build resilience under `src/sanity/fetch.ts`: the helper retries a flaky *read*, this re-runs the *page*
- `sanity.config.ts` — embedded Studio config (projectId/dataset via env, schema types, structure + vision plugins); read by `/studio` and the Sanity CLI (Phase 1.04)
- `sanity.cli.ts` — Sanity CLI config (projectId/dataset from env; tolerant of missing values) (Phase 1.04)
- `.env.local` — **git-ignored** (`.env*`); `NEXT_PUBLIC_SANITY_PROJECT_ID` / `_DATASET` / `_API_VERSION`. No token, not committed (Phase 1.04). `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is **not** set here — 3.03's step (D-2.07-2)
- `.env.example` — **committed** template (Phase 2.07): `NEXT_PUBLIC_FORMSPREE_ENDPOINT=` (empty) + a one-line comment. Trackable because `.gitignore` carries `!.env.example` after `.env*`; every other `.env*` stays ignored (D-2.07-2). No secret. **2.09** added `SANITY_API_WRITE_TOKEN=` (empty) with a comment — the ingestion write token, documented but never valued here; the real value lives only in `.env.local` (D-2.09-5)
- `.gitignore` — Next default + `/dist` (2.01) + **`!.env.example`** negation (2.07, D-2.07-2) + **`/.claude/worktrees/`** (added with the book drop) — Claude Code worktrees are local scratch and carry a full build output each; verified at 3.11 that `git ls-files .claude/worktrees` returns nothing
- `eslint.config.mjs` — ESLint flat config (Next 15 `FlatCompat`: next/core-web-vitals + next/typescript). **3.09:** ignore globs anchored with `**/` (`**/.next/**`, `**/out/**`, `**/build/**`, `.claude/**`, `_to_delete/**`) — the root-only patterns let a git worktree's own `.next` be parsed in full and `npm run lint` died with a V8 OOM before reaching a source file
- `postcss.config.mjs` — PostCSS config (`@tailwindcss/postcss`)
- `components.json` — shadcn/ui config (style new-york, base color neutral, Lucide icons)
- `public/crest.svg` — **new 3.05b**, the crest's **scalable master** (916×1294 viewBox, 79 KB, pure vector paths — no embedded raster, no script, no external refs). A traced, repaired rebuild of the same artwork, not a new crest: the source scan was clipped on its right edge and its pennant point cut off flat; the rebuild closes the point on the artwork's own measured diagonals and evens the margins. Club blue unchanged at `#125C9A` (D-crest-1). **This is what the header and the hero render**, via `next/image` + `unoptimized` — Next refuses to optimize SVG without `dangerouslyAllowSVG`, and an SVG has no raster variants worth generating (D-3.05b-7)
- `public/crest.png` — official FK Belasica club crest, **re-exported from the vector master at 3.05b** (still **864×1220**, so `layout.tsx`'s Open Graph block stays correct and untouched). Transparent outside the pennant outline (D-crest-2). Canonical asset for the Open Graph card and the favicon lineage; not rendered on-page (chore-crest-logo; rebuilt 3.05b)
- `public/crest-ui.webp` — 400×565 raster UI derivative, re-exported at 3.05b. Retained as the raster fallback and as the `/crest.png` lineage's source; **nothing references it** since the header and hero moved to `/crest.svg` (D-3.05-11; D-3.05b-7)
- `public/*.svg` — create-next-app default static assets (next/vercel/file/globe/window)

## Audits (`docs/audits/`) — new at 3.09
- `docs/audits/Part-3-Phase-09-baseline.md` — the **pre-change** Lighthouse numbers for all nine sampled routes × {mobile, desktop}, plus the exact method (tool version, exact CLI command, median-of-3 rule). **Re-run this method at 3.11** to verify the launch; it is the contract, the harness script was scratch
- `docs/audits/Part-3-Phase-09-results.md` — the same table post-fix, side by side with the baseline and a note per row on what moved it; the two routes still at 94 on mobile with the measured tradeoff that was refused (D-3.09-8); First Load JS per route; the woff2 preload count and which four faces they are; and the full manual WCAG 2.2 AA evidence (contrast, target size, focus-not-obscured, reflow at 375/320, text spacing, the lightbox's eight behaviours, `StatTable`, reduced-motion and no-JS, and each of the six WCAG 2.2 criteria recorded pass or N/A)
## ✅ Committed by accident — REMOVED at 3.11 (found at 3.07)
- ~~`_to_delete/`~~ — **gone.** The 11 tracked files of git plumbing junk (`HEAD.lock`, `index.lock`, `index.lock.2`, `index.lock.stale`, 7 × `tmp_obj_*`), committed to `main` during Phase 3.06a and left in place by 3.07, were deleted in commit `7ef3547` alongside the book drop. Verified at 3.11: absent from the working tree **and** from `git ls-files`. ⚠️ **The two untracked strays are a separate matter and are NOT confirmed handled** — `git-index.lock.leftover` and `sanity_token_transfer.tmp` lived only in Lazar's working tree, never in git, so no commit could have removed them; neither is present in the tree now, but whether `sanity_token_transfer.tmp` was **shredded or merely deleted** is unknown to this phase. Its name suggests it held a **token** — carried forward as a human step
- `.claude/launch.json` — dev-server config for the in-app browser preview (added during 3.06a). Harmless and useful; noted here only because it was never mapped

## Not present (deliberately)
- `.github/workflows/` — no CI/review workflow; the Claude review Action was dropped for this project (D-1.01-4)
